# Port the Vite dev proxy targets (Flask). See apps/visualizer/backend/.env FLASK_PORT.
# VITE_BACKEND_PORT=5001

# Comma-separated hostnames allowed for the Vite dev server (e.g. Tailscale Funnel / tunnel hostname).
# DEV_SERVER_ALLOWED_HOSTS=your-machine.tail1234.ts.net
