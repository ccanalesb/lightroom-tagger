/** Default log-summary fields for Job test fixtures (generated Job type requires them). */
export const JOB_TEST_DEFAULTS = {
  warning_count: 0,
  error_count: 0,
  last_log_at: null,
  error_severity: null,
  cancel_noop: null,
  cancel_noop_reason: null,
} as const;
