# Parked: {Capability}

Retired {YYYY-MM-DD} · tag `parked/{slug}` · sha `{short-sha}` · [map #218](https://github.com/ccanalesb/lightroom-tagger/issues/218)

## What it did

{One paragraph, user-facing. What could a photographer do with it?}

<!-- Use the ubiquitous language from CONTEXT.md — catalog image, description, score,
     perspective, match, instagram dump — so a future reader recognises the vocabulary. -->

## Why it was retired

{The real reason, in plain terms.}

<!-- Include numbers where they exist: the threshold used, the cost or time per image, the
     observed precision, the size of the population it ran on.

     Where a number was never measured, SAY SO PLAINLY. "The per-image vision cost was
     never measured; the judgement was made on wall-clock feel" is a useful sentence. An
     invented number is worse than an admitted gap, because a future reader will plan
     against it. -->

## To revive this, one of these would have to change

- {A condition, not a plan.}

<!-- What would have to become true for the retirement to be worth reversing — cheaper
     models, a different data source, a narrower scope, a precision threshold actually met?

     If nothing would, say that too. Some things are parked because they were the wrong
     idea, not because they were early, and that is the most useful finding of all. -->

---

**Where it lives:** `git checkout parked/{slug}` · key paths: `{modules and pages deleted}` · data dropped: `{tables dropped, and any export artifact}` · prior design docs: {links to docs/plans/*, reports, brainstorms — these stay where they are}
