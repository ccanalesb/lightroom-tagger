---
task_id: 260420-840
status: complete
completed: "2026-04-23"
commits:
  - c89530f  # feat(image-view): adapters — no score zeroing
  - 8b243d1  # feat(image-view): ImageMetadataBadges + ImagePerspectiveBreakdown
  - 4164414  # feat(image-view): ImageTile grid/strip/list/compact
  - 55c672f  # feat(image-view): ImageDetailModal shell + catalog/IG sections
  - 03eea96  # test(image-view): cross-entry parity
  - e31e36f  # refactor(image-view): consolidate modal/tile
  - 10e51e1  # PR review feedback — image-view cluster
---

# SUMMARY: 260420-840 Consolidate image metadata display

## What was built

**Backend (Task 1):**
- `GET /api/images/<image_type>/<image_key>` on the images blueprint
- `get_identity_scores_for_catalog_key(conn, key)` in `identity_service.py` reusing `_SCORES_BASE_SQL` and aggregation rules
- Returns full `ImageView` superset: catalog row + description + identity `per_perspective[]` + `aggregate_score`; Instagram returns empty identity fields
- Optional `?score_perspective=<slug>`; when omitted: `catalog_perspective_score: null` + `available_score_perspectives: string[]`
- 404 for unknown key, 400 for invalid `image_type`
- Tests in `apps/visualizer/backend/tests/test_images_detail_api.py`

**Frontend (Tasks 2–12):**
- `components/image-view/adapters.ts` — `fromImageDetail`, `fromCatalogListRow`, `fromUnpostedRow`, `fromBestPhotoRow`, `fromPostNextRow`, `fromInstagramRow`; no score zeroing for modal path
- `components/ui/badges/ScorePill.tsx` + barrel export from `badges/index.ts`
- `components/image-view/ImageMetadataBadges.tsx` — core badge row with `primaryScoreSource: 'identity' | 'catalog' | 'none'`
- `components/image-view/ImagePerspectiveBreakdown.tsx` — compact `per_perspective[]` pill row for modal + `BestPhotosGrid` inline expand
- `components/image-view/ImageTile.tsx` — `variant: 'grid' | 'strip' | 'list' | 'compact'`; composes `ImageMetadataBadges`
- `components/image-view/ImageDetailModal.tsx` — single modal shell; on mount calls `ImagesAPI.getImageDetail` → `fromImageDetail`; branches on `image_type` for catalog vs Instagram body; full-screen sheet below `md`, centered at `md+`; scroll lock + Escape; focus trap via `useFocusTrap`
- `components/image-view/CatalogImageDetailSections.tsx` — catalog body (scoring UI, `ImageScoresPanel`, run-scoring)
- `components/image-view/InstagramImageDetailSections.tsx` — Instagram body (match UI, `MatchStatusDisplay`)
- `components/image-view/index.ts` — public API re-exports

**Migration (Tasks 8–10):** All 6 entry points migrated:
- `CatalogTab`, `CatalogImageCard`, `UnpostedCatalogPanel` — use `ImageDetailModal`; no more `unpostedRowToCatalogStub` score zeroing
- `TopPhotosStrip`, `BestPhotosGrid`, `PostNextSuggestionsPanel` — use `ImageDetailModal`; `bestPhotoToCatalogStub` / `candidateToCatalogStub` removed; inline `per_perspective` table replaced by `ImagePerspectiveBreakdown`
- `InstagramTab`, `InstagramImageCard` — use `ImageDetailModal` with `image_type: 'instagram'`

**Cleanup (Task 11):**
- `components/catalog/CatalogImageModal.tsx` — deleted
- `components/instagram/ImageDetailsModal.tsx` — deleted
- All stub adapters that zeroed scores removed; `catalogStubForDeepLink` preserved as key-only modal trigger

**Tests (Task 12 + per-migration):**
- `image-view/__tests__/adapters.test.ts` — `fromImageDetail` + list adapters produce stable `ImageView` keys
- `image-view/__tests__/ImageDetailModal.parity.test.tsx` — same `aggregate_score` / `per_perspective` from catalog, unposted, and identity entry points
- `image-view/__tests__/ImageDetailModal.test.tsx`, `ImageMetadataBadges.test.tsx`, `ImageTile.test.tsx`
- All 248 frontend tests pass; `tsc --noEmit` clean; `vite build` clean

## Verification

- `rg "CatalogImageModal|ImageDetailsModal" apps/visualizer/frontend/src/` — zero matches (comment only in `InstagramImageDetailSections.tsx`)
- `npx tsc --noEmit` — clean
- `npx vitest run` — 248 passed (45 files)
- `GET /api/images/catalog/<key>` returns identity scores for scored images; modal shows same breakdown from all entry points
