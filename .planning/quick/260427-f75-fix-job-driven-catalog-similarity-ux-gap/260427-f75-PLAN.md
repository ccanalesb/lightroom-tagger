---
status: planned
quick_id: 260427-f75
created: 2026-04-27
---

# Quick Task 260427-f75: Fix job-driven catalog similarity UX gaps

## Goal

Make Phase 6/7 similarity and stack features behave like the rest of the app: job-driven, reviewable, and visually clean. Remove hidden/on-demand similarity as the primary workflow, move stack actions out of grid cards, and fix job observability issues found during live validation.

## Tasks

### 1. Add Job-Driven Catalog Similarity Results

Files:
- `lightroom_tagger/core/database.py`
- `lightroom_tagger/core/clip_similarity.py`
- `apps/visualizer/backend/jobs/handlers.py`
- `apps/visualizer/backend/library_db.py`
- `apps/visualizer/backend/api/images.py`
- backend tests

Action:
- Add derived tables for catalog similarity groups/candidates.
- Add `batch_catalog_similarity` job that reads existing CLIP embeddings and writes durable similarity groups without touching the Lightroom catalog.
- Add an API endpoint for listing latest catalog similarity groups for review.
- Ensure logs summarize groups/candidates created instead of per-item spam.

Verify:
- Unit test a tiny embedded catalog where a high-similarity pair becomes one durable group.
- API test returns the persisted group in catalog image shape.

Done:
- User can start a batch similarity job and later review materialized results.

### 2. Clean Up Similarity And Stack UI

Files:
- `apps/visualizer/frontend/src/components/image-view/ImageDetailModal.tsx`
- `apps/visualizer/frontend/src/components/images/CatalogTab.tsx`
- `apps/visualizer/frontend/src/components/processing/MatchingTab.tsx`
- `apps/visualizer/frontend/src/pages/ProcessingPage.tsx`
- `apps/visualizer/frontend/src/services/api.ts`
- frontend tests

Action:
- Remove/de-emphasize `More like this` on-demand similarity from the image modal.
- Keep grid stack display to a compact badge only.
- Keep stack member/action controls inside the image detail modal.
- Add a clear Processing action to enqueue catalog similarity and a small review surface for latest groups.

Verify:
- Frontend tests prove grid tiles show only the stack badge, no `Show stack` button.
- Frontend tests prove Processing can enqueue the catalog similarity batch job.

Done:
- Grid proportions are restored and similarity is discoverable as a job workflow.

### 3. Fix Job Lifecycle, Log Quality, And Processing Order

Files:
- `apps/visualizer/backend/database.py`
- `apps/visualizer/backend/jobs/runner.py`
- `apps/visualizer/backend/jobs/handlers.py`
- backend tests

Action:
- Stop overwriting `started_at` on every progress update.
- Make stack detection process burst segments newest-to-oldest.
- Throttle stack-detection progress logs to aggregate milestones.

Verify:
- Unit tests cover started-at preservation and newest-first stack processing.
- Existing stack/embed tests remain green.

Done:
- Job Queue timestamps are trustworthy, and logs describe outcomes instead of flooding progress rows.
