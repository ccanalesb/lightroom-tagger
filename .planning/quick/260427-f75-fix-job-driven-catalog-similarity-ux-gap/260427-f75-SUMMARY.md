---
status: complete
quick_id: 260427-f75
completed: 2026-04-27
---

# Quick Task 260427-f75 Summary

## Outcome

Implemented a job-driven catalog similarity path and cleaned up the stack/similarity UI problems found during live validation.

## Changes

- Added derived catalog similarity tables plus `batch_catalog_similarity`, which reads existing CLIP embeddings and materializes reviewable catalog similarity groups without touching the Lightroom catalog.
- Added `GET /api/images/catalog-similarity-groups` and frontend API support.
- Added a Processing → Vision Matching card to start `Find Similar Photos` and preview latest materialized groups.
- Removed the on-demand `More like this` modal section as the primary similarity workflow.
- Removed grid-level stack expansion/actions; catalog grid now keeps only the compact stack badge, with stack controls remaining in the detail modal.
- Fixed job `started_at` preservation so progress updates no longer overwrite the true start time.
- Made stack detection process burst segments newest-first and throttle progress logs to aggregate summaries.

## Verification

- Backend: `52 passed`
  - `test_handlers_batch_catalog_similarity.py`
  - `test_images_clip_similar_api.py`
  - `test_job_runner.py`
  - `test_handlers_batch_stack_detect.py`
  - `test_jobs_api.py`
- Frontend: `18 passed`
  - `CatalogTab.test.tsx`
  - `ImageDetailModal.test.tsx`
  - `MatchingTab.test.tsx`
- Frontend ESLint on modified files: passed with `--max-warnings 0`.
- Frontend `tsc --noEmit`: blocked by pre-existing `SearchPage.test.tsx` `.at()` lib-target errors, unrelated to this quick task.
