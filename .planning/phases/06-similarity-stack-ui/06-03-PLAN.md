---
phase: 06-similarity-stack-ui
plan: 3
type: execute
wave: 3
depends_on: ["06-02-PLAN"]
files_modified:
  - apps/visualizer/backend/api/images.py
  - apps/visualizer/backend/api/identity.py
  - apps/visualizer/backend/tests/test_images_clip_similar_api.py
autonomous: true
requirements:
  - SIM-02
  - STACK-03
user_setup: []
must_haves:
  truths:
    - "GET /api/images/catalog/{key}/similar returns catalog-shaped images with distance/similarity and JSON meta.clip_model_id"
    - "GET /api/images/stacks/{stack_id}/members returns only keys for that stack, ordered for UI strip (document order in code comment)"
  artifacts:
    - path: apps/visualizer/backend/api/images.py
      provides: similar + stack members routes
  key_links:
    - from: similar route
      to: run_clip_similar_for_seed
      via: "hydrate with query_catalog_images_by_keys + _rows_to_catalog_api_images"
---

<objective>
Expose **SIM-02** and **STACK-03** server-side: similar-images GET route (CLIP-only) and stack members GET route; pass through stack fields from 06-01/06-02 on catalog and best-photos JSON.

Purpose: Machine-verifiable API contract for frontend 06-04 and Phase 7 reuse (D-02, D-04).

Output: Extended Flask blueprints + `test_images_clip_similar_api.py`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@apps/visualizer/backend/api/images.py (`_clamp_pagination`, `_rows_to_catalog_api_images`, `list_catalog_images` ~543+)
@apps/visualizer/backend/utils/responses.py (`error_not_found`, `error_bad_request`)
@apps/visualizer/backend/tests/test_images_semantic_search_api.py (app fixture pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: GET catalog similar + GET stack members</name>
  <read_first>
    - apps/visualizer/backend/api/images.py (existing `@bp.route` style, `with_db`, `_clamp_pagination` ~201–220, `semantic_search_images` or `list_catalog_images` for param reuse)
    - lightroom_tagger/core/clip_similarity.py (exports from 06-02)
    - lightroom_tagger/core/database.py (`query_catalog_images_by_keys`)
    - .planning/phases/06-similarity-stack-ui/06-UI-SPEC.md (error copy keys for missing embedding — use exact strings in JSON `error` field or `meta` for structured clients)
  </read_first>
  <files>apps/visualizer/backend/api/images.py</files>
  <action>
  1. Register **`GET /api/images/catalog/<image_key>/similar`** (path segment name must match existing catalog routes’ encoding; use `urllib.parse` / Flask `&lt;path:image_key&gt;` only if current catalog routes use it — **match the existing `/api/images/catalog/&lt;key&gt;/thumbnail` style** in the same file).
  2. **Query parameters** (align names with `list_catalog_images`): `limit` (default 50), `offset` (default 0), and the same optional filter params the catalog list accepts (`posted`, `month`, `keyword`, `min_rating`, `date_from`, `date_to`, `color_label`, `analyzed`, `score_perspective`, `min_score`, `sort_by_score`, `sort_by_date`, `description_search`, `dominant_colors`, `mood_tags`, `has_repetition` — pass through to `run_clip_similar_for_seed` / filter helper as 06-02 defined). Use `_clamp_pagination(limit, offset)`.
  3. **Missing CLIP row (locked contract, cross-plan with 06-02):** When `run_clip_similar_for_seed` raises **`NoClipEmbeddingError`** (seed has no `image_clip_embeddings` row), catch it and respond **404** with JSON `{"error": "Visual similarity is unavailable"}` (exact substring **Visual similarity is unavailable** in the response body per **06-UI-SPEC**). Optional `detail` subfield is allowed if UI-SPEC allows; do not return 400 for this case. This matches the **06-02** plan: library raises `NoClipEmbeddingError` only; the route performs the HTTP mapping.
  4. On success, response JSON shape: `{"images": [...], "total": N, "meta": { "clip_model_id": "clip-ViT-B-32", "clip_embed_dim": 512, ... }}` where each image is from `_rows_to_catalog_api_images` and **augmented** with `similarity: float` in `[0,1]` computed as `max(0.0, min(1.0, 1.0 - float(distance)))` per semantic_search pattern, and `why_matched` string optional, e.g. `Visual match (NN%)` with NN = `round(similarity * 100)` (exact format documented in one place).
  5. Register **`GET /api/images/stacks/<int:stack_id>/members`**: return `{"items": [ catalog rows ] }` with each member as catalog-shaped objects via `query_catalog_images_by_keys` + `_rows_to_catalog_api_images` for keys from `SELECT image_key FROM image_stack_members WHERE stack_id = ? ORDER BY image_key ASC` (or `ORDER BY m.image_key`); if `stack_id` not found, **404** with `error_not_found` style message containing **stack** (grep-verifiable).
  6. **stack_id** must be validated as positive integer; reject non-numeric 404/400 per existing patterns.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend && PYTHONPATH=.:../.. python -m pytest tests/test_images_clip_similar_api.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
  - `rg "catalog.*similar" apps/visualizer/backend/api/images.py` OR `rg "/similar" apps/visualizer/backend/api/images.py` exits 0.
  - `rg "stacks" apps/visualizer/backend/api/images.py` matches a route containing `members`.
  - `run_clip_similar_for_seed` or `knn_clip_catalog_keys` imported from `lightroom_tagger.core.clip_similarity` appears in `images.py` (check with `rg "clip_similarity" apps/visualizer/backend/api/images.py` exits 0).
  - `tests/test_images_clip_similar_api.py` uses `create_app` + `app.test_client()` and asserts: missing embedding returns 404; success returns `meta["clip_model_id"] == "clip-ViT-B-32"`.
  </acceptance_criteria>
  <done>Two routes live, wired to core helpers, with automated API tests.</done>
</task>

<task type="auto">
  <name>Task 2: Best-photos JSON + catalog list JSON include stack fields</name>
  <read_first>
    - apps/visualizer/backend/api/identity.py (`best_photos` route)
    - apps/visualizer/backend/api/images.py (`_rows_to_catalog_api_images` and catalog GET)
  </read_first>
  <files>apps/visualizer/backend/api/images.py, apps/visualizer/backend/api/identity.py</files>
  <action>
  1. Ensure **catalog list** and **by-keys** plumbing passes `stack_id`, `stack_member_count`, `is_stack_representative` from DB rows into API dicts inside `_rows_to_catalog_api_images` (integer/boolean JSON types; `null` when absent).
  2. **best_photos** `items` from `rank_best_photos` must expose the same three keys on each item when present (serialize from identity_service dicts without renaming).
  3. Add at least one assertion in `test_images_clip_similar_api.py` or extend `test_images_catalog_api.py` to verify `stack_member_count` key appears in a catalog response when fixture has stack (or document skip if no fixture — then create minimal insert in the test’s temp DB per `init_database` patterns).
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend && PYTHONPATH=.:../.. python -m pytest tests/test_images_clip_similar_api.py tests/test_images_catalog_api.py -q --tb=line -k "stack or similar or catalog" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
  - `rg "stack_member_count" apps/visualizer/backend/api/images.py` exits 0.
  - `rg "is_stack_representative" apps/visualizer/backend/api/images.py` exits 0.
  - `pytest` command above exits 0.
  </acceptance_criteria>
  <done>STACK-03 and SIM-02 API DTOs complete for the Electron/browser client.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP → Application | `image_key` from path and query params; `stack_id` integer |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | I | `image_key` path | mitigate | Reject keys that don’t exist in `images` before heavy KNN; follow existing catalog thumbnail route behavior. |
| T-06-03-02 | I | `stack_id` | mitigate | 404 for unknown `stack_id`; int converter only. |
| T-06-03-03 | S | Thumbnail path reuse | accept | Thumbnails use existing signed path pattern / same as catalog. |
| T-06-03-04 | T | query params to SQL | mitigate | Reuse `query_catalog_images` / clip_similarity param binding; never concatenate raw FTS. |
| T-06-03-05 | D | N+1 on similar route | accept | One KNN + one batch by_keys per request; no per-thumbnail DoS in scope. |
</threat_model>

<verification>Backend pytest for new/updated tests; optional `ruff`/`black` on touched files if project CI requires.</verification>

<success_criteria>SIM-02 and STACK-03 server contracts implemented and tested.</success_criteria>

<output>After completion, create `.planning/phases/06-similarity-stack-ui/06-03-SUMMARY.md`</output>
