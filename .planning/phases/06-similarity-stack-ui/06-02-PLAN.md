---
phase: 06-similarity-stack-ui
plan: 2
type: execute
wave: 2
depends_on: ["06-01-PLAN"]
files_modified:
  - lightroom_tagger/core/clip_similarity.py
  - lightroom_tagger/core/database.py
  - lightroom_tagger/core/test_clip_similarity.py
autonomous: true
requirements:
  - SIM-02
user_setup: []
must_haves:
  truths:
    - "KNN runs only on table image_clip_embeddings (512-d cosine) with seed from same table"
    - "Seed key is never returned; keys failing primary-grid visibility or catalog pre-filters are dropped in KNN order"
  artifacts:
    - path: lightroom_tagger/core/clip_similarity.py
      provides: knn + run_clip_similar_catalog_keys
  key_links:
    - from: knn query
      to: "FROM image_clip_embeddings WHERE embedding MATCH ? AND k = ?"
      via: sqlite-vec vec0
---

<objective>
Implement **SIM-02** core library: CLIP-only visual similarity (D-05) with order-preserving post-filtering against catalog criteria and the same **primary grid visibility** rule as `06-01-PLAN.md` (exclude non-representative stack members from similar results).

Purpose: Isolated testable module before Flask wiring.

Output: `clip_similarity.py` + `test_clip_similarity.py`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@lightroom_tagger/core/clip_embedding_service.py (CLIP_EMBED_MODEL_ID, CLIP_EMBED_DIM)
@lightroom_tagger/core/semantic_search.py (knn_embedded_catalog_keys pattern, distance→similarity)
@.planning/phases/06-similarity-stack-ui/06-CONTEXT.md (D-05, D-06)
</context>

<tasks>

<task type="auto">
  <name>Task 1: clip_similarity module — KNN + ordered filter + seed guard</name>
  <read_first>
    - lightroom_tagger/core/semantic_search.py (function `knn_embedded_catalog_keys` ~67–90)
    - lightroom_tagger/core/clip_embedding_service.py (lines 1–40 for model id/dim)
    - lightroom_tagger/core/database.py (`get_clip` / `image_clip_embeddings` if present; `catalog_key_is_primary_grid_row` or equivalent from 06-01)
    - .planning/phases/06-similarity-stack-ui/06-RESEARCH.md (Pattern 1 &amp; 2, Pitfall 1)
  </read_first>
  <files>lightroom_tagger/core/clip_similarity.py, lightroom_tagger/core/database.py</files>
  <action>
  Create **new file** `lightroom_tagger/core/clip_similarity.py` with:
  1. `KNN_K_MAX = 500` (match order-of-magnitude to semantic search clamping) and a documented default over-fetch for filtering.
  2. `def knn_clip_catalog_keys(db: sqlite3.Connection, query_vec_blob: bytes, *, k: int) -&gt; list[tuple[str, float]]` — `SELECT image_key, distance FROM image_clip_embeddings WHERE embedding MATCH ? AND k = ?` (same pattern as `knn_embedded_catalog_keys` but table **`image_clip_embeddings`** only; never reference `image_text_embeddings`).
  3. `def get_clip_embedding_blob_for_key(db, image_key: str) -&gt; bytes | None` — `SELECT embedding FROM image_clip_embeddings WHERE image_key = ?`
  4. `def run_clip_similar_for_seed(db, seed_key: str, *, knn_k: int, limit: int, offset: int, **catalog_filter_kwargs) -&gt; tuple[list[tuple[str, float]], dict[str, Any]]` where:
     - `catalog_filter_kwargs` are the **same optional filter names** as `query_catalog_images` (or a documented strict subset: `posted`, `month`, `min_rating`, `date_from`, `date_to`, `color_label`, `analyzed`, `score_perspective`, `min_score`, `sort_by_score`, `sort_by_date`, `description_search`, `dominant_colors`, `mood_tags`, `has_repetition`) — **if** passing full kwargs is too heavy, implement a helper `def filter_order_keys_in_catalog(db, keys: list[str], **kwargs) -> list[str]` in `database.py` that reuses the same WHERE building as `query_catalog_images` with `i.key IN (...)` and preserves input key order; document the subset in the module docstring.
     - Load seed blob; if `None`, return `([], {"error": "no_clip_embedding"})` or raise `ValueError("no clip embedding for seed")` — **planner pick one**; 06-03 API must map to 404/JSON per UI-SPEC.
     - KNN: `knn_k = min(KNN_K_MAX, max(50, (limit + offset) * 20))` (or equivalent documented formula).
     - Remove **seed_key** from the neighbor list.
     - For each neighbor in distance order, keep it only if: (a) it passes `filter_order_keys` / catalog filters, and (b) `catalog_key_is_primary_grid_row(db, key)` is True (from 06-01) so non-rep members never appear in similar lists.
     - Return ordered list of **(key, distance)** for the first `offset`+`limit` after filtering, sliced to the requested page; meta dict must include `clip_model_id: "clip-ViT-B-32"` and `clip_embed_dim: 512` for client transparency (D-05).
  5. Do **not** import or call `embed_query_to_vec_blob` (text) or `knn_embedded_catalog_keys` (text table).
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest lightroom_tagger/core/test_clip_similarity.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
  - `rg "image_text_embeddings" lightroom_tagger/core/clip_similarity.py` exits **1** (no matches in new module).
  - `rg "image_clip_embeddings" lightroom_tagger/core/clip_similarity.py` exits 0.
  - `rg "clip-ViT-B-32" lightroom_tagger/core/clip_similarity.py` exits 0.
  - `test_clip_similarity.py` includes a test that a seed key never appears in result keys, and a test that KNN is invoked against `image_clip_embeddings` (mock `sqlite3` or temp DB with vec0 per existing init patterns).
  </acceptance_criteria>
  <done>CLIP-only similarity pipeline implemented and unit-tested; database helper for ordered filter present if used.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB reads | KNN and filters use parameter binding; keys from KNN are validated through catalog helpers. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | T | KNN + filter | mitigate | `k` clamped to KNN_K_MAX; all SQL parametrized. |
| T-06-02-02 | I | N/A in library layer | accept | — |
| T-06-02-03 | S | Forged keys in KNN output | accept | Library assumes DB integrity; same trust as catalog. |
</threat_model>

<verification>pytest `lightroom_tagger/core/test_clip_similarity.py` green.</verification>

<success_criteria>SIM-02 library layer complete; no text-embedding code paths in module.</success_criteria>

<output>After completion, create `.planning/phases/06-similarity-stack-ui/06-02-SUMMARY.md`</output>
