---
plan: 03-06
phase: 3
wave: 6
title: "Tests — RRF, batch_text_embed handler, semantic-search API"
depends_on: [03-03, 03-04, 03-05]
requirements: [NLS-03, NLS-04]
files_modified:
  - lightroom_tagger/core/test_semantic_rrf.py
  - apps/visualizer/backend/tests/test_handlers_batch_text_embed.py
  - apps/visualizer/backend/tests/test_images_semantic_search_api.py
  - apps/visualizer/backend/tests/test_library_db.py
  - apps/visualizer/backend/tests/test_jobs_api.py
autonomous: true
---

# Plan 03-06: Tests — RRF, batch_text_embed handler, semantic-search API

## Objective

Lock **deterministic RRF** behavior with unit tests (no `SentenceTransformer`), prove **`batch_text_embed`** writes `image_text_embeddings` rows under mocked encode, and prove **`POST /api/images/semantic-search`** returns **`metadata.missing_embeddings_count`** plus per-row **`score` / `why_matched` / `thumbnail_url`** with **encoded mocked** (**NLS-03**, **NLS-04**).

## Tasks

<task id="03-06-T1" title="RRF unit tests + hybrid matrix (research a–d)">
<read_first>
- /Users/ccanales/projects/lightroom-tagger/lightroom_tagger/core/semantic_search.py (reason: `rrf_scores_from_ranks`, `sort_keys_by_rrf_scores`, `RRF_K`, `run_semantic_hybrid_search`)
- /Users/ccanales/projects/lightroom-tagger/lightroom_tagger/core/database.py (reason: temp-db `init_database`, description + embedding fixtures)
</read_first>
<action>
Add `lightroom_tagger/core/test_semantic_rrf.py` with:

**RRF helpers (no DB):**

1. `test_rrf_asymmetric_lists_b_wins` — `lists={"fts": ["a", "b"], "vec": ["b"]}`: `a` only appears in fts at rank 1 → score `1/61`; `b` appears fts rank 2 + vec rank 1 → `1/62 + 1/61` which is **greater** than `1/61`; assert sort order `["b", "a"]`.

2. `test_rrf_single_list` — only `{"fts": ["x","y"]}`; assert output keys are exactly `{x,y}` and scores equal `1/61`, `1/62` respectively.

Use `pytest.approx` for floats.

**`run_semantic_hybrid_search` matrix (name and assert each):**

3. **`test_hybrid_matrix_a_fts_empty_returns_no_rows_fts_no_match` (a — embedding-only would violate D-08):** Semantic index **non-empty** (at least one `image_text_embeddings` row), FTS leg returns **zero** candidates for the chosen `fts_match`, KNN would return rows for the same `query_vec_blob`. Assert **no** result rows, `total == 0`, `meta.fts_no_match is True`, and `meta.missing_embeddings_count` is a non-negative int.

4. **`test_hybrid_matrix_b_fts_only_when_knn_empty` (b — FTS-only RRF leg):** Embeddings exist, FTS returns keys, but KNN returns **no** pairs (fixture: no vec rows within retrieval, or patch `knn_embedded_catalog_keys` to `[]` for that case). Assert ordering/scores reduce to **FTS-only**; per-row `why_matched` is **`"FTS match"`** where there is no vec rank.

5. **`test_hybrid_matrix_c_fts_and_vec_both_contribute` (c):** Fixture where both lists are non-empty and overlap; assert fused order is **not** identical to raw FTS order alone; at least one row that has **both** ranks shows `why_matched` **`FTS match · embedding: X.XX`** (similarity two decimals).

6. **`test_hybrid_matrix_d_semantic_index_empty_degrades_fts_only` (d):** `COUNT(*)` on `image_text_embeddings` is 0; assert **`meta.semantic_index_empty is True`**, results follow **FTS-only** BM25 ordering (no vec post-filter), and degradation matches plan 03-04.

Implement (3–6) with temp DB + minimal `images` / `image_descriptions` / FTS rows consistent with Phase 1 schema; stub or bind `query_vec_blob` deterministically. Prefer sqlite in-memory or temp file via `init_database` pattern from `test_database.py`.
</action>
<acceptance_criteria>
- [ ] `pytest lightroom_tagger/core/test_semantic_rrf.py -q` exits 0
- [ ] `grep -n 'rrf_scores_from_ranks' lightroom_tagger/core/test_semantic_rrf.py` exits 0
- [ ] `grep -n 'test_hybrid_matrix_a_fts_empty_returns_no_rows_fts_no_match' lightroom_tagger/core/test_semantic_rrf.py` exits 0
- [ ] `grep -n 'test_hybrid_matrix_b_fts_only_when_knn_empty' lightroom_tagger/core/test_semantic_rrf.py` exits 0
- [ ] `grep -n 'test_hybrid_matrix_c_fts_and_vec_both_contribute' lightroom_tagger/core/test_semantic_rrf.py` exits 0
- [ ] `grep -n 'test_hybrid_matrix_d_semantic_index_empty_degrades_fts_only' lightroom_tagger/core/test_semantic_rrf.py` exits 0
</acceptance_criteria>
</task>

<task id="03-06-T2" title="Handler tests for batch_text_embed">
<read_first>
- /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/tests/test_handlers_batch_describe.py (reason: `JobRunner` mock + `@patch('jobs.handlers.init_database')` pattern)
- /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/jobs/handlers.py (reason: `handle_batch_text_embed` name)
</read_first>
<action>
Add `apps/visualizer/backend/tests/test_handlers_batch_text_embed.py`:

1. Use temp dir + real `init_database` **without** patching when integration is stable; **preferred**: `@patch('jobs.handlers.init_database')` returning `init_database(real_path)` is unnecessary — follow `test_handlers_batch_describe` style: patch `init_database` to return connection opened on temp `LIBRARY_DB`.

2. `test_batch_text_embed_zero_work_completes` — empty catalog → `complete_job` called with `embedded==0` (mirror describe zero test).

3. `test_batch_text_embed_writes_vec_row` — insert one `images` row + `store_image_description` with non-empty summary; monkeypatch `jobs.handlers.embed_texts` to return `np.ones((1,768), dtype=np.float32)`; run handler; assert `SELECT COUNT(*) FROM image_text_embeddings` is 1 on library db.

Use `JobsRunner` / mock from sibling tests; set `os.environ['LIBRARY_DB']` to temp path.

</action>
<acceptance_criteria>
- [ ] `PYTHONPATH=apps/visualizer/backend pytest apps/visualizer/backend/tests/test_handlers_batch_text_embed.py -q` exits 0
- [ ] `grep -n 'handle_batch_text_embed' apps/visualizer/backend/tests/test_handlers_batch_text_embed.py` exits 0
</acceptance_criteria>
</task>

<task id="03-06-T3" title="API tests semantic-search">
<read_first>
- /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/tests/test_images_nl_search_api.py (reason: Flask `app.test_client()`, `monkeypatch` env)
- /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/tests/test_images_catalog_api.py (reason: `store_image` + `store_image_description` fixtures)
</read_first>
<action>
Add `apps/visualizer/backend/tests/test_images_semantic_search_api.py`:

1. Fixture: temp library db with **two** catalog images, descriptions with distinct `description_search_document` tokens (`alpha` vs `beta`), both embedded manually via `upsert_image_text_embedding` + **fixed blob** (can use `sqlite_vec.serialize_float32` on a numpy vector) **or** via calling `upsert` after handler test pattern.

2. `monkeypatch.setattr('lightroom_tagger.core.embedding_service.embed_query_to_vec_blob', lambda q: fixed_blob)` so search is deterministic.

3. `test_semantic_search_returns_metadata_and_row_extras` — `POST /api/images/semantic-search` with JSON `{"query":"alpha beta","limit":10,"offset":0}`; assert `200`; body has `metadata.missing_embeddings_count` int, `metadata.rrf_k == 60`, each image has `score`, `why_matched`, `thumbnail_url` starting with `/api/images/catalog/` and ending with `/thumbnail`.

4. `test_semantic_search_rejects_short_query` — `{"query":"a"}` → `400`.

5. Patch `LIBRARY_DB` / app db wiring exactly like `test_images_nl_search_api.py`.

</action>
<acceptance_criteria>
- [ ] `PYTHONPATH=apps/visualizer/backend pytest apps/visualizer/backend/tests/test_images_semantic_search_api.py -q` exits 0
- [ ] `grep -n 'semantic-search' apps/visualizer/backend/tests/test_images_semantic_search_api.py` exits 0
</acceptance_criteria>
</task>

<task id="03-06-T4" title="jobs_requiring_catalog includes batch_text_embed">
<read_first>
- /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/tests/test_library_db.py (reason: asserts on set membership)
- /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/tests/test_jobs_api.py (reason: `jobs_requiring_catalog` endpoint expectations)
</read_first>
<action>
Update assertions:

- `apps/visualizer/backend/tests/test_library_db.py`: assert `'batch_text_embed' in JOB_TYPES_REQUIRING_CATALOG`
- `apps/visualizer/backend/tests/test_jobs_api.py`: extend any test that enumerates expected catalog job types to include **`batch_text_embed`** if it lists an explicit set; if only checks `batch_describe`, add a dedicated assertion line for `batch_text_embed` in the `jobs_requiring_catalog` response list

</action>
<acceptance_criteria>
- [ ] `grep -n 'batch_text_embed' apps/visualizer/backend/tests/test_library_db.py` exits 0
- [ ] `PYTHONPATH=apps/visualizer/backend pytest apps/visualizer/backend/tests/test_library_db.py apps/visualizer/backend/tests/test_jobs_api.py -q` exits 0
</acceptance_criteria>
</task>

## Verification

```bash
cd /Users/ccanales/projects/lightroom-tagger
PYTHONPATH=apps/visualizer/backend pytest \
  lightroom_tagger/core/test_semantic_rrf.py \
  apps/visualizer/backend/tests/test_handlers_batch_text_embed.py \
  apps/visualizer/backend/tests/test_images_semantic_search_api.py \
  apps/visualizer/backend/tests/test_library_db.py \
  apps/visualizer/backend/tests/test_jobs_api.py -q
```

## must_haves

- [ ] Goal-backward: CI can run **sqlite-vec** `init_database` + semantic search path with **mocked** encoder (no model download).
- [ ] Goal-backward: **RRF k=60** behavior is **asserted numerically** in at least one test.
- [ ] Goal-backward: **NLS-04** response keys are asserted on the wire (`score`, `why_matched`, `thumbnail_url`, `metadata.missing_embeddings_count`).
- [ ] Goal-backward: **`test_semantic_rrf.py`** names and asserts hybrid matrix cases **(a)** empty FTS / `fts_no_match`, **(b)** FTS-only RRF, **(c)** both legs, **(d)** semantic index empty / FTS degradation.
