---
plan: 15-06
phase: 15
wave: 5
depends_on: [15-05]
files_modified:
  - lightroom_tagger/core/identity_service/_legacy.py
  - lightroom_tagger/core/identity_service/__init__.py
  - lightroom_tagger/core/identity_service/aggregates.py
  - lightroom_tagger/core/identity_service/ranking.py
  - lightroom_tagger/core/identity_service/style_fingerprint.py
  - lightroom_tagger/core/identity_service/suggest_post.py
autonomous: true
requirements: [REFACTOR-04]
---

# Plan 15-06 — `identity_service/` package split

Split `lightroom_tagger/core/identity_service.py` (697L) into `identity_service/` submodules per `15-RESEARCH.md`: `aggregates.py`, `ranking.py`, `style_fingerprint.py`, `suggest_post.py`; preserve `from lightroom_tagger.core.identity_service import …` for `api/identity.py`, `api/images/catalog.py`, and tests.

<task id="15-06-T01" type="execute">
  <title>Scaffold identity_service package</title>
  <read_first>
    - lightroom_tagger/core/identity_service.py — monolith
    - .planning/phases/15-service-modules-boundary-policy/15-RESEARCH.md — identity inventory
    - apps/visualizer/backend/api/identity.py — imported symbol list
  </read_first>
  <action>
    1. List all public and module-private names in `identity_service.py` (`compute_image_aggregate_scores`, `compute_single_image_aggregate_scores`, `rank_best_photos`, `build_style_fingerprint`, `suggest_what_to_post_next`, plus `_SCORES_BASE_SQL`, `_WORD_RE`, helpers).
    2. Create `lightroom_tagger/core/identity_service/`; move monolith to `_legacy.py`; remove flat `identity_service.py`.
    3. Create stub files: `aggregates.py`, `ranking.py`, `style_fingerprint.py`, `suggest_post.py` (docstring only).
    4. Add `__init__.py` barrel: `from ._legacy import (` … all names … `)`, `__all__` complete.
    5. `pytest lightroom_tagger/core/test_identity_service.py lightroom_tagger/core/test_database_stacks.py -x -q` passes.
  </action>
  <acceptance_criteria>
    - `test ! -e lightroom_tagger/core/identity_service.py` exits 0
    - `python -c "from lightroom_tagger.core.identity_service import rank_best_photos, build_style_fingerprint, suggest_what_to_post_next"` exits 0
    - `pytest lightroom_tagger/core/test_identity_service.py -x -q` exits 0
  </acceptance_criteria>
</task>

<task id="15-06-T02" type="execute">
  <title>Move aggregates.py and style_fingerprint.py out of _legacy</title>
  <read_first>
    - lightroom_tagger/core/identity_service/_legacy.py — lines around `_SCORES_BASE_SQL` through `compute_single_image_aggregate_scores`; `build_style_fingerprint` region
  </read_first>
  <action>
    1. Move `_SCORES_BASE_SQL`, `_WORD_RE`, `_RATIONALE_PREVIEW_MAX`, `_active_perspective_slugs`, `_default_min_perspectives`, `_tokenize_rationale`, `_truncate_rationale`, `compute_image_aggregate_scores`, `compute_single_image_aggregate_scores` into `identity_service/aggregates.py`.
    2. Move `_aggregate_histogram`, `build_style_fingerprint` into `identity_service/style_fingerprint.py`.
    3. Remove those definitions from `_legacy.py`; update `identity_service/__init__.py` to import from `.aggregates` / `.style_fingerprint` (not `_legacy`) for these names.
  </action>
  <acceptance_criteria>
    - `grep -q "^def compute_image_aggregate_scores" lightroom_tagger/core/identity_service/aggregates.py` exits 0
    - `grep -q "^def build_style_fingerprint" lightroom_tagger/core/identity_service/style_fingerprint.py` exits 0
    - `wc -l lightroom_tagger/core/identity_service/aggregates.py | awk '{print $1}'` ≤ 400
    - `wc -l lightroom_tagger/core/identity_service/style_fingerprint.py | awk '{print $1}'` ≤ 400
    - `pytest lightroom_tagger/core/test_identity_service.py -x -q` exits 0
  </acceptance_criteria>
</task>

<task id="15-06-T03" type="execute">
  <title>Move ranking.py and suggest_post.py; delete _legacy</title>
  <read_first>
    - lightroom_tagger/core/identity_service/_legacy.py — ranking helpers + `rank_best_photos`, `_posted_catalog_keys_sql`, `suggest_what_to_post_next`
    - lightroom_tagger/core/posting_analytics.py — import surface used by `suggest_what_to_post_next`
  </read_first>
  <action>
    1. Move `_image_meta_map`, `_stack_non_representative_keys`, `_stack_fields_for_image_keys`, `rank_best_photos` into `identity_service/ranking.py`.
    2. Move `_posted_catalog_keys_sql`, `suggest_what_to_post_next` into `identity_service/suggest_post.py`; keep `from lightroom_tagger.core.posting_analytics import get_posting_frequency` (or equivalent) **only** inside `suggest_post.py` or barrel edge to avoid cycles.
    3. Delete `_legacy.py`; update `__init__.py` to import all exported names from the four submodules + re-export `get_vision_model` dependencies **none** (identity does not need vision).
    4. Ensure `compute_single_image_aggregate_scores` remains importable from package root for `api/images/catalog.py`.
  </action>
  <acceptance_criteria>
    - `test ! -f lightroom_tagger/core/identity_service/_legacy.py` exits 0
    - `grep -q "^def suggest_what_to_post_next" lightroom_tagger/core/identity_service/suggest_post.py` exits 0
    - `test $(wc -l < lightroom_tagger/core/identity_service/ranking.py | tr -d " ") -le 400` exits 0
    - `test $(wc -l < lightroom_tagger/core/identity_service/suggest_post.py | tr -d " ") -le 400` exits 0
    - `pytest lightroom_tagger/core/ apps/visualizer/backend/tests/ -x -q` exits 0
  </acceptance_criteria>
</task>

## Verification

Nyquist sampling: [15-VALIDATION.md](./15-VALIDATION.md) (Per-Task Verification Map).

### Must-Haves (goal-backward)
- [ ] Flat `identity_service.py` removed; package barrel preserves API imports
- [ ] Each identity submodule ≤ 400 lines

### Automated checks
```bash
pytest lightroom_tagger/core/test_identity_service.py -x -q
python -c "from lightroom_tagger.core.identity_service import compute_single_image_aggregate_scores, rank_best_photos"
```

### Manual smoke
- [ ] `python -c "from lightroom_tagger.core.identity_service import build_style_fingerprint, rank_best_photos, suggest_what_to_post_next"` exits 0
