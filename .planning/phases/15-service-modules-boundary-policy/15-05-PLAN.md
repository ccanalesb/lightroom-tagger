---
plan: 15-05
phase: 15
wave: 4
depends_on: [15-04]
files_modified:
  - lightroom_tagger/core/matcher/_legacy.py
  - lightroom_tagger/core/matcher/__init__.py
  - lightroom_tagger/core/matcher/candidates.py
  - lightroom_tagger/core/matcher/text_scores.py
  - lightroom_tagger/core/matcher/description_batch.py
  - lightroom_tagger/core/matcher/vision_batch.py
  - lightroom_tagger/core/matcher/score_with_vision.py
  - lightroom_tagger/core/matcher/matching.py
  - lightroom_tagger/core/path_utils.py
autonomous: true
requirements: [REFACTOR-04]
---

# Plan 15-05 — `matcher/` package + path helper extraction

Split `lightroom_tagger/core/matcher.py` (747L) into `matcher/` per `.planning/.../15-RESEARCH.md` function inventory: `candidates.py`, `text_scores.py`, `description_batch.py`, `vision_batch.py`, `score_with_vision.py`, `matching.py`; extract UNC/mount normalization from `score_candidates_with_vision` into `path_utils.py`.

<task id="15-05-T01" type="execute">
  <title>Scaffold matcher package (_legacy + barrel + stubs)</title>
  <read_first>
    - lightroom_tagger/core/matcher.py — full monolith
    - .planning/phases/15-service-modules-boundary-policy/15-RESEARCH.md — matcher inventory table
    - .planning/phases/14-database-images-api-split/14-01-SUMMARY.md — scaffold sequence
  </read_first>
  <action>
    1. AST-list every module-level name exported today from `matcher.py` (functions, classes, module constants like `BATCH_MAX_TOKENS_ESCALATION`).
    2. Create `lightroom_tagger/core/matcher/`; move `matcher.py` → `matcher/_legacy.py` (no flat `core/matcher.py` remains).
    3. Create stubs with docstrings only: `candidates.py`, `text_scores.py`, `description_batch.py`, `vision_batch.py`, `score_with_vision.py`, `matching.py`.
    4. Add `matcher/__init__.py` that imports **all** historical symbols via `from ._legacy import (` … `)` + `__all__`.
    5. Run `pytest lightroom_tagger/core/test_matcher.py -x -q` — must pass before any body moves.
  </action>
  <acceptance_criteria>
    - `test -f lightroom_tagger/core/matcher/_legacy.py` exits 0
    - `test ! -e lightroom_tagger/core/matcher.py` exits 0
    - `python -c "from lightroom_tagger.core.matcher import score_candidates_with_vision, find_candidates_by_date, match_image"` exits 0
    - `pytest lightroom_tagger/core/test_matcher.py -x -q` exits 0
  </acceptance_criteria>
</task>

<task id="15-05-T02" type="execute">
  <title>Populate candidates.py and text_scores.py; rewire barrel</title>
  <read_first>
    - lightroom_tagger/core/matcher/_legacy.py — `query_by_exif`, `find_candidates_by_date`, `text_similarity`, `score_candidates`
    - lightroom_tagger/core/analyzer/image_prep.py — `VIDEO_EXTENSIONS` import source for `find_candidates_by_date`
  </read_first>
  <action>
    1. Move `query_by_exif`, `find_candidates_by_date` into `matcher/candidates.py` (preserve signatures; fix imports to use `lightroom_tagger.core.analyzer` barrel or `analyzer.image_prep` for `VIDEO_EXTENSIONS` as appropriate).
    2. Move `text_similarity`, `score_candidates` into `matcher/text_scores.py`.
    3. Delete those definitions from `_legacy.py`.
    4. Update `matcher/__init__.py` to import these four names from `.candidates` / `.text_scores` instead of `_legacy`.
  </action>
  <acceptance_criteria>
    - `grep -q "^def query_by_exif" lightroom_tagger/core/matcher/candidates.py` exits 0
    - `grep -q "^def score_candidates" lightroom_tagger/core/matcher/text_scores.py` exits 0
    - `test $(wc -l < lightroom_tagger/core/matcher/candidates.py | tr -d " ") -le 400` exits 0
    - `test $(wc -l < lightroom_tagger/core/matcher/text_scores.py | tr -d " ") -le 400` exits 0
    - `pytest lightroom_tagger/core/test_matcher.py -x -q` exits 0
  </acceptance_criteria>
</task>

<task id="15-05-T03" type="execute">
  <title>Populate description_batch.py and vision_batch.py</title>
  <read_first>
    - lightroom_tagger/core/matcher/_legacy.py — `_compute_desc_scores_for_candidates`, `BATCH_MAX_TOKENS_ESCALATION`, `_call_batch_chunk`
  </read_first>
  <action>
    1. Move `_compute_desc_scores_for_candidates` into `matcher/description_batch.py` (keep name underscore-prefixed).
    2. Move `BATCH_MAX_TOKENS_ESCALATION`, `_call_batch_chunk` into `matcher/vision_batch.py`.
    3. Remove moved symbols from `_legacy.py`; update `__init__.py` imports from new modules; ensure `score_candidates_with_vision` still importable from package root during transition via `_legacy` until T04 moves it.
  </action>
  <acceptance_criteria>
    - `grep -q "^def _compute_desc_scores_for_candidates" lightroom_tagger/core/matcher/description_batch.py` exits 0
    - `grep -q "BATCH_MAX_TOKENS_ESCALATION" lightroom_tagger/core/matcher/vision_batch.py` exits 0
    - `wc -l lightroom_tagger/core/matcher/description_batch.py lightroom_tagger/core/matcher/vision_batch.py` each prints first field ≤ 400
    - `pytest lightroom_tagger/core/test_matcher.py -x -q` exits 0
  </acceptance_criteria>
</task>

<task id="15-05-T04" type="execute">
  <title>Extract path normalization to path_utils; implement score_with_vision.py</title>
  <read_first>
    - lightroom_tagger/core/matcher/_legacy.py — `score_candidates_with_vision` body (path UNC / `mount_point` handling)
    - lightroom_tagger/core/path_utils.py — existing `resolve_catalog_path`
    - `.planning/.../15-RESEARCH.md` — “normalize_catalog_candidate_path” extraction note
  </read_first>
  <action>
    1. Add a new helper in `lightroom_tagger/core/path_utils.py` (name exactly `normalize_match_filesystem_path`) that encapsulates the UNC → `/Volumes/…` / `mount_point` join / existence-check prelude currently embedded in `score_candidates_with_vision` (move verbatim logic; same return types as the inlined code).
    2. Move `score_candidates_with_vision` into `matcher/score_with_vision.py`; replace the inlined path normalization block with a call to `normalize_match_filesystem_path(...)`.
    3. Wire imports inside `score_with_vision.py` to use `description_batch`, `vision_batch`, `text_scores`, `candidates` modules as needed (avoid circular imports).
    4. Remove `score_candidates_with_vision` from `_legacy.py`; export from `__init__.py` via `.score_with_vision`.
  </action>
  <acceptance_criteria>
    - `grep -q "^def normalize_match_filesystem_path" lightroom_tagger/core/path_utils.py` exits 0
    - `grep -q "^def score_candidates_with_vision" lightroom_tagger/core/matcher/score_with_vision.py` exits 0
    - `wc -l lightroom_tagger/core/matcher/score_with_vision.py | awk '{print $1}'` ≤ 400
    - `pytest lightroom_tagger/core/test_matcher.py -x -q` exits 0
  </acceptance_criteria>
</task>

<task id="15-05-T05" type="execute">
  <title>Populate matching.py and delete _legacy.py</title>
  <read_first>
    - lightroom_tagger/core/matcher/_legacy.py — remaining `match_image`, `match_batch`
    - lightroom_tagger/core/matcher/__init__.py
  </read_first>
  <action>
    1. Move `match_image`, `match_batch` into `matcher/matching.py`; import `score_candidates_with_vision` from `.score_with_vision` and DB helpers as needed.
    2. Delete `_legacy.py` when it contains no executable code.
    3. Update `__init__.py` to import exclusively from `matcher` submodules (no `_legacy`).
  </action>
  <acceptance_criteria>
    - `test ! -f lightroom_tagger/core/matcher/_legacy.py` exits 0
    - `grep -q "_legacy" lightroom_tagger/core/matcher/__init__.py` exits 1
    - `python -c "from lightroom_tagger.core.matcher import match_batch, score_candidates_with_vision"` exits 0
    - `pytest lightroom_tagger/core/ -x -q` exits 0
  </acceptance_criteria>
</task>

## Verification

Nyquist sampling: [15-VALIDATION.md](./15-VALIDATION.md) (Per-Task Verification Map).

### Must-Haves (goal-backward)
- [ ] `matcher/` package exists; flat `core/matcher.py` is gone
- [ ] Every new matcher submodule file is ≤ 400 lines (`wc -l` per file)
- [ ] `normalize_match_filesystem_path` lives in `path_utils.py`

### Automated checks
```bash
pytest lightroom_tagger/core/test_matcher.py -x -q
python -c "from lightroom_tagger.core.matcher import find_candidates_by_date, match_image, score_candidates_with_vision"
```

### Manual smoke
- [ ] `python -c "from lightroom_tagger.scripts.match_instagram_dump import main"` imports successfully
