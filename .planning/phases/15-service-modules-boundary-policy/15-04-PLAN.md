---
plan: 15-04
phase: 15
wave: 3
depends_on: [15-03]
files_modified:
  - lightroom_tagger/core/analyzer/_legacy.py
  - lightroom_tagger/core/analyzer/__init__.py
  - lightroom_tagger/core/analyzer/image_prep.py
  - lightroom_tagger/core/analyzer/image_inspect.py
  - lightroom_tagger/core/analyzer/vision_compare.py
  - lightroom_tagger/core/analyzer/description.py
  - lightroom_tagger/core/vision_client.py
  - lightroom_tagger/core/scoring_service.py
  - lightroom_tagger/core/test_analyzer.py
  - apps/visualizer/backend/jobs/handlers/matching.py
  - lightroom_tagger/cli.py
  - lightroom_tagger/scripts/analyze_instagram_images.py
  - lightroom_tagger/lightroom/enricher.py
  - lightroom_tagger/instagram/crawler.py
autonomous: true
requirements: [REFACTOR-04]
---

# Plan 15-04 — Analyzer submodule population + ADR-0001 retirements

Move implementation from `analyzer/_legacy.py` into `image_prep.py`, `image_inspect.py`, `vision_compare.py`, and `description.py` per ADR-0001; retire `analyze_image`, `run_local_agent`, `run_vision_ollama`; route remaining vision paths through `ProviderRegistry` / `_compare_via_provider` / `_describe_image_via_provider`.

<task id="15-04-T01" type="execute">
  <title>Extract image_prep.py and image_inspect.py from _legacy</title>
  <read_first>
    - lightroom_tagger/core/analyzer/_legacy.py — symbols per ADR-0001 mapping
    - docs/adr/0001-split-analyzer.md — exact symbol ownership
    - lightroom_tagger/core/analyzer/__init__.py — current export list
  </read_first>
  <action>
    1. Implement `lightroom_tagger/core/analyzer/image_prep.py` containing: `RAW_EXTENSIONS`, `VIDEO_EXTENSIONS`, `VISION_MAX_DIMENSION`, `VISION_COMPRESS_QUALITY`, `compress_image`, `convert_raw_to_jpg`, `get_viewable_path` (move bodies from `_legacy.py`; preserve signatures and docstrings).
    2. Implement `lightroom_tagger/core/analyzer/image_inspect.py` containing: `compute_phash`, `extract_exif`.
    3. Remove those symbols’ definitions from `_legacy.py`; add imports in `_legacy.py` from `.image_prep` and `.image_inspect` only if still needed as reassembly layer — prefer updating `__init__.py` to import these symbols directly from `.image_prep` / `.image_inspect` and shrinking `_legacy.py` accordingly.
    4. Update `analyzer/__init__.py` so `image_prep` / `image_inspect` symbols are imported from their submodules (`from .image_prep import (` … `)`, `from .image_inspect import (` … `)`) and are **not** duplicated from `_legacy`.
  </action>
  <acceptance_criteria>
    - `wc -l lightroom_tagger/core/analyzer/image_prep.py | awk '{print $1}'` prints an integer ≤ 400
    - `wc -l lightroom_tagger/core/analyzer/image_inspect.py | awk '{print $1}'` prints an integer ≤ 400
    - `python -c "from lightroom_tagger.core.analyzer.image_prep import RAW_EXTENSIONS, compress_image; from lightroom_tagger.core.analyzer.image_inspect import compute_phash"` exits 0
    - `pytest lightroom_tagger/core/test_analyzer.py -x -q -k "compress or phash or exif or raw"` exits 0 (if no such tests match, run full `pytest lightroom_tagger/core/test_analyzer.py -x -q` instead)
  </acceptance_criteria>
</task>

<task id="15-04-T02" type="execute">
  <title>Extract description.py (describe pipeline) and wire imports</title>
  <read_first>
    - lightroom_tagger/core/analyzer/_legacy.py — `DESCRIPTION_PROMPT`, `build_description_prompt`, `parse_description_response`, `describe_image`, `_describe_image_via_provider`, any `run_local_agent` usage inside `describe_image`
    - lightroom_tagger/core/vision_client.py — imports from `analyzer` today (`build_description_prompt`, `parse_description_response` etc.)
    - docs/adr/0001-split-analyzer.md
  </read_first>
  <action>
    1. Implement `lightroom_tagger/core/analyzer/description.py` with: module constant `DESCRIPTION_PROMPT`, functions `build_description_prompt`, `parse_description_response`, `describe_image`, `_describe_image_via_provider` moved verbatim from `_legacy.py` (adjust imports inside the module: `from lightroom_tagger.core.exceptions import …` as needed; import `compress_image`/`get_viewable_path` from `.image_prep` if referenced).
    2. Remove those definitions from `_legacy.py`.
    3. Refactor `describe_image` in `description.py` so all generation flows go through `_describe_image_via_provider` (no `run_local_agent`); complete before task 15-04-T04 deletes `run_local_agent`.
    4. Update `vision_client.py` imports to pull `build_description_prompt` / `parse_description_response` from `lightroom_tagger.core.analyzer.description` **or** from the public `lightroom_tagger.core.analyzer` barrel; pick one style and apply consistently.
    5. Update `analyzer/__init__.py` to import description symbols from `.description`.
  </action>
  <acceptance_criteria>
    - `! grep -q "^def describe_image" lightroom_tagger/core/analyzer/_legacy.py` exits 0 (definition lives in `description.py`)
    - `grep -q "^def _describe_image_via_provider" lightroom_tagger/core/analyzer/description.py` exits 0
    - `wc -l lightroom_tagger/core/analyzer/description.py | awk '{print $1}'` ≤ 400
    - `pytest lightroom_tagger/core/test_analyzer.py lightroom_tagger/core/test_vision_client.py -x -q` exits 0
  </acceptance_criteria>
</task>

<task id="15-04-T03" type="execute">
  <title>Extract vision_compare.py and remove run_vision_ollama / None-provider shortcut</title>
  <read_first>
    - lightroom_tagger/core/analyzer/_legacy.py — `MAX_TOKENS_ESCALATION`, `_compare_via_provider`, `compare_with_vision`, `parse_vision_response`, `vision_score`, `run_vision_ollama`
    - docs/adr/0001-split-analyzer.md — retirement of `run_vision_ollama`
  </read_first>
  <action>
    1. Implement `lightroom_tagger/core/analyzer/vision_compare.py` containing moved implementations for `MAX_TOKENS_ESCALATION`, `_compare_via_provider`, `compare_with_vision`, `parse_vision_response`, `vision_score` from `_legacy.py`.
    2. Refactor `compare_with_vision` so the **`provider_id is None`** branch calls the same provider-resolution path as `_compare_via_provider` (via `ProviderRegistry` / default provider ID) and **never** calls `run_vision_ollama`.
    3. Delete the `run_vision_ollama` function entirely from the codebase (no shim).
    4. Remove vision-compare symbols from `_legacy.py`; update `__init__.py` to import from `.vision_compare`.
    5. Update or delete tests in `lightroom_tagger/core/test_analyzer.py` that patch `run_vision_ollama` so they instead mock `vision_client.compare_images` / provider stack or `_compare_via_provider` behavior — after this task `rg "run_vision_ollama" lightroom_tagger/core/test_analyzer.py` returns zero lines.
  </action>
  <acceptance_criteria>
    - `rg "^def run_vision_ollama" lightroom_tagger` returns zero lines
    - `grep -q "^def _compare_via_provider" lightroom_tagger/core/analyzer/vision_compare.py` exits 0
    - `wc -l lightroom_tagger/core/analyzer/vision_compare.py | awk '{print $1}'` ≤ 400
    - `pytest lightroom_tagger/core/test_analyzer.py -x -q` exits 0
  </acceptance_criteria>
</task>

<task id="15-04-T04" type="execute">
  <title>Remove run_local_agent; migrate scoring_service describe path</title>
  <read_first>
    - lightroom_tagger/core/scoring_service.py — `run_local_agent` import and call site (~line 240)
    - lightroom_tagger/core/analyzer/description.py — ensure `_describe_image_via_provider` is the supported seam
    - lightroom_tagger/core/analyzer/_legacy.py — any remaining `run_local_agent`
  </read_first>
  <action>
    1. Replace scoring path in `scoring_service.py` that calls `run_local_agent(compressed, user_prompt=...)` with a call into the OpenAI-compat describe stack (e.g. `from lightroom_tagger.core.analyzer import _describe_image_via_provider` with an explicit `provider_id` resolved through the same registry used elsewhere, or `vision_client` helper if one already wraps text generation) — **no** direct `ollama.chat` from `scoring_service.py`.
    2. Remove `run_local_agent` import from `scoring_service.py`.
    3. Delete `run_local_agent` function definition from the analyzer package (last location: `description.py` or `_legacy.py` depending on prior moves).
    4. Update or delete `test_run_local_agent_calls_ollama` and any tests referencing `run_local_agent` in `lightroom_tagger/core/test_analyzer.py`; after this task `rg "run_local_agent" lightroom_tagger/core/test_analyzer.py` returns zero lines.
  </action>
  <acceptance_criteria>
    - `rg "^def run_local_agent" lightroom_tagger` returns zero lines
    - `grep -q "run_local_agent" lightroom_tagger/core/scoring_service.py` exits 1 (no substring `run_local_agent`)
    - `pytest lightroom_tagger/core/test_scoring_service.py lightroom_tagger/core/ -x -q` exits 0 (if `test_scoring_service.py` does not exist, run `pytest lightroom_tagger/core/ -x -q`)
  </acceptance_criteria>
</task>

<task id="15-04-T05" type="execute">
  <title>Retire analyze_image — migrate callers and delete function</title>
  <read_first>
    - apps/visualizer/backend/jobs/handlers/matching.py — `analyze_image` usage (~lines 322, 395)
    - lightroom_tagger/cli.py — import + call ~505
    - lightroom_tagger/scripts/analyze_instagram_images.py
    - lightroom_tagger/lightroom/enricher.py
    - lightroom_tagger/instagram/crawler.py
    - lightroom_tagger/core/test_analyzer.py — `test_analyze_image_returns_all_signals`
  </read_first>
  <action>
    1. Replace every production use of `analyze_image(path)` with an inline sequence equivalent to the old function body: `phash = compute_phash(path)`, `exif = extract_exif(path)`, `structured = describe_image(path)`, then build the same result dict keys `phash`, `exif`, `description` (`structured.get('summary', '')`), `structured_description`.
    2. Update imports at each callsite to import `compute_phash`, `extract_exif`, `describe_image` from `lightroom_tagger.core.analyzer` (barrel) instead of `analyze_image`.
    3. Rewrite `test_analyze_image_returns_all_signals` in `test_analyzer.py` to assert the composed pipeline (patch/mocking `compute_phash`, `extract_exif`, `describe_image` as needed) without calling `analyze_image`.
    4. Delete the `analyze_image` function definition from the analyzer package.
    5. Remove `analyze_image` from `analyzer/__init__.py` exports and `__all__`.
  </action>
  <acceptance_criteria>
    - `rg "\banalyze_image\b" lightroom_tagger apps --glob "*.py"` returns zero lines
    - `python -c "from lightroom_tagger.core.analyzer import compute_phash, describe_image"` exits 0
    - `pytest lightroom_tagger/core/test_analyzer.py -x -q` exits 0
  </acceptance_criteria>
</task>

<task id="15-04-T06" type="execute">
  <title>Delete _legacy.py when empty; verify package imports</title>
  <read_first>
    - lightroom_tagger/core/analyzer/_legacy.py — remainder after extractions
    - lightroom_tagger/core/analyzer/__init__.py — must list only submodule imports
  </read_first>
  <action>
    1. Move any remaining stray constants/functions out of `_legacy.py` into the correct submodule (or delete dead code if proven unused by `rg` + tests).
    2. When `_legacy.py` contains no code (or only a re-export stub), delete `lightroom_tagger/core/analyzer/_legacy.py`.
    3. Ensure `analyzer/__init__.py` contains explicit imports from `.image_prep`, `.image_inspect`, `.description`, `.vision_compare` only (plus config-backed `get_vision_model`/`get_description_model` imports if still re-exported from barrel).
    4. Confirm `get_vision_model` / `get_description_model` are imported from `lightroom_tagger.core.config` inside `__init__.py` if exported at package root.
  </action>
  <acceptance_criteria>
    - `test ! -f lightroom_tagger/core/analyzer/_legacy.py` exits 0
    - `grep -q "_legacy" lightroom_tagger/core/analyzer/__init__.py` exits 1
    - `pytest lightroom_tagger/core/ -x -q` exits 0
    - `python -c "from lightroom_tagger.core.analyzer import compress_image, compute_phash, compare_with_vision, describe_image, get_vision_model"` exits 0
  </acceptance_criteria>
</task>

## Verification

Nyquist sampling: [15-VALIDATION.md](./15-VALIDATION.md) (Per-Task Verification Map).

### Must-Haves (goal-backward)
- [ ] `rg "^def (analyze_image|run_local_agent|run_vision_ollama)\b" lightroom_tagger` returns zero lines
- [ ] All four analyzer submodules exist with real implementations; `_legacy.py` is gone

**Note (D-02 alignment):** Analyzer-specific path helpers (`compress_image`, `get_viewable_path`) land in `analyzer/image_prep` per ADR-0001; cross-module path helpers (UNC/mount normalization) go to `path_utils` — both satisfy D-02's aggressive extraction intent.

### Automated checks
```bash
pytest apps/visualizer/backend/tests/ -x -q
pytest lightroom_tagger/core/ -x -q
rg "analyze_image|run_local_agent|run_vision_ollama" lightroom_tagger apps --glob "*.py"
```

### Manual smoke
- [ ] `python -c "from lightroom_tagger.core.matcher import score_candidates_with_vision"` imports (still depends on analyzer barrel)
