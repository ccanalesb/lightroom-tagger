---
plan: 15-01
phase: 15
wave: 1
depends_on: []
files_modified:
  - lightroom_tagger/core/exceptions/__init__.py
  - lightroom_tagger/core/exceptions/provider_errors.py
  - lightroom_tagger/core/exceptions/db_errors.py
  - lightroom_tagger/core/database/stacks.py
  - lightroom_tagger/core/database/__init__.py
  - lightroom_tagger/core/provider_errors.py
  - apps/visualizer/backend/api/descriptions.py
  - apps/visualizer/backend/api/images/search.py
  - apps/visualizer/backend/jobs/handlers/common.py
  - apps/visualizer/backend/tests/test_descriptions_api.py
  - lightroom_tagger/core/analyzer.py
  - lightroom_tagger/core/fallback.py
  - lightroom_tagger/core/matcher.py
  - lightroom_tagger/core/nl_catalog_search.py
  - lightroom_tagger/core/retry.py
  - lightroom_tagger/core/scoring_service.py
  - lightroom_tagger/core/test_fallback.py
  - lightroom_tagger/core/test_provider_errors.py
  - lightroom_tagger/core/test_retry.py
  - lightroom_tagger/core/test_vision_client.py
  - lightroom_tagger/core/vision_client.py
autonomous: true
requirements: [REFACTOR-04]
---

## Nyquist validation

Sampling strategy, per-wave scopes, and sign-off criteria live in [.planning/phases/15-service-modules-boundary-policy/15-VALIDATION.md](./15-VALIDATION.md).

**Automated commands (this wave / baseline):**

- Per-task: follow the [Per-Task Verification Map](./15-VALIDATION.md#per-task-verification-map); minimum after core-affecting commits — `pytest lightroom_tagger/core/ -x -q`
- End of wave 1 — `pytest lightroom_tagger/core/ -x -q`
- Architecture test and size script — added in plan **15-07** only: `pytest lightroom_tagger/core/test_architecture.py -x -q`, `bash scripts/check_core_file_sizes.sh`

---

# Plan 15-01 — `exceptions/` package (ADR-0004 prerequisite)

Establish `lightroom_tagger/core/exceptions/` as the single import root for provider and DB-layer errors; migrate `StackMutationError` out of `database/stacks.py`; retire direct `from lightroom_tagger.core.provider_errors` imports.

<task id="15-01-T01" type="execute">
  <title>Create exceptions package with provider_errors module</title>
  <read_first>
    - docs/adr/0004-shared-exceptions.md — target package layout and import rules
    - lightroom_tagger/core/provider_errors.py — full class bodies, RETRYABLE_ERRORS, NOT_RETRYABLE_ERRORS to preserve
    - lightroom_tagger/core/test_provider_errors.py — asserts imports that must keep working
  </read_first>
  <action>
    1. Create directory `lightroom_tagger/core/exceptions/`.
    2. Add `lightroom_tagger/core/exceptions/provider_errors.py` by moving the entire contents of `lightroom_tagger/core/provider_errors.py` unchanged (all classes: ProviderError, RateLimitError, TimeoutError, ConnectionError, ModelUnavailableError, ContextLengthError, PayloadTooLargeError, AuthenticationError, InvalidRequestError; constants RETRYABLE_ERRORS and NOT_RETRYABLE_ERRORS).
    3. Add `lightroom_tagger/core/exceptions/__init__.py` that re-exports every public name from `provider_errors` via explicit imports (e.g. `from .provider_errors import (...)`) and defines `__all__` listing every re-exported symbol.
  </action>
  <acceptance_criteria>
    - `test -f lightroom_tagger/core/exceptions/__init__.py` exits 0
    - `grep -q "class ProviderError" lightroom_tagger/core/exceptions/provider_errors.py` exits 0
    - `grep -q "RETRYABLE_ERRORS" lightroom_tagger/core/exceptions/provider_errors.py` exits 0
    - `python -c "from lightroom_tagger.core.exceptions import ProviderError, RateLimitError, RETRYABLE_ERRORS"` exits 0
  </acceptance_criteria>
</task>

<task id="15-01-T02" type="execute">
  <title>Add db_errors.StackMutationError and wire database/stacks</title>
  <read_first>
    - lightroom_tagger/core/database/stacks.py — StackMutationError class definition (lines 9–14)
    - lightroom_tagger/core/database/__init__.py — `from .stacks import (` block including StackMutationError
    - docs/adr/0004-shared-exceptions.md
  </read_first>
  <action>
    1. Create `lightroom_tagger/core/exceptions/db_errors.py` containing the `StackMutationError` class exactly as today in `stacks.py` (subclass `ValueError`, `__init__(self, message: str, *, status_code: int = 400)` calling `super().__init__(message)` and setting `self.status_code`).
    2. Update `lightroom_tagger/core/exceptions/__init__.py` to re-export `StackMutationError` from `db_errors` and append `StackMutationError` to `__all__`.
    3. Remove the `StackMutationError` class definition from `lightroom_tagger/core/database/stacks.py`; add top import `from lightroom_tagger.core.exceptions import StackMutationError` (or `from lightroom_tagger.core.exceptions.db_errors import StackMutationError` if inner imports are required — prefer the package root re-export per ADR).
    4. In `lightroom_tagger/core/database/__init__.py`, remove `StackMutationError` from the `from .stacks import (` tuple; add `from lightroom_tagger.core.exceptions import StackMutationError` before or alongside stack imports; keep `StackMutationError` in the module’s public `__all__` / re-export surface unchanged for `from lightroom_tagger.core.database import StackMutationError`.
  </action>
  <acceptance_criteria>
    - `grep -n "^class StackMutationError" lightroom_tagger/core/database/stacks.py` returns no matches (exit 1 from grep is expected; confirm with `! grep -q "^class StackMutationError" lightroom_tagger/core/database/stacks.py`)
    - `grep -q "^class StackMutationError" lightroom_tagger/core/exceptions/db_errors.py` exits 0
    - `python -c "from lightroom_tagger.core.database import StackMutationError; from lightroom_tagger.core.exceptions import StackMutationError as E; assert StackMutationError is E"` exits 0
    - `pytest lightroom_tagger/core/test_database_stacks.py -x -q` exits 0
  </acceptance_criteria>
</task>

<task id="15-01-T03" type="execute">
  <title>Bulk-migrate imports to exceptions and delete provider_errors module</title>
  <read_first>
    - lightroom_tagger/core/provider_errors.py — symbol list to preserve in exceptions package
    - docs/adr/0004-shared-exceptions.md — one canonical import root
  </read_first>
  <action>
    1. For every `.py` file under `lightroom_tagger/` and `apps/visualizer/` that contains `lightroom_tagger.core.provider_errors`, replace with `from lightroom_tagger.core.exceptions import ...` preserving the imported names (known set from repo: `apps/visualizer/backend/api/descriptions.py`, `api/images/search.py`, `jobs/handlers/common.py`, `tests/test_descriptions_api.py`, `lightroom_tagger/core/analyzer.py`, `core/fallback.py`, `core/matcher.py`, `core/nl_catalog_search.py`, `core/retry.py`, `core/scoring_service.py`, `core/vision_client.py`, `core/test_fallback.py`, `core/test_provider_errors.py`, `core/test_retry.py`, `core/test_vision_client.py`; update lazy `from lightroom_tagger.core.provider_errors import ...` blocks inside `analyzer.py` (lines ~385, ~601, ~610) and `scoring_service.py` (~208) to `lightroom_tagger.core.exceptions`).
    2. Delete `lightroom_tagger/core/provider_errors.py`.
    3. Ensure `lightroom_tagger/core/test_provider_errors.py` imports only from `lightroom_tagger.core.exceptions`.
  </action>
  <acceptance_criteria>
    - `rg "from lightroom_tagger\.core\.provider_errors|import lightroom_tagger\.core\.provider_errors" --glob "*.py" lightroom_tagger apps` returns zero lines
    - `test ! -f lightroom_tagger/core/provider_errors.py` exits 0
    - `rg "from lightroom_tagger\.core\.exceptions import" --glob "*.py" lightroom_tagger/core/vision_client.py` returns at least one line
    - `pytest lightroom_tagger/core/test_provider_errors.py lightroom_tagger/core/test_fallback.py lightroom_tagger/core/test_retry.py lightroom_tagger/core/test_vision_client.py -x -q` exits 0
    - `pytest lightroom_tagger/core/ -x -q` exits 0
  </acceptance_criteria>
</task>

## Verification

Nyquist sampling and the per-task command matrix: [15-VALIDATION.md](./15-VALIDATION.md).

### Must-Haves (goal-backward)
- [ ] `lightroom_tagger/core/exceptions/__init__.py` exists and re-exports provider errors plus `StackMutationError`
- [ ] `StackMutationError` is defined only under `lightroom_tagger/core/exceptions/db_errors.py`, not in `database/stacks.py`
- [ ] No remaining imports of `lightroom_tagger.core.provider_errors` anywhere in `lightroom_tagger/` or `apps/`

### Automated checks
```bash
pytest lightroom_tagger/core/ -x -q
python -c "from lightroom_tagger.core.exceptions import StackMutationError, ProviderError, RETRYABLE_ERRORS"
python -c "from lightroom_tagger.core.database import StackMutationError"
```

### Manual smoke
- [ ] `python -c "from apps.visualizer.backend.api.images.stacks import …"` not required if unchanged; confirm `from lightroom_tagger.core.database import StackMutationError` still works for API code paths
