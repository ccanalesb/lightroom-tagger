---
plan: 15-02
phase: 15
wave: 2
depends_on: [15-01]
files_modified:
  - lightroom_tagger/core/config.py
  - lightroom_tagger/core/analyzer.py
  - lightroom_tagger/core/description_service.py
  - lightroom_tagger/core/scoring_service.py
  - lightroom_tagger/core/matcher.py
  - lightroom_tagger/core/test_analyzer.py
  - lightroom_tagger/core/test_matcher.py
  - lightroom_tagger/scripts/match_instagram_dump.py
autonomous: true
requirements: [REFACTOR-04]
---

# Plan 15-02 — Model config helpers in `config.py` (ADR-0001)

Move `get_vision_model()` and `get_description_model()` implementations from `analyzer.py` into `config.py`; update production and test imports so matcher/scoring/describe paths resolve models via `config` (analyzer may re-export for stability until package split).

<task id="15-02-T01" type="execute">
  <title>Implement get_vision_model and get_description_model in config.py</title>
  <read_first>
    - lightroom_tagger/core/config.py — existing `Config.vision_model`, `load_config`, imports
    - lightroom_tagger/core/analyzer.py — lines 21–38 (`get_vision_model`, `VISION_MODEL` assignment, `get_description_model`)
  </read_first>
  <action>
    1. Add `import os` to `config.py` if not already present (file already uses `os` elsewhere — skip if redundant).
    2. Add two functions to `config.py`:
       - `def get_vision_model() -> str:` — body: if `'VISION_MODEL' in os.environ`, return `os.environ['VISION_MODEL']`; else `return load_config().vision_model`.
       - `def get_description_model() -> str:` — body: if `'DESCRIPTION_VISION_MODEL' in os.environ`, return `os.environ['DESCRIPTION_VISION_MODEL']`; else `return get_vision_model()`.
    3. Remove the `get_vision_model` and `get_description_model` function definitions from `analyzer.py` (lines 21–38); remove the module-level `VISION_MODEL = os.environ.get(...)` line (line 27) if no remaining references inside `analyzer.py`.
    4. At top of `analyzer.py` after existing imports, add: `from lightroom_tagger.core.config import get_description_model, get_vision_model` so symbols remain attributes of `lightroom_tagger.core.analyzer` for `from lightroom_tagger.core.analyzer import get_vision_model` callers.
  </action>
  <acceptance_criteria>
    - `grep -q "^def get_vision_model" lightroom_tagger/core/config.py` exits 0
    - `grep -q "^def get_description_model" lightroom_tagger/core/config.py` exits 0
    - `! grep -q "^def get_vision_model" lightroom_tagger/core/analyzer.py` exits 0
    - `python -c "from lightroom_tagger.core.config import get_vision_model, get_description_model; from lightroom_tagger.core.analyzer import get_vision_model as gv2; assert gv2 is get_vision_model"` exits 0
  </acceptance_criteria>
</task>

<task id="15-02-T02" type="execute">
  <title>Point services, matcher, and scripts at config getters</title>
  <read_first>
    - lightroom_tagger/core/scoring_service.py — current imports from `analyzer` (lines ~20–30)
    - lightroom_tagger/core/description_service.py — line 8 import line
    - lightroom_tagger/core/matcher.py — every `from lightroom_tagger.core.analyzer import get_vision_model` site
    - lightroom_tagger/scripts/match_instagram_dump.py — line 21 import
    - lightroom_tagger/core/test_matcher.py — patch targets string `lightroom_tagger.core.analyzer.get_vision_model`
  </read_first>
  <action>
    1. In `scoring_service.py`, add a dedicated line `from lightroom_tagger.core.config import get_description_model` and remove `get_description_model` from the `from lightroom_tagger.core.analyzer import (` block; keep all other analyzer imports unchanged.
    2. In `description_service.py`, replace the single analyzer import line with two imports: `from lightroom_tagger.core.analyzer import VIDEO_EXTENSIONS, describe_image` and `from lightroom_tagger.core.config import get_description_model`.
    3. In `matcher.py`, replace `from lightroom_tagger.core.analyzer import get_vision_model` (and combined imports) so `get_vision_model` comes from `lightroom_tagger.core.config` while `compare_with_vision`, `vision_score`, `VIDEO_EXTENSIONS` still come from `lightroom_tagger.core.analyzer` as today.
    4. In `scripts/match_instagram_dump.py`, replace `from lightroom_tagger.core.analyzer import get_vision_model` with `from lightroom_tagger.core.config import get_vision_model`.
    5. In `core/test_matcher.py`, replace every patch path `lightroom_tagger.core.analyzer.get_vision_model` with `lightroom_tagger.core.config.get_vision_model`.
  </action>
  <acceptance_criteria>
    - `grep -E '^from lightroom_tagger\.core\.config import .*\bget_description_model\b' lightroom_tagger/core/scoring_service.py` exits 0
    - `grep -q "from lightroom_tagger.core.analyzer import VIDEO_EXTENSIONS, describe_image" lightroom_tagger/core/description_service.py` exits 0
    - `grep "from lightroom_tagger.core.config import get_vision_model" lightroom_tagger/core/matcher.py` exits 0 (or equivalent line that imports `get_vision_model` from config)
    - `grep "core.analyzer.get_vision_model" lightroom_tagger/core/test_matcher.py` returns zero lines
    - `pytest lightroom_tagger/core/test_matcher.py lightroom_tagger/core/test_analyzer.py lightroom_tagger/core/test_description_service.py -x -q` exits 0
  </acceptance_criteria>
</task>

## Verification

Nyquist sampling and per-wave command matrix: [15-VALIDATION.md](./15-VALIDATION.md) (see Per-Task Verification Map).

### Must-Haves (goal-backward)
- [ ] `config.py` defines `get_vision_model` and `get_description_model` with the same env/config precedence as pre-move `analyzer.py`
- [ ] No duplicate `def get_vision_model` / `def get_description_model` in `analyzer.py`

### Automated checks
```bash
pytest lightroom_tagger/core/ -x -q
python -c "from lightroom_tagger.core.config import get_vision_model; from lightroom_tagger.core.analyzer import get_vision_model as a; assert a is get_vision_model"
```

### Manual smoke
- [ ] `python -c "from lightroom_tagger.scripts.match_instagram_dump import main"` imports without ImportError (module-level import of `get_vision_model`)
