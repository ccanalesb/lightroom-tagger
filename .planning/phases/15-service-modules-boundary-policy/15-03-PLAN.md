---
plan: 15-03
phase: 15
wave: 3
depends_on: [15-02]
files_modified:
  - lightroom_tagger/core/analyzer/_legacy.py
  - lightroom_tagger/core/analyzer/__init__.py
  - lightroom_tagger/core/analyzer/image_prep.py
  - lightroom_tagger/core/analyzer/image_inspect.py
  - lightroom_tagger/core/analyzer/vision_compare.py
  - lightroom_tagger/core/analyzer/description.py
autonomous: true
requirements: [REFACTOR-04]
---

# Plan 15-03 — `analyzer/` package scaffold (Phase 14 pattern)

Replace flat `lightroom_tagger/core/analyzer.py` with package `lightroom_tagger/core/analyzer/`: move the monolith to `_legacy.py`, add four domain stub modules per ADR-0001, barrel-reexport the full prior namespace with explicit `from ._legacy import (...)` (no `import *`).

<task id="15-03-T01" type="execute">
  <title>Baselinetop-level definable symbols on analyzer monolith</title>
  <read_first>
    - lightroom_tagger/core/analyzer.py — full file before rename
    - .planning/phases/14-database-images-api-split/14-01-SUMMARY.md — AST/barrel discipline (128 names)
    - .planning/phases/15-service-modules-boundary-policy/15-03-SUMMARY.md — **created by this task**: committed sorted export list (symbol count / names) for barrel T02
  </read_first>
  <action>
    1. Run an AST walk over `lightroom_tagger/core/analyzer.py` collecting every top-level `FunctionDef`, `AsyncFunctionDef`, `ClassDef`, and module-level `Assign` / `AnnAssign` targets that are public-style names (include leading-underscore private names if they are imported by tests or siblings — match Phase 14 rule: export every assign target and definable used as module API).
    2. Record the sorted export list in commit message or phase notes; expected names include at minimum: `RAW_EXTENSIONS`, `VIDEO_EXTENSIONS`, `VISION_MAX_DIMENSION`, `VISION_COMPRESS_QUALITY`, `DESCRIPTION_PROMPT`, `MAX_TOKENS_ESCALATION`, `build_description_prompt`, `parse_description_response`, `compress_image`, `convert_raw_to_jpg`, `get_viewable_path`, `analyze_image`, `compute_phash`, `extract_exif`, `describe_image`, `_describe_image_via_provider`, `run_local_agent`, `run_external_agent`, `compare_with_vision`, `_compare_via_provider`, `parse_vision_response`, `run_vision_ollama`, `vision_score`, `get_vision_model`, `get_description_model` (the latter two re-imported from config in the monolith after plan 15-02).
  </action>
  <acceptance_criteria>
    - Documented export list is committed or appears in `15-03-SUMMARY.md` with ≥ 20 symbol names
    - The list includes `compress_image`, `compute_phash`, `compare_with_vision`, `describe_image`, `get_vision_model`, `analyze_image`
  </acceptance_criteria>
</task>

<task id="15-03-T02" type="execute">
  <title>Create analyzer/ package with _legacy + stubs + barrel</title>
  <read_first>
    - lightroom_tagger/core/analyzer.py — source to move
    - lightroom_tagger/core/database/__init__.py — barrel-only import style reference
    - docs/adr/0001-split-analyzer.md — submodule filenames
  </read_first>
  <action>
    1. Create directory `lightroom_tagger/core/analyzer/`.
    2. `git mv` (or move) `lightroom_tagger/core/analyzer.py` → `lightroom_tagger/core/analyzer/_legacy.py` (no sibling `analyzer.py` file remains).
    3. Create stub files containing only a module docstring: `image_prep.py`, `image_inspect.py`, `vision_compare.py`, `description.py`.
    4. Create `lightroom_tagger/core/analyzer/__init__.py` that imports every exported symbol from `._legacy` via a single parenthesized `from ._legacy import (` … `)` block listing all names from T01, plus `__all__` tuple matching that list exactly.
    5. Fix any relative-path or `__file__` assumptions in `_legacy.py` if introduced (analyzer currently has none per `15-CONTEXT.md`; verify with `rg "__file__" lightroom_tagger/core/analyzer/_legacy.py` → empty).
  </action>
  <acceptance_criteria>
    - `test -d lightroom_tagger/core/analyzer && test -f lightroom_tagger/core/analyzer/__init__.py && test -f lightroom_tagger/core/analyzer/_legacy.py` exits 0
    - `test ! -e lightroom_tagger/core/analyzer.py` exits 0
    - `python -c "from lightroom_tagger.core.analyzer import compress_image, compute_phash, compare_with_vision, describe_image, get_vision_model"` exits 0
    - `pytest lightroom_tagger/core/test_analyzer.py -x -q` exits 0
  </acceptance_criteria>
</task>

## Verification

Nyquist sampling: [15-VALIDATION.md](./15-VALIDATION.md) (Per-Task Verification Map).

### Must-Haves (goal-backward)
- [ ] Import path `from lightroom_tagger.core.analyzer import …` still resolves (package replaces module)
- [ ] All four ADR-0001 submodule files exist under `analyzer/` (stubs acceptable in this plan)

### Automated checks
```bash
pytest lightroom_tagger/core/ -x -q
python -c "import lightroom_tagger.core.analyzer as a; assert hasattr(a, 'compress_image')"
```

### Manual smoke
- [ ] `python -c "from lightroom_tagger.core.vision_client import compare_images_batch"` still imports (transitive analyzer package)
