---
plan: 15-07
phase: 15
wave: 6
depends_on: [15-06]
files_modified:
  - docs/architecture.md
  - pyproject.toml
  - scripts/check_core_file_sizes.sh
  - lightroom_tagger/core/test_architecture.py
  - Makefile
autonomous: true
requirements: [REFACTOR-04, REFACTOR-05]
---

## Nyquist validation

Full sampling strategy, per-task commands, and sign-off checklist: [.planning/phases/15-service-modules-boundary-policy/15-VALIDATION.md](./15-VALIDATION.md).

**Automated commands (wave 6 / enforcement):**

- `bash scripts/check_core_file_sizes.sh` — line budget gate for `lightroom_tagger/core/**/*.py` (non-test); **introduced in this plan (15-07-T03)**
- `pytest lightroom_tagger/core/test_architecture.py -x -q` — line budget + import rules in test (15-07-T04); covers D-05/D-06 including API sibling imports
- `pytest lightroom_tagger/core/ -x -q` — core regression after boundary refactors
- `pytest -x -q` — full suite before phase sign-off / verify-work

CI integration (GitHub Actions) is deferred — scripts and pytest are written to be wired when `.github/workflows` exists (see Must-Haves; REFACTOR-05 automated check without Actions until then).

---

# Plan 15-07 — Boundary documentation + CI-sized enforcement

Add `docs/architecture.md` (layer model, 400-line rule, import policy), reference it from `pyproject.toml` near Ruff, add `scripts/check_core_file_sizes.sh`, and `lightroom_tagger/core/test_architecture.py` mirroring the script’s line budget plus import-graph guards (`15-CONTEXT` D-05/D-06).

<task id="15-07-T01" type="execute">
  <title>Create docs/architecture.md boundary policy</title>
  <read_first>
    - docs/adr/0003-pipeline-layer.md — handler → pipeline → service intent
    - .planning/phases/15-service-modules-boundary-policy/15-CONTEXT.md — D-05 bullet list
    - docs/adr/0002-split-database.md — repository layer example
  </read_first>
  <action>
    1. Create `docs/architecture.md` containing, at minimum, these **verbatim section headings** (Markdown `##`): `## Layers`, `## File size budget`, `## What belongs in each layer`, `## Import rules`.
    2. Under `## Layers`, include a **mermaid** `flowchart TB` with nodes `handlers`, `services`, `database` (or `repository`) and directed edges: `handlers --> services` and `services --> database`. Add a short prose sentence stating pipelines are the target orchestration layer per ADR-0003 even if `pipelines.py` is not implemented yet.
    3. Under `## File size budget`, state explicitly: non-test Python files under `lightroom_tagger/core/` must be **≤ 400** physical lines (`wc -l` style, inclusive of blank lines).
    4. Under `## What belongs in each layer`, enumerate at least four bullet rows mapping: HTTP/Flask handlers (job metadata, progress callbacks); **services** (`lightroom_tagger/core/*` domain logic); **database package** (persistence SQL); **exceptions package** (typed errors).
    5. Under `## Import rules`, state explicitly: (a) modules under `lightroom_tagger/core/` must not import application packages such as `apps.visualizer` (they may import stdlib and third-party libs only from the service layer perspective); (b) API layer modules (`apps/visualizer/backend/api/*.py`) may import from `lightroom_tagger.core.*` but MUST NOT import sibling api modules (e.g. `api/identity.py` must not import from `api/images.py`). Cross-api coupling should flow through shared helpers or the service layer.
  </action>
  <acceptance_criteria>
    - `test -f docs/architecture.md` exits 0
    - `grep -q "^## File size budget" docs/architecture.md` exits 0
    - `grep -q "flowchart TB" docs/architecture.md` exits 0
    - `grep -q "400" docs/architecture.md` exits 0
    - `grep -q "Import rules" docs/architecture.md` exits 0
    - `grep -q "must not import sibling api modules" docs/architecture.md` exits 0
  </acceptance_criteria>
</task>

<task id="15-07-T02" type="execute">
  <title>Link policy from pyproject.toml near Ruff</title>
  <read_first>
    - pyproject.toml — `[tool.ruff]` starts ~line 75
    - docs/architecture.md — path to cite
  </read_first>
  <action>
    1. Insert immediately **before** the `[tool.ruff]` table a comment line **exactly**: `# Module boundary policy: docs/architecture.md (max file size, import layers).`
    2. Do not change Ruff rule selection or ignores.
  </action>
  <acceptance_criteria>
    - `grep -F "# Module boundary policy: docs/architecture.md (max file size, import layers)." pyproject.toml` exits 0
    - The line occurs above the `[tool.ruff]` table (visual inspection or `awk '/Module boundary/{mbr=NR} /^\[tool\.ruff\]/{ruff=NR} END{exit (mbr && ruff && mbr < ruff) ? 0 : 1}' pyproject.toml` exits 0)
  </acceptance_criteria>
</task>

<task id="15-07-T03" type="execute">
  <title>Add scripts/check_core_file_sizes.sh</title>
  <read_first>
    - .planning/phases/15-service-modules-boundary-policy/15-RESEARCH.md — recommended find/xargs sketch
  </read_first>
  <action>
    1. Add executable script `scripts/check_core_file_sizes.sh` with `set -euo pipefail` that:
       - Resolves repo root as `cd "$(dirname "$0")/.."` before scanning.
       - Scans **all** `lightroom_tagger/core/**/*.py` files **except** `**/test_*.py`.
       - Prints `FAIL <lines> <path>` and exits status **1** if any file exceeds **400** lines; exits **0** if none exceed.
    2. `chmod +x scripts/check_core_file_sizes.sh`.
  </action>
  <acceptance_criteria>
    - `test -x scripts/check_core_file_sizes.sh` exits 0
    - `bash scripts/check_core_file_sizes.sh` exits 0 (post phase-15 refactors) or the plan executor commits follow-up splits until it exits 0
    - `head -1 scripts/check_core_file_sizes.sh` prints `#!/usr/bin/env bash` or `#!/bin/bash`
  </acceptance_criteria>
</task>

<task id="15-07-T04" type="execute">
  <title>Add lightroom_tagger/core/test_architecture.py</title>
  <read_first>
    - docs/architecture.md — numeric limit and import statements to encode
    - lightroom_tagger/core/test_analyzer.py — pytest style reference
  </read_first>
  <action>
    1. Create `lightroom_tagger/core/test_architecture.py` using **stdlib only** (`ast`, `pathlib`).
    2. Implement `test_core_python_files_respect_line_budget` looping over `(REPO_ROOT / "lightroom_tagger" / "core").rglob("*.py")` excluding files whose name starts with `test_`, asserting each file’s line count ≤ **400**.
    3. Implement `test_core_modules_do_not_import_apps_packages` over the same non-test paths: parse AST; for every imported module name (`import foo` / `from foo import`), if the **top-level** package is exactly `apps`, fail with the file path.
    4. Implement `test_api_modules_do_not_import_sibling_api_modules`: under `apps/visualizer/backend/api/` (non-test `*.py`), parse AST for imports; any import whose target resolves to another module under the same `api/` package (sibling submodules, e.g. `api.identity` importing `api.images` or `from api.images import …`) must fail with both paths. Allow same-module and stdlib/third-party imports. This encodes **D-06** api-layer rule: no cross-imports between sibling `api/` modules (keep API coupling localized via shared helpers or upward layers as architecture.md describes).
  </action>
  <acceptance_criteria>
    - `test -f lightroom_tagger/core/test_architecture.py` exits 0
    - `pytest lightroom_tagger/core/test_architecture.py -x -q` exits 0
    - `grep -q "def test_core_python_files_respect_line_budget" lightroom_tagger/core/test_architecture.py` exits 0
    - `grep -q "def test_core_modules_do_not_import_apps_packages" lightroom_tagger/core/test_architecture.py` exits 0
    - `grep -q "def test_api_modules_do_not_import_sibling_api_modules" lightroom_tagger/core/test_architecture.py` exits 0
  </acceptance_criteria>
</task>

<task id="15-07-T05" type="execute">
  <title>Makefile convenience target</title>
  <read_first>
    - Makefile — current targets
  </read_first>
  <action>
    1. Append a phony target `check-core-sizes` that runs `bash scripts/check_core_file_sizes.sh`.
    2. If `Makefile` uses `.PHONY`, include `check-core-sizes` in the `.PHONY` list; if not, add a `.PHONY: check-core-sizes` line before the target.
  </action>
  <acceptance_criteria>
    - `grep -q "^check-core-sizes:" Makefile` exits 0
    - `make -n check-core-sizes` prints `bash scripts/check_core_file_sizes.sh`
    - `make check-core-sizes` exits 0
  </acceptance_criteria>
</task>

## Verification

Nyquist sign-off checklist and full task matrix: [15-VALIDATION.md](./15-VALIDATION.md).

### Must-Haves (goal-backward)
- [ ] `docs/architecture.md` exists and matches D-05/D-06 expectations
- [ ] `bash scripts/check_core_file_sizes.sh` returns exit 0 on clean tree
- [ ] `pytest lightroom_tagger/core/test_architecture.py -x -q` passes
- [ ] `pyproject.toml` comment points to `docs/architecture.md` immediately above `[tool.ruff]`
- [ ] **CI integration (GitHub Actions) deferred** — `check_core_file_sizes.sh`, `make check-core-sizes`, and `test_architecture.py` are the automated checks for REFACTOR-05-style enforcement until `.github/workflows` is added; no workflow wiring in this phase.

### Automated checks
```bash
bash scripts/check_core_file_sizes.sh
pytest lightroom_tagger/core/test_architecture.py -x -q
pytest -x -q
```

### Manual smoke
- [ ] Read `docs/architecture.md` and confirm diagram renders in GitHub / viewer
