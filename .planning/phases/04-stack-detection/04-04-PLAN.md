---
plan: "04-04"
phase: 4
wave: 3
title: "Tests for stack schema, config API, checkpoint, and batch_stack_detect"
depends_on: ["04-03"]
requirements: ["STACK-01"]
scope_note: "Tests cover STACK-01 only; no STACK-02 scenarios."
files_modified:
  - apps/visualizer/backend/tests/test_lt_config_api.py
  - apps/visualizer/backend/tests/test_job_checkpoint.py
  - apps/visualizer/backend/tests/test_library_db.py
  - apps/visualizer/backend/tests/test_jobs_api.py
  - apps/visualizer/backend/tests/test_handlers_batch_stack_detect.py
autonomous: true
---

<objective>
Add automated tests mirroring `test_handlers_batch_text_embed.py`, `test_job_checkpoint.py`, and `test_lt_config_api.py` patterns: stack-detection config GET/PUT, fingerprint stability for `fingerprint_batch_stack_detect`, catalog job registration assertions, and handler behavior (zero work, burst creates stack, incremental skip, bad dates counted, checkpoint resume, force rebuild clears stacks).
</objective>

<tasks>
  <task id="T1" title="Extend test_library_db and test_jobs_api for batch_stack_detect">
    <read_first>
      - apps/visualizer/backend/tests/test_library_db.py (reason: `JOB_TYPES_REQUIRING_CATALOG` assertion pattern)
      - apps/visualizer/backend/tests/test_jobs_api.py (reason: `jobs_requiring_catalog` JSON assertion)
    </read_first>
    <action>
      1. In `apps/visualizer/backend/tests/test_library_db.py`, add assertion that `'batch_stack_detect' in JOB_TYPES_REQUIRING_CATALOG` (extend existing test that lists `batch_text_embed`).
      2. In `apps/visualizer/backend/tests/test_jobs_api.py`, add assertion that response body `jobs_requiring_catalog` (or equivalent key used in file) contains `'batch_stack_detect'` alongside existing types.
    </action>
    <acceptance_criteria>
      - `rg -n "batch_stack_detect" apps/visualizer/backend/tests/test_library_db.py` exits 0
      - `rg -n "batch_stack_detect" apps/visualizer/backend/tests/test_jobs_api.py` exits 0
    </acceptance_criteria>
  </task>
  <task id="T2" title="test_lt_config_api stack-detection routes">
    <read_first>
      - apps/visualizer/backend/tests/test_lt_config_api.py (reason: monkeypatch `LT_CONFIG_YAML`, Flask test client pattern)
      - apps/visualizer/backend/api/lt_config.py (reason: route paths and JSON shape)
    </read_first>
    <action>
      In `apps/visualizer/backend/tests/test_lt_config_api.py`, add tests:
      1. `GET /api/config/stack-detection` returns 200 and JSON `{"stack_burst_delta_ms": 2000}` (or whatever default) on fresh temp YAML.
      2. `PUT /api/config/stack-detection` with `{"stack_burst_delta_ms": 3000}` updates YAML and subsequent GET returns 3000.
      3. `PUT` with `0` or negative int returns 400 and error payload from `error_bad_request` pattern (match existing tests' assertion style).
    </action>
    <acceptance_criteria>
      - `rg -n "stack-detection" apps/visualizer/backend/tests/test_lt_config_api.py` exits 0 with at least 3 matches
      - `rg -n "stack_burst_delta_ms" apps/visualizer/backend/tests/test_lt_config_api.py` exits 0
    </acceptance_criteria>
  </task>
  <task id="T3" title="test_job_checkpoint fingerprint_batch_stack_detect">
    <read_first>
      - apps/visualizer/backend/tests/test_job_checkpoint.py (reason: `fingerprint_batch_text_embed` stability and permutation tests)
      - apps/visualizer/backend/jobs/checkpoint.py (reason: `fingerprint_batch_stack_detect` signature)
    </read_first>
    <action>
      In `apps/visualizer/backend/tests/test_job_checkpoint.py`, add tests for `fingerprint_batch_stack_detect`:
      1. Same inputs produce same hash; permuting `image_keys` argument order does not change hash (sort inside fingerprint).
      2. Changing `resolved_delta_ms` changes hash.
      3. Changing `force_mode` from `"incremental"` to `"full"` changes hash.
      4. With **identical** `image_keys` and **identical** `resolved_delta_ms`, `fingerprint_batch_stack_detect(..., force_mode="full")` ≠ `fingerprint_batch_stack_detect(..., force_mode="preserve_edited")` (locks in 04-03 fingerprint contract distinguishing those modes).
      Use small example lists `["a","b"]` and metadata dict `{}` or minimal stubs as needed.
    </action>
    <acceptance_criteria>
      - `rg -n "fingerprint_batch_stack_detect" apps/visualizer/backend/tests/test_job_checkpoint.py` exits 0 with at least 2 matches
      - `rg -n "force_mode" apps/visualizer/backend/tests/test_job_checkpoint.py` exits 0
      - Test coverage asserts `fingerprint_batch_stack_detect` differs between `force_mode="full"` and `force_mode="preserve_edited"` for the same `keys` and `resolved_delta_ms` (e.g. explicit `assert fp_full != fp_preserve` in T3).
    </acceptance_criteria>
  </task>
  <task id="T4" title="New test_handlers_batch_stack_detect.py">
    <read_first>
      - apps/visualizer/backend/tests/test_handlers_batch_text_embed.py (reason: tmp_path DB, `init_database`, `LIBRARY_DB` env, mock runner, patches)
      - apps/visualizer/backend/jobs/handlers.py (reason: handler name and metadata keys)
      - lightroom_tagger/core/database.py (reason: how to insert minimal `images` rows for tests — grep `INSERT INTO images` in tests or use helpers)
    </read_first>
    <action>
      Create `apps/visualizer/backend/tests/test_handlers_batch_stack_detect.py` with at least:
      1. **Zero work:** empty `images` table → `runner.complete_job` once with `stacks_created == 0` and all five result keys present and integers.
      2. **Burst:** insert three `images` rows with same parseable `date_taken` within 1s and `delta_ms=2000` → one stack, `stack_size==3`, three member rows, `representative_key` equals key chosen by ORDER BY rule (set distinct `rating` so winner is deterministic).
      3. **Skip no date:** one row `date_taken` NULL → `images_skipped_no_date` greater than or equal to 1; that key not in `image_stack_members`. In this test case, assert `add_job_log` was called with a message containing `skipped` or the skipped count.
      4. **Incremental:** after (2), second run with default `force` does not duplicate stacks; `images_skipped_already_stacked` greater than or equal to 3 (or exact if deterministic).
      5. **Force rebuild:** `force: true` clears prior stacks then recreates same counts as fresh burst test.
      Use `monkeypatch.setenv("LIBRARY_DB", str(db_path))`, mock `load_config` returning `MagicMock(db_path=..., stack_burst_delta_ms=2000)` consistent with text embed tests, and `MagicMock` runner with `runner.db` set.
    </action>
    <acceptance_criteria>
      - `test -f apps/visualizer/backend/tests/test_handlers_batch_stack_detect.py` exits 0
      - `rg -n "handle_batch_stack_detect" apps/visualizer/backend/tests/test_handlers_batch_stack_detect.py` exits 0
      - `rg -n "images_skipped_no_date" apps/visualizer/backend/tests/test_handlers_batch_stack_detect.py` exits 0
      - `test_handlers_batch_stack_detect.py` contains mock assertion for `add_job_log` call with skipped-no-date count message
      - `cd apps/visualizer/backend && python -m pytest tests/test_handlers_batch_stack_detect.py -q` exits 0
    </acceptance_criteria>
  </task>
  <task id="T5" title="Optional checkpoint resume test">
    <read_first>
      - apps/visualizer/backend/tests/test_handlers_batch_text_embed.py (reason: checkpoint persistence mocking if present)
      - apps/visualizer/backend/jobs/handlers.py (reason: checkpoint body field names for stack job)
    </read_first>
    <action>
      If `runner.persist_checkpoint` / `get_job` can be mocked cleanly, add one test that simulates mid-job resume: first call stops after partial `processed_image_keys`, second call completes without double-inserting members (assert unique constraint on `image_key` not violated). If too invasive for Phase 4, mark test `pytest.skip` with reason string containing `batch_stack_detect checkpoint resume` — prefer implementing if runner mocks already exist in repo.
    </action>
    <acceptance_criteria>
      - Either: `rg -n "checkpoint" apps/visualizer/backend/tests/test_handlers_batch_stack_detect.py` exits 0 with a test function name containing `resume` or `checkpoint`
      - Or: `rg -n "pytest.skip.*batch_stack_detect checkpoint resume" apps/visualizer/backend/tests/test_handlers_batch_stack_detect.py` exits 0
    </acceptance_criteria>
  </task>
</tasks>

<verification>
From `apps/visualizer/backend`:
- `python -m pytest tests/test_handlers_batch_stack_detect.py tests/test_job_checkpoint.py tests/test_lt_config_api.py tests/test_library_db.py tests/test_jobs_api.py -q --tb=short` exits 0
</verification>

<must_haves>
- [ ] New handler test file follows `test_handlers_batch_text_embed` fixtures style (`tmp_path`, `LIBRARY_DB`, mocked runner)
- [ ] Fingerprint tests prove permutation invariance and sensitivity to `delta_ms` / `force_mode`, including **`full` vs `preserve_edited`** with identical keys and `resolved_delta_ms`
- [ ] Config API tests cover GET, successful PUT, and rejection of invalid milliseconds
- [ ] `batch_stack_detect` appears in both `test_library_db` and `test_jobs_api` assertions
</must_haves>
