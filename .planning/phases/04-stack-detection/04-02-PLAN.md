---
plan: "04-02"
phase: 4
wave: 1
title: "Config stack_burst_delta_ms, API, and StackDetectionSettingsPanel"
depends_on: []
requirements: ["STACK-01"]
scope_note: "STACK-01 only; STACK-02 not part of Phase 4."
files_modified:
  - lightroom_tagger/core/config.py
  - apps/visualizer/backend/api/lt_config.py
  - apps/visualizer/frontend/src/services/api.ts
  - apps/visualizer/frontend/src/components/processing/SettingsTab.tsx
  - apps/visualizer/frontend/src/components/processing/StackDetectionSettingsPanel.tsx
autonomous: true
---

<objective>
Persist default burst window `stack_burst_delta_ms: int = 2000` in `Config` and `config.yaml`, expose GET/PUT `/api/config/stack-detection` mirroring catalog/Instagram config routes, add `ConfigAPI.getStackDetection` / `putStackDetection` in the frontend, and render `StackDetectionSettingsPanel` in `SettingsTab.tsx` following `CatalogSettingsPanel` UX patterns.
</objective>

<tasks>
  <task id="T1" title="Extend Config dataclass, defaults, and YAML updater">
    <read_first>
      - lightroom_tagger/core/config.py (reason: `Config` fields, `load_config` `defaults` dict, `update_config_yaml_instagram_dump_path` pattern)
      - .planning/phases/04-stack-detection/04-CONTEXT.md D-06 (reason: field name and default 2000)
    </read_first>
    <action>
      In `lightroom_tagger/core/config.py`:
      1. Add `stack_burst_delta_ms: int = 2000` to the `Config` dataclass (place near other simple ints).
      2. Add `"stack_burst_delta_ms": 2000` to the `defaults` dict inside `load_config` so missing YAML keys get 2000.
      3. Add `update_config_yaml_stack_burst_delta_ms(config_file: str, stack_burst_delta_ms: int) -> None` that loads YAML (or `{}`), sets `data["stack_burst_delta_ms"] = int_value`, and `yaml.safe_dump` with the same kwargs as `update_config_yaml_instagram_dump_path` (`default_flow_style=False`, `sort_keys=False`, `allow_unicode=True`).
      4. Validate in the updater: raise `ValueError` if `stack_burst_delta_ms` is less than 1 (integer milliseconds; minimum 1).
    </action>
    <acceptance_criteria>
      - `rg -n "stack_burst_delta_ms" lightroom_tagger/core/config.py` exits 0 with at least 3 matches (dataclass, defaults, updater function name)
      - `rg -n "def update_config_yaml_stack_burst_delta_ms" lightroom_tagger/core/config.py` exits 0
      - `rg -n "\"stack_burst_delta_ms\": 2000" lightroom_tagger/core/config.py` exits 0
    </acceptance_criteria>
  </task>
  <task id="T2" title="Flask routes GET/PUT /stack-detection on lt_config blueprint">
    <read_first>
      - apps/visualizer/backend/api/lt_config.py (reason: mirror `get_instagram_dump` / `put_instagram_dump`)
      - lightroom_tagger/core/config.py (reason: import new updater)
    </read_first>
    <action>
      In `apps/visualizer/backend/api/lt_config.py`:
      1. Import `update_config_yaml_stack_burst_delta_ms` from `lightroom_tagger.core.config`.
      2. Add `@bp.route('/stack-detection', methods=['GET'])` `get_stack_detection`: `cfg = load_config(LT_CONFIG_YAML)`; return `jsonify({"stack_burst_delta_ms": int(cfg.stack_burst_delta_ms)})` (ensure int).
      3. Add `@bp.route('/stack-detection', methods=['PUT'])` `put_stack_detection`: parse JSON; require `"stack_burst_delta_ms"` key; must be `int` (reject float string); if value is less than 1 return `error_bad_request` with explicit message; call `update_config_yaml_stack_burst_delta_ms(LT_CONFIG_YAML, value)`; return `jsonify({"stack_burst_delta_ms": value, "ok": True})`.
    </action>
    <acceptance_criteria>
      - `rg -n "stack-detection" apps/visualizer/backend/api/lt_config.py` exits 0 with at least 2 lines (GET and PUT decorators or route strings)
      - `rg -n "update_config_yaml_stack_burst_delta_ms" apps/visualizer/backend/api/lt_config.py` exits 0
      - `rg -n "stack_burst_delta_ms" apps/visualizer/backend/api/lt_config.py` exits 0
    </acceptance_criteria>
  </task>
  <task id="T3" title="Frontend ConfigAPI + StackDetectionSettingsPanel + SettingsTab">
    <read_first>
      - apps/visualizer/frontend/src/services/api.ts (reason: `ConfigAPI` object and `invalidateAll` pattern)
      - apps/visualizer/frontend/src/components/images/CatalogSettingsPanel.tsx (reason: state/refresh/save pattern)
      - apps/visualizer/frontend/src/components/processing/SettingsTab.tsx (reason: where to insert panel)
      - .cursor/rules/frontend-design.md (reason: project UI guidelines if any conflict)
    </read_first>
    <action>
      1. In `apps/visualizer/frontend/src/services/api.ts`, extend `ConfigAPI` with:
         - `getStackDetection: () => request<{ stack_burst_delta_ms: number }>('/config/stack-detection')`
         - `putStackDetection: async (stackBurstDeltaMs: number) => { await request PUT '/config/stack-detection' with body JSON.stringify({ stack_burst_delta_ms: stackBurstDeltaMs }); call invalidateAll(['jobs.health']) at minimum; return typed result with ok: boolean }`
      2. Create `apps/visualizer/frontend/src/components/processing/StackDetectionSettingsPanel.tsx`: numeric input for milliseconds, label text that states this is the default burst window for `batch_stack_detect` (user-facing: “burst” / “time between shots” wording), Load on mount via `getStackDetection`, Save via `putStackDetection`, same error/loading pattern as `CatalogSettingsPanel`.
      3. In `SettingsTab.tsx`, import `StackDetectionSettingsPanel` and render it after `<InstagramDumpSettingsPanel />` inside the `space-y-6` container.
    </action>
    <acceptance_criteria>
      - `rg -n "getStackDetection|putStackDetection" apps/visualizer/frontend/src/services/api.ts` exits 0
      - `test -f apps/visualizer/frontend/src/components/processing/StackDetectionSettingsPanel.tsx` exits 0
      - `rg -n "StackDetectionSettingsPanel" apps/visualizer/frontend/src/components/processing/SettingsTab.tsx` exits 0
      - `rg -n "stack-detection" apps/visualizer/frontend/src/services/api.ts` exits 0
    </acceptance_criteria>
  </task>
</tasks>

<verification>
From repo root (adjust if test runner differs):
- `cd apps/visualizer/backend && python -m pytest tests/test_lt_config_api.py -q --tb=no -k stack` — if no tests yet, run full `test_lt_config_api.py` after 04-04 adds cases; for this wave, run `python -c "from lightroom_tagger.core.config import Config, load_config; c=Config(); assert c.stack_burst_delta_ms==2000"` exits 0
- `cd apps/visualizer/frontend && npx tsc --noEmit` exits 0 (or project’s standard `npm run typecheck` if defined in package.json)
</verification>

<must_haves>
- [ ] `load_config` supplies default `stack_burst_delta_ms` 2000 when YAML omits the key
- [ ] `PUT /api/config/stack-detection` rejects values less than 1 with HTTP 400 via `error_bad_request`
- [ ] Settings UI can read and persist the value end-to-end
</must_haves>
