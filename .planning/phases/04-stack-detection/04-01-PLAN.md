---
plan: "04-01"
phase: 4
wave: 1
title: "Library DB schema — image_stacks and image_stack_members"
depends_on: []
requirements: ["STACK-01"]
scope_note: "Phase 4 delivers STACK-01 only; STACK-02 (pHash clustering) is excluded per D-01 — ROADMAP/REQUIREMENTS may still list STACK-02 for later work."
files_modified:
  - lightroom_tagger/core/database.py
autonomous: true
---

<objective>
Add idempotent SQLite tables `image_stacks` and `image_stack_members` with `UNIQUE(image_key)` on members, `representative_key NOT NULL`, denormalized `stack_size`, `user_modified` scaffold for Phase 7, and wire creation into `init_database` via a dedicated `_migrate_image_stacks` helper so every library DB gets the schema on open.
</objective>

<tasks>
  <task id="T1" title="Add _migrate_image_stacks and call from init_database">
    <read_first>
      - lightroom_tagger/core/database.py (reason: see `init_database` flow around `_migrate_image_text_embeddings_vec0` and `conn.commit()`; match migration style)
      - .planning/phases/04-stack-detection/04-RESEARCH.md §8 Schema Design (reason: exact DDL and index names)
    </read_first>
    <action>
      In `lightroom_tagger/core/database.py`:
      1. Define `_migrate_image_stacks(conn: sqlite3.Connection) -> None` that runs idempotent DDL only: **omit `user_version` for this migration** and rely solely on `CREATE TABLE IF NOT EXISTS` + `CREATE UNIQUE INDEX IF NOT EXISTS` (per RESEARCH §8). **No conditional branch** on versioning.
      2. Tables and constraints:
         - `image_stacks`: columns `stack_id INTEGER PRIMARY KEY AUTOINCREMENT`, `representative_key TEXT NOT NULL`, `stack_size INTEGER NOT NULL DEFAULT 0`, `user_modified INTEGER NOT NULL DEFAULT 0` (SQLite bool), `created_at TEXT NOT NULL DEFAULT (datetime('now'))`.
         - `image_stack_members`: `stack_id INTEGER NOT NULL REFERENCES image_stacks(stack_id) ON DELETE CASCADE`, `image_key TEXT NOT NULL`, `PRIMARY KEY (stack_id, image_key)`.
      3. Indexes exactly as in RESEARCH: `uq_image_stacks_representative` on `image_stacks(representative_key)`; `uq_image_stack_members_image_key` on `image_stack_members(image_key)`.
      4. Call `_migrate_image_stacks(conn)` from `init_database` after `_migrate_image_text_embeddings_vec0(conn)` and before `seed_perspectives_from_prompts_dir(conn)` (or immediately after vec migration — keep ordering documented in code comment: stacks depend on `images` existing, which is already true).
      5. Ensure `ON DELETE CASCADE` works when tests use the connection: if the project enables foreign keys globally, rely on that; if not, add `PRAGMA foreign_keys = ON` in `init_database` once per connection if consistent with existing `database.py` patterns (grep first; only add if missing).
      6. Place comment on the line immediately above `def _migrate_image_stacks`: `# Stack mutations from batch jobs must use library_write(context manager).`
    </action>
    <acceptance_criteria>
      - `rg -n "def _migrate_image_stacks" lightroom_tagger/core/database.py` exits 0
      - `rg -n "CREATE TABLE IF NOT EXISTS image_stacks" lightroom_tagger/core/database.py` exits 0
      - `rg -n "image_stack_members" lightroom_tagger/core/database.py` exits 0
      - `rg -n "user_modified" lightroom_tagger/core/database.py` exits 0
      - `rg -n "uq_image_stack_members_image_key" lightroom_tagger/core/database.py` exits 0
      - `rg -n "_migrate_image_stacks\\(conn\\)" lightroom_tagger/core/database.py` shows a call inside `init_database`
      - `rg -n "Stack mutations from batch jobs must use library_write" lightroom_tagger/core/database.py` exits 0
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run from repo root:
- `python -c "import sqlite3, tempfile, os; from lightroom_tagger.core.database import init_database; p=os.path.join(tempfile.gettempdir(),'lt_stack_schema_test.db'); os.path.exists(p) and os.remove(p); c=init_database(p); rows=c.execute(\"SELECT name FROM sqlite_master WHERE type='table' AND name IN ('image_stacks','image_stack_members')\").fetchall(); assert len(rows)==2; c.close(); os.remove(p)"` exits 0
- `rg "image_stacks|image_stack_members" lightroom_tagger/core/database.py | head -20` shows CREATE and migration call
</verification>

<must_haves>
- [ ] Fresh `init_database` creates both `image_stacks` and `image_stack_members` with `UNIQUE` on `image_stack_members.image_key`
- [ ] `representative_key` is `NOT NULL`; `user_modified` defaults to 0; CASCADE from stacks to members is declared on `stack_id`
- [ ] `_migrate_image_stacks` is invoked from `init_database` so existing workflows pick up tables automatically
</must_haves>
