---
phase: 8
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - apps/visualizer/backend/jobs/handlers.py
  - apps/visualizer/backend/jobs/checkpoint.py
  - apps/visualizer/frontend/src/stores/matchOptionsContext.tsx
  - apps/visualizer/frontend/src/components/matching/AdvancedOptions.tsx
  - apps/visualizer/frontend/src/components/processing/MatchingTab.tsx
  - apps/visualizer/backend/tests/test_handlers_single_match.py
autonomous: true
requirements:
  - MATCH-04
---

<objective>
Plumb `skip_undescribed` from the vision-match job metadata through `handle_vision_match` → `match_dump_media`, include it in `fingerprint_vision_match` for checkpoint correctness, expose `skipUndescribed` in the React match options (default `true`), add an Advanced Options toggle disabled when `descWeight === 0`, and lock behavior with a backend handler test.
</objective>

<tasks>

<task id="03-handlers-checkpoint">
<read_first>
- /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/jobs/handlers.py (`handle_vision_match` ~296–448; `fingerprint_vision_match` call ~314–326; `match_dump_media` call ~429–448)
- /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/jobs/checkpoint.py (`fingerprint_vision_match` ~102–132)
- /Users/ccanales/projects/lightroom-tagger/lightroom_tagger/scripts/match_instagram_dump.py (`match_dump_media` signature including `skip_undescribed` from Wave 2)
</read_first>
<action>
In `handle_vision_match`, after `force_descriptions = metadata.get('force_descriptions', False)`, add:

`skip_undescribed = bool(metadata.get('skip_undescribed', True))`

Pass `skip_undescribed=skip_undescribed` into `match_dump_media` as a keyword argument alongside existing kwargs.

Update the `fingerprint_vision_match(...)` call to pass `skip_undescribed=skip_undescribed` as a keyword argument.

In `checkpoint.py`, extend `fingerprint_vision_match` signature with `skip_undescribed: bool` (place after `force_descriptions` or alphabetically in the payload — use sorted payload keys already; add `"skip_undescribed": bool(skip_undescribed)` to the `payload` dict inside the function).

Ensure any other call sites of `fingerprint_vision_match` in the repo pass the new argument (grep and update).
</action>
<acceptance_criteria>
- `rg "skip_undescribed" apps/visualizer/backend/jobs/handlers.py` shows ≥ 3 lines (read + fingerprint + match_dump_media).
- `rg "skip_undescribed" apps/visualizer/backend/jobs/checkpoint.py` shows the key inside the `payload` dict literal.
- `python -c "from jobs.checkpoint import fingerprint_vision_match; print(fingerprint_vision_match(threshold=0.7, weights={'phash':0,'description':0,'vision':1}, month=None, year=None, last_months=None, media_key=None, force_reprocess=False, force_descriptions=False, skip_undescribed=True, provider_id=None, provider_model=None, max_workers=4))"` run from `apps/visualizer/backend` exits 0 and prints a 64-char hex string (executor verifies working directory or `PYTHONPATH`).
- `rg 'skip_undescribed' apps/visualizer/backend/jobs/checkpoint.py` returns at least one match (field present in the fingerprint payload).
- `rg "fingerprint_vision_match\\(" apps/visualizer/backend` lists only call sites that include `skip_undescribed=` after edits.
</acceptance_criteria>
</task>

<task id="03-frontend-options">
<read_first>
- /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend/src/stores/matchOptionsContext.tsx (full file)
- /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend/src/components/matching/AdvancedOptions.tsx (full file)
- /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend/src/components/processing/MatchingTab.tsx (metadata block ~44–54)
</read_first>
<action>
**matchOptionsContext.tsx:** Add `skipUndescribed: boolean` to `MatchOptions`. Set `DEFAULT_OPTIONS.skipUndescribed` to `true`. Ensure `updateOption` typings include the new key.

**AdvancedOptions.tsx:** Extend props with `skipUndescribed: boolean`, `onSkipUndescribedChange: (value: boolean) => void`. After the `WeightSlider` for `ADVANCED_WEIGHT_DESC` (description / `descWeight`), render a checkbox (or accessible toggle) with visible label exactly `Skip undescribed`. Set `disabled={descWeight === 0}` on the input; when disabled, apply Tailwind classes so the control appears greyed out (e.g. `opacity-50 cursor-not-allowed` on a wrapper).

**MatchingTab.tsx:** In the `metadata` object passed to `JobsAPI.create('vision_match', metadata)`, add `skip_undescribed: options.skipUndescribed`. Spread `{...options}` into `AdvancedOptions` already passes scalar fields — add `onSkipUndescribedChange={(v) => updateOption('skipUndescribed', v)}` explicitly alongside other handlers.

**resetOptions:** Preserve `providerId` / `providerModel` / `maxWorkers` like today; reset `skipUndescribed` to `true` (same as `DEFAULT_OPTIONS.skipUndescribed`).
</action>
<acceptance_criteria>
- `matchOptionsContext.tsx` contains `skipUndescribed` in `interface MatchOptions` and `DEFAULT_OPTIONS`.
- `AdvancedOptions.tsx` contains the exact string `Skip undescribed` as visible label text.
- `AdvancedOptions.tsx` contains `disabled={descWeight === 0}` or `descWeight === 0` in the `disabled=` expression for the skip control.
- `MatchingTab.tsx` contains `skip_undescribed:` in the metadata object.
- `MatchingTab.tsx` contains `onSkipUndescribedChange`.
</acceptance_criteria>
</task>

<task id="03-test-handlers">
<read_first>
- /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/tests/test_handlers_single_match.py
- /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/jobs/handlers.py (post-edit)
</read_first>
<action>
Add test `test_handle_vision_match_passes_skip_undescribed` mirroring `test_handle_vision_match_passes_media_key`: same patches (`match_dump_media`, `init_database`, `load_config`, etc.). Call `handle_vision_match(runner, 'test-job', {'skip_undescribed': False})`. Assert `mock_match.call_args.kwargs.get('skip_undescribed') is False`.

Add second assertion for default: call with `{}` metadata and assert `kwargs.get('skip_undescribed') is True` (or `== True`).
</action>
<acceptance_criteria>
- `test_handlers_single_match.py` contains function name `test_handle_vision_match_passes_skip_undescribed`.
- `pytest /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/tests/test_handlers_single_match.py -q` exits 0 (run from backend dir with correct `PYTHONPATH` per project norms — executor uses the same invocation as other backend tests in CI).
- File contains the literal `skip_undescribed` at least twice.
</acceptance_criteria>
</task>

</tasks>

<verification>
From repo root (adjust if backend tests use a wrapper):

```bash
cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend && python -m pytest tests/test_handlers_single_match.py -q
```

Typecheck frontend:

```bash
cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend && npx tsc --noEmit
```

Grep wiring:

```bash
rg -n "skip_undescribed|skipUndescribed" apps/visualizer/backend/jobs/handlers.py apps/visualizer/backend/jobs/checkpoint.py apps/visualizer/frontend/src/stores/matchOptionsContext.tsx apps/visualizer/frontend/src/components/matching/AdvancedOptions.tsx apps/visualizer/frontend/src/components/processing/MatchingTab.tsx
```
</verification>

<must_haves>
- **MATCH-04 / SC-5–8:** Job metadata `skip_undescribed` (default `true`) reaches `match_dump_media`; checkpoint fingerprint includes the flag; UI exposes `skipUndescribed` with default ON, disabled when description weight is 0; `description` weight slider remains unchanged and functional.
</must_haves>

<success_criteria>
Wave 3 completes when handler tests and `tsc` pass and all grep checks confirm end-to-end wiring of `skip_undescribed`.
</success_criteria>
