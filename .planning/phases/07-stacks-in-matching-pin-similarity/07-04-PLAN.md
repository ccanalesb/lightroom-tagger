---
phase: 07-stacks-in-matching-pin-similarity
plan: 4
type: execute
wave: 2
depends_on: ["07-01-PLAN"]
files_modified:
  - apps/visualizer/backend/api/images.py
  - lightroom_tagger/core/clip_similarity.py
  - apps/visualizer/frontend/src/pages/SearchPage.tsx
  - apps/visualizer/frontend/src/services/api.ts
  - apps/visualizer/frontend/src/components/images/ImageDetailModal.tsx
  - apps/visualizer/backend/tests/test_images_chat_search_api.py
  - apps/visualizer/frontend/src/pages/__tests__/SearchPage.test.tsx
autonomous: true
requirements:
  - NLS-06
user_setup: []
must_haves:
  truths:
    - "Pin behavior allows one active pinned image at a time"
    - "With pin active, search uses intersection-first flow (pin candidates then text refinement)"
    - "Pin similarity failures degrade to normal text search with visible inactive-pin warning"
  artifacts:
    - path: apps/visualizer/frontend/src/pages/SearchPage.tsx
      provides: single-pin UX, active/inactive pin state, replacement behavior
    - path: apps/visualizer/backend/api/images.py
      provides: pin-aware search orchestration and fallback metadata
  key_links:
    - from: chat request with pin
      to: pin-aware backend orchestration
      via: pin key in request payload and fallback metadata in response
---

<objective>
Deliver NLS-06 pin-to-similar behavior in Search: single active pin, intersection-first refinement, and non-blocking visible fallback when pin similarity is unavailable.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-stacks-in-matching-pin-similarity/07-CONTEXT.md
@.planning/phases/05-image-embed-search-chat/05-CONTEXT.md
@.planning/phases/06-similarity-stack-ui/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Backend pin-aware search orchestration</name>
  <files>apps/visualizer/backend/api/images.py, lightroom_tagger/core/clip_similarity.py</files>
  <action>
  Extend chat search request/response contract to accept a single `pinned_image_key`. Build candidate set from pin similarity first, then apply text/NL refinement within that candidate set. On pin similarity failure, return fallback metadata and continue text search.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest apps/visualizer/backend/tests/test_images_chat_search_api.py -k pin -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
  - Pin-aware path does not block response on similarity failure.
  - Response metadata includes pin_state: active/inactive and fallback_reason when applicable.
  - New behavior remains backward-compatible for requests without a pin.
  </acceptance_criteria>
  <done>Backend chat search supports pin-aware refinement and graceful fallback metadata.</done>
</task>

<task type="auto">
  <name>Frontend pin state and replacement UX</name>
  <files>apps/visualizer/frontend/src/pages/SearchPage.tsx, apps/visualizer/frontend/src/services/api.ts</files>
  <action>
  Implement single active pin state: pin action sets active key, pinning a different image replaces prior key, unpin clears state. Surface "Pinned to ..." indicator and inactive warning state from metadata.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend && npm run vitest -- SearchPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
  - Only one pin can be active at once.
  - Re-pin replaces existing pin without page refresh.
  - Inactive pin warning is visible and non-blocking.
  </acceptance_criteria>
  <done>Search page enforces single-pin behavior and displays active/inactive pin state.</done>
</task>

<task type="auto" tdd="true">
  <name>Pin flow tests (backend + frontend)</name>
  <files>apps/visualizer/backend/tests/test_images_chat_search_api.py, apps/visualizer/frontend/src/pages/__tests__/SearchPage.test.tsx</files>
  <action>
  Add tests for active-pin request path, replacement behavior, fallback metadata handling, and visible inactive warning UI.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest apps/visualizer/backend/tests/test_images_chat_search_api.py -q --tb=short</automated>
    <automated>cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend && npm run vitest -- SearchPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
  - Backend tests cover pin present/absent and fallback behavior.
  - Frontend tests assert single-pin replacement and inactive warning rendering.
  </acceptance_criteria>
  <done>Pin flow tests cover normal, replacement, and fallback cases.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | D | Similarity unavailable | mitigate | Graceful fallback path with explicit metadata and UI messaging. |
| T-07-04-02 | T | Pin parameter misuse | mitigate | Validate pin key existence/shape before similarity stage. |
</threat_model>

<verification>Pin behavior tests pass for normal, replacement, and fallback paths.</verification>

<success_criteria>NLS-06 shipped with predictable one-pin behavior and resilient fallback UX.</success_criteria>

<output>After completion, create `.planning/phases/07-stacks-in-matching-pin-similarity/07-04-SUMMARY.md`.</output>
