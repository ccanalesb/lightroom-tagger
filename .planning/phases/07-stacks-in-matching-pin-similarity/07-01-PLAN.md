---
phase: 07-stacks-in-matching-pin-similarity
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/visualizer/backend/jobs/handlers.py
  - lightroom_tagger/core/match_instagram_dump.py
  - lightroom_tagger/core/database.py
  - apps/visualizer/backend/tests/test_handlers_single_match.py
autonomous: true
requirements:
  - STACK-04
user_setup: []
must_haves:
  truths:
    - "Instagram matching compares against stack representatives only"
    - "A confirmed representative match applies across stack members by default"
    - "Conflict policy is non-destructive: skip conflicted members and report counts"
  artifacts:
    - path: apps/visualizer/backend/jobs/handlers.py
      provides: stack-wide apply flow and conflict accounting in match handler path
    - path: lightroom_tagger/core/database.py
      provides: representative/member resolution and partial-apply persistence helpers
  key_links:
    - from: representative match result
      to: stack-wide apply function
      via: "apply non-conflicted members, skip conflicted members"
---

<objective>
Implement STACK-04 backend behavior: run matching against stack representatives and, when matched, apply to full stack with partial-apply conflict handling and explicit result counters.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-stacks-in-matching-pin-similarity/07-CONTEXT.md
@.planning/phases/06-similarity-stack-ui/06-CONTEXT.md
@.planning/phases/04-stack-detection/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Representative-only candidate matching</name>
  <files>lightroom_tagger/core/match_instagram_dump.py, apps/visualizer/backend/jobs/handlers.py</files>
  <action>
  Ensure candidate selection for Instagram matching uses stack representatives only (or solo non-stack rows). Keep existing selection filters intact.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest apps/visualizer/backend/tests/test_handlers_single_match.py -k representative -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
  - Representative-only behavior is covered by tests with mixed rep/member fixtures.
  - No non-representative member is sent to comparison pipeline.
  </acceptance_criteria>
  <done>Matching pipeline receives representative-only comparison candidates.</done>
</task>

<task type="auto">
  <name>Stack-wide apply with conflict-safe policy</name>
  <files>lightroom_tagger/core/database.py, apps/visualizer/backend/jobs/handlers.py</files>
  <action>
  Add/extend apply helper that maps representative match to all member keys in the stack. If a member has an existing conflicting match, skip it and continue. Return detailed counts.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest apps/visualizer/backend/tests/test_handlers_single_match.py -k conflict -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
  - Returned payload includes applied_count, skipped_conflicts_count, skipped_other_count.
  - Existing conflicting matches are not overwritten by default.
  - Handler/job logs include count summary for UI visibility.
  </acceptance_criteria>
  <done>Stack-wide apply enforces skip-on-conflict policy and reports counters.</done>
</task>

<task type="auto" tdd="true">
  <name>Backend tests for representative + partial-apply behavior</name>
  <files>apps/visualizer/backend/tests/test_handlers_single_match.py</files>
  <action>
  Add tests for: representative-only matching input set, full-stack apply on success, and partial-apply when one or more members conflict.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest apps/visualizer/backend/tests/test_handlers_single_match.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
  - Tests assert non-destructive conflict behavior.
  - Tests assert count reporting and successful partial completion.
  </acceptance_criteria>
  <done>Representative matching and partial-apply conflict tests pass.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | T | Match apply path | mitigate | Parameterized DB writes and explicit conflict checks before updates. |
| T-07-01-02 | R | Job result traceability | mitigate | Structured counters and logs for each apply operation. |
</threat_model>

<verification>Representative-only + partial-apply tests green; no overwrite-on-conflict regressions.</verification>

<success_criteria>STACK-04 backend contract implemented with deterministic conflict-safe stack application.</success_criteria>

<output>After completion, create `.planning/phases/07-stacks-in-matching-pin-similarity/07-01-SUMMARY.md`.</output>
