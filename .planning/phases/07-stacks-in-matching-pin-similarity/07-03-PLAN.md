---
phase: 07-stacks-in-matching-pin-similarity
plan: 3
type: execute
wave: 2
depends_on: ["07-01-PLAN", "07-02-PLAN"]
files_modified:
  - apps/visualizer/frontend/src/components/images/CatalogTab.tsx
  - apps/visualizer/frontend/src/components/image-view/ImageDetailModal.tsx
  - apps/visualizer/frontend/src/components/matching/MatchDetailModal.tsx
  - apps/visualizer/frontend/src/components/ui/ConfirmUndoAction.tsx
  - apps/visualizer/frontend/src/services/api.ts
  - apps/visualizer/frontend/src/components/images/__tests__/CatalogTab.test.tsx
autonomous: true
requirements:
  - STACK-05
user_setup: []
must_haves:
  truths:
    - "Stack edit controls are in Images stack-expanded UI"
    - "Destructive actions use confirm modal + undo toast"
    - "Confirm+undo flow is shared with reject-match behavior (DRY)"
  artifacts:
    - path: apps/visualizer/frontend/src/components/ui/ConfirmUndoAction.tsx
      provides: generic reusable confirm+undo interaction flow
    - path: apps/visualizer/frontend/src/components/images/CatalogTab.tsx
      provides: stack edit control surface in expanded rows
  key_links:
    - from: stack edit UI action
      to: stack mutation API
      via: shared confirm+undo wrapper and optimistic refresh
---

<objective>
Deliver the Phase 7 stack editing UI with a reusable confirm+undo component pattern used by both stack edits and reject-match actions.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-stacks-in-matching-pin-similarity/07-CONTEXT.md
@.planning/phases/06-similarity-stack-ui/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Create shared confirm+undo component</name>
  <files>apps/visualizer/frontend/src/components/ui/ConfirmUndoAction.tsx, apps/visualizer/frontend/src/components/matching/MatchDetailModal.tsx</files>
  <action>
  Extract or implement a generic confirm+undo interaction abstraction and migrate existing reject-match usage to this component without behavior regression.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend && npm run vitest -- MatchDetailModal.test.tsx</automated>
  </verify>
  <acceptance_criteria>
  - Reject-match flow still passes tests using shared component.
  - Shared component supports action label, confirmation text, undo callback, timeout.
  </acceptance_criteria>
  <done>Shared confirm+undo component is in place and reused by reject-match flow.</done>
</task>

<task type="auto">
  <name>Wire stack edit actions in Images UI</name>
  <files>apps/visualizer/frontend/src/components/images/CatalogTab.tsx, apps/visualizer/frontend/src/components/image-view/ImageDetailModal.tsx, apps/visualizer/frontend/src/services/api.ts</files>
  <action>
  Add split/merge/change-representative controls in stack-expanded rows and route them through the shared confirm+undo flow and new backend endpoints.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend && npm run vitest -- CatalogTab.test.tsx</automated>
  </verify>
  <acceptance_criteria>
  - Controls are visible only where stack context exists.
  - Confirm step appears before mutation is sent.
  - Undo window is displayed and executes rollback when available.
  </acceptance_criteria>
  <done>Images stack edit controls are wired through shared confirm+undo flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Frontend tests for stack edit UX</name>
  <files>apps/visualizer/frontend/src/components/images/__tests__/CatalogTab.test.tsx</files>
  <action>
  Add tests for control visibility, confirmation gate, mutation call dispatch, and undo action dispatch.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend && npm run vitest -- CatalogTab.test.tsx</automated>
  </verify>
  <acceptance_criteria>
  - At least one test asserts shared confirm+undo flow invocation.
  - At least one test covers undo callback path.
  </acceptance_criteria>
  <done>Frontend tests validate stack edit interaction and undo behavior.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | T | UI destructive actions | mitigate | Confirmation gate + undo window before irreversible commit state. |
| T-07-03-02 | R | User action ambiguity | mitigate | Explicit action labels and toast feedback with outcome state. |
</threat_model>

<verification>Frontend stack edit tests pass; reject-match regression checks remain green.</verification>

<success_criteria>Stack edit UX shipped in Images UI with DRY shared confirm+undo interaction model.</success_criteria>

<output>After completion, create `.planning/phases/07-stacks-in-matching-pin-similarity/07-03-SUMMARY.md`.</output>
