---
phase: 07-stacks-in-matching-pin-similarity
plan: 5
type: execute
wave: 3
depends_on: ["07-03-PLAN", "07-04-PLAN"]
files_modified:
  - apps/visualizer/backend/tests/test_stack_matching_integration.py
  - apps/visualizer/frontend/src/components/images/__tests__/MatchesTab.test.tsx
  - apps/visualizer/frontend/src/pages/__tests__/SearchPage.test.tsx
  - .planning/phases/07-stacks-in-matching-pin-similarity/07-VERIFICATION.md
autonomous: true
requirements:
  - STACK-04
  - STACK-05
  - NLS-06
user_setup: []
must_haves:
  truths:
    - "Integration checks cover representative-only matching to full-stack apply behavior"
    - "Integration checks cover pin flow with single-pin replacement and fallback"
    - "Phase verification artifact captures pass/fail for all critical paths"
  artifacts:
    - path: apps/visualizer/backend/tests/test_stack_matching_integration.py
      provides: end-to-end representative-only matching assertions
    - path: .planning/phases/07-stacks-in-matching-pin-similarity/07-VERIFICATION.md
      provides: requirement trace and verification outcomes
  key_links:
    - from: phase requirements
      to: test and verification matrix
      via: explicit STACK-04/05/NLS-06 mapping
---

<objective>
Add final integration coverage and produce the phase verification record proving Phase 7 behavior is complete and regression-safe.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-stacks-in-matching-pin-similarity/07-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Representative-only matching integration checks</name>
  <files>apps/visualizer/backend/tests/test_stack_matching_integration.py, apps/visualizer/frontend/src/components/images/__tests__/MatchesTab.test.tsx</files>
  <action>
  Add integration scenarios that verify representative-only compare input and full-stack apply output (including conflict-skipped counts) are surfaced through backend and reflected in UI status.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest apps/visualizer/backend/tests/test_stack_matching_integration.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
  - One scenario verifies representative-only compare set.
  - One scenario verifies partial-apply conflict counters and non-overwrite behavior.
  </acceptance_criteria>
  <done>Representative-only matching integration coverage is in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Pin flow integration checks</name>
  <files>apps/visualizer/frontend/src/pages/__tests__/SearchPage.test.tsx</files>
  <action>
  Add integration-level UI flow checks for pin replacement and inactive-pin fallback messaging after similarity failure.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend && npm run vitest -- SearchPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
  - Single active pin replacement behavior is asserted end-to-end in Search page flow.
  - Fallback warning state is asserted when backend marks pin inactive.
  </acceptance_criteria>
  <done>Pin flow integration checks validate replacement and fallback behavior.</done>
</task>

<task type="auto">
  <name>Write phase verification artifact</name>
  <files>.planning/phases/07-stacks-in-matching-pin-similarity/07-VERIFICATION.md</files>
  <action>
  Record requirement-to-test mapping and outcome checklist for STACK-04, STACK-05, and NLS-06, including fallback and conflict-handling paths.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && gsd-sdk query verify references .planning/phases/07-stacks-in-matching-pin-similarity/07-VERIFICATION.md || true</automated>
  </verify>
  <acceptance_criteria>
  - Verification file includes all phase requirements and explicit pass/fail status.
  - Any unresolved gaps are documented with clear owner and next action.
  </acceptance_criteria>
  <done>Phase verification artifact captures requirement trace and outcomes.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | R | Release confidence | mitigate | Integration checks + verification matrix before closeout. |
| T-07-05-02 | T | Regression drift | mitigate | Cross-layer tests for match/apply and pin fallback contracts. |
</threat_model>

<verification>Integration suites and verification checklist complete with requirement traceability.</verification>

<success_criteria>Phase 7 has auditable verification proving representative matching, stack edits, and pin flow are production-ready.</success_criteria>

<output>After completion, create `.planning/phases/07-stacks-in-matching-pin-similarity/07-05-SUMMARY.md`.</output>
