---
phase: 07.1-phase-7-remediation-fixes
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/visualizer/backend/jobs/handlers.py
  - lightroom_tagger/core/database.py
  - apps/visualizer/backend/tests/test_handlers_batch_embed_image.py
autonomous: true
requirements:
  - Remediation
must_haves:
  truths:
    - "batch_embed_image resolves catalog filepaths before existence checks and encoding"
    - "embed job fails fast when storage accessibility is broadly broken"
    - "embed completion payload includes grouped skip/failure reason counters"
  artifacts:
    - path: apps/visualizer/backend/jobs/handlers.py
      provides: resolved-path preflight, grouped reason accounting, stable progress/checkpoint behavior
    - path: apps/visualizer/backend/tests/test_handlers_batch_embed_image.py
      provides: regression tests for resolve/preflight/grouped reason contracts
  key_links:
    - from: raw images.filepath
      to: encode_images input
      via: resolve_filepath -> os.path.isfile -> buffered encode path list
---

<objective>
Harden `batch_embed_image` runtime behavior so path resolution and storage accessibility are validated up front, and job outcomes return grouped diagnostics that frontend surfaces can render clearly.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07.1-phase-7-remediation-fixes/07.1-CONTEXT.md
@.planning/phases/07.1-phase-7-remediation-fixes/07.1-RESEARCH.md
@apps/visualizer/backend/jobs/handlers.py
@lightroom_tagger/core/database.py
</context>

<tasks>

<task type="auto">
  <name>Resolve paths before embed IO and encoding</name>
  <files>apps/visualizer/backend/jobs/handlers.py, lightroom_tagger/core/database.py</files>
  <read_first>
    apps/visualizer/backend/jobs/handlers.py
    lightroom_tagger/core/database.py
    lightroom_tagger/core/description_service.py
    apps/visualizer/backend/tests/test_handlers_batch_embed_image.py
  </read_first>
  <action>
  Update `_handle_batch_embed_image_inner` so each selected image key resolves its DB filepath through `resolve_filepath(filepath)` before existence checks and before appending to encode batch buffers. Keep checkpoint semantics (`processed_pairs`, fingerprint, progress updates) unchanged while replacing raw-path `os.path.isfile(filepath)` checks with resolved-path checks.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest apps/visualizer/backend/tests/test_handlers_batch_embed_image.py -k "resolve or filepath" -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
  - `handlers.py` imports and uses `resolve_filepath` for embed candidate path handling.
  - No encode path is sent to `encode_images` without prior resolution and file existence validation.
  - Existing checkpoint fields (`job_type`, `fingerprint`, `processed_pairs`, `total_at_start`) remain present.
  </acceptance_criteria>
  <done>Embed IO path contract uses shared path resolution and preserves checkpoint behavior.</done>
</task>

<task type="auto">
  <name>Add startup preflight fail-fast for broad path inaccessibility</name>
  <files>apps/visualizer/backend/jobs/handlers.py</files>
  <read_first>
    apps/visualizer/backend/jobs/handlers.py
    .planning/phases/07.1-phase-7-remediation-fixes/07.1-CONTEXT.md
    .planning/phases/07.1-phase-7-remediation-fixes/07.1-RESEARCH.md
  </read_first>
  <action>
  Introduce a bounded preflight sample over `remaining` embed keys before the main encode loop. For each sampled key, classify path readiness (`no_row`, `empty_path`, `unresolved_or_missing`). If failure ratio exceeds an explicit threshold (implemented as constants in handler), fail job immediately with a single actionable message instructing mount/share validation instead of running the full batch.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest apps/visualizer/backend/tests/test_handlers_batch_embed_image.py -k preflight -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
  - Preflight sample size and fail-fast ratio are explicit constants in code (not magic literals).
  - When threshold is met, job status becomes failed with an actionable storage accessibility error.
  - When threshold is not met, embed flow proceeds into normal batching path.
  </acceptance_criteria>
  <done>Embed job exits early on systemic storage/path failures with actionable diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Emit grouped skip/failure reason payload and add backend tests</name>
  <files>apps/visualizer/backend/jobs/handlers.py, apps/visualizer/backend/tests/test_handlers_batch_embed_image.py</files>
  <read_first>
    apps/visualizer/backend/jobs/handlers.py
    apps/visualizer/backend/tests/test_handlers_batch_embed_image.py
    apps/visualizer/frontend/src/components/jobs/JobDetailModal.tsx
  </read_first>
  <action>
  Track grouped counters throughout embed execution using stable keys: `no_row`, `empty_path`, `unresolved_or_missing`, and `encode_failed`. Include these counters in `runner.complete_job(... result ...)` under a dedicated payload object (for example `skip_reason_counts`). Add tests covering grouped counter values for mixed input conditions and per-image encode fallback failures.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest apps/visualizer/backend/tests/test_handlers_batch_embed_image.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
  - Completion payload includes grouped reason counts with deterministic keys.
  - Tests assert grouped count values for no-row, empty-path, missing/unresolved, and encode-failure scenarios.
  - Existing top-level result keys (`embedded`, `skipped`, `failed`, `total`) remain intact.
  </acceptance_criteria>
  <done>Backend returns concise grouped diagnostics suitable for frontend rendering and troubleshooting.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-04 | T | Embed filepath contract | mitigate | Resolve-before-IO pipeline and explicit file existence checks before encode. |
| T-07.1-05 | D | Batch embed execution | mitigate | Preflight fail-fast prevents long-running no-op workloads during mount outages. |
| T-07.1-06 | R | Operational diagnostics | mitigate | Grouped reason counters in result payload + warnings in logs for postmortem traceability. |
</threat_model>

<verification>
Backend tests confirm resolved-path behavior, preflight threshold behavior, and grouped reason payload shape while preserving checkpoint and result compatibility.
</verification>

<success_criteria>
`batch_embed_image` no longer silently burns time on inaccessible paths and exposes clear structured reasons for skipped/failed rows.
</success_criteria>

<output>After completion, create `.planning/phases/07.1-phase-7-remediation-fixes/07.1-02-SUMMARY.md`.</output>
