---
phase: 07.1-phase-7-remediation-fixes
plan: 2
subsystem: embed-job-backend
requirements-completed:
  - Remediation
duration: "~45 min"
completed: "2026-04-26T19:24:00Z"
---

# Phase 7.1 Plan 2: Embed path hardening and grouped diagnostics

**One-liner:** `batch_embed_image` now resolves catalog paths before IO, fails fast when sampled path accessibility is broadly broken, and returns grouped `skip_reason_counts` diagnostics (`no_row`, `empty_path`, `unresolved_or_missing`, `encode_failed`) in completion payloads.

## Commits

| Task | Hash | Message |
|------|------|---------|
| 1 | `working-tree` | feat(07.1-02): resolve catalog filepaths before embed checks and encoding |
| 2 | `working-tree` | feat(07.1-02): add preflight fail-fast for systemic path accessibility failures |
| 3 | `working-tree` | test(07.1-02): add grouped skip/failure reason payload coverage |

## Key files

| File | Role |
|------|------|
| `apps/visualizer/backend/jobs/handlers.py` | Added `resolve_filepath` path classification, preflight thresholds, and grouped reason accounting |
| `apps/visualizer/backend/tests/test_handlers_batch_embed_image.py` | Added regression tests for resolve-before-encode, preflight fail-fast, and grouped reason payload |

## Deviations from Plan

- None - plan executed as scoped.

## Verification

```text
/Users/ccanales/projects/lightroom-tagger/.venv/bin/python -m pytest apps/visualizer/backend/tests/test_handlers_batch_embed_image.py -q --tb=short  → 9 passed
```

## Self-Check: PASSED

- No encode path is sent to `encode_images` before `resolve_filepath` + existence validation.
- Preflight sample/ratio thresholds are explicit constants (`_EMBED_PREFLIGHT_SAMPLE_SIZE`, `_EMBED_PREFLIGHT_FAIL_RATIO`).
- Completion payload preserves legacy totals and adds deterministic grouped diagnostics under `skip_reason_counts`.
