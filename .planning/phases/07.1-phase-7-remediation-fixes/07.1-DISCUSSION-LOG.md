# Phase 7.1: Phase 7 remediation fixes - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md; this log preserves alternatives considered.

**Date:** 2026-04-26
**Phase:** 07.1-phase-7-remediation-fixes
**Areas discussed:** embed flow discoverability, filepath resolution contract

---

## Embed flow discoverability

| Option | Description | Selected |
|--------|-------------|----------|
| Keep current fallback-only trigger | Users can start embed jobs only from `ImageDetailModal` after a similarity failure | |
| Add first-class Processing trigger + direct Job Queue navigation | Provide an explicit job trigger in Processing and a direct `/processing?tab=jobs` action after enqueue | ✓ |

**User's choice:** Add first-class Processing trigger and direct Job Queue navigation.
**Notes:** User explicitly flagged the current lack of discoverable UI trigger as a core remediation issue.

---

## Filepath handling for embed reliability

| Option | Description | Selected |
|--------|-------------|----------|
| Keep raw DB filepath checks at runtime | Continue using `images.filepath` directly for `os.path.isfile` and encode input | |
| Resolve/preflight paths and surface grouped skip diagnostics | Normalize paths before encode, fail fast on broad storage access failures, and return grouped skip reasons | ✓ |

**User's choice:** Resolve/preflight paths with grouped diagnostics.
**Notes:** User explicitly called out bad use of original filepath and requested remediation in this phase.

---

## Claude's Discretion

- Exact Processing placement for the embed trigger control
- Preflight sample size and fail-fast threshold
- Final grouped skip-reason key names

## Deferred Ideas

- Additional Phase 7 remediation areas beyond these two locked defects (representative matching edge cases, stack-edit UX hardening)
