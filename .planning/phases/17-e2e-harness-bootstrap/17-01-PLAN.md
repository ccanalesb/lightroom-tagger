---
plan: 17-01
wave: 1
title: "E2E pytest isolation and directory scaffold"
slug: e2e-pytest-isolation-scaffold
depends_on: []
files_modified:
  - apps/visualizer/backend/tests/conftest.py
  - apps/visualizer/backend/tests/e2e/pytest.ini
  - apps/visualizer/backend/tests/e2e/harness/__init__.py
  - apps/visualizer/backend/tests/e2e/fixtures/README.md
autonomous: true
requirements:
  - TEST-03
---

<objective>
Add `tests/e2e/` under the visualizer backend with a dedicated `pytest.ini`, package scaffold for the harness (`harness/__init__.py`), a documented `fixtures/` directory, and a parent-tree `collect_ignore` entry so **`pytest tests/`** (backend default CI/local unit invocation) never collects E2E tests even after `tests/e2e/test_*.py` exists (`17-CONTEXT.md` D-01–D-03).
</objective>

<tasks>
  <task id="17-01-T1">
    <title>Create `tests/e2e/pytest.ini`</title>
    <read_first>
      - `.planning/phases/17-e2e-harness-bootstrap/17-CONTEXT.md` (D-02, E2E invocation and isolation)
      - `.planning/phases/17-e2e-harness-bootstrap/17-PATTERNS.md` §2 verbatim `pytest.ini` suggestion
      - apps/visualizer/backend/tests/conftest.py (current tree layout; sibling `e2e/` path)
    </read_first>
    <action>
      Create **`apps/visualizer/backend/tests/e2e/pytest.ini`** with exactly this `[pytest]` block (adapt only if tooling forbids duplication of `markers` names; semantics must remain):

```ini
[pytest]
testpaths = .
python_files = test_*.py
python_functions = test_*
markers =
    e2e: tests that spin up browser + stack (slow; not for unit CI)
addopts =
    -ra
```
    </action>
    <acceptance_criteria>
      - `rg -n "^\[pytest\]$|^testpaths = \\.$|^markers =$|e2e: tests that spin up browser" apps/visualizer/backend/tests/e2e/pytest.ini` exits 0
      - `test -f /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/tests/e2e/pytest.ini` exits 0
    </acceptance_criteria>
  </task>

  <task id="17-01-T2">
    <title>Scaffold harness and fixtures packages</title>
    <read_first>
      - `.planning/phases/17-e2e-harness-bootstrap/17-PATTERNS.md` (File manifest rows for harness + fixtures)
      - `.planning/phases/17-e2e-harness-bootstrap/17-CONTEXT.md` (fixture deliverables D-08–D-10)
      - `apps/visualizer/backend/tests/conftest.py` (existing tree `pytest` / path precedent for documenting invocation and layout)
    </read_first>
    <action>
      1. Create **`apps/visualizer/backend/tests/e2e/harness/__init__.py`** as an empty file (package marker).

      2. Create **`apps/visualizer/backend/tests/e2e/fixtures/README.md`** documenting in plain language:
      - Canonical committed paths: **`catalog.lrcat`** (minimal real Lightroom catalog slice) and **`library_seed.db`** (seeded SQLite for `LIBRARY_DB`), per `17-CONTEXT.md` D-08–D-10.
      - Regeneration commands from later plans: **`python export_fixture.py --library`** and **`python export_fixture.py --catalog-from PATH`** (executor will wire these filenames in Plan 02).
      - E2E runtime copies **`library_seed.db`** into session temp dirs (D-12) — cite `shutil.copy` pattern only, no prose “should”.
    </action>
    <acceptance_criteria>
      - `test -f /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/tests/e2e/harness/__init__.py` exits 0
      - `test -f /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/tests/e2e/fixtures/README.md` exits 0
      - `rg -n "catalog\\.lrcat|library_seed\\.db|export_fixture\\.py --library|export_fixture\\.py --catalog-from" apps/visualizer/backend/tests/e2e/fixtures/README.md` exits 0
    </acceptance_criteria>
  </task>

  <task id="17-01-T3">
    <title>Exclude `tests/e2e` from default collection</title>
    <read_first>
      - apps/visualizer/backend/tests/conftest.py (full file before edit)
      - `.planning/phases/17-e2e-harness-bootstrap/17-CONTEXT.md` (two separate pytest invocations; default `pytest tests/` must not collect E2E)
    </read_first>
    <action>
      Edit **`apps/visualizer/backend/tests/conftest.py`**: append a module-level **`collect_ignore = ["e2e"]`** (list with single string **`e2e`**) placed after the **`sys.path.insert`** lines and before end of file, with **one-line comment**: `# Exclude tests/e2e — run with: pytest -c tests/e2e/pytest.ini tests/e2e/` (exact substring of the invocation in the comment).
    </action>
    <acceptance_criteria>
      - `rg -n "collect_ignore\\s*=\\s*\\[\"e2e\"\\]" apps/visualizer/backend/tests/conftest.py` exits 0
      - `rg -n "pytest -c tests/e2e/pytest.ini tests/e2e/" apps/visualizer/backend/tests/conftest.py` exits 0
    </acceptance_criteria>
  </task>
</tasks>

<verification>
From repo root:

1. **`test -f /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/tests/e2e/pytest.ini && test -f /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/tests/e2e/harness/__init__.py`**

2. **`cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend && python -m pytest tests/e2e/ -c tests/e2e/pytest.ini --collect-only -q`** — exit code **`5`** (no tests collected yet) **or** **`0`** (if collection empty is tolerated by pytest version — document actual exit code in SUMMARY).

3. **`cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend && python -m pytest tests/ --collect-only -q`** — output MUST NOT contain substring **`tests/e2e/`** yet (until plan 03–04 adds tests; re-run note: after Phase 17 complete this check becomes: default collection must not list **`e2e`** paths — see Plan 04).
</verification>

<must_haves>
- [ ] `apps/visualizer/backend/tests/e2e/pytest.ini` exists with `testpaths = .` and declared `e2e` marker line starting with **`e2e:`**
- [ ] `apps/visualizer/backend/tests/e2e/harness/__init__.py` and **`fixtures/README.md`** exist under `tests/e2e/`
- [ ] **`apps/visualizer/backend/tests/conftest.py`** defines **`collect_ignore = ["e2e"]`** and comment containing **`pytest -c tests/e2e/pytest.ini tests/e2e/`**
- [ ] No files created under **`tests/e2e/`** named **`test_*.py`** in this plan (smoke deferred to Plan 04)
</must_haves>
