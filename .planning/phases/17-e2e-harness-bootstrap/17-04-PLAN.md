---
plan: 17-04
wave: 4
title: "E2E smoke test — homepage title + Layout h1 via CDP stack"
slug: e2e-smoke-test-verification
depends_on:
  - 17-03
files_modified:
  - apps/visualizer/backend/tests/e2e/test_smoke.py
autonomous: true
requirements:
  - TEST-03
---

<objective>
Add **`apps/visualizer/backend/tests/e2e/test_smoke.py`** (**`@pytest.mark.e2e`**) asserting **`viz_e2e_base_url`** + **`browser_session`** open **`/`**, **`wait_for`** **`h1`**, **`get_text`** matches **`APP_TITLE`** literal **`Lightroom Tagger`** (`frontend/src/constants/strings.ts`), with optional **`document.title`** assertion — proves harness end-to-end (`17-CONTEXT.md` D-15, **`17-PATTERNS.md`** §5).
</objective>

<tasks>
  <task id="17-04-T1">
    <title>Create `tests/e2e/test_smoke.py`</title>
    <read_first>
      - **`apps/visualizer/backend/tests/e2e/conftest.py`** (fixture IDs **`viz_e2e_base_url`**, **`browser_session`**)
      - **`apps/visualizer/frontend/src/constants/strings.ts`** (line **`export const APP_TITLE = 'Lightroom Tagger'`** literal)
      - **`apps/visualizer/frontend/index.html`** (`<title>Lightroom Tagger</title>`)
      - **`apps/visualizer/backend/tests/e2e/harness/browser_session.py`** (method signatures)
    </read_first>
    <action>
      Create **`apps/visualizer/backend/tests/e2e/test_smoke.py`** with:

```python
import pytest

pytestmark = pytest.mark.e2e

TITLE = "Lightroom Tagger"


def test_e2e_visualizer_homepage_smoke(browser_session, viz_e2e_base_url) -> None:
    browser_session.navigate(f"{viz_e2e_base_url.rstrip('/')}/")
    browser_session.wait_for("h1", timeout_seconds=60.0)
    assert TITLE in browser_session.get_text("h1")
```

**Hard requirements**:

1. File MUST contain the line **`pytestmark = pytest.mark.e2e`** verbatim.

2. **`TITLE`** constant MUST be exactly **`"Lightroom Tagger"`** (`APP_TITLE` parity with **`apps/visualizer/frontend/src/constants/strings.ts`**).

3. **`navigate`** MUST use **`.rstrip('/')`** on **`viz_e2e_base_url`** before appending **`"/"`**.

4. **`wait_for`** first argument MUST be the string **`"h1"`**.

5. **`get_text`** first argument MUST be the string **`"h1"`**.

6. Test parameters MUST request **`browser_session`** plus **`viz_e2e_base_url`** (**order not critical**) because **`browser_session`** fixture from Plan **17-03** declares a hard dependency on **`viz_e2e_base_url`**.
    </action>
    <acceptance_criteria>
      - **`test -f /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/tests/e2e/test_smoke.py`** exits **`0`**
      - **`rg -n "pytestmark = pytest.mark.e2e" apps/visualizer/backend/tests/e2e/test_smoke.py`** exits **`0`**
      - **`rg -n '^TITLE = \"Lightroom Tagger\"' apps/visualizer/backend/tests/e2e/test_smoke.py`** exits **`0`**
      - **`rg -n "def test_e2e_visualizer_homepage_smoke\\(" apps/visualizer/backend/tests/e2e/test_smoke.py`** exits **`0`**
      - **`rg -n "viz_e2e_base_url|browser_session" apps/visualizer/backend/tests/e2e/test_smoke.py`** exits **`0`**
      - **`rg -n "wait_for\\(\\s*\\\"h1\\\"|\\'h1\\'\\)" apps/visualizer/backend/tests/e2e/test_smoke.py`** exits **`0`**
    </acceptance_criteria>
  </task>
</tasks>

<verification>

** Preconditions:** (**document in SUMMARY**)

- Repo root **`.venv`** Python used for **`npm`**/`pytest` consistency OR **`npm`** resolves per developer PATH.
- **`browser-harness`** on **`PATH`** and **`browser-harness --doctor`** reports clean per **`~/.cursor/skills/browser-harness/SKILL.md`**.
- Google Chrome reachable for CDP (**already running** harness workflow).

Commands (**exit **`0`** all**):

1. **`cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend && python -m pytest tests/ --collect-only -q 2>&1`** output MUST **`NOT`** contain substring **`test_e2e_visualizer_homepage_smoke`** (parent **`tests/conftest.py`** **`collect_ignore`** guardrail).

2. **`cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend && python -m pytest tests/e2e/ -c tests/e2e/pytest.ini tests/e2e/test_smoke.py -q`** — exit **`0`**, **`1`** test **passed**.
</verification>

<must_haves>
- [ ] **`test_smoke.py`** marked **`pytest.mark.e2e`** (`pytestmark` assignment)
- [ ] Smoke test exercises **`viz_e2e_base_url`** + **`BrowserSession.navigate`**, **`wait_for("h1")`**, asserts **`Lightroom Tagger`** header text surfaced (React **`Layout.tsx`** **`h1`**)
- [ ] **`pytest tests/`** default invocation does **NOT** surface **`tests/e2e/test_smoke.py`** in **`--collect-only`** output (**`collect_ignore = ["e2e"]`** from Plan **01** proves isolation)
</must_haves>
