---
plan: 13-01
wave: 1
depends_on: []
phase: 13
files_modified:
  - apps/visualizer/backend/jobs/handlers.py
  - apps/visualizer/backend/jobs/handlers/_legacy.py
  - apps/visualizer/backend/jobs/handlers/__init__.py
  - apps/visualizer/backend/jobs/handlers/analyze.py
  - apps/visualizer/backend/jobs/handlers/embed.py
  - apps/visualizer/backend/jobs/handlers/matching.py
  - apps/visualizer/backend/jobs/handlers/stacks.py
  - apps/visualizer/backend/jobs/handlers/instagram.py
  - apps/visualizer/backend/jobs/handlers/common.py
autonomous: true
requirements_addressed: [REFACTOR-01]
---

```xml
<objective>
Replace the flat module file `apps/visualizer/backend/jobs/handlers.py` with package `apps/visualizer/backend/jobs/handlers/`, keeping all handler code in `_legacy.py` temporarily, exposing `JOB_HANDLERS` from `__init__.py` with `__all__ = ("JOB_HANDLERS",)`, plus **interim** package-level re-exports from `_legacy`: every symbol that **13-01-T00** proves is patched or imported as `jobs.handlers.<name>` (flat path, no submodule segment) must be bound on the package — not only the 15 handlers and four privates. Add empty family stubs. Fix relative imports: `path_setup` once in `__init__.py`; `checkpoint` uses `..checkpoint` in `_legacy.py`. Zero logic changes; full backend pytest must pass.
</objective>

<threat_model>
- Accidental behavior change: omitting import rewrites in `_legacy.py` breaks handler registration or path setup.
- Broken import chains: wrong relative path (`jobs.handlers.path_setup` does not exist) if `path_setup` is left in `_legacy.py`.
- Silent failure: `jobs/handlers.py` and `jobs/handlers/` must not both exist — only the package after this commit.
Mitigations: explicit grep criteria for `..checkpoint`, `path_setup` only in `__init__.py`, smoke import of `JOB_HANDLERS`, full pytest.
</threat_model>

<tasks>
  <task id="13-01-T00">
    <title>Inventory every patch target binding on the handlers package (tests + callers)</title>
    <read_first>
apps/visualizer/backend/tests/ (grep-driven sweep; include integration tests)
apps/visualizer/backend/ (optional: `rg "patch\\(['\\\"]jobs\\.handlers\\."` outside tests/)
    </read_first>
    <action>
**Before** `handlers/__init__.py` is written, build a merged, sorted-unique list of **attribute names** that must remain resolvable on `jobs.handlers` during the scaffold (everything tests monkeypatch on the package root).

Run from repo root (adjust path if tests move):
- `grep -rh "patch('jobs\\.handlers\\." apps/visualizer/backend/tests/ | sort -u`
- `grep -rh 'patch("jobs\\.handlers\\.' apps/visualizer/backend/tests/ | sort -u`

Merge, dedupe, and parse each line: record the **flat** patch targets only — those where the string has the form `patch('jobs.handlers.<id>'` or `patch("jobs.handlers.<id>"` with no `.` inside the path (e.g. `jobs.handlers.add_job_log` → `add_job_log`). Lines that already patch a submodule path (`jobs.handlers.embed.encode_images`, etc.) belong to later phases; they need not be re-exported on the package root in T01.

Also record any `patch("jobs\\.handlers\\.` double-quote variants. Extend with **`rg "patch\\(['\\\"]jobs\\.handlers\\."`** over non-test backend code if anything patches the package outside `tests/`.

Typical names beyond the 15 handler callables: `init_database`, `add_job_log`, `load_config`, `os`, `get_job`, `update_job_field`, `require_library_db`, `match_dump_media`, `encode_images`, `embed_texts`, `fingerprint_*`, `list_*` helpers, inner handlers (`_handle_*_inner`), `*_single_image`, etc. — **the grep output is authoritative**, not this example list.

Save the merged list in the commit message body, a short `HANDLERS-SHIM-EXPORTS.txt` under `.planning/phases/13-handlers-split-per-job-family/`, or equivalent so reviewers can diff it against `__init__.py` imports.
    </action>
    <acceptance_criteria>
The merged `sort -u` outputs from both `grep -rh` commands exist in the artifact above (or pasted in PR) with no hand-waved omissions.

Every **flat** `jobs.handlers.<name>` patch target that refers to a symbol still defined in `_legacy.py` after the move appears in the re-export plan for `__init__.py` (next task).
    </acceptance_criteria>
  </task>

  <task id="13-01-T01">
    <title>Scaffold handlers package and relocate monolith to _legacy.py</title>
    <depends_on>13-01-T00</depends_on>
    <read_first>
apps/visualizer/backend/jobs/handlers.py (full file)
apps/visualizer/backend/jobs/path_setup.py
.planning/phases/13-handlers-split-per-job-family/13-CONTEXT.md
.planning/phases/13-handlers-split-per-job-family/13-RESEARCH.md
.cursor/rules/backend-restart.mdc
    </read_first>
    <action>
Create directory `apps/visualizer/backend/jobs/handlers/`. Move (not copy) `apps/visualizer/backend/jobs/handlers.py` to `apps/visualizer/backend/jobs/handlers/_legacy.py` so the path `apps/visualizer/backend/jobs/handlers.py` no longer exists as a file.

In `_legacy.py` apply these edits exactly:
1. Remove lines `from . import path_setup as _path_setup  # noqa: F401` and the blank line after it if redundant — path_setup will load only from `handlers/__init__.py`.
2. Change `from .checkpoint import (` to `from ..checkpoint import (` (same symbol list: fingerprint_batch_describe through fingerprint_vision_match).

Leave all other imports and the trailing `JOB_HANDLERS = { ... }` dict (lines ~3833–3849) unchanged inside `_legacy.py` for this plan.

Add `apps/visualizer/backend/jobs/handlers/__init__.py` with:
- `from . import path_setup as _path_setup  # noqa: F401 — side-effect import; keep once here only`
- `from ._legacy import JOB_HANDLERS`
- **Interim test-compat re-exports:** from `._legacy`, import **every** symbol listed in **13-01-T00** that is a **flat** `patch('jobs.handlers.<name>')` / `patch("jobs.handlers.<name>")` target and is still implemented in `_legacy.py` — this is **normative** and supersedes a hand-curated list. Tests patch module-level bindings (`init_database`, `add_job_log`, `load_config`, `os`, `get_job`, `update_job_field`, `require_library_db`, `match_dump_media`, `encode_images`, `list_*`, `fingerprint_*`, `_score_single_image`, inner `_*_inner`, etc.); omitting any T00 name breaks `unittest.mock.patch`.
  - **Minimum** set (must remain covered if grep finds them): all 15 `JOB_HANDLERS` callables; direct-import helpers used by tests: `_select_instagram_keys`, `_resolve_date_window`, `_build_burst_segments`, `_catalog_cache_stage_mapped_progress`.
- `__all__ = ("JOB_HANDLERS",)` — **only** `JOB_HANDLERS` in `__all__` per D-02; other names remain importable as package attributes for the transition (removed again in plan 13-07).

Add placeholder modules (each file: module docstring only, no imports), paths:
- `apps/visualizer/backend/jobs/handlers/common.py` — docstring: """Shared helpers for job handlers (scaffold; populated in plan 13-02)."""
- `apps/visualizer/backend/jobs/handlers/analyze.py` — """Describe/score/batch-analyze handlers (scaffold; plan 13-07)."""
- `apps/visualizer/backend/jobs/handlers/embed.py` — """Embedding handlers (scaffold; plan 13-04)."""
- `apps/visualizer/backend/jobs/handlers/matching.py` — """Vision match / catalog prep handlers (scaffold; plan 13-05)."""
- `apps/visualizer/backend/jobs/handlers/stacks.py` — """Stack detection, similarity, cache-build handlers (scaffold; plan 13-06)."""
- `apps/visualizer/backend/jobs/handlers/instagram.py` — """Instagram analysis/import handlers (scaffold; plan 13-03)."""

Do not import these stub modules from `__init__.py` yet (stubs are inert).

Optional: if `job-ui-contract` / `job-handler-flag.sh` still reference the deleted file path `handlers.py`, note for a follow-up — do not change frontend in this refactor-only plan unless the hook fails CI.
    </action>
    <acceptance_criteria>
`test -f apps/visualizer/backend/jobs/handlers/_legacy.py` exits 0; `test ! -f apps/visualizer/backend/jobs/handlers.py` exits 0.

`apps/visualizer/backend/jobs/handlers/_legacy.py` contains the substring `from ..checkpoint import (`.

`apps/visualizer/backend/jobs/handlers/_legacy.py` does NOT contain the substring `path_setup`.

`apps/visualizer/backend/jobs/handlers/__init__.py` contains the line `from . import path_setup as _path_setup  # noqa: F401` and contains `from ._legacy import JOB_HANDLERS`.

`apps/visualizer/backend/jobs/handlers/__init__.py` contains `__all__ = ("JOB_HANDLERS",)`.

`apps/visualizer/backend/jobs/handlers/__init__.py` imports `handle_batch_analyze` from `._legacy` (line containing `handle_batch_analyze` in a `from ._legacy import` block or multi-import).

Command `cd apps/visualizer/backend && python -c "from jobs.handlers import JOB_HANDLERS; assert isinstance(JOB_HANDLERS, dict) and len(JOB_HANDLERS)==15"` exits 0.

Command `cd apps/visualizer/backend && python -c "from jobs.handlers import handle_batch_analyze, _resolve_date_window, _build_burst_segments"` exits 0.

**Patch-target resolution:** For every **flat** `<name>` in the T00 inventory, `getattr(importlib.import_module('jobs.handlers'), '<name>')` succeeds (or `from jobs.handlers import <name>` for public identifiers) — automate a loop over the T00 list so every patch string still resolves after the scaffold.

Command `cd apps/visualizer/backend && python -c "import app"` exits 0.

Command `cd apps/visualizer/backend && pytest` exits 0.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- Import smoke: `from jobs.handlers import JOB_HANDLERS` with 15 keys.
- T00 inventory saved; `__init__.py` re-exports every flat `patch('jobs.handlers.*')` name from T00 still defined in `_legacy`.
- Full suite: `cd apps/visualizer/backend && pytest` — 0 failures (baseline ~663 tests per 13-VALIDATION.md).
- No coexisting `handlers.py` file under `jobs/` next to the `handlers/` package.
</verification>

<must_haves>
- `JOB_HANDLERS` keys unchanged from monolith (`analyze_instagram`, `instagram_import`, `vision_match`, `enrich_catalog`, `prepare_catalog`, `batch_describe`, `single_describe`, `single_score`, `batch_score`, `batch_analyze`, `batch_stack_detect`, `batch_catalog_similarity`, `batch_text_embed`, `batch_embed_image`, `catalog_cache_build`).
- `app.py` line `from jobs.handlers import JOB_HANDLERS` still resolves without code change to `app.py`.
- Single `path_setup` side-effect import in `handlers/__init__.py` only.
</must_haves>
```
