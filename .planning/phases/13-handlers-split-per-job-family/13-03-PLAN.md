---
plan: 13-03
wave: 3
depends_on: [02]
phase: 13
files_modified:
  - apps/visualizer/backend/jobs/handlers/instagram.py
  - apps/visualizer/backend/jobs/handlers/_legacy.py
  - apps/visualizer/backend/jobs/handlers/__init__.py
  # tests/: none for 13-03 — see `<test_files_inventory>` in plan body (concrete grep evidence).
autonomous: true
requirements_addressed: [REFACTOR-01]
---

```xml
<test_files_inventory>
**Backend tests — no file changes required for this plan** (instagram handler extraction only). Grep evidence (repo baseline): `rg "patch\(['\"]jobs\.handlers\.(handle_analyze_instagram|handle_instagram_import)\b" apps/visualizer/backend/tests/` returns empty; `rg "from jobs\.handlers import.*handle_(analyze_instagram|instagram_import)" apps/visualizer/backend/tests/` returns empty. Integration tests refer to job **types** `'analyze_instagram'` / `'instagram_import'` without patching handler callables.

Note: `apps/visualizer/backend/tests/test_handlers_catalog_cache_build.py` patches `jobs.handlers.list_instagram_dump_keys_needing_clip_embedding` (helper still on package root until stacks/embed plans) — not in scope for 13-03 retargeting.
</test_files_inventory>

<objective>
Move `handle_analyze_instagram` and `handle_instagram_import` from `_legacy.py` to `instagram.py`. Assemble `JOB_HANDLERS` in `__init__.py` (import handlers from `instagram` + remaining symbols from `_legacy` until removed). Update tests when grep finds `@patch` / `patch()` or imports that must follow symbols into `instagram.py` — if the inventory shows none, skip test edits. Full pytest green.
</objective>

<threat_model>
- Duplicate registration if `JOB_HANDLERS` still defined in `_legacy` and `__init__`.
- Wrong patch strings: tests still patch `jobs.handlers.add_job_log` — must become `jobs.handlers.instagram.add_job_log` for names imported into `instagram.py`.
Mitigations: remove `JOB_HANDLERS` dict from `_legacy.py` in this commit; define single dict in `__init__.py`; grep tests for `handle_analyze_instagram`, `handle_instagram_import`, `instagram`.
</threat_model>

<tasks>
  <task id="13-03-T01">
    <title>Extract instagram.py and rewire registry (+ tests only if grep finds patch/import targets)</title>
    <read_first>
apps/visualizer/backend/jobs/handlers/_legacy.py
apps/visualizer/backend/jobs/handlers/common.py
apps/visualizer/backend/jobs/handlers/__init__.py
.planning/phases/13-handlers-split-per-job-family/13-PATTERNS.md
    </read_first>
    <action>
1. Create real `instagram.py`: move `handle_analyze_instagram` and `handle_instagram_import` (full bodies, lines ~401–466 per RESEARCH) from `_legacy.py` into `instagram.py`.

2. At top of `instagram.py`, add imports needed by those handlers (copy from `_legacy.py`: stdlib, `database`, `library_db`, `lightroom_tagger.*`, `from ..checkpoint import` only symbols used by these two handlers). Import shared helpers via `from .common import ...` (list every `common` symbol the bodies use).

3. Delete the two handler definitions from `_legacy.py`.

4. Remove `JOB_HANDLERS = {` block from `_legacy.py` entirely.

5. Rewrite `handlers/__init__.py` to:
   - Keep `from . import path_setup as _path_setup  # noqa: F401`
   - `from .instagram import handle_analyze_instagram, handle_instagram_import`
   - Import all remaining handler callables still defined in `_legacy` (every key not yet extracted) — use explicit imports matching current `JOB_HANDLERS` keys.
   - Define `JOB_HANDLERS = { ... }` mapping string keys to the imported callables; keys for instagram must be `'analyze_instagram'` and `'instagram_import'` pointing at functions from `instagram`.

6. Grep tests under `apps/visualizer/backend/tests/` for:
   - `handle_analyze_instagram`, `handle_instagram_import`
   - patch strings `jobs.handlers.` that reference names imported into `instagram.py` after the move
   Update to `from jobs.handlers.instagram import ...` for direct handler imports; update `@patch('jobs.handlers.X')` to `@patch('jobs.handlers.instagram.X')` for each `X` that now lives in `instagram.py` (e.g. `add_job_log` if bound there).

7. If `lightroom_tagger/scripts/test_import_instagram_dump.py` contains the string `jobs.handlers._select_instagram_keys`, leave for plan 02/common path — only update if it references moved instagram handlers.

8. Confirm no `from . import path_setup` appears in `instagram.py`.
    </action>
    <acceptance_criteria>
`instagram.py` contains `def handle_analyze_instagram(` and `def handle_instagram_import(`.

`_legacy.py` does NOT contain `def handle_analyze_instagram` or `def handle_instagram_import`.

`_legacy.py` does NOT contain the line `JOB_HANDLERS =`.

`__init__.py` contains `from .instagram import handle_analyze_instagram, handle_instagram_import` and defines `JOB_HANDLERS` with keys `'analyze_instagram'` and `'instagram_import'`.

`rg "JOB_HANDLERS" apps/visualizer/backend/jobs/handlers/_legacy.py` returns no matches.

Command `cd apps/visualizer/backend && python -c "from jobs.handlers import JOB_HANDLERS; assert JOB_HANDLERS['analyze_instagram'].__module__ == 'jobs.handlers.instagram'"` exits 0.

Command `cd apps/visualizer/backend && pytest` exits 0.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- Single `JOB_HANDLERS` definition in `handlers/__init__.py`.
- Full pytest green.
</verification>

<must_haves>
- All 15 job types still registered with same string keys.
- No duplicate handler bodies in `_legacy` and `instagram`.
</must_haves>
```
