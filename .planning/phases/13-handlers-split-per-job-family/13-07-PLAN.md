---
plan: 13-07
wave: 7
depends_on: [06]
phase: 13
files_modified:
  - apps/visualizer/backend/jobs/handlers/analyze.py
  - apps/visualizer/backend/jobs/handlers/_legacy.py
  - apps/visualizer/backend/jobs/handlers/__init__.py
  - apps/visualizer/backend/tests/test_handlers_batch_analyze.py
  - apps/visualizer/backend/tests/test_handlers_batch_describe.py
  - apps/visualizer/backend/tests/test_handlers_batch_score.py
  - apps/visualizer/backend/tests/test_handlers_date_window.py
autonomous: true
requirements_addressed: [REFACTOR-01]
---

```xml
<objective>
Move the analyze/describe/score family from `_legacy.py` to `analyze.py`, including `_select_catalog_keys_missing_visual_tags` and all analyze-local helpers (`_score_single_image` through `_handle_batch_analyze_inner` per RESEARCH). Remove `_legacy.py` when empty. Final `__init__.py` assembles `JOB_HANDLERS` only from submodules (`instagram`, `embed`, `matching`, `stacks`, `analyze`) with no `_legacy` import. Full pytest green; `apps/visualizer/backend/jobs/handlers/_legacy.py` must not exist after this commit.
</objective>

<threat_model>
- Leaving dead code or empty `_legacy.py`.
- Tests still patch `jobs.handlers.fingerprint_batch_describe` — must be `jobs.handlers.analyze.fingerprint_batch_describe` or `jobs.checkpoint.fingerprint_batch_describe`.
- Mixed tests (`test_handlers_date_window.py`) import `_resolve_date_window` from common and handlers from analyze — split imports correctly.
Mitigations: `test ! -f .../_legacy.py`; grep for `_legacy` in `__init__.py`; full pytest.
</threat_model>

<tasks>
  <task id="13-07-T01">
    <title>Extract analyze.py, finalize JOB_HANDLERS, delete _legacy.py</title>
    <read_first>
apps/visualizer/backend/jobs/handlers/_legacy.py (entire remainder — should be analyze-only before this task)
apps/visualizer/backend/jobs/handlers/common.py
apps/visualizer/backend/jobs/handlers/__init__.py
apps/visualizer/backend/tests/test_handlers_batch_analyze.py
apps/visualizer/backend/tests/test_handlers_batch_describe.py
apps/visualizer/backend/tests/test_handlers_batch_score.py
apps/visualizer/backend/tests/test_handlers_date_window.py
.planning/phases/13-handlers-split-per-job-family/13-RESEARCH.md (Analyze range ~1127–2465)
    </read_first>
    <action>
1. Populate `analyze.py`: move from `_legacy.py` everything in RESEARCH **analyze** span from `_score_single_image` through end of `_handle_batch_analyze_inner` (~1127–2465), including `handle_single_describe`, `handle_single_score`, `handle_batch_describe`, `handle_batch_score`, `handle_batch_analyze`, `_select_catalog_keys_missing_visual_tags` (lines ~308–344), and all private helpers used only by this family.

2. Move any remaining top-of-file constants from `_legacy.py` that belong to analyze only (none should remain that duplicate `common` / other families — if any throttle constant only used in analyze, move with analyze).

3. `analyze.py` imports: `from ..checkpoint import` fingerprint symbols used by analyze (`fingerprint_batch_describe`, `fingerprint_batch_score`, `fingerprint_catalog_keys`, etc.); `from .common import ...` for shared helpers; full `lightroom_tagger` / `database` imports as required.

4. Delete all moved code from `_legacy.py`. If any import-only lines remain, delete file entirely — target state is **no** `_legacy.py`.

5. Update `__init__.py` to **final D-02 shape**:
   - Keep only `path_setup` side-effect and imports needed to build `JOB_HANDLERS`.
   - Remove **all** interim symbols re-exported from `_legacy` / package root for tests (plan 13-01–13-06): `__init__.py` must **not** contain `from ._legacy` (file deleted) and must **not** import individual `handle_*` for re-export — only construct `JOB_HANDLERS = { ... }` from `.instagram`, `.embed`, `.matching`, `.stacks`, `.analyze` imports.
   - `__all__ = ("JOB_HANDLERS",)` only.

6. Test file updates:
   - `test_handlers_batch_analyze.py`, `test_handlers_batch_describe.py`, `test_handlers_batch_score.py`: imports from `jobs.handlers.analyze`; patches `@patch('jobs.handlers.analyze.add_job_log')` etc.; fingerprint patch prefer `jobs.checkpoint.fingerprint_batch_describe` OR `jobs.handlers.analyze.fingerprint_batch_describe` depending on where imported — must match **actual** module binding after `analyze.py` imports checkpoint.
   - `test_handlers_date_window.py`: `from jobs.handlers.common import _resolve_date_window`; `from jobs.handlers.analyze import handle_batch_describe, handle_batch_score` (or as used); patch `jobs.handlers.analyze.init_database` if that name is bound there, else upstream `lightroom_tagger.core.database.init_database`.

7. Delete file `apps/visualizer/backend/jobs/handlers/_legacy.py` wholly.

8. Run `rg "_legacy" apps/visualizer/backend/jobs/handlers/` — only allow matches in comments/docs if any; **not** in `__init__.py` imports.
    </action>
    <acceptance_criteria>
`analyze.py` contains `def handle_batch_analyze(` and `def _select_catalog_keys_missing_visual_tags(`.

Shell command `test ! -f apps/visualizer/backend/jobs/handlers/_legacy.py` exits 0.

`apps/visualizer/backend/jobs/handlers/__init__.py` does not contain the substring `_legacy`.

`__init__.py` defines `JOB_HANDLERS` with exactly 15 keys and imports handler callables only from `.instagram`, `.embed`, `.matching`, `.stacks`, `.analyze` (no `_legacy`).

`rg "^from \\._legacy import|^from jobs\\.handlers\\._legacy" apps/visualizer/backend/jobs/handlers/__init__.py` returns no matches (empty output).

Command `cd apps/visualizer/backend && python -c "from jobs.handlers import JOB_HANDLERS; k=set(JOB_HANDLERS); assert k=={'analyze_instagram','instagram_import','vision_match','enrich_catalog','prepare_catalog','batch_describe','single_describe','single_score','batch_score','batch_analyze','batch_stack_detect','batch_catalog_similarity','batch_text_embed','batch_embed_image','catalog_cache_build'}"` exits 0.

Command `ls apps/visualizer/backend/jobs/handlers/` stdout does not include `_legacy.py`.

Command `cd apps/visualizer/backend && pytest` exits 0.

**No flat package-root patch targets remain (blocking, CHK-05 / 13-VALIDATION.md):**  
`rg -P "patch\\(['\\\"]jobs\\.handlers\\.(?!instagram\\.|embed\\.|analyze\\.|matching\\.|stacks\\.|common\\.)[a-zA-Z_][a-zA-Z0-9_]*['\\\"]" apps/visualizer/backend/tests/` returns **empty** (exit code 1). This rejects strings like `patch('jobs.handlers.add_job_log')` while allowing `patch('jobs.handlers.embed.encode_images')`. (A naive `grep` on `jobs\\.handlers\\.[a-z]` alone is **not** sufficient — it matches the leading letter of submodule paths too.)

Command sanity-check (optional): same `rg -P` over `apps/visualizer/backend/` outside tests — should be empty for handler patches.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- No `_legacy.py` on disk.
- `pytest` full suite green.
- **Blocking:** same `rg -P …` as final acceptance criterion — zero flat `jobs.handlers.<symbol>` patch targets; only `jobs.handlers.<family>.…` paths.
</verification>

<must_haves>
- Public API unchanged: `from jobs.handlers import JOB_HANDLERS` from `app.py`.
- Analyze-only helper `_select_catalog_keys_missing_visual_tags` lives in `analyze.py` (not `common.py`).
</must_haves>
```
