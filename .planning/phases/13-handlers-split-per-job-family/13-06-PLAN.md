---
plan: 13-06
wave: 6
depends_on: [05]
phase: 13
files_modified:
  - apps/visualizer/backend/jobs/handlers/stacks.py
  - apps/visualizer/backend/jobs/handlers/_legacy.py
  - apps/visualizer/backend/jobs/handlers/__init__.py
  - apps/visualizer/backend/tests/test_handlers_batch_stack_detect.py
  - apps/visualizer/backend/tests/test_handlers_catalog_cache_build.py
  - apps/visualizer/backend/tests/test_handlers_batch_catalog_similarity.py
autonomous: true
requirements_addressed: [REFACTOR-01]
---

```xml
<objective>
Move stacks/similarity/cache-build family from `_legacy.py` to `stacks.py`, including single-consumer helpers: `_catalog_cache_stage_mapped_progress`, `_CatalogCacheStageRunner`, `_normalize_stack_detect_force`, `_parse_date_taken_utc`, `_build_burst_segments`, `_select_stack_representative_key`, `_catalog_similarity_why_matched_line`, inner handlers for similarity/stack/cache-build, and handler entrypoints `handle_batch_catalog_similarity`, `handle_batch_stack_detect`, `handle_catalog_cache_build`. Move constants `_CATALOG_SIMILARITY_SUMMARY_EVERY`, `_STACK_DETECT_SUMMARY_EVERY`. Add **one-way** `from .embed import _handle_batch_embed_image_inner` (and any other `embed` inner symbols) inside `stacks.py` for `handle_catalog_cache_build` composite per RESEARCH. Update listed tests. Full pytest green.
</objective>

<threat_model>
- Circular import: `stacks` imports `embed` inner — `embed` must not import `stacks`.
- Composite job: wrong inner imports break cache build without failing import time.
Mitigations: grep `embed import stacks` in `embed.py` — must be empty; run `test_handlers_catalog_cache_build.py`.
</threat_model>

<tasks>
  <task id="13-06-T01">
    <title>Extract stacks.py including cache stage runner and composite wiring</title>
    <read_first>
apps/visualizer/backend/jobs/handlers/_legacy.py
apps/visualizer/backend/jobs/handlers/common.py
apps/visualizer/backend/jobs/handlers/embed.py
apps/visualizer/backend/jobs/handlers/__init__.py
apps/visualizer/backend/tests/test_handlers_batch_stack_detect.py
apps/visualizer/backend/tests/test_handlers_catalog_cache_build.py
apps/visualizer/backend/tests/test_handlers_batch_catalog_similarity.py
.planning/phases/13-handlers-split-per-job-family/13-RESEARCH.md (lines 3161+, circular import table)
    </read_first>
    <action>
1. Populate `stacks.py` with all symbols from `_legacy.py` in RESEARCH range **79–133** (`_catalog_cache_stage_mapped_progress`, `_CatalogCacheStageRunner`) and **3161–3831** (stack helpers through end of `_handle_catalog_cache_build_inner`), plus entrypoint handlers `handle_batch_catalog_similarity`, `handle_batch_stack_detect`, `handle_catalog_cache_build`.

2. Move constants `_CATALOG_SIMILARITY_SUMMARY_EVERY`, `_STACK_DETECT_SUMMARY_EVERY` into `stacks.py` (not `common`).

3. In `stacks.py` imports: `from .common import ...` as needed; `from ..checkpoint import` symbols for stack/embed/cache fingerprinting as in monolith; **required one-way inner imports from embed**, e.g. `from .embed import _handle_batch_embed_image_inner`, `_handle_batch_text_embed_inner` only if referenced by `_handle_catalog_cache_build_inner` — verify against actual `_legacy.py` call graph before choosing imports.

4. Remove moved blocks from `_legacy.py`.

5. Update `__init__.py` to import `handle_batch_catalog_similarity`, `handle_batch_stack_detect`, `handle_catalog_cache_build` from `.stacks` and set JOB_HANDLERS keys `'batch_catalog_similarity'`, `'batch_stack_detect'`, `'catalog_cache_build'`.

6. Test updates:
   - `test_handlers_batch_stack_detect.py`: `from jobs.handlers.stacks import _build_burst_segments, handle_batch_stack_detect` (or separate lines); patch `jobs.handlers.stacks.*`.
   - `test_handlers_batch_catalog_similarity.py`: import handler from `jobs.handlers.stacks`; patches to `jobs.handlers.stacks.*`.
   - `test_handlers_catalog_cache_build.py`: keep `from jobs.handlers import JOB_HANDLERS`; patch inner functions on `jobs.handlers.stacks` / `jobs.handlers.embed` as appropriate (`_handle_catalog_similarity_inner` → `jobs.handlers.stacks._handle_catalog_similarity_inner`, `_catalog_cache_stage_mapped_progress` → `jobs.handlers.stacks._catalog_cache_stage_mapped_progress`, list/embed patches → `embed` module).

7. No `path_setup` in `stacks.py`.
    </action>
    <acceptance_criteria>
`stacks.py` contains `class _CatalogCacheStageRunner` and `def _catalog_cache_stage_mapped_progress(`.

`stacks.py` contains `def handle_batch_stack_detect(` and `def handle_catalog_cache_build(`.

`stacks.py` contains `def _build_burst_segments(`.

`_legacy.py` does not contain `def handle_catalog_cache_build` or `def _build_burst_segments`.

`embed.py` does not contain the substring `from .stacks import` or `from . import stacks`.

`__init__.py` maps `'catalog_cache_build'` to a callable from `stacks`.

Command `cd apps/visualizer/backend && pytest tests/test_handlers_batch_stack_detect.py tests/test_handlers_catalog_cache_build.py tests/test_handlers_batch_catalog_similarity.py` exits 0.

Command `cd apps/visualizer/backend && pytest` exits 0.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- Composite cache build test passes (embed ↔ stacks inner calls work).
- Full pytest green.
</verification>

<must_haves>
- One-way dependency: `embed` does not import `stacks`.
- Stage runner + mapper colocated in `stacks.py` per D-05.
</must_haves>
```
