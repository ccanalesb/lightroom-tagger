---
plan: 13-04
wave: 4
depends_on: [03]
phase: 13
files_modified:
  - apps/visualizer/backend/jobs/handlers/embed.py
  - apps/visualizer/backend/jobs/handlers/_legacy.py
  - apps/visualizer/backend/jobs/handlers/__init__.py
  - apps/visualizer/backend/tests/test_handlers_batch_embed_image.py
  - apps/visualizer/backend/tests/test_handlers_batch_text_embed.py
autonomous: true
requirements_addressed: [REFACTOR-01]
---

```xml
<objective>
Move `handle_batch_text_embed`, `_handle_batch_text_embed_inner`, `handle_batch_embed_image`, `_handle_batch_embed_image_inner` and embed-only module constants (including `_PREFLIGHT_RNG_SEED`, `_BATCH_EMBED_IMAGE_SIZE`, `_EMBED_PREFLIGHT_SAMPLE_SIZE`, `_EMBED_PREFLIGHT_FAIL_RATIO`, `_EMBED_SKIP_DETAIL_LOG_LIMIT`, `_EMBED_SUMMARY_LOG_EVERY`) from `_legacy.py` to `embed.py`. Update `__init__.py` imports for `batch_text_embed` / `batch_embed_image`. Update monkeypatch and `unittest.mock.patch` targets in embed tests to `jobs.handlers.embed.*`. Full pytest green.
</objective>

<threat_model>
- Patch drift: `test_handlers_batch_embed_image` uses `import jobs.handlers as job_handlers` — monkeypatch must target `jobs.handlers.embed` (or `import jobs.handlers.embed as job_handlers`).
- Name `_PREFLIGHT_RNG_SEED`: keep exact name in `embed.py`; tests use `monkeypatch.setattr(jobs.handlers.embed, '_PREFLIGHT_RNG_SEED', ...)` or equivalent.
Mitigations: explicit acceptance greps on patch strings; run full pytest.
</threat_model>

<tasks>
  <task id="13-04-T01">
    <title>Extract embed family and fix embed test patches</title>
    <read_first>
apps/visualizer/backend/jobs/handlers/_legacy.py
apps/visualizer/backend/jobs/handlers/common.py
apps/visualizer/backend/jobs/handlers/__init__.py
apps/visualizer/backend/tests/test_handlers_batch_embed_image.py
apps/visualizer/backend/tests/test_handlers_batch_text_embed.py
.planning/phases/13-handlers-split-per-job-family/13-RESEARCH.md (Embed family + test patches)
    </read_first>
    <action>
1. Populate `embed.py`: move line ranges per RESEARCH from `**2468–3159**` — `handle_batch_text_embed`, `_handle_batch_text_embed_inner`, `handle_batch_embed_image`, `_handle_batch_embed_image_inner`, plus constants from monolith lines 63–76 that belong in embed (everything under RESEARCH table row: `_BATCH_EMBED_IMAGE_SIZE`, `_EMBED_PREFLIGHT_SAMPLE_SIZE`, `_EMBED_PREFLIGHT_FAIL_RATIO`, `_EMBED_SKIP_DETAIL_LOG_LIMIT`, `_EMBED_SUMMARY_LOG_EVERY`, `_PREFLIGHT_RNG_SEED`). **Do not** move `_CHECKPOINT_MAX_ENTRIES` (already in `common.py`).

2. At top of `embed.py`: imports from stdlib, `database`, `library_db`, `lightroom_tagger.core.*` as required by moved code; `from ..checkpoint import` only needed fingerprint symbols (`fingerprint_batch_embed_image`, `fingerprint_batch_text_embed`, `fingerprint_catalog_keys` if referenced, etc. — mirror `_legacy` imports for this block); `from .common import ...` for all helpers used from common.

3. Remove moved symbols from `_legacy.py`.

4. Update `__init__.py`: add `from .embed import handle_batch_embed_image, handle_batch_text_embed` (and no re-exports of privates per D-02); wire `JOB_HANDLERS['batch_text_embed']` and `JOB_HANDLERS['batch_embed_image']` to these imports; remove `_legacy` imports of these two handlers if present.

5. `test_handlers_batch_embed_image.py`:
   - Change alias to `import jobs.handlers.embed as embed_mod` (or patch `embed_mod.encode_images`, etc.).
   - Replace `@patch("jobs.handlers.add_job_log")` with `@patch("jobs.handlers.embed.add_job_log")` (same for `load_config`, `init_database`, `require_library_db`, `os.getenv` — whatever names are imported **into embed.py**).
   - Replace `@patch("jobs.handlers.encode_images")` if present with `@patch("jobs.handlers.embed.encode_images")`.
   - `monkeypatch.setattr(..., "_EMBED_PREFLIGHT_SAMPLE_SIZE", ...)` etc. — target module `jobs.handlers.embed` (import as `import jobs.handlers.embed as embed_mod` then `monkeypatch.setattr(embed_mod, "_EMBED_PREFLIGHT_SAMPLE_SIZE", ...)`).
   - `from jobs.handlers.embed import handle_batch_embed_image` for handler import.

6. `test_handlers_batch_text_embed.py`: `from jobs.handlers.embed import handle_batch_text_embed`; patch prefixes `jobs.handlers.embed.*`; if code did `monkeypatch.setattr(handlers, "embed_texts", ...)` with `handlers` = package, change to setattr on `jobs.handlers.embed` where `embed_texts` is bound.

7. Do not add `path_setup` to `embed.py`.
    </action>
    <acceptance_criteria>
`embed.py` contains `def handle_batch_text_embed(` and `def handle_batch_embed_image(`.

`embed.py` contains `_PREFLIGHT_RNG_SEED: int | None = None` (or equivalent typing) and `_BATCH_EMBED_IMAGE_SIZE =`.

`_legacy.py` does not contain `def handle_batch_text_embed` or `def handle_batch_embed_image`.

`__init__.py` imports `handle_batch_embed_image` and `handle_batch_text_embed` from `.embed`.

`test_handlers_batch_embed_image.py` contains the substring `jobs.handlers.embed` at least once (patch or import path updated off flat `jobs.handlers.` for embed-bound names).

**No flat embed-specific patch roots remain in embed tests** (blocking, CHK-04):  
`grep -rE "patch\(['\"]jobs\.handlers\.(encode_images|_EMBED|_PREFLIGHT)" apps/visualizer/backend/tests/test_handlers_batch_embed_image.py apps/visualizer/backend/tests/test_handlers_batch_text_embed.py` returns **0** lines (covers both quote styles; extend the alternation if those two files still patch other embed-only symbols on the flat package root).

Command `cd apps/visualizer/backend && pytest tests/test_handlers_batch_embed_image.py tests/test_handlers_batch_text_embed.py` exits 0.

Command `cd apps/visualizer/backend && pytest` exits 0.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- Grep `handle_batch_embed_image` / `handle_batch_text_embed` defined only in `embed.py`.
- Full backend pytest green.
</verification>

<must_haves>
- Embedding behavior unchanged (CLIP/text embed checkpoints and logs preserved).
- `embed.py` remains a **leaf**: no imports from `stacks` or `matching`.
</must_haves>
```
