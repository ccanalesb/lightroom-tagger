---
plan: 13-02
wave: 2
depends_on: [01]
phase: 13
files_modified:
  - apps/visualizer/backend/jobs/handlers/common.py
  - apps/visualizer/backend/jobs/handlers/_legacy.py
  - apps/visualizer/backend/jobs/handlers/__init__.py
  - apps/visualizer/backend/tests/test_handlers_date_window.py
  - apps/visualizer/backend/tests/test_select_instagram_keys.py
autonomous: true
requirements_addressed: [REFACTOR-01]
---

```xml
<objective>
Move true cross-family helpers and their supporting constants from `_legacy.py` into `common.py`, re-export them into `_legacy.py` via `from .common import ...`, with no behavioral change. Full pytest green.
</objective>

<threat_model>
- Accidental behavior change: wrong symbol set moved or circular imports between `common.py` and `_legacy.py`.
- Missing constants: `_INSTAGRAM_NOT_VIDEO_SQL` / `_LEGACY_DATE_FILTER_MONTHS` must move with `_select_instagram_keys` / `_resolve_date_window` or callers break.
Mitigations: `common.py` imports only stdlib/third-party/`database`/`library_db`/`lightroom_tagger.*` as needed — no imports from `analyze`, `embed`, `matching`, `stacks`, `instagram`; grep for symbol definitions in `common.py`; full pytest.
</threat_model>

<tasks>
  <task id="13-02-T01">
    <title>Implement common.py and wire _legacy imports</title>
    <read_first>
apps/visualizer/backend/jobs/handlers/_legacy.py (full file — locate exact blocks to move)
.planning/phases/13-handlers-split-per-job-family/13-RESEARCH.md (Cross-Family Helpers)
.cursor/rules/job-log-contract.mdc
    </read_first>
    <action>
Replace the scaffold docstring-only `common.py` with a real module. Cut-paste from `_legacy.py` into `common.py` these definitions in top-to-bottom dependency order (preserve trailing content order as in monolith):

1. Constant `_CHECKPOINT_MAX_ENTRIES` (line ~63 in current `_legacy`).
2. Constant `_LEGACY_DATE_FILTER_MONTHS` (lines ~189–190).
3. Constant `_CATALOG_NOT_VIDEO_SQL` (block starting ~line 137).
4. Constant `_INSTAGRAM_NOT_VIDEO_SQL` (block starting ~line 149).
5. Functions: `_resolve_library_db_or_fail`, `_failure_severity_from_exception`, `_resolve_date_window`, `_select_catalog_keys`, `_select_instagram_keys` — include their full bodies as they exist in `_legacy.py` today.

Add to `common.py` the imports those bodies require (mirror the imports used by those symbols from `_legacy.py`: e.g. `database`, `library_db.require_library_db`, `AuthenticationError` / `InvalidRequestError` from `lightroom_tagger.core.provider_errors`, `from __future__ import annotations`, typing, etc.). Do **not** move `_select_catalog_keys_missing_visual_tags` (plan 13-07 / analyze-only).

Remove the moved blocks from `_legacy.py` (constants + functions listed above).

At the top of `_legacy.py` after its remaining imports, add:
`from .common import (` then list every moved public name: `_CHECKPOINT_MAX_ENTRIES`, `_LEGACY_DATE_FILTER_MONTHS`, `_CATALOG_NOT_VIDEO_SQL`, `_INSTAGRAM_NOT_VIDEO_SQL`, `_resolve_library_db_or_fail`, `_failure_severity_from_exception`, `_resolve_date_window`, `_select_catalog_keys`, `_select_instagram_keys` `)`.

Ensure `_legacy.py` still references those names unqualified (no `common.` prefix on thousands of call sites).

Edit tests to import common helpers from `common` (**same commit**):
- `apps/visualizer/backend/tests/test_handlers_date_window.py`: `from jobs.handlers.common import _resolve_date_window`
- `apps/visualizer/backend/tests/test_select_instagram_keys.py`: `from jobs.handlers.common import _select_instagram_keys`

Update `handlers/__init__.py`: remove `_resolve_date_window` and `_select_instagram_keys` from any `from ._legacy import (...)` re-export list (those names no longer live in `_legacy` as definitions — they are only in `common`). Do **not** re-export them from `__init__.py`; tests now use `jobs.handlers.common` per acceptance criteria below.
    </action>
    <acceptance_criteria>
`apps/visualizer/backend/jobs/handlers/common.py` contains the exact substring `def _resolve_library_db_or_fail(`.

`common.py` contains `def _failure_severity_from_exception(`.

`common.py` contains `def _select_catalog_keys(`.

`common.py` contains `def _select_instagram_keys(`.

`common.py` contains `def _resolve_date_window(`.

`common.py` contains the line `_CHECKPOINT_MAX_ENTRIES =` and assignments/blocks for `_CATALOG_NOT_VIDEO_SQL`, `_INSTAGRAM_NOT_VIDEO_SQL`, `_LEGACY_DATE_FILTER_MONTHS`.

`_legacy.py` contains `from .common import (` listing all nine symbols above.

`_legacy.py` does NOT contain a second definition line `def _resolve_library_db_or_fail(` (only the import from common).

`test_handlers_date_window.py` contains `from jobs.handlers.common import _resolve_date_window`.

`test_select_instagram_keys.py` contains `from jobs.handlers.common import _select_instagram_keys`.

`apps/visualizer/backend/jobs/handlers/__init__.py` does not contain the substring `_resolve_date_window`.

`apps/visualizer/backend/jobs/handlers/__init__.py` does not contain the substring `_select_instagram_keys`.

Command `cd apps/visualizer/backend && python -c "from jobs.handlers.common import _resolve_date_window, _select_instagram_keys"` exits 0.

Command `cd apps/visualizer/backend && pytest` exits 0.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- Grep: moved helpers exist only once as `def` in `common.py`.
- `cd apps/visualizer/backend && pytest` full suite green.
</verification>

<must_haves>
- No circular import on `import jobs.handlers`.
- Handler behavior unchanged (same logs/progress semantics — contract preserved).
</must_haves>
```
