---
plan: 13-05
wave: 5
depends_on: [04]
phase: 13
files_modified:
  - apps/visualizer/backend/jobs/handlers/matching.py
  - apps/visualizer/backend/jobs/handlers/_legacy.py
  - apps/visualizer/backend/jobs/handlers/__init__.py
  - apps/visualizer/backend/tests/test_handlers_single_match.py
  - apps/visualizer/backend/tests/test_stack_matching_integration.py
autonomous: true
requirements_addressed: [REFACTOR-01]
---

```xml
<objective>
Move matching family from `_legacy.py` to `matching.py`: `_expand_matches_for_lightroom_writes`, `handle_vision_match`, `handle_enrich_catalog`, `handle_prepare_catalog`, plus family-local constant `_VISION_MATCH_PREFILTER_SUMMARY_EVERY` (lines ~71 in monolith). Update `__init__.py` and matching-related tests (imports + patch paths to `jobs.handlers.matching.*`). Full pytest green.
</objective>

<threat_model>
- Losing `_expand_matches_for_lightroom_writes` local to matching (must not land in `common.py` per D-05).
- Integration tests still patch `jobs.handlers.match_dump_media` — must follow where `match_dump_media` is imported (likely `matching.py`).
Mitigations: grep for `vision_match`, `enrich_catalog`, `prepare_catalog`, `match_dump_media`, `expand_matches`.
</threat_model>

<tasks>
  <task id="13-05-T01">
    <title>Extract matching.py and update tests</title>
    <read_first>
apps/visualizer/backend/jobs/handlers/_legacy.py
apps/visualizer/backend/jobs/handlers/common.py
apps/visualizer/backend/jobs/handlers/__init__.py
apps/visualizer/backend/tests/test_handlers_single_match.py
apps/visualizer/backend/tests/test_stack_matching_integration.py
.planning/phases/13-handlers-split-per-job-family/13-RESEARCH.md (Matching family lines ~468–1126)
    </read_first>
    <action>
1. Populate `matching.py` with moved code from `_legacy.py`:
   - `_expand_matches_for_lightroom_writes`
   - `handle_vision_match`, `handle_enrich_catalog`, `handle_prepare_catalog`
   - Constant `_VISION_MATCH_PREFILTER_SUMMARY_EVERY` from the top constants block (do not duplicate `_CHECKPOINT_MAX_ENTRIES` in matching — import from `.common`).

2. Imports in `matching.py`: full set required by bodies including `from ..checkpoint import` symbols used (`fingerprint_vision_match`, `fingerprint_catalog_keys`, etc. — copy from current `_legacy` for this family only); `from .common import ...` for shared helpers; third-party `lightroom_tagger.*`; `database` / `library_db` as needed.

3. Remove all moved definitions from `_legacy.py`.

4. Update `__init__.py`: `from .matching import handle_vision_match, handle_enrich_catalog, handle_prepare_catalog`; map keys `'vision_match'`, `'enrich_catalog'`, `'prepare_catalog'`; drop `_legacy` imports for these three.

5. Update tests:
   - `test_handlers_single_match.py`: `from jobs.handlers.matching import handle_vision_match` (or equivalent); change `@patch('jobs.handlers....')` to `jobs.handlers.matching....` for names bound in `matching.py`.
   - `test_stack_matching_integration.py`: same pattern for `handle_vision_match` and patches.

6. No `path_setup` in `matching.py`.
    </action>
    <acceptance_criteria>
`matching.py` contains `def _expand_matches_for_lightroom_writes(`.

`matching.py` contains `def handle_vision_match(`, `def handle_enrich_catalog(`, `def handle_prepare_catalog(`.

`matching.py` contains `_VISION_MATCH_PREFILTER_SUMMARY_EVERY =`.

`_legacy.py` contains none of the three `def handle_` strings above for matching handlers.

`__init__.py` wires `'vision_match'`, `'enrich_catalog'`, `'prepare_catalog'` to imports from `matching`.

Command `cd apps/visualizer/backend && pytest tests/test_handlers_single_match.py tests/test_stack_matching_integration.py` exits 0.

Command `cd apps/visualizer/backend && pytest` exits 0.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- Matching handlers defined only in `matching.py`.
- Full pytest green.
</verification>

<must_haves>
- `_expand_matches_for_lightroom_writes` remains in `matching.py` only (single consumer, D-05).
</must_haves>
```
