---
phase: 08-embedding-prefilter-and-cache-pipeline
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - lightroom_tagger/core/clip_similarity.py
  - lightroom_tagger/scripts/match_instagram_dump.py
  - lightroom_tagger/core/test_clip_similarity.py
  - apps/visualizer/backend/tests/test_handlers_single_match.py
autonomous: true
requirements:
  - MATCH-02
must_haves:
  truths:
    - "`shortlist_catalog_candidates_by_clip` returns at most `top_k` keys and only keys from the input candidate set"
    - "`match_dump_media` accepts `clip_top_k` (default 50) and shortlists representative-filtered candidates before any `score_candidates_with_vision` path (D-03)"
  artifacts:
    - path: lightroom_tagger/core/clip_similarity.py
      provides: CLIP KNN shortlist helper with candidate-set intersection
    - path: lightroom_tagger/scripts/match_instagram_dump.py
      provides: pre-filter insertion point after rep filter, before vision candidate build
    - path: lightroom_tagger/core/test_clip_similarity.py
      provides: shortlist subset and cardinality invariants
  key_links:
    - from: 08-PATTERNS.md Pattern A/B
      to: match_dump_media shortlist step
      via: "insert after `catalog_key_is_primary_grid_row` filter; KNN over-fetch + intersect allowed keys"
    - from: image_clip_embeddings + get_clip_embedding_blob_for_key
      to: shortlist_catalog_candidates_by_clip
      via: "seed blob from `get_clip_embedding_blob_for_key(db, insta_media_key)`"
---

<objective>
Implement the CLIP cosine shortlist core in `clip_similarity.py` and integrate it into `match_dump_media` after the Phase 7 representative-only filter and before `vision_candidates` / `score_candidates_with_vision`, so phash, description, and vision scoring all see only the shortlist (D-03).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-embedding-prefilter-and-cache-pipeline/08-CONTEXT.md
@.planning/phases/08-embedding-prefilter-and-cache-pipeline/08-RESEARCH.md
@.planning/phases/08-embedding-prefilter-and-cache-pipeline/08-PATTERNS.md
@.planning/phases/08-embedding-prefilter-and-cache-pipeline/08-VALIDATION.md
@.cursor/rules/shared-derivation.mdc
</context>

<tasks>

<task type="auto">
  <name>Add CLIP shortlist helper</name>
  <files>lightroom_tagger/core/clip_similarity.py</files>
  <read_first>
    lightroom_tagger/core/clip_similarity.py
    lightroom_tagger/core/database.py
    .planning/phases/08-embedding-prefilter-and-cache-pipeline/08-PATTERNS.md
    .planning/phases/08-embedding-prefilter-and-cache-pipeline/08-RESEARCH.md
  </read_first>
  <action>
  Add `def shortlist_catalog_candidates_by_clip(db, insta_media_key: str, candidate_keys: list[str], top_k: int) -> list[str]:` that:
  - Normalizes `top_k` with `max(1, min(int(top_k), 500))` (hard cap aligned with `KNN_K_MAX = 500`).
  - Loads seed embedding via `get_clip_embedding_blob_for_key(db, insta_media_key)`; if missing, return `[]` (no inline embed; D-01 applies to batch job, not here).
  - Uses the same over-fetch pattern as `list_pin_similarity_candidate_keys`: `need_neighbors = max(0, top_k - 1)`, `knn_k = min(KNN_K_MAX, max(50, need_neighbors * 20))` when `need_neighbors > 0` else `1`, then `knn_k = min(KNN_K_MAX, max(knn_k, 1))`.
  - Calls `knn_clip_catalog_keys(db, blob, k=knn_k)` and iterates in distance order, keeping only keys in `set(candidate_keys)`, stopping when `len(out) == top_k`.
  - Returns keys in KNN order (no alphabetical reordering).
  Document in a one-line comment that candidate keys are already representative-only per Phase 7.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest lightroom_tagger/core/test_clip_similarity.py -k shortlist -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
  - `grep -n 'def shortlist_catalog_candidates_by_clip' lightroom_tagger/core/clip_similarity.py` returns a match.
  - `shortlist_catalog_candidates_by_clip` docstring or adjacent comment states representative-only precondition.
  - New tests in `test_clip_similarity.py` assert: (1) output length ≤ `top_k`, (2) `set(output) ⊆ set(input candidate_keys)`, (3) order matches first-seen KNN order among allowed keys (use mocked `knn_clip_catalog_keys` if needed).
  </acceptance_criteria>
  <done>CLIP shortlist helper exists with unit-tested invariants.</done>
</task>

<task type="auto">
  <name>Wire shortlist into match_dump_media (D-03)</name>
  <files>lightroom_tagger/scripts/match_instagram_dump.py</files>
  <read_first>
    lightroom_tagger/scripts/match_instagram_dump.py
    lightroom_tagger/core/clip_similarity.py
    lightroom_tagger/core/matcher.py
    .planning/phases/08-embedding-prefilter-and-cache-pipeline/08-RESEARCH.md
    .planning/phases/08-embedding-prefilter-and-cache-pipeline/08-CONTEXT.md
  </read_first>
  <action>
  Extend `match_dump_media` with keyword parameter `clip_top_k: int = 50` (propagate from all internal call sites in this file if any).
  Immediately after the representative-only filter block and the `if not candidates: continue` that skips empty windows, and **before** the loop that builds `vision_candidates` and **before** `score_candidates_with_vision`:
  - Let `dw_in = len(candidates)`.
  - Build `cand_keys = [c['key'] for c in candidates if c.get('key')]`.
  - Call `shortlist_catalog_candidates_by_clip(db, dump_media['media_key'], cand_keys, clip_top_k)` → `short_keys`.
  - Replace `candidates` with `[c for c in candidates if c.get('key') in short_keys]` preserving the order of `short_keys` (build an index map from key → row).
  - Increment a new stats counter e.g. `stats['clip_shortlisted'] += 1` when `dw_in > len(short_keys)` or always increment a batch counter per media — pick one documented key e.g. `stats['clip_shortlist_applied']` for “media rows where shortlist ran”.
  - Optional debug `log_callback`: `f'[{media_key}] CLIP shortlist: date_window_in={dw_in} clip_shortlist_out={len(candidates)}'` at `debug` level only (D-07 throttled summaries are handler plan 2; avoid per-image `info` spam).
  Do **not** implement MATCH-03 wider-search fallback when `short_keys` is empty: skip scoring for that media per existing empty-candidate paths.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest lightroom_tagger/core/test_clip_similarity.py apps/visualizer/backend/tests/test_handlers_single_match.py -k 'shortlist or clip' -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
  - `grep -n 'clip_top_k' lightroom_tagger/scripts/match_instagram_dump.py` shows `match_dump_media` signature default `50`.
  - `grep -n 'shortlist_catalog_candidates_by_clip' lightroom_tagger/scripts/match_instagram_dump.py` returns ≥1 match after the representative filter block.
  - `grep -n 'score_candidates_with_vision' lightroom_tagger/scripts/match_instagram_dump.py` — confirm shortlist mutation of `candidates` appears **above** the first `score_candidates_with_vision` call in the same code path (line numbers ascending).
  </acceptance_criteria>
  <done>match_dump_media gates the full scoring stack on the CLIP shortlist.</done>
</task>

<task type="auto">
  <name>D-03 gating regression: score_candidates_with_vision sees only the shortlist</name>
  <files>apps/visualizer/backend/tests/test_handlers_single_match.py</files>
  <read_first>
    apps/visualizer/backend/tests/test_handlers_single_match.py
    lightroom_tagger/scripts/match_instagram_dump.py
    lightroom_tagger/core/clip_similarity.py
    .planning/phases/08-embedding-prefilter-and-cache-pipeline/08-VALIDATION.md
  </read_first>
  <action>
  Add a pytest case named `test_shortlist_gates_score_candidates_with_vision` (or similar containing the substring `shortlist`) that:
  - Builds an Instagram dump media row with a date window producing ≥ `dw_in = 8` representative-only catalog candidates.
  - Patches `lightroom_tagger.core.clip_similarity.shortlist_catalog_candidates_by_clip` (or its imported alias inside `match_instagram_dump`) to return a deterministic sublist of length `min(3, dw_in)`.
  - Patches `lightroom_tagger.core.matcher.score_candidates_with_vision` (or wherever `match_dump_media` actually calls it) with `unittest.mock.MagicMock`, returning `[]`.
  - Calls `match_dump_media(..., clip_top_k=3)` (smallest practical bound for the assertion).
  - Asserts the mock was called at least once AND every call's `candidates` argument has `len(candidates) <= 3` AND every `candidates[i]['key']` is in the shortlisted keys returned by the patched shortlister.
  - Asserts NO call was made with the full pre-shortlist candidate list (defensive: assert no call args length > 3).
  Place the test alongside existing `vision_match` handler tests in `test_handlers_single_match.py` (do NOT create `test_checkpoint_fingerprints.py`; VALIDATION.md will be updated to point here).
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest apps/visualizer/backend/tests/test_handlers_single_match.py -k shortlist -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
  - `grep -nE 'def test_.*shortlist.*score_candidates_with_vision|def test_shortlist_gates_score_candidates_with_vision' apps/visualizer/backend/tests/test_handlers_single_match.py` returns ≥1 match.
  - Pytest selector `-k shortlist` collects ≥1 test in `test_handlers_single_match.py`.
  - Test fails if `match_dump_media` is reverted to call `score_candidates_with_vision` with the pre-shortlist candidate list.
  </acceptance_criteria>
  <done>D-03 gating is regression-proof.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | T | `shortlist_catalog_candidates_by_clip` | mitigate | Intersect every returned key with the input `candidate_keys` set; never append KNN keys outside the date-window representative set. |
| T-08-01-02 | I | KNN neighbor leakage | mitigate | Do not log raw embedding blobs; debug logs may include counts and `media_key` only. |
| T-08-01-03 | R | Ordering / determinism | mitigate | Preserve KNN iteration order when rebuilding `candidates` so scoring order stays stable for a fixed DB. |
</threat_model>

<verification>
Unit tests cover shortlist ⊆ candidates and ≤k; `match_dump_media` wires `clip_top_k`, applies the shortlist before `score_candidates_with_vision`, and a D-03 gating test asserts the scorer never receives more than `clip_top_k` candidates.
</verification>

<success_criteria>
MATCH-02 core library path shortlists date-window representatives before any LLM/description scoring runs on those candidates.
</success_criteria>

<output>After completion, create `.planning/phases/08-embedding-prefilter-and-cache-pipeline/08-01-SUMMARY.md`.</output>
