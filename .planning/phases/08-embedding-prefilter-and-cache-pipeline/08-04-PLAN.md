---
phase: 08-embedding-prefilter-and-cache-pipeline
plan: 4
type: execute
wave: 4
depends_on:
  - 3
files_modified:
  - apps/visualizer/backend/jobs/handlers.py
  - apps/visualizer/backend/library_db.py
  - apps/visualizer/backend/jobs/checkpoint.py
  - apps/visualizer/backend/tests/test_handlers_catalog_cache_build.py
autonomous: true
requirements:
  - CACHE-01
must_haves:
  truths:
    - "Job type `catalog_cache_build` runs embed (catalog+Instagram) → `batch_stack_detect` → `batch_catalog_similarity` in one process with one cancel scope"
    - "Job log contains stage banner lines with prefix `[catalog-cache-build]` and substrings `stage=embed`, `stage=stack`, `stage=similarity` (D-08)"
  artifacts:
    - path: apps/visualizer/backend/jobs/handlers.py
      provides: handle_catalog_cache_build + JOB_HANDLERS registration
    - path: apps/visualizer/backend/library_db.py
      provides: JOB_TYPES_REQUIRING_CATALOG includes catalog_cache_build
    - path: apps/visualizer/backend/tests/test_handlers_catalog_cache_build.py
      provides: ordering, cancel, and stage log assertions
  key_links:
    - from: _handle_batch_embed_image_inner (or equivalent)
      to: catalog_cache_build stage 1
      via: "metadata `{ image_type: 'catalog_and_instagram' }` + inherited force/month flags from chain metadata"
    - from: handle_batch_stack_detect core
      to: stage 2
      via: "shared inner callable or direct call with same runner/db/job_id contract"
---

<objective>
Add composite job type `catalog_cache_build` that runs the three catalog-cache stages in-process with cancel propagation, warn-and-proceed semantics for incomplete prerequisites (08-RESEARCH), and D-08 structured stage logging — without introducing job dependency graphs, FAISS, or schema migrations.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-embedding-prefilter-and-cache-pipeline/08-CONTEXT.md
@.planning/phases/08-embedding-prefilter-and-cache-pipeline/08-RESEARCH.md
@.planning/phases/08-embedding-prefilter-and-cache-pipeline/08-VALIDATION.md
@.cursor/rules/job-log-contract.mdc
@.cursor/rules/gsd-live-validation.mdc
@.cursor/rules/backend-restart.mdc
</context>

<tasks>

<task type="auto">
  <name>Register catalog_cache_build job type</name>
  <files>apps/visualizer/backend/library_db.py, apps/visualizer/backend/jobs/handlers.py, apps/visualizer/backend/jobs/checkpoint.py</files>
  <read_first>
    apps/visualizer/backend/library_db.py
    apps/visualizer/backend/jobs/handlers.py
    apps/visualizer/backend/jobs/checkpoint.py
    .planning/phases/08-embedding-prefilter-and-cache-pipeline/08-PATTERNS.md
  </read_first>
  <action>
  Add `'catalog_cache_build'` to `JOB_TYPES_REQUIRING_CATALOG` frozenset in `library_db.py`.
  In `handlers.py`, add `handle_catalog_cache_build` and register in `JOB_HANDLERS` dict with key exactly `'catalog_cache_build'`.
  Add `def fingerprint_catalog_cache_build(metadata: dict) -> str` in `checkpoint.py` hashing canonical JSON including keys: `"embedding_model_id"` (reuse `CLIP_EMBED_MODEL_ID` constant import), `"embedding_dim"` (`CLIP_EMBED_DIM`), `"force_embed"`, `"force_stack"`, `"force_similarity"` (bools default False from `metadata.get`), `"image_type": "catalog_and_instagram"` fixed literal for the embed stage scope, and any month/year/`min_rating` fields forwarded from metadata if the standalone jobs already use them (mirror `fingerprint_batch_embed_image` / stack / similarity fingerprints — union of parameters that would change work done).
  Pass fingerprint into the same checkpoint resume pattern used by other long jobs if `catalog_cache_build` uses checkpoints; if no checkpoint, still implement fingerprint for job identity in tests.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest apps/visualizer/backend/tests/test_handlers_catalog_cache_build.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
  - `grep -n "'catalog_cache_build'" apps/visualizer/backend/library_db.py` matches inside `JOB_TYPES_REQUIRING_CATALOG`.
  - `grep -n "'catalog_cache_build'" apps/visualizer/backend/jobs/handlers.py` matches in `JOB_HANDLERS` mapping.
  - `grep -n 'def fingerprint_catalog_cache_build' apps/visualizer/backend/jobs/checkpoint.py` returns a match.
  </acceptance_criteria>
  <done>Composite job type is registered and fingerprinted.</done>
</task>

<task type="auto">
  <name>Implement handle_catalog_cache_build chain with D-08 logs</name>
  <files>apps/visualizer/backend/jobs/handlers.py</files>
  <read_first>
    apps/visualizer/backend/jobs/handlers.py
    .planning/phases/08-embedding-prefilter-and-cache-pipeline/08-RESEARCH.md
    .cursor/rules/job-log-contract.mdc
    .cursor/skills/investigate-job/SKILL.md
  </read_first>
  <action>
  Implement `handle_catalog_cache_build(runner, job_id, metadata)` to:
  1. **Preamble** — `add_job_log(..., 'info', '[catalog-cache-build] chain_start embed→stack_detect→catalog_similarity')`.
  2. **stage=embed** — log `[catalog-cache-build] stage=embed status=start` then invoke the existing embed pipeline with metadata at minimum `{ 'image_type': 'catalog_and_instagram', 'force': metadata.get('force_embed', False) }` merged with any month/year keys accepted by `batch_embed_image`. On completion log `[catalog-cache-build] stage=embed status=complete embedded={n} skipped={s} failed={f}` (use actual counters from embed result or reuse summary variables). If prior embeddings missing, **warn** via `add_job_log(..., 'warning', '[catalog-cache-build] stage=embed warning=incomplete_embeddings count={k} proceeding')` when `k>0` detected via DB counts (implement cheap count query or reuse stats — do not block chain).
  3. **stage=stack** — `if runner.is_cancelled(job_id): return` then log `stage=stack status=start`, run stack-detect stage (reuse inner logic from `handle_batch_stack_detect` with `force=metadata.get('force_stack', False)`). Log complete line with skip reasons summary if available.
  4. **stage=similarity** — same cancel guard, `stage=similarity status=start`, run catalog similarity stage (`force=metadata.get('force_similarity', False)`), log complete with skip counts.
  Use **structured** stage lines only (no per-image spam). Progress 5→100% split ~33% per stage.
  Refactor minimally: extract private `_run_embed_stage_for_chain`, `_run_stack_stage_for_chain`, `_run_similarity_stage_for_chain` **or** call existing handler functions with a flag — choose lowest-duplication approach consistent with codebase.
  Do **not** enqueue three separate jobs; single `job_id` end-to-end.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest apps/visualizer/backend/tests/test_handlers_catalog_cache_build.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
  - `grep -n '\[catalog-cache-build\]' apps/visualizer/backend/jobs/handlers.py` returns ≥4 matches (start + per-stage markers).
  - `test_handlers_catalog_cache_build.py` contains `test_chain_runs_stages_in_order` and it exits 0.
  - Test `test_chain_honors_cancel_between_stages` or equivalent exits 0 (cancel after stage 1 must not run stage 3 — use mocked runner).
  - `grep -n 'stage=similarity' apps/visualizer/backend/tests/test_handlers_catalog_cache_build.py` ≥1.
  </acceptance_criteria>
  <done>Composite handler runs ordered stages with D-08 logging and cancel checks.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | D | `catalog_cache_build` | mitigate | Single job respects `runner.is_cancelled` between stages; progress bounded; reuse existing worker trust model. |
| T-08-04-02 | R | Operator traceability | mitigate | Stage banners and skip/warn counts in logs (D-08). |
| T-08-04-03 | E | Long-running work | accept | Same elevation posture as other batch jobs; no new privileged API. |
| T-08-04-04 | T | Metadata force flags | mitigate | Pass through only known keys `force_embed`, `force_stack`, `force_similarity`; defaults False. |
</threat_model>

<verification>
New integration test module proves stage order, cancel propagation, and log banner substrings.
</verification>

<success_criteria>
CACHE-01 backend delivers one-shot cache pipeline with observable per-stage narrative and no parallel-job orchestration gap.
</success_criteria>

<output>After completion, create `.planning/phases/08-embedding-prefilter-and-cache-pipeline/08-04-SUMMARY.md`.</output>
