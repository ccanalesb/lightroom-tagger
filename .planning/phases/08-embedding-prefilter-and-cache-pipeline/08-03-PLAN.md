---
phase: 08-embedding-prefilter-and-cache-pipeline
plan: 3
type: execute
wave: 3
depends_on:
  - 2
files_modified:
  - lightroom_tagger/core/database.py
  - apps/visualizer/backend/jobs/handlers.py
  - apps/visualizer/backend/jobs/checkpoint.py
  - apps/visualizer/backend/tests/test_handlers_batch_embed_image.py
autonomous: true
requirements:
  - CACHE-01
must_haves:
  truths:
    - "`batch_embed_image` accepts `image_type: 'catalog_and_instagram'` and processes the union of catalog keys and Instagram dump keys missing CLIP rows"
    - "`fingerprint_batch_embed_image` changes when embed scope switches between `catalog` and `catalog_and_instagram` so catalog-only checkpoints do not skip new Instagram work"
  artifacts:
    - path: lightroom_tagger/core/database.py
      provides: list helpers for Instagram dump rows needing `image_clip_embeddings`
    - path: apps/visualizer/backend/jobs/handlers.py
      provides: embed handler branches + filepath resolution for dump rows
    - path: apps/visualizer/backend/jobs/checkpoint.py
      provides: fingerprint distinguishes catalog vs catalog+Instagram scope
  key_links:
    - from: instagram_dump_media
      to: list_instagram_dump_keys_needing_clip_embedding
      via: "SQL over dump table joined with vec0 missing rows"
    - from: upsert_image_clip_embedding
      to: IG rows
      via: "same write path as catalog"
---

<objective>
Extend `batch_embed_image` per D-01 so Instagram dump media receive persisted CLIP embeddings in `image_clip_embeddings`, with database listing helpers and fingerprint scope that differentiates catalog-only from catalog+Instagram runs.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-embedding-prefilter-and-cache-pipeline/08-CONTEXT.md
@.planning/phases/08-embedding-prefilter-and-cache-pipeline/08-RESEARCH.md
@.planning/phases/08-embedding-prefilter-and-cache-pipeline/08-PATTERNS.md
@.planning/phases/08-embedding-prefilter-and-cache-pipeline/08-VALIDATION.md
@.cursor/rules/job-log-contract.mdc
@.cursor/rules/backend-restart.mdc
</context>

<tasks>

<task type="auto">
  <name>Database helpers for Instagram CLIP backlog</name>
  <files>lightroom_tagger/core/database.py</files>
  <read_first>
    lightroom_tagger/core/database.py
    lightroom_tagger/core/clip_similarity.py
    .planning/phases/08-embedding-prefilter-and-cache-pipeline/08-RESEARCH.md
  </read_first>
  <action>
  Add `def list_instagram_dump_keys_needing_clip_embedding(db, force: bool = False) -> list[str]:` that returns `instagram_dump_media.media_key` values lacking a row in `image_clip_embeddings` for the active CLIP model/dim (mirror the filter logic in `list_catalog_keys_needing_clip_embedding` — reuse the same `CLIP_EMBED_MODEL_ID` / dim predicates used elsewhere in this module).
  When `force is True`, return all dump keys that have resolvable paths (or all keys if that matches catalog `force` semantics — match `list_catalog_keys_needing_clip_embedding` / force list pattern already in this file).
  Use `resolve_filepath` for any path fields read from `instagram_dump_media` before classifying embeddable rows.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest apps/visualizer/backend/tests/test_handlers_batch_embed_image.py -k instagram -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
  - `grep -n 'def list_instagram_dump_keys_needing_clip_embedding' lightroom_tagger/core/database.py` returns a match.
  - Handler or tests call the helper when `image_type == 'catalog_and_instagram'` (verified via grep across `handlers.py` or test mocks).
  </acceptance_criteria>
  <done>Instagram backlog listing exists for the embed job.</done>
</task>

<task type="auto">
  <name>Extend fingerprint_batch_embed_image for scope</name>
  <files>apps/visualizer/backend/jobs/checkpoint.py</files>
  <read_first>
    apps/visualizer/backend/jobs/checkpoint.py
    apps/visualizer/backend/jobs/handlers.py
    .planning/phases/08-embedding-prefilter-and-cache-pipeline/08-RESEARCH.md
  </read_first>
  <action>
  In `fingerprint_batch_embed_image` payload, add key `"image_type"` value exactly one of `'catalog'` | `'catalog_and_instagram'` derived from `str(metadata.get("image_type", "catalog"))` after normalizing: if metadata requests Instagram union, serialize as `'catalog_and_instagram'` (reject or map legacy `'instagram'` only if already present in codebase — prefer **only** adding `'catalog_and_instagram'` as the union sentinel per 08-RESEARCH Pattern F).
  Ensure `fingerprint_batch_embed_image(metadata_catalog) != fingerprint_batch_embed_image(metadata_union)` when `force`, `min_rating`, and months match but `image_type` differs.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest apps/visualizer/backend/tests/test_handlers_batch_embed_image.py -k "instagram or fingerprint" -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
  - `grep -n 'catalog_and_instagram' apps/visualizer/backend/jobs/checkpoint.py` or fingerprint payload construction references the union sentinel in canonical JSON.
  - Test asserts two fingerprints differ when only `image_type` changes between `'catalog'` and `'catalog_and_instagram'`.
  </acceptance_criteria>
  <done>Embed job resume distinguishes Instagram-inclusive scope.</done>
</task>

<task type="auto">
  <name>Branch _handle_batch_embed_image_inner for catalog_and_instagram</name>
  <files>apps/visualizer/backend/jobs/handlers.py</files>
  <read_first>
    apps/visualizer/backend/jobs/handlers.py
    lightroom_tagger/core/database.py
    .planning/phases/08-embedding-prefilter-and-cache-pipeline/08-CONTEXT.md
    .planning/phases/08-embedding-prefilter-and-cache-pipeline/08-PATTERNS.md
  </read_first>
  <action>
  Replace the hard failure `batch_embed_image only supports catalog images` path with:
  - `image_type == 'catalog'`: existing catalog-only code path unchanged.
  - `image_type == 'catalog_and_instagram'`: compute ordered work list = catalog keys (existing list helpers) **plus** Instagram dump keys from `list_instagram_dump_keys_needing_clip_embedding`, dedupe if any key collision (log `warning` once if intersection non-empty with count).
  - Preserve `cancel_scope`, checkpointing, and `_EMBED_SUMMARY_LOG_EVERY` pattern; add skip reason buckets for Instagram-specific failures (e.g. missing file) consistent with existing `skip_reason_counts`.
  Do **not** compute CLIP inside `vision_match` for Instagram — this task is batch embed only (D-01).
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest apps/visualizer/backend/tests/test_handlers_batch_embed_image.py -k instagram -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
  - `grep -n "batch_embed_image only supports catalog" apps/visualizer/backend/jobs/handlers.py` returns **no** matches (string removed).
  - `grep -n "catalog_and_instagram" apps/visualizer/backend/jobs/handlers.py` ≥1.
  - Test exercises `JobsAPI` metadata `image_type: 'catalog_and_instagram'` (or handler entry) and asserts `upsert_image_clip_embedding` called for at least one mocked Instagram key when DB fixture includes a dump row without embedding.
  </acceptance_criteria>
  <done>batch_embed_image covers Instagram rows under the union image_type.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | T | File paths for IG rows | mitigate | Use `resolve_filepath` + same path classification as catalog embed; skip unreadable paths with bucketed skip reasons. |
| T-08-03-02 | R | Checkpoints | mitigate | Fingerprint includes `image_type` sentinel `catalog_and_instagram` so toggling scope invalidates stale checkpoints. |
| T-08-03-03 | I | Logs | mitigate | Throttled embed summaries only; no full filesystem paths in `info` if they contain PII (follow existing embed log redaction). |
| T-08-03-04 | T | Key collision catalog ∩ IG | mitigate | Detect intersection, log warning, deterministic dedupe policy (e.g. process once). |
</threat_model>

<verification>
Extended embed tests cover Instagram scope and fingerprint differentiation; handler branches on `catalog_and_instagram`.
</verification>

<success_criteria>
CACHE-01 backend prerequisite: Instagram CLIP vectors are first-class cached artifacts keyed like catalog embeddings, ready for the composite cache chain embed stage.
</success_criteria>

<output>After completion, create `.planning/phases/08-embedding-prefilter-and-cache-pipeline/08-03-SUMMARY.md`.</output>
