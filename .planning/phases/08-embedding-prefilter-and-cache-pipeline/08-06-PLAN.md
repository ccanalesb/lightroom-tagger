---
phase: 08-embedding-prefilter-and-cache-pipeline
plan: 6
type: execute
wave: 6
depends_on:
  - 4
  - 5
files_modified:
  - apps/visualizer/frontend/src/components/processing/CatalogCacheTab.tsx
  - apps/visualizer/frontend/src/constants/strings.ts
  - apps/visualizer/frontend/src/components/processing/__tests__/CatalogCacheTab.test.tsx
autonomous: true
requirements:
  - CACHE-01
must_haves:
  truths:
    - "CatalogCacheTab primary CTA calls `JobsAPI.create('catalog_cache_build', metadata)`"
    - "Advanced section renders imported `AdvancedOptions` from `apps/visualizer/frontend/src/components/matching/AdvancedOptions.tsx` (D-05)"
  artifacts:
    - path: apps/visualizer/frontend/src/components/processing/CatalogCacheTab.tsx
      provides: Build cache chain + Advanced individual jobs + prepare_catalog
    - path: apps/visualizer/frontend/src/constants/strings.ts
      provides: CTA label **Build catalog cache**, enqueue success, prepare_catalog copy
    - path: apps/visualizer/frontend/src/components/processing/__tests__/CatalogCacheTab.test.tsx
      provides: CTA + AdvancedOptions import assertion
  key_links:
    - from: catalog_cache_build job type (backend Plan 4)
      to: CatalogCacheTab CTA
      via: "JobsAPI.create type string `catalog_cache_build`"
    - from: AdvancedOptions
      to: CatalogCacheTab
      via: "named import `AdvancedOptions` — `grep AdvancedOptions CatalogCacheTab.tsx`"
---

<objective>
Implement D-04/D-05 on `CatalogCacheTab`: primary **Build catalog cache** chain CTA, success affordance to Job Queue, and Advanced disclosure reusing `AdvancedOptions` with individual `batch_embed_image` (`image_type: 'catalog'` and `catalog_and_instagram` labels per spec), `batch_stack_detect`, `batch_catalog_similarity`, and `prepare_catalog` with UI-SPEC copy for pre-compress.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-embedding-prefilter-and-cache-pipeline/08-UI-SPEC.md
@.planning/phases/08-embedding-prefilter-and-cache-pipeline/08-CONTEXT.md
@.cursor/rules/frontend-design.md
@.cursor/rules/dry-callsite-sweep.mdc
@.cursor/rules/job-ui-contract.mdc
</context>

<tasks>

<task type="auto">
  <name>Catalog cache pipeline copy constants</name>
  <files>apps/visualizer/frontend/src/constants/strings.ts</files>
  <read_first>
    apps/visualizer/frontend/src/constants/strings.ts
    .planning/phases/08-embedding-prefilter-and-cache-pipeline/08-UI-SPEC.md
  </read_first>
  <action>
  Add constants:
  - `CATALOG_CACHE_BUILD_CTA` = `Build catalog cache` (exact).
  - `CATALOG_CACHE_BUILD_SUCCESS` = `Catalog cache build started! Check the Job Queue tab for progress.` (exact per 08-UI-SPEC Alerts section).
  - `CATALOG_CACHE_EMBED_CATALOG_LABEL` — short label for Advanced embed catalog-only action.
  - `CATALOG_CACHE_EMBED_CATALOG_IG_LABEL` — label clarifying catalog + Instagram embed (`image_type: 'catalog_and_instagram'`).
  - `CATALOG_CACHE_PREPARE_CATALOG_TITLE` = `Pre-compress catalog images` (exact).
  - `CATALOG_CACHE_PREPARE_CATALOG_HELPER` — full helper paragraph from 08-UI-SPEC (RAW → JPEG sentence + optional laziness).
  Use `ADVANCED_OPTIONS_TITLE` for disclosure title (reuse existing export).
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend && npm run lint -- src/constants/strings.ts</automated>
  </verify>
  <acceptance_criteria>
  - `grep -n 'Build catalog cache' apps/visualizer/frontend/src/constants/strings.ts` matches `CATALOG_CACHE_BUILD_CTA` value.
  - `grep -n 'Pre-compress catalog images' apps/visualizer/frontend/src/constants/strings.ts` matches title constant.
  </acceptance_criteria>
  <done>Catalog cache UI copy is centralized.</done>
</task>

<task type="auto">
  <name>CatalogCacheTab chain CTA + AdvancedOptions reuse</name>
  <files>apps/visualizer/frontend/src/components/processing/CatalogCacheTab.tsx</files>
  <read_first>
    apps/visualizer/frontend/src/components/processing/CatalogCacheTab.tsx
    apps/visualizer/frontend/src/components/processing/MatchingTab.tsx
    apps/visualizer/frontend/src/components/matching/AdvancedOptions.tsx
    apps/visualizer/frontend/src/services/api.ts
    apps/visualizer/frontend/src/constants/strings.ts
    apps/visualizer/frontend/src/pages/ProcessingPage.tsx
    .planning/phases/08-embedding-prefilter-and-cache-pipeline/08-UI-SPEC.md
  </read_first>
  <action>
  - **Primary:** Button `CATALOG_CACHE_BUILD_CTA` calls `JobsAPI.create('catalog_cache_build', {})` or metadata `{}` unless chain accepts optional force flags — pass through only if UI adds toggles (default empty dict).
  - **Success:** Show alert/banner using `CATALOG_CACHE_BUILD_SUCCESS` and a secondary control that navigates to `/processing?tab=jobs` reusing `PROCESSING_OPEN_JOB_QUEUE` or the same pattern as 07.1 remediation.
  - **Advanced:** Import `AdvancedOptions` from `apps/visualizer/frontend/src/components/matching/AdvancedOptions.tsx`. Mirror `MatchingTab` state for `isOpen`, provider, threshold, weights sliders, maxWorkers, skipUndescribed — use the same default values as `MatchingTab` for DRY behavior (users may not tune these for cache jobs, but component requires props).
  - Inside Advanced region (below or beside AdvancedOptions body per layout that preserves single disclosure): add buttons that enqueue:
    - `JobsAPI.create('batch_embed_image', { image_type: 'catalog' })` with label `CATALOG_CACHE_EMBED_CATALOG_LABEL`
    - `JobsAPI.create('batch_embed_image', { image_type: 'catalog_and_instagram' })` with label `CATALOG_CACHE_EMBED_CATALOG_IG_LABEL`
    - `JobsAPI.create('batch_stack_detect', {})` (or existing metadata shape from prior tab)
    - `JobsAPI.create('batch_catalog_similarity', {})`
    - `JobsAPI.create('prepare_catalog', {})` titled `CATALOG_CACHE_PREPARE_CATALOG_TITLE` + helper `CATALOG_CACHE_PREPARE_CATALOG_HELPER`
  - **De-emphasize** any standalone embed block that duplicates the Advanced embed actions — one primary story: chain first (08-UI-SPEC § Catalog Cache #3).
  Do not introduce a second advanced-component implementation.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend && npm run test -- --run src/components/processing/__tests__/CatalogCacheTab.test.tsx</automated>
  </verify>
  <acceptance_criteria>
  - `grep -n "from '" apps/visualizer/frontend/src/components/processing/CatalogCacheTab.tsx | grep AdvancedOptions` — file must import from path containing `matching/AdvancedOptions`.
  - Test asserts primary button triggers `JobsAPI.create('catalog_cache_build', expect.anything())`.
  - Test asserts `AdvancedOptions` appears when advanced toggle opens (e.g. `getByText` for `ADVANCED_OPTIONS_TITLE` or button text from strings).
  - `grep -n 'catalog_and_instagram' apps/visualizer/frontend/src/components/processing/CatalogCacheTab.tsx` ≥1.
  - `grep -n 'matching/AdvancedOptions' apps/visualizer/frontend/src/components/processing/CatalogCacheTab.tsx` ≥1 (import path).
  - After enqueue, UI exposes navigation to `/processing?tab=jobs` (existing test pattern).
  </acceptance_criteria>
  <done>Catalog cache surface owns pipeline triggers per D-04/D-05.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | T | Job metadata | mitigate | Chain uses fixed type `catalog_cache_build`; Advanced actions use explicit typed payloads `image_type` as documented. |
| T-08-06-02 | I | DRY violation | mitigate | Single `AdvancedOptions` import; test fails if removed. |
| T-08-06-03 | D | Accidental double enqueue | mitigate | Disable buttons while `isStarting` true per existing patterns. |
</threat_model>

<verification>
Vitest confirms composite job enqueue, AdvancedOptions reuse, and queue route affordance.
</verification>

<success_criteria>
CACHE-01 UI exposes the composite cache build and individual stages from the catalog cache tab with DRY Advanced disclosure.
</success_criteria>

<output>After completion, create `.planning/phases/08-embedding-prefilter-and-cache-pipeline/08-06-SUMMARY.md`.</output>
