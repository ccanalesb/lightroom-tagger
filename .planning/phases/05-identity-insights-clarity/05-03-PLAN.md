---
phase: 5
plan: 03
slug: identity-page-narrative-intros
wave: 3
depends_on: [01, 02]
files_modified:
  - apps/visualizer/frontend/src/constants/strings.ts
  - apps/visualizer/frontend/src/pages/IdentityPage.tsx
  - apps/visualizer/frontend/src/components/identity/StyleFingerprintPanel.tsx
  - apps/visualizer/frontend/src/components/identity/BestPhotosGrid.tsx
  - apps/visualizer/frontend/src/components/identity/PostNextSuggestionsPanel.tsx
  - apps/visualizer/frontend/src/pages/IdentityPage.test.tsx
requirements:
  - IDENT-05
autonomous: true
---

# Plan 03: Identity page order + section intros (IDENT-05)

## Objective

Reorder the Identity page to **style fingerprint → best photos → post next**, and add the narrative section intros from `05-UI-SPEC.md` under each section `h2`, including every early-return branch in `StyleFingerprintPanel` so loading/error/empty states stay coherent.

## Context

- Plan 03 edits to `BestPhotosGrid.tsx` (intros and any header structure) **follow** Plan 02’s overlay / hide-posted wiring on that file — run Plan 02 first so grid changes do not conflict.
- Headings live **inside** child panel components, not only `IdentityPage.tsx`; intros must be added next to each `h2` in those components.
- `StyleFingerprintPanel` has four JSX return paths (loading, error, empty, main); each includes an `h2` with `id="identity-fingerprint-heading"` — add the same intro `<p className="text-sm text-text-secondary">` immediately after each `h2` in all four paths.
- `05-CONTEXT.md` D-04: no extra card styling beyond existing grid vs list — do not add new card variants.

## Tasks

<task id="01" type="execute">
<title>strings.ts: three INTRO exports (exact UI-SPEC copy)</title>
<read_first>
- apps/visualizer/frontend/src/constants/strings.ts — existing `IDENTITY_PAGE_*` / `IDENTITY_SECTION_*` block
- .planning/phases/05-identity-insights-clarity/05-UI-SPEC.md — Copywriting Contract rows for the three intros
</read_first>
<action>
Add three exported string constants with **exact** text from `05-UI-SPEC.md` (Copywriting Contract section):

1. `IDENTITY_INTRO_STYLE_FINGERPRINT` — *"Your aggregate scores and rationale patterns across active critique lenses. This is the "shape" of your work before you pick individual shots to share."*

2. `IDENTITY_INTRO_BEST_PHOTOS` — *"A ranked set of your strongest catalog images—highest equal-weight aggregate first. "Posted" on a tile means that catalog photo already appears on Instagram."*

3. `IDENTITY_INTRO_POST_NEXT` — *"Unposted candidates the server flags as worth sharing next, with a short reason on each row."*

Place them adjacent to existing Identity section constants. Preserve straight double quotes inside strings using appropriate TypeScript escaping if needed.
</action>
<acceptance_criteria>
- `grep -n "IDENTITY_INTRO_STYLE_FINGERPRINT" apps/visualizer/frontend/src/constants/strings.ts` returns a match
- `grep -n "shape" apps/visualizer/frontend/src/constants/strings.ts` matches the substring `shape` inside `IDENTITY_INTRO_STYLE_FINGERPRINT` value (the UI-SPEC sentence contains the word `shape`)
- `grep -n "equal-weight aggregate" apps/visualizer/frontend/src/constants/strings.ts` returns a match
- `grep -n "Unposted candidates the server" apps/visualizer/frontend/src/constants/strings.ts` returns a match
</acceptance_criteria>
</task>

<task id="02" type="execute">
<title>IdentityPage: reorder sections</title>
<read_first>
- apps/visualizer/frontend/src/pages/IdentityPage.tsx — current child order
</read_first>
<action>
Replace the three child components so JSX order is exactly:

1. `<StyleFingerprintPanel />`
2. `<BestPhotosGrid />`
3. `<PostNextSuggestionsPanel />`

Remove any duplicate wrappers; keep `space-y-8` on the outer container unchanged.
</action>
<acceptance_criteria>
- `grep -n "StyleFingerprintPanel" apps/visualizer/frontend/src/pages/IdentityPage.tsx` appears **before** `grep -n "BestPhotosGrid"` when viewing file top-to-bottom (line numbers: `StyleFingerprintPanel` line < `BestPhotosGrid` line < `PostNextSuggestionsPanel` line)
- `cd apps/visualizer/frontend && npx tsc --noEmit` exits 0
</acceptance_criteria>
</task>

<task id="03" type="execute">
<title>StyleFingerprintPanel: intro in all four return branches</title>
<read_first>
- apps/visualizer/frontend/src/components/identity/StyleFingerprintPanel.tsx — all `return (` blocks
- apps/visualizer/frontend/src/constants/strings.ts — `IDENTITY_INTRO_STYLE_FINGERPRINT`
</read_first>
<action>
Import `IDENTITY_INTRO_STYLE_FINGERPRINT` from `../../constants/strings`.

In **each** of the four return branches (loading ~lines 93–107, error ~110–124, empty ~127–141, main ~144+), immediately after the `<h2 id="identity-fingerprint-heading" className="text-card-title text-text">` line, insert:

`<p className="text-sm text-text-secondary">{IDENTITY_INTRO_STYLE_FINGERPRINT}</p>`

before the `<Card` (or next sibling in that branch’s structure). All four branches must contain the string `IDENTITY_INTRO_STYLE_FINGERPRINT` exactly once each.
</action>
<acceptance_criteria>
- `grep -c "IDENTITY_INTRO_STYLE_FINGERPRINT" apps/visualizer/frontend/src/components/identity/StyleFingerprintPanel.tsx` outputs `4`
- `cd apps/visualizer/frontend && npx tsc --noEmit` exits 0
</acceptance_criteria>
</task>

<task id="04" type="execute">
<title>BestPhotosGrid + PostNextSuggestionsPanel: intros after h2</title>
<read_first>
- apps/visualizer/frontend/src/components/identity/BestPhotosGrid.tsx — section header block
- apps/visualizer/frontend/src/components/identity/PostNextSuggestionsPanel.tsx — section header block
</read_first>
<action>
1. In `BestPhotosGrid.tsx`, import `IDENTITY_INTRO_BEST_PHOTOS`. After the `<h2 id="identity-best-photos-heading" ...>` and **before** `<Card padding="md">`, add `<p className="text-sm text-text-secondary">{IDENTITY_INTRO_BEST_PHOTOS}</p>`.

2. In `PostNextSuggestionsPanel.tsx`, import `IDENTITY_INTRO_POST_NEXT`. After `<h2 id="identity-post-next-heading" ...>` and **before** `<Card padding="md">`, add `<p className="text-sm text-text-secondary">{IDENTITY_INTRO_POST_NEXT}</p>`.

Do not remove or replace existing `IDENTITY_BEST_PHOTOS_HELP` / `IDENTITY_POST_NEXT_HELP` paragraphs inside the cards.
</action>
<acceptance_criteria>
- `grep -n "IDENTITY_INTRO_BEST_PHOTOS" apps/visualizer/frontend/src/components/identity/BestPhotosGrid.tsx` returns a match
- `grep -n "IDENTITY_INTRO_POST_NEXT" apps/visualizer/frontend/src/components/identity/PostNextSuggestionsPanel.tsx` returns a match
- `grep -n "identity-best-photos-heading" apps/visualizer/frontend/src/components/identity/BestPhotosGrid.tsx` line number is **less than** `grep -n "IDENTITY_INTRO_BEST_PHOTOS"` line number, which is **less than** `grep -n "<Card padding" apps/visualizer/frontend/src/components/identity/BestPhotosGrid.tsx` first Card after h2
- `cd apps/visualizer/frontend && npx tsc --noEmit` exits 0
</acceptance_criteria>
</task>

<task id="05" type="execute">
<title>IdentityPage.test.tsx: snapshot or order sanity</title>
<read_first>
- apps/visualizer/frontend/src/pages/IdentityPage.test.tsx — full file
</read_first>
<action>
Update `IdentityPage.test.tsx` (or add assertions) so the test verifies section **order** in the DOM: the style fingerprint heading text (`IDENTITY_SECTION_STYLE_FINGERPRINT` constant) appears before the best-photos heading (`IDENTITY_SECTION_BEST_PHOTOS`) in `document.body.innerHTML` indexOf order:

- `expect(body.indexOf(IDENTITY_SECTION_STYLE_FINGERPRINT) < body.indexOf(IDENTITY_SECTION_BEST_PHOTOS)).toBe(true)`
- Similarly `body.indexOf(IDENTITY_SECTION_BEST_PHOTOS) < body.indexOf(IDENTITY_SECTION_POST_NEXT)` must be true.

Rename the existing test `it('renders page title and section headings', ...)` to `it('renders page title and section headings in narrative order', ...)` when you add these assertions (or add a dedicated `it(...)` block).

Import the three `IDENTITY_SECTION_*` constants from `../constants/strings` if not already imported.
</action>
<acceptance_criteria>
- `grep -n "indexOf(IDENTITY_SECTION" apps/visualizer/frontend/src/pages/IdentityPage.test.tsx` returns at least two lines
- `cd apps/visualizer/frontend && npx vitest run src/pages/IdentityPage.test.tsx` exits 0
</acceptance_criteria>
</task>

## Verification

```bash
cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend && npx tsc --noEmit && npx vitest run
```

## must_haves

- [ ] Identity page vertical order is fingerprint → best photos → post next
- [ ] Each of the three sections has a `text-sm text-text-secondary` intro paragraph under its `h2` with UI-SPEC copy sourced from `strings.ts`
