---
phase: "19"
plan: 19-04
type: implementation
wave: 2
title: "Reconstruction fallback, automated tests, offline smoke checklist"
slug: comparison-pool-reconstruction-tests
depends_on:
  - 19-03
files_modified:
  - lightroom_tagger/scripts/generate_comparison_pool_report.py
  - lightroom_tagger/scripts/test_comparison_pool_report.py
  - lightroom_tagger/scripts/test_match_instagram_dump.py
autonomous: true
requirements_addressed: []
---

<objective>
Implement best-effort pool **reconstruction** when no snapshot exists (**19-CONTEXT.md D-02**), prove behavior with pytest per **`19-VALIDATION.md`**, extend matcher tests to assert snapshot inserts post-score, and document manual **`file://`** smoke steps (**D-05** offline validation). Satisfy folded diagnostic todo intent (**D-14**): unmatched cases inspect real or labeled-reconstructed pools before prompt changes.
</objective>

<decision_coverage>
- **D-02:** Runtime reconstruction using **`find_candidates_by_date` → rejected filter → `catalog_key_is_primary_grid_row` → `shortlist_catalog_candidates_by_clip`** with **current** DB state; HTML shows banner **`Reconstructed — not exact run evidence`** (exact string).
- **D-14:** Tests + manual checklist demonstrate the diagnostic workflow is usable without product UI.
- **D-01, D-03–D-13:** Regressed by automated tests cross-checking snapshot vs report paths; primary **D-13** enforced via **`main`** HTML slice test.
- **Nyquist / live validation:** No new HTTP routes or LLM integrations — scoped pytest + manual **offline** smoke only (see **`19-VALIDATION.md` §12**).
</decision_coverage>

<threat_model>
**ASVS level:** 1  
**Block on severity:** high  

**Threats and mitigations:**
- **T-19-recon-LLM / network:** Reconstruction uses **read-only** DB helpers and CLIP shortlist only — **do not** call **`score_candidates_with_vision`** or LLM clients inside reconstruction path for this phase (grid shows catalog keys + thumbnails from current paths; scoring cells may show **“n/a (reconstructed)”** literals where evidence is unavailable, or join **`vision_comparisons`** read-only if present — choose **`n/a`** literals only to avoid implying historic scores).
- **T-19-test_path_leak:** Test fixtures must use temp dirs under **`/tmp`** or **`pytest` tmp_path**; assert **`main`** slice excludes those absolute paths.

**Residual:** Reconstructed grid may be misleading if embeddings drift — mitigated by banner (**D-02**).
</threat_model>

<tasks>
  <task id="19-04-T1">
    <title>Implement reconstruction branch in report generator</title>
    <read_first>
      - lightroom_tagger/scripts/generate_comparison_pool_report.py (`write_comparison_pool_report`, **`# PHASE19_RECONSTRUCT_HOOK`**)
      - lightroom_tagger/scripts/match_instagram_dump.py (representative + CLIP ordering pipeline — copy logic, not call **`match_dump_media`**)
      - lightroom_tagger/core/database/__init__.py (`get_rejected_pairs`, `catalog_key_is_primary_grid_row`)
      - lightroom_tagger/core/matcher/__init__.py or candidates import path for **`find_candidates_by_date`**
      - lightroom_tagger/core/clip_similarity.py (`shortlist_catalog_candidates_by_clip` signature)
    </read_first>
    <action>
      In **`lightroom_tagger/scripts/generate_comparison_pool_report.py`** replace the **`PHASE19_RECONSTRUCT_BODY_TBD`** placeholder with function **`reconstruct_vision_pool_for_report(`**  
      **`db: sqlite3.Connection, dump_media: dict, *, clip_top_k: int`**  
      **`) -> list[dict]`**  
      returning list of dicts shaped like **`vision_candidates`** minimal keys **`key`**, **`local_path`** (via **`resolve_catalog_path(catalog_img.get('filepath',''))`**) mirroring **`match_instagram_dump`** ordering:

      1. **`candidates = find_candidates_by_date(db, dump_media, days_before=90)`**
      2. Filter out **`get_rejected_pairs`** contains **`(c['key'], dump_media['media_key'])`**
      3. Filter **`catalog_key_is_primary_grid_row`**
      4. **`shortlist_catalog_candidates_by_clip(db, dump_media['media_key'], cand_keys, clip_top_k)`** preserving order

      In **`write_comparison_pool_report`**, when **`fetch_comparison_pool_snapshot_bundle`** returns **`(None, [])`**, call **`reconstruct_vision_pool_for_report`** with **`clip_top_k`** default **`50`** (same default as matcher unless CLI later adds flag — hardcode **`50`** in const **`_DEFAULT_CLIP_TOP_K_REPORT = 50`** at module top). Render primary grid thumbnails for reconstructed keys using same compression helper; show banner **`Reconstructed — not exact run evidence`** above that card’s **`h2`**.

      Remove **`# PHASE19_RECONSTRUCT_HOOK`** comment once implemented (acceptance will grep function name instead).
    </action>
    <acceptance_criteria>
      - `rg -n "def reconstruct_vision_pool_for_report" lightroom_tagger/scripts/generate_comparison_pool_report.py` exits 0
      - `rg -n "Reconstructed — not exact run evidence" lightroom_tagger/scripts/generate_comparison_pool_report.py` exits 0
      - `rg -n "shortlist_catalog_candidates_by_clip" lightroom_tagger/scripts/generate_comparison_pool_report.py` exits 0
      - `rg -n "_DEFAULT_CLIP_TOP_K_REPORT = 50" lightroom_tagger/scripts/generate_comparison_pool_report.py` exits 0
    </acceptance_criteria>
  </task>

  <task id="19-04-T2">
    <title>Add `test_comparison_pool_report.py` suite</title>
    <read_first>
      - `.planning/phases/19-generate-read-only-html-comparison-pool-report/19-VALIDATION.md` (task IDs 19-03-01 / 19-04-01, pytest `-k comparison_pool_report`)
      - lightroom_tagger/scripts/generate_comparison_pool_report.py (entry points)
      - lightroom_tagger/core/database/db_init.py (`init_database`)
    </read_first>
    <action>
      Create **`lightroom_tagger/scripts/test_comparison_pool_report.py`** with tests:

      1. **`test_comparison_pool_report_writes_html_and_assets(tmp_path, monkeypatch)`** — Build minimal library DB via **`init_database(str(tmp_path / 'lib.db'))`**, insert one **`instagram_dump_media`** row with **`processed=0`**, **`last_attempted_at='2026-01-01T00:00:00'`**, **`media_key='fixture/1'`**, **`file_path`** pointing to a **1×1 PNG** **`tmp_path` file you create, **`date_folder='202601'`**; insert **`insert_comparison_pool_snapshot`** with **`results=[{...}]`** stub containing required keys and one catalog key; insert minimal **`images` row** for that **`catalog_key`** with **`filepath`** to another tiny test image under **`tmp_path`**. Run **`write_comparison_pool_report(...)`** into **`tmp_path/'out'`**. Assert **`(tmp_path/'out'/'report.html').exists()`** and **`(tmp_path/'out'/'assets').is_dir()`** and any **`rg`/`read_text()`** shows **`src=\"assets/`**.

      2. **`test_comparison_pool_report_primary_has_no_absolute_paths(tmp_path)`** — Generate HTML as above; read text; slice between **`<main id="lt-primary-comparison-pool">`** and **`</main>`**; assert slice **does not contain** substring **`/Users/`** and **does not contain** **`/tmp/`** (escape-proof paths in primary — use relative **`file_path`** in fixture rows only or empty **`debug_resolved_path`**).

      3. **`test_comparison_pool_report_reconstructed_banner(tmp_path)`** — Same as (1) but **skip** **`insert_comparison_pool_snapshot`**; assert file contains **`Reconstructed — not exact run evidence`**.

      Register names so **`pytest -q -k comparison_pool_report`** collects them (**`test_comparison_pool_report_`** prefix satisfies **`-k comparison_pool_report`**).
    </action>
    <acceptance_criteria>
      - `test -f /Users/ccanales/projects/lightroom-tagger/lightroom_tagger/scripts/test_comparison_pool_report.py` exits 0
      - `cd /Users/ccanales/projects/lightroom-tagger && pytest lightroom_tagger/scripts/test_comparison_pool_report.py -q -k comparison_pool_report` exits 0
      - `rg -n "test_comparison_pool_report_" lightroom_tagger/scripts/test_comparison_pool_report.py` exits 0
    </acceptance_criteria>
  </task>

  <task id="19-04-T3">
    <title>Assert snapshot persistence from `match_dump_media` (mocked vision)</title>
    <read_first>
      - lightroom_tagger/scripts/test_match_instagram_dump.py (patch style)
      - lightroom_tagger/scripts/match_instagram_dump.py (loop structure)
      - lightroom_tagger/core/database/match_pool_snapshots.py
    </read_first>
    <action>
      Append to **`lightroom_tagger/scripts/test_match_instagram_dump.py`** a new test **`test_match_dump_media_persists_comparison_pool_snapshot`** that:

      1. Creates real **`sqlite3`** / **`init_database`** temp DB under **`tmp_path`** with minimal **`instagram_dump_media`**, **`images`**, and prerequisites so **`find_candidates_by_date`** / CLIP can be patched.

      2. **`@patch('lightroom_tagger.scripts.match_instagram_dump.score_candidates_with_vision')`** returning deterministic **two-entry** **`results` list** with distinct **`catalog_key`** values and **`total_score`** **0.9** and **0.5**.

      3. Patch **`find_candidates_by_date`** to return two fake catalog rows matching those keys (with **`filepath`**).

      4. Patch **`shortlist_catalog_candidates_by_clip`** to return both keys in fixed order.

      5. Run **`match_dump_media(db, ...)`** with **`media_key`** set to the dump row, **`threshold=0.99`** so no **`store_match`**, ensuring attempted path executes.

      6. Query **`SELECT COUNT(*) AS c FROM comparison_pool_snapshots`** → assert **`c == 1`**; **`SELECT COUNT(*) AS c FROM comparison_pool_snapshot_candidates`** → assert **`c == 2`**.
    </action>
    <acceptance_criteria>
      - `rg -n "test_match_dump_media_persists_comparison_pool_snapshot" lightroom_tagger/scripts/test_match_instagram_dump.py` exits 0
      - `cd /Users/ccanales/projects/lightroom-tagger && pytest lightroom_tagger/scripts/test_match_instagram_dump.py -q -k persists_comparison_pool_snapshot` exits 0
    </acceptance_criteria>
  </task>

  <task id="19-04-T4">
    <title>Record manual offline smoke checklist (executor SUMMARY artifact)</title>
    <read_first>
      - `.planning/phases/19-generate-read-only-html-comparison-pool-report/19-VALIDATION.md` § Manual-Only Verifications
    </read_first>
    <action>
      When executing this plan, create **`.planning/phases/19-generate-read-only-html-comparison-pool-report/19-04-SUMMARY.md`** containing a markdown checklist with these **verbatim** checklist lines (exact bullets):
      - **`- [ ] Opened generated report via `file://` URL`**
      - **`- [ ] Confirmed candidate thumbnails load from `assets/` with backend/frontend stopped`**
      - **`- [ ] Expanded one `lt-pool-debug` details panel; confirmed extra fields`**
      - **`- [ ] Searched primary `main` region for absolute `/Users` or `/tmp` paths — none visible`**

      (Executor checks boxes after performing steps.)
    </action>
    <acceptance_criteria>
      - After execution, **`test -f /Users/ccanales/projects/lightroom-tagger/.planning/phases/19-generate-read-only-html-comparison-pool-report/19-04-SUMMARY.md`** exits 0
      - `rg -n "Opened generated report via \`file://\` URL" .planning/phases/19-generate-read-only-html-comparison-pool-report/19-04-SUMMARY.md` exits 0
    </acceptance_criteria>
  </task>
</tasks>

<verification>
From repo root (`.planning/phases/19-generate-read-only-html-comparison-pool-report/19-VALIDATION.md`):

1. **`pytest lightroom_tagger/scripts/test_match_instagram_dump.py -q`**

2. **`pytest lightroom_tagger/scripts/test_comparison_pool_report.py apps/visualizer/backend/tests/test_handlers_single_match.py -q -k "match_dump or comparison_pool_report"`**

Both commands must exit **0**.

3. Manual: executor completes **`19-04-SUMMARY.md`** checklist (**D-05** / Nyquist manual layer).
</verification>

<must_haves>
- [ ] Reconstruction path implemented; banner visible when no snapshot (**D-02**).
- [ ] Automated tests **`comparison_pool_report`** + snapshot persistence test green.
- [ ] Primary HTML slice excludes **`/Users/`** and **`/tmp/`** in **`test_comparison_pool_report_primary_has_no_absolute_paths`** (**D-13** automated guard).
- [ ] **`19-04-SUMMARY.md`** exists with **`file://`** smoke instructions (**19-VALIDATION.md**).
- [ ] Folded todo intent documented: report enables pre-prompt-change diagnosis (**D-14**).
</must_haves>
