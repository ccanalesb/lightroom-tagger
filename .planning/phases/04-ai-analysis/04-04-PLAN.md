---
phase: 4
plan: 04
title: "Batch describe: 12-month window, min_rating metadata, and SQL alignment"
type: feature
wave: 1
depends_on:
  - "06"
files_modified:
  - lightroom_tagger/core/database.py
  - apps/visualizer/backend/jobs/handlers.py
  - apps/visualizer/frontend/src/components/processing/DescriptionsTab.tsx
  - apps/visualizer/backend/tests/test_handlers_batch_describe.py
autonomous: true
requirements:
  - AI-03
---

<objective>
Fix `12months` date scoping in `handle_batch_describe`, add optional `min_rating` (catalog-only) to batch metadata end-to-end, extend `get_undescribed_catalog_images` for rating, and cover contracts with handler tests.
</objective>

<tasks>

### Task 1: Extend undescribed catalog query for min_rating
**Type:** modify
**Files:** `lightroom_tagger/core/database.py`

<read_first>
- lightroom_tagger/core/database.py (`def get_undescribed_catalog_images` ~lines 1329–1342)
- apps/visualizer/backend/jobs/handlers.py (`handle_batch_describe` catalog branches ~lines 563–576)
</read_first>

<action>
1. Change signature to `def get_undescribed_catalog_images(db: sqlite3.Connection, months: int | None = None, min_rating: int | None = None) -> list[dict]:`.
2. After the existing `WHERE d.image_key IS NULL` block, if `min_rating is not None`, append ` AND i.rating >= ?` and bind `min_rating`.
3. Keep `months` behavior unchanged: when `months` is set, retain `AND i.date_taken >= date('now', ?)` with `f'-{months} months'`.
</action>

<acceptance_criteria>
- `rg "def get_undescribed_catalog_images" lightroom_tagger/core/database.py -A 5` shows `min_rating: int | None = None`.
- `rg "i.rating >=" lightroom_tagger/core/database.py` matches within `get_undescribed_catalog_images` (context: use `rg -n "get_undescribed_catalog_images" -A 20` manually if needed).
</acceptance_criteria>

### Task 2: Batch handler months map and min_rating filtering
**Type:** modify
**Files:** `apps/visualizer/backend/jobs/handlers.py`

<read_first>
- apps/visualizer/backend/jobs/handlers.py (`handle_batch_describe` from line 536 through 592)
- lightroom_tagger/core/database.py (`get_undescribed_catalog_images` after Task 1)
</read_first>

<action>
1. After reading `date_filter`, set `months = {'3months': 3, '6months': 6, '12months': 12}.get(date_filter)` (replace the old dict missing `12months`).
2. Read `min_rating = metadata.get('min_rating')`; if value is not `None`, coerce with `int(min_rating)` inside try/except; on invalid type skip or treat as `None` — **concrete:** `min_rating_raw = metadata.get('min_rating'); min_rating = int(min_rating_raw) if min_rating_raw is not None else None`.
3. For catalog + `force`: when building SQL `SELECT key FROM images`, if `months` is set keep date clause; additionally if `min_rating is not None`, append ` AND rating >= ?` with binding `min_rating`.
4. For catalog + not `force`: call `get_undescribed_catalog_images(lib_db, months=months, min_rating=min_rating)`.
5. For the Instagram selection path (`image_type in ('instagram', 'both')` Instagram-only keys), do **not** apply `min_rating`. When `image_type` is `both`, the handler runs a catalog block then an Instagram block — apply `min_rating` in the **catalog** block only (i.e., `image_type in ('catalog', 'both')` catalog selection path uses `min_rating`; the Instagram half of `both` ignores it).
6. Update the docstring comment for `date_filter` to `# all, 3months, 6months, 12months`.
</action>

<acceptance_criteria>
- `rg "'12months': 12" apps/visualizer/backend/jobs/handlers.py` returns a match.
- `rg "min_rating" apps/visualizer/backend/jobs/handlers.py` returns at least 3 matches.
- `rg "get_undescribed_catalog_images" apps/visualizer/backend/jobs/handlers.py` shows call includes `min_rating=min_rating`.
</acceptance_criteria>

### Task 3: DescriptionsTab UI and job metadata
**Type:** modify
**Files:** `apps/visualizer/frontend/src/components/processing/DescriptionsTab.tsx`

<read_first>
- apps/visualizer/frontend/src/components/processing/DescriptionsTab.tsx
- apps/visualizer/frontend/src/components/images/CatalogTab.tsx (`min_rating` select pattern if present; else `ImagesAPI` params for `min_rating`)
</read_first>

<action>
1. Add state `batchMinRating: number | null` default `null` representing “any rating”.
2. Add a `<select>` labeled **Minimum rating (catalog)** with options: value `""` label “Any”, `1` through `5` for “1+ stars” … “5+ stars” — use same visual classes as the Date Range select in this file (`w-full px-3 py-2 rounded-base border border-border ...`).
3. In `startDescriptions` metadata object, when `batchMinRating` is not null, set `min_rating: batchMinRating` (integer).
4. Add helper text under the select: `Applies to catalog (and both); Instagram-only batches ignore this filter.`
</action>

<acceptance_criteria>
- `rg "batchMinRating" apps/visualizer/frontend/src/components/processing/DescriptionsTab.tsx` returns at least 3 matches.
- `rg "min_rating" apps/visualizer/frontend/src/components/processing/DescriptionsTab.tsx` returns a match inside `metadata` construction.
</acceptance_criteria>

### Task 4: Handler tests for 12months and min_rating
**Type:** test
**Files:** `apps/visualizer/backend/tests/test_handlers_batch_describe.py`

<read_first>
- apps/visualizer/backend/tests/test_handlers_batch_describe.py (entire file or existing test patterns)
- apps/visualizer/backend/jobs/handlers.py (`handle_batch_describe`)
</read_first>

<action>
1. If file does not exist, create it with pytest imports and a fixture pattern consistent with `apps/visualizer/backend/tests/test_handlers_single_match.py` (MagicMock runner, patch DB as needed).
2. Add test that patches `get_undescribed_catalog_images` (or inspects SQL calls via `init_database` mock) to assert `handle_batch_describe` passes `months=12` when `metadata={'image_type':'catalog','date_filter':'12months','force':False}` — **concrete approach:** mock `get_undescribed_catalog_images` with `unittest.mock.patch` and assert called with `months=12`.
3. Add test that mock `get_undescribed_catalog_images` is called with `min_rating=3` when metadata contains `'min_rating': 3` and `image_type` is `catalog`.
4. Add test that `months` dict includes 12: patch at point of `months =` by running handler with `date_filter` `12months` and assert internal behavior — simplest: patch `get_undescribed_catalog_images` and assert kwargs `months=12` for `12months`.
</action>

<acceptance_criteria>
- File exists: `test_handlers_batch_describe.py` under `apps/visualizer/backend/tests/`.
- `rg "12months" apps/visualizer/backend/tests/test_handlers_batch_describe.py` returns a match.
- `rg "min_rating" apps/visualizer/backend/tests/test_handlers_batch_describe.py` returns a match.
- `pytest apps/visualizer/backend/tests/test_handlers_batch_describe.py -q` exits 0.
</acceptance_criteria>

</tasks>

<verification>
Enqueue `batch_describe` with `date_filter: '12months'` and confirm logs/SQL use 12-month window. Enqueue with `min_rating: 4` and confirm only catalog images with `rating >= 4` are selected (manual or unit tests).
</verification>

<must_haves>
- Batch description scope matches user-selected timeframe including 12 months, and catalog batches can prioritize rated work (AI-03).
- Default “unanalyzed only” when `force` is false remains unchanged.
</must_haves>
