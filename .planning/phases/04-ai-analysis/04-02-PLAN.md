---
phase: 4
plan: 02
title: "Catalog grid AI badges, score pill, and analyzed filter UI"
type: feature
wave: 2
depends_on:
  - "01"
files_modified:
  - apps/visualizer/frontend/src/services/api.ts
  - apps/visualizer/frontend/src/components/images/CatalogTab.tsx
  - apps/visualizer/frontend/src/components/catalog/CatalogImageCard.tsx
autonomous: true
requirements:
  - AI-05
  - AI-06
---

<objective>
Mirror the Phase 3 `posted` catalog filter pattern for `analyzed`, extend `CatalogImage` types and `ImagesAPI.listCatalog` query string, and show `Badge variant="accent"` label `AI` plus a compact best-perspective score pill on catalog cards when description data is present.
</objective>

<tasks>

### Task 1: Types and API client for catalog description fields
**Type:** modify
**Files:** `apps/visualizer/frontend/src/services/api.ts`

<read_first>
- apps/visualizer/frontend/src/services/api.ts (`ImagesAPI.listCatalog`, `CatalogImage` interface, `ImageDescription` / `PerspectiveScore` types)
- apps/visualizer/frontend/src/components/images/CatalogTab.tsx (how `posted` is passed into `ImagesAPI.listCatalog`)
</read_first>

<action>
1. Extend `CatalogImage` with optional fields: `ai_analyzed?: boolean`, `description_summary?: string | null`, `description_best_perspective?: string | null`, `description_perspectives?: ImageDescription['perspectives']` (reuse the existing `perspectives` shape from `ImageDescription`).
2. In `ImagesAPI.listCatalog`, add optional parameter `analyzed?: boolean | null` to the function argument object (same option bag pattern as existing `posted`, `month`, etc.).
3. When `analyzed === true`, append `analyzed=true` to `URLSearchParams`; when `analyzed === false`, append `analyzed=false`; when `undefined` or `null`, omit the param.
</action>

<acceptance_criteria>
- `rg "analyzed" apps/visualizer/frontend/src/services/api.ts` includes matches in both `listCatalog` and `CatalogImage`.
- `rg "description_best_perspective" apps/visualizer/frontend/src/services/api.ts` returns a match.
</acceptance_criteria>

### Task 2: Catalog tab Status filter extended for AI analyzed
**Type:** modify
**Files:** `apps/visualizer/frontend/src/components/images/CatalogTab.tsx`

<read_first>
- apps/visualizer/frontend/src/components/images/CatalogTab.tsx (entire file: `posted` select, `clearFilters`, `hasActiveFilters`, debounced fetch)
- apps/visualizer/frontend/src/services/api.ts (`ImagesAPI.listCatalog` after Task 1)
</read_first>

<action>
1. Add React state `analyzedFilter` with type `'all' | 'analyzed' | 'not_analyzed'`, default `'all'`.
2. Add a `<select>` labeled **AI status** (or **Analyzed**) with three `<option value="all">` All, `<option value="analyzed">` Analyzed only, `<option value="not_analyzed">` Not analyzed — mirror spacing/classes of the existing **Status** (posted) select per `DESIGN.md` (14px label, `rounded-base border border-border`, focus ring `focus:ring-accent`).
3. Pass to `ImagesAPI.listCatalog`: when `analyzedFilter === 'analyzed'` pass `analyzed: true`; when `'not_analyzed'` pass `analyzed: false`; when `'all'` omit `analyzed` (or pass `undefined`).
4. Include `analyzedFilter` in the debounced dependency array that triggers catalog reload (same as `posted`).
5. Reset `analyzedFilter` to `'all'` inside `clearFilters` alongside other filters; include it in `hasActiveFilters` when not `'all'`.
6. Add file-level comment `// AI-06: catalog analyzed filter (IG-06 pattern for posted)` within 5 lines of the new select.
</action>

<acceptance_criteria>
- `rg "analyzedFilter" apps/visualizer/frontend/src/components/images/CatalogTab.tsx` returns at least 3 matches.
- `rg "AI-06" apps/visualizer/frontend/src/components/images/CatalogTab.tsx` returns a match.
- `rg "analyzed:" apps/visualizer/frontend/src/components/images/CatalogTab.tsx` returns at least 1 match.
</acceptance_criteria>

### Task 3: Catalog card AI badge and score pill
**Type:** modify
**Files:** `apps/visualizer/frontend/src/components/catalog/CatalogImageCard.tsx`

<read_first>
- apps/visualizer/frontend/src/components/catalog/CatalogImageCard.tsx (file being modified)
- apps/visualizer/frontend/src/components/DescriptionPanel/CompactView.tsx (score pill markup and `descriptionScoreColor`, `DESCRIPTION_PERSPECTIVE_LABELS`)
- apps/visualizer/frontend/DESIGN.md (Badge: variant `accent`, 12px weight 500)
- apps/visualizer/frontend/src/utils/scoreColorClasses.ts (export `descriptionScoreColor`)
- apps/visualizer/frontend/src/components/DescriptionPanel/perspectiveLabels.ts (`DESCRIPTION_PERSPECTIVE_LABELS`)
</read_first>

<action>
1. Import `Badge` (already), `descriptionScoreColor` from `../../utils/scoreColorClasses`, `DESCRIPTION_PERSPECTIVE_LABELS` from `../DescriptionPanel/perspectiveLabels`.
2. In the badge row `flex-wrap` div (same row as Posted, rating, Pick), when `image.ai_analyzed` is true, render `<Badge variant="accent">AI</Badge>`.
3. When `image.description_best_perspective` is truthy and `image.description_perspectives` has that key with a numeric `score`, render a `<span>` matching CompactView’s pill: `className={\`text-xs px-1.5 py-0.5 rounded font-medium ${descriptionScoreColor(perspective.score)}\`}` with inner text `{(DESCRIPTION_PERSPECTIVE_LABELS[best] || best)} {perspective.score}/10` where `best = image.description_best_perspective` and `perspective = image.description_perspectives[best]`.
4. Do not import full `CompactView` to avoid circular deps; duplicate only the pill span structure from `CompactView.tsx` lines 19–24.
</action>

<acceptance_criteria>
- `rg "variant=\"accent\">AI" apps/visualizer/frontend/src/components/catalog/CatalogImageCard.tsx` returns a match OR `rg ">AI</Badge>" apps/visualizer/frontend/src/components/catalog/CatalogImageCard.tsx` returns a match.
- `rg "descriptionScoreColor" apps/visualizer/frontend/src/components/catalog/CatalogImageCard.tsx` returns a match.
- `npm run lint --prefix apps/visualizer/frontend` exits 0.
</acceptance_criteria>

</tasks>

<verification>
Open Images → Catalog: change **Analyzed** filter and confirm network requests include `analyzed=true|false`. With backend plan 01 deployed, cards show **AI** badge and score pill when `description_best_perspective` and scores exist.
</verification>

<must_haves>
- Users can distinguish analyzed photos in the grid and filter the catalog by analyzed state (AI-05, AI-06).
- UI follows existing catalog filter and badge-row patterns from Phase 3.
</must_haves>
