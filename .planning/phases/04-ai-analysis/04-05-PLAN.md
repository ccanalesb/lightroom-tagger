---
phase: 4
plan: 05
title: "Provider health probe, connection badges, and description defaults UX"
type: feature
wave: 1
depends_on:
  - "04"
files_modified:
  - lightroom_tagger/core/provider_registry.py
  - apps/visualizer/backend/api/providers.py
  - apps/visualizer/backend/tests/test_providers_api.py
  - apps/visualizer/frontend/src/services/api.ts
  - apps/visualizer/frontend/src/components/providers/ProviderCard.tsx
  - apps/visualizer/frontend/src/components/processing/ProvidersTab.tsx
  - apps/visualizer/frontend/src/components/processing/DescriptionsTab.tsx
autonomous: true
requirements:
  - AI-01
---

<objective>
Add a reachable/unreachable probe per provider for the API, show `Badge variant="success"` or `variant="error"` for live connection status on each provider card, and surface `defaults.description` for batch description jobs without overwriting vision-comparison defaults in shared match options.
</objective>

<tasks>

### Task 1: Registry probe method
**Type:** modify
**Files:** `lightroom_tagger/core/provider_registry.py`

<read_first>
- lightroom_tagger/core/provider_registry.py (`get_client`, `list_models`, `_is_available`)
- lightroom_tagger/core/provider_errors.py (exception types if wrapping errors)
</read_first>

<action>
1. Add method `def probe_connection(self, provider_id: str) -> tuple[bool, str | None]:` on `ProviderRegistry`.
2. If `provider_id not in self._providers`, raise `KeyError` (caller maps to 404).
3. In `try`: `client = self.get_client(provider_id)`; call `client.models.list()` (OpenAI SDK) with a short timeout if the SDK supports `timeout=` on the call — if not, rely on default and catch timeouts as failure.
4. Return `(True, None)` on success.
5. On any exception `exc`, return `(False, str(exc))` without raising.
</action>

<acceptance_criteria>
- `rg "def probe_connection" lightroom_tagger/core/provider_registry.py` returns a match.
- `rg "models.list" lightroom_tagger/core/provider_registry.py` returns a match.
</acceptance_criteria>

### Task 2: Flask route GET provider health
**Type:** modify
**Files:** `apps/visualizer/backend/api/providers.py`

<read_first>
- apps/visualizer/backend/api/providers.py (blueprint routes, `_get_registry`)
- apps/visualizer/backend/app.py (blueprint url_prefix for providers if needed)
</read_first>

<action>
1. Register `@bp.route("/<provider_id>/health", methods=["GET"])` **before** any `/<provider_id>/models/...` routes that could capture `health` as a path segment — if ordering is problematic, use path `/health/<provider_id>` instead; **concrete choice:** `@bp.route("/<provider_id>/health", methods=["GET"])` placed after `list_providers` route and before `/<provider_id>/models/<path:model_id>` in the file.
2. Handler: `registry = _get_registry()`; `try: ok, detail = registry.probe_connection(provider_id)` except `KeyError: return jsonify({"error": "Unknown provider"}), 404`.
3. If `ok`, return `jsonify({"reachable": True})` with status 200.
4. If not `ok`, return `jsonify({"reachable": False, "error": detail})` with status **200** (UI distinguishes by `reachable` flag) — **concrete:** status 200 body `{"reachable": false, "error": "..."}` so frontend does not treat as HTTP error for badge coloring.
</action>

<acceptance_criteria>
- `rg "/health" apps/visualizer/backend/api/providers.py` returns a match.
- `rg "probe_connection" apps/visualizer/backend/api/providers.py` returns a match.
- `rg "reachable" apps/visualizer/backend/api/providers.py` returns a match.
</acceptance_criteria>

### Task 3: Backend test for health route
**Type:** test
**Files:** `apps/visualizer/backend/tests/test_providers_api.py`

<read_first>
- apps/visualizer/backend/tests/test_providers_api.py (or create file following test_images_catalog_api.py client pattern)
- apps/visualizer/backend/api/providers.py
</read_first>

<action>
1. Patch `ProviderRegistry.probe_connection` to return `(True, None)` and `GET /api/providers/test-provider/health` — use exact prefix from app registration (`/api/providers/`).
2. Assert status 200 and JSON `reachable` is `True`.
3. Second test: patch returns `(False, 'boom')`; assert JSON `reachable` is `False` and `error` contains `boom`.
</action>

<acceptance_criteria>
- `rg "health" apps/visualizer/backend/tests/test_providers_api.py` returns a match.
- `pytest apps/visualizer/backend/tests/test_providers_api.py -q` exits 0 (create minimal file if missing).
</acceptance_criteria>

### Task 4: Frontend API and ProviderCard connection badge
**Type:** modify
**Files:** `apps/visualizer/frontend/src/services/api.ts`, `apps/visualizer/frontend/src/components/providers/ProviderCard.tsx`, `apps/visualizer/frontend/src/components/processing/ProvidersTab.tsx`

<read_first>
- apps/visualizer/frontend/src/services/api.ts (`ProvidersAPI` object)
- apps/visualizer/frontend/src/components/providers/ProviderCard.tsx
- apps/visualizer/frontend/src/components/processing/ProvidersTab.tsx
- apps/visualizer/frontend/DESIGN.md (semantic success `#1aae39`, error `#e03e3e` mapped via Badge variants)
</read_first>

<action>
1. In `api.ts`, add `health: (providerId: string) => request<{ reachable: boolean; error?: string }>(\`/providers/${encodeURIComponent(providerId)}/health\`)` under `ProvidersAPI`.
2. Extend `ProviderCardProps` with optional `connectionReachable: boolean | null` (`null` = unknown/not loaded) and optional `connectionError?: string | null`.
3. In the card header next to the existing static `provider.available` badge: if `connectionReachable is True`, render `<Badge variant="success">Reachable</Badge>`; if `False`, render `<Badge variant="error">Unreachable</Badge>`; if `null`, render nothing or `<Badge variant="default">Not tested</Badge>` — **concrete:** show **Reachable** / **Unreachable** only when not null; omit "Not tested" to keep UI minimal.
4. In `ProvidersTab`, add `useState<Record<string, boolean | null>>({})` for reachability; `useEffect` when `providers` loads (non-empty), `Promise.all(providers.map(p => ProvidersAPI.health(p.id).then(r => [p.id, r.reachable] as const).catch(() => [p.id, false] as const)))` then merge into state map.
5. Pass `connectionReachable={reachability[provider.id] ?? null}` into each `ProviderCard`.
</action>

<acceptance_criteria>
- `rg "health:" apps/visualizer/frontend/src/services/api.ts` returns a match.
- `rg "connectionReachable" apps/visualizer/frontend/src/components/providers/ProviderCard.tsx` returns a match.
- `rg "ProvidersAPI.health" apps/visualizer/frontend/src/components/processing/ProvidersTab.tsx` returns a match.
- `rg "Unreachable" apps/visualizer/frontend/src/components/providers/ProviderCard.tsx` returns a match.
</acceptance_criteria>

### Task 5: Description defaults for batch jobs only
**Type:** modify
**Files:** `apps/visualizer/frontend/src/components/processing/DescriptionsTab.tsx`

<read_first>
- apps/visualizer/frontend/src/components/processing/DescriptionsTab.tsx (after plan 04 changes — depends_on 04)
- apps/visualizer/frontend/src/services/api.ts (`ProvidersAPI.getDefaults` return type)
</read_first>

<action>
1. Add state `descProviderId: string | null` and `descProviderModel: string | null`, both initial `null`.
2. `useEffect` on mount: `ProvidersAPI.getDefaults().then((defaults) => { const block = defaults.description; if (block?.provider) { setDescProviderId(block.provider); setDescProviderModel(block.model ?? null); } }).catch(console.error)`.
3. Replace `ProviderModelSelect` usage (if it uses only `useMatchOptions`) with values from `descProviderId` / `descProviderModel` and updaters that set local state (do not call `updateOption` for provider fields for this tab).
4. When building `metadata` in `startDescriptions`, use `descProviderId` / `descProviderModel` instead of `options.providerId` / `options.providerModel` for `provider_id` and `provider_model`.
5. Keep `max_workers` from `options.maxWorkers` via `useMatchOptions`.
</action>

<acceptance_criteria>
- `rg "defaults.description" apps/visualizer/frontend/src/components/processing/DescriptionsTab.tsx` OR `rg "getDefaults()" apps/visualizer/frontend/src/components/processing/DescriptionsTab.tsx` returns at least 2 matches.
- `rg "descProviderId" apps/visualizer/frontend/src/components/processing/DescriptionsTab.tsx` returns at least 3 matches.
</acceptance_criteria>

### Task 6: Providers tab — edit description defaults
**Type:** modify
**Files:** `apps/visualizer/frontend/src/components/processing/ProvidersTab.tsx`

<read_first>
- apps/visualizer/frontend/src/components/processing/ProvidersTab.tsx
- apps/visualizer/frontend/src/components/ui/ProviderModelSelect.tsx (props interface)
- apps/visualizer/frontend/src/services/api.ts (`ProvidersAPI.updateDefaults`)
</read_first>

<action>
1. Add state loaded from `ProvidersAPI.getDefaults()` for `descriptionProvider` and `descriptionModel` strings | null.
2. Below `FallbackOrderPanel`, add a `Card` titled **Default models** with subsection **Descriptions**: render `ProviderModelSelect` bound to description defaults; on change update local state only; add **Save** button calling `ProvidersAPI.updateDefaults({ description: { provider: <id>, model: <model> } })` with exact keys `provider` and `model` matching backend `registry.update_defaults` expectations (inspect `provider_registry.update_defaults` for nested shape — must match existing `defaults` object structure in `providers.json`).
3. On successful save, show no alert (or brief inline success text) — **concrete:** `setSaveMsg('Saved')` with timeout 2s.
</action>

<acceptance_criteria>
- `rg "updateDefaults" apps/visualizer/frontend/src/components/processing/ProvidersTab.tsx` returns a match.
- `rg "description" apps/visualizer/frontend/src/components/processing/ProvidersTab.tsx` matches within the new Defaults UI (e.g. `rg "Descriptions" apps/visualizer/frontend/src/components/processing/ProvidersTab.tsx` returns a match).
</acceptance_criteria>

</tasks>

<verification>
Load Processing → Providers: each card eventually shows Reachable or Unreachable. Save description defaults and confirm `GET /api/providers/defaults` returns `description.provider` / `description.model`. Descriptions tab uses those for new batch jobs without changing Matching tab provider selection.
</verification>

<must_haves>
- Operators can validate provider connectivity and configure default description provider/model (AI-01).
- Batch describe uses configured description defaults independently of vision comparison defaults.
</must_haves>
