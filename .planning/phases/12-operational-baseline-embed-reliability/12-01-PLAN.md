---
plan: 12-01
wave: 1
phase: 12
title: "Backend: embed preflight fail-fast (>50%), skip payload verification, compression noise, providers test"
depends_on: []
files_modified:
  - apps/visualizer/backend/jobs/handlers.py
  - apps/visualizer/backend/tests/test_handlers_batch_embed_image.py
  - apps/visualizer/backend/tests/test_handlers_batch_analyze.py
  - apps/visualizer/frontend/src/components/processing/CatalogCacheTab.tsx
  - lightroom_tagger/core/analyzer.py
  - lightroom_tagger/core/description_service.py
  - lightroom_tagger/core/test_analyzer.py
  - lightroom_tagger/core/test_description_service.py
autonomous: true
requirements:
  - OPS-02
  - OPS-03
  - OPS-04
  - OPS-05
---

## Objective

Align the embed preflight with D-03–D-06: abort non-chain jobs when **more than half** of the sampled paths are unreachable (**strict >50%**; exactly half continues), with one actionable message. Confirm the embed result `skip_reason_counts` keys match D-07 (no schema change expected). Silence per-image JPEG compression stdout spam when describing from an existing vision-cache hit during `batch_analyze`’s nested describe pass, and emit a single info job log summarizing how many images skipped redundant compression when that count is positive (D-10–D-11). Re-verify `TestDefaults` remains green (OPS-05 / D-12). Touch `CatalogCacheTab.tsx` once with a source-only comment so `job-ui-contract` clears after `handlers.py` edits.

## Verification criteria (wave)

- `cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend && pytest tests/test_handlers_batch_embed_image.py -k preflight -x -q` exits **0**
- `cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend && pytest tests/test_handlers_batch_embed_image.py -k "skip_reason_counts or grouped_skip" -x -q` exits **0** (fallback: single test module path if `-k` too narrow — `pytest tests/test_handlers_batch_embed_image.py::test_batch_embed_image_reports_grouped_skip_reason_counts -x -q`)
- `cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend && pytest tests/test_handlers_batch_analyze.py -x -q` exits **0**
- `cd /Users/ccanales/projects/lightroom-tagger && python -m pytest lightroom_tagger/core/test_analyzer.py lightroom_tagger/core/test_description_service.py -x -q` exits **0**
- `cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend && pytest tests/test_providers_api.py::TestDefaults -x -q` exits **0**

## must_haves

Goal-backward verification for this plan:

- [ ] `_EMBED_PREFLIGHT_FAIL_RATIO` in `handlers.py` is **`0.5`** and non-chain embed jobs **`fail`** (no embedding loop) when `fail_ratio > _EMBED_PREFLIGHT_FAIL_RATIO` (equivalently `sample_failed_count * 2 > sample_size`), **not** when `fail_ratio` equals **`0.5`** (e.g. **4/8** continues; **5/8** aborts); except **`chain_mode`** still **never** hard-fails on preflight (warn + continue when `fail_ratio > _EMBED_PREFLIGHT_FAIL_RATIO`).
- [ ] Fail message and error job log text include **`network share`** and **`not mounted`** (or **`unmounted`** as part of **`Check your mount`**) and **`{sample_failed_count}/{sample_size} sampled paths unreachable`**.
- [ ] `skip_reason_counts` success payload keeps keys **`no_row`**, **`empty_path`**, **`unresolved_or_missing`**, **`encode_failed`** documented in grep-only task.
- [ ] **`compress_image(..., silent=True)`** omits stdout line starting with **` Compressed:`**; vision-cache catalog describe passes **`silent=True`** only when **`get_or_create_cached_image`** returned a usable **`cached_path`** used as `image_for_describe`.
- [ ] **`nested_analyze_checkpoint=True`** describe path calls **`add_job_log(..., 'info',`** with message matching **`^\\d+ images already compressed, skipped\\.$`** when telemetry count **`> 0`**.
- [ ] **`CatalogCacheTab.tsx`** contains comment substring **`job-ui-contract`**
- [ ] **`pytest apps/visualizer/backend/tests/test_providers_api.py::TestDefaults -x -q`** reports **4 passed**

## Tasks

<task id="12-01-01" type="execute">
<title>Embed preflight: abort when sampled failures strictly exceed half (&gt;50%, non-chain)</title>
<read_first>
- `/Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/jobs/handlers.py` — constants `_EMBED_PREFLIGHT_SAMPLE_SIZE`, `_EMBED_PREFLIGHT_FAIL_RATIO` (~lines 64–75); block `sample_size = min(len(remaining)` through `Continuing — missing files` (~2940–3005); `chain_mode` / `_catalog_cache_chain` semantics
- `/Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/tests/test_handlers_batch_embed_image.py` — `test_batch_embed_image_preflight_fails_fast_when_paths_inaccessible`, `test_batch_embed_image_preflight_warns_and_continues_on_partial_miss`, `test_batch_embed_image_preflight_does_not_abort_in_chain_mode`
- `/Users/ccanales/projects/lightroom-tagger/.planning/phases/12-operational-baseline-embed-reliability/12-CONTEXT.md` — D-03–D-06
</read_first>
<action>
Set `_EMBED_PREFLIGHT_FAIL_RATIO = 0.5` (keep constant name).

Replace preflight branching so that:

1. Compute `fail_ratio = sample_failed_count / sample_size` unchanged.
2. If `fail_ratio > _EMBED_PREFLIGHT_FAIL_RATIO` (strict **>** half; **_EMBED_PREFLIGHT_FAIL_RATIO** remains **`0.5`** — e.g. **5/8** aborts, **4/8** does not):
   - Build `abort_msg = f"{sample_failed_count}/{sample_size} sampled paths unreachable — this usually means your network share is not mounted. Check your mount and retry."` (literal double-hyphen **` — `** before **this**).

3. **`if chain_mode and fail_ratio > _EMBED_PREFLIGHT_FAIL_RATIO`**: preserve **warning-only** behaviour: **`add_job_log(runner.db, job_id, 'warning',`** detailed `preflight_msg` (reuse existing verbose `Embed preflight: … Examples:` paragraph from current code **`preflight_msg` variable ~2976–2983**) **+**suffix **`Continuing — missing files will be skipped per-image.`** — do **not** call `runner.fail_job` for **`chain_mode`**.

4. **`elif fail_ratio > _EMBED_PREFLIGHT_FAIL_RATIO`** (implicitly not chain): **`add_job_log(runner.db, job_id, 'error', abort_msg)`** then **`runner.fail_job(job_id, abort_msg, severity='critical')`** then **`return`** (never enter embed loop).

5. Remove obsolete branch that **`fail_job` only when `fail_ratio >= 1.0 and not chain_mode`** with message containing **`All sampled images are inaccessible`**.

6. For `fail_ratio <= _EMBED_PREFLIGHT_FAIL_RATIO` (includes **exactly half**, e.g. **4/8**): emit **no** abort and **no** preflight warning (delete old soft-warning path keyed off `preflight_msg`/`_EMBED_PREFLIGHT_FAIL_RATIO` that logged **`Continuing — missing`** for partial misses).

Rewrite tests:

- **`test_batch_embed_image_preflight_fails_fast_when_paths_inaccessible`** — update assertions: **`fail_message` contains `network share`** and **`not mounted`** or **`mounted`** substring as part of unreachable sentence; **`All sampled images are inaccessible`** **must NOT** appear; still **`runner.fail_job.assert_called_once()`**.

- Rename and rewrite **`test_batch_embed_image_preflight_warns_and_continues_on_partial_miss`** (or replace) so the scenario uses **>** half unreachable in the fixed sample (**e.g. patch `_EMBED_PREFLIGHT_SAMPLE_SIZE` to `8`, **five** missing paths): expect **`runner.fail_job.assert_called_once()`**, **`runner.complete_job.assert_not_called()`**, and error log / **`fail_job`** argument includes **`network share`** / **`sampled paths unreachable`**. Docstring **`>50%`** abort (**5/8**), not **`>=50%`**.

- Add **`test_batch_embed_image_preflight_continues_at_exact_half_failures`**: patch **`_EMBED_PREFLIGHT_SAMPLE_SIZE` to `8`**, **four** unreachable + **four** good paths → **`fail_ratio == 0.5`** → assert **`runner.fail_job` not called** and **`runner.complete_job` called** (boundary: **4/8** continues).

- Keep **`test_batch_embed_image_preflight_continues_when_under_half_failures`** (or equivalent): e.g. **four** catalog rows with **one** bad path, patch sample **`4`** → **25%** misses; assert **`runner.fail_job` not called** and **`runner.complete_job` called**.
</action>
<acceptance_criteria>
- `grep -n _EMBED_PREFLIGHT_SAMPLE_SIZE /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/jobs/handlers.py` shows a numeric value **≥ 20** and **≤ 50** inclusive (D-03 range)
- `handlers.py` line defining `_EMBED_PREFLIGHT_FAIL_RATIO` contains **`0.5`**
- `handlers.py` preflight uses **strict** over-half check: **`fail_ratio > _EMBED_PREFLIGHT_FAIL_RATIO`** (or equivalent **`sample_failed_count * 2 > sample_size`**) — **not** **`>=`** against **`0.5`**
- `handlers.py` contains **`sampled paths unreachable`** and **`network share`** and **`Check your mount`**
- `handlers.py` does **not** contain **`All sampled images are inaccessible`** (literal string)
- `pytest /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/tests/test_handlers_batch_embed_image.py -k preflight -x -q` exits **0**
</acceptance_criteria>
</task>

<task id="12-01-02" type="execute">
<title>OPS-03 backend: grep-verify skip_reason_counts keys unchanged</title>
<read_first>
- `/Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/jobs/handlers.py` — search `skip_reason_counts` construction and `'no_row'` / `'empty_path'` literals near embed completion (~2815–2840 if present)
- `/Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/tests/test_handlers_batch_embed_image.py` — **`test_batch_embed_image_reports_grouped_skip_reason_counts`**
- `/Users/ccanales/projects/lightroom-tagger/.planning/phases/12-operational-baseline-embed-reliability/12-CONTEXT.md` — D-07 mapping
</read_first>
<action>
Inspect successful completion payload assembly for **`batch_embed_image`**. Confirm the dict keyed **`skip_reason_counts`** includes Python string keys **`"no_row"`**, **`"empty_path"`**, **`"unresolved_or_missing"`**, **`"encode_failed"`** (matching D-07 semantics: missing file/unreachable → **`unresolved_or_missing`**, empty path → **`empty_path`**, no DB row → **`no_row`**; **`encode_failed`** remains optional fourth counter).

Make **zero** behavioural changes unless a key spelling diverges — if already correct, add a **five-line-max** contiguous comment immediately above **`skip_reason_counts`** assignment documenting D-07 label mapping (**`unresolved_or_missing` → Missing file**, etc.).
</action>
<acceptance_criteria>
- `grep -n "skip_reason_counts" /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/jobs/handlers.py` shows at least one assignment whose dict literals include **`"no_row"`**, **`"empty_path"`**, **`"unresolved_or_missing"`**, and **`"encode_failed"`**
- **`pytest /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/tests/test_handlers_batch_embed_image.py::test_batch_embed_image_reports_grouped_skip_reason_counts -x -q`** exits **0**
</acceptance_criteria>
</task>

<task id="12-01-03" type="execute">
<title>OPS-04: silent compress_image + telemetry + nested batch_analyze summary log</title>
<read_first>
- `/Users/ccanales/projects/lightroom-tagger/lightroom_tagger/core/analyzer.py` — **`compress_image`**, **`_describe_image_via_provider`**
- `/Users/ccanales/projects/lightroom-tagger/lightroom_tagger/core/description_service.py` — **`describe_matched_image`**
- `/Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/jobs/handlers.py` — **`_describe_single_image`**, **`_run_describe_pass`** (through **`result_summary`** ~1699–1727)
- `/Users/ccanales/projects/lightroom-tagger/.planning/phases/12-operational-baseline-embed-reliability/12-CONTEXT.md` — D-10/D-11
</read_first>
<action>

1. **`compress_image`** — add **`silent: bool = False`** before the closing **`)`** of the parameter list (**after `quality`**). Guard **both** `print(...)` branches so they run **only when `not silent`**.

2. **`describe_image`** — add **`silent_compression: bool = False`** (**after **`user_prompt`**) → forward into **`_describe_image_via_provider`**.

3. **`_describe_image_via_provider`** — call **`compress_image(viewable, ...)` with **`silent=silent_compression`** (pass through existing **`max_size`/`quality`** if present; otherwise default call).

4. **`describe_matched_image`** — add kw-only **`telemetry: dict | None = None`**. When **`cached_path`** is truthy, **`cached_path == image_for_describe`**, and **`os.path.exists(cached_path)`**: call **`describe_image(..., silent_compression=True)`**; if **`telemetry is not None`**, then **`with telemetry['_lock']:`** do **`telemetry['silent_compression_skips'] += 1`** (caller must initialize **`telemetry['silent_compression_skips'] = 0`** alongside **`'_lock'`**).

5. **`_describe_single_image`** — add **`telemetry=None`**; pass **`telemetry=telemetry`** into **`describe_matched_image`** only for catalog.

6. **`_run_describe_pass`** — immediately **after** **`consecutive_failures = 0`** (~line 1573), set **`telemetry = None`**, then **`if nested_analyze_checkpoint:`** **`telemetry = {'silent_compression_skips': 0, '_lock': threading.Lock()}`**. Pass **`telemetry=telemetry`** into every **`_describe_single_image`** call in both parallel and sequential branches (pass **`None`** when not nested). After both loops complete without cancel/fail short-circuit, before **`result_summary`**: **`if nested_analyze_checkpoint and telemetry is not None and telemetry['silent_compression_skips'] > 0:`** **`add_job_log(runner.db, job_id, 'info', f"{telemetry['silent_compression_skips']} images already compressed, skipped.")`**. Import **`threading`** in `handlers.py` if missing.

7. **Tests**
   - **`lightroom_tagger/core/test_analyzer.py`**: **`test_compress_image_silent_suppresses_prints`** using **`capsys`** — success path must not write **`Compressed:`** to **`capsys.readouterr().out`** when **`silent=True`**.
   - **`lightroom_tagger/core/test_description_service.py`**: patch **`get_or_create_cached_image`** to return **`str(tmp_path / 'c.jpg')`**, create minimal valid JPEG at that path; assert **`describe_image`** mock called with **`silent_compression=True`**.
   - **`test_handlers_batch_analyze.py`**: new **`test_batch_analyze_compression_already_done_silent`** — same patches as **`test_batch_analyze_runs_describe_then_score`** (`load_config`, `init_database`, `getenv`, `require_library_db`, **`_score_single_image`**, **`add_job_log`**) plus **`patch('lightroom_tagger.core.description_service.get_or_create_cached_image')`** returning path to that JPEG, **`patch('lightroom_tagger.core.description_service.describe_image')`** returning **`{'summary':'x','composition':{},'perspectives':{},'technical':{},'subjects':[],'best_perspective':'s'}`**. **`get_undescribed_catalog_images`** one row. Metadata **`max_workers: 1`**. Assert **`add_job_log`** called with **`'1 images already compressed, skipped.'`** as message arg (level **`info`**).

</action>
<acceptance_criteria>
- `analyzer.py` signature line for **`def compress_image`** contains **`silent: bool = False`**
- `handlers.py` contains literal **`images already compressed, skipped.`** (including terminal period)
- **`python -m pytest /Users/ccanales/projects/lightroom-tagger/lightroom_tagger/core/test_analyzer.py::test_compress_image_silent_suppresses_prints -x -q`** exits **0** (adjust test name if executor names differently — then grep test file for **`silent_suppresses`**)
- **`pytest /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/tests/test_handlers_batch_analyze.py::test_batch_analyze_compression_already_done_silent -x -q`** exits **0**
</acceptance_criteria>
</task>

<task id="12-01-04" type="execute">
<title>OPS-05: verify TestDefaults + job-ui-contract noop touch</title>
<read_first>
- `/Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/tests/test_providers_api.py` — class **`TestDefaults`**
- `/Users/ccanales/projects/lightroom-tagger/.cursor/rules/job-ui-contract.mdc` — clearing **`handlers.py`** edits
- `/Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend/src/components/processing/CatalogCacheTab.tsx` — file header
</read_first>
<action>

1. Run **`pytest /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend/tests/test_providers_api.py::TestDefaults -x -v`** and capture **`4 passed`** in terminal output (**no source edits if green**).

2. On **new line immediately after** the file’s closing **`}`** that ends **`export`** / component (EOF), append **`// job-ui-contract: touched with phase 12 backend batch job changes (no UI change)`** preserving final newline (TypeScript permits file-trailing **`//`** comments outside the JSX tree).

</action>
<acceptance_criteria>
- Shell command **`cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend && pytest tests/test_providers_api.py::TestDefaults -x -q 2>&1 | tail -n 1`** output contains **`4 passed`** substring **OR** **`passed`** count line shows **4**.

- **`rg "job-ui-contract: touched with phase 12" /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend/src/components/processing/CatalogCacheTab.tsx`** exits **0**

</acceptance_criteria>
</task>
