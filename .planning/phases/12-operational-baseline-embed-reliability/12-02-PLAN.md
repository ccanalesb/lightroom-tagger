---
plan: 12-02
wave: 2
phase: 12
title: "Frontend: embed discoverability sweep; JobDetailModal skip breakdown (OPS-03 UI)"
depends_on:
  - "12-01"
files_modified:
  - apps/visualizer/frontend/src/constants/strings.ts
  - apps/visualizer/frontend/src/components/jobs/JobDetailModal.tsx
  - apps/visualizer/frontend/src/components/jobs/__tests__/JobDetailModal.test.tsx
autonomous: true
requirements:
  - OPS-01
  - OPS-03
---

## Objective

Complete OPS-01 by **evidence-only** repo sweep: confirm whether any surface besides `SearchPage.tsx` surfaces **`no_clip_embedding`** or the backend’s **“Visual similarity is unavailable”** string; if hits exist, apply **`12-UI-SPEC.md`** D-02 pattern (**`SEARCH_PIN_HELP_EMBED`**, **`SEARCH_PIN_LINK_CACHE`**, **`Link`** to **`PROCESSING_CATALOG_CACHE_ROUTE`**, secondary **`PROCESSING_JOB_QUEUE_ROUTE`** + **`PROCESSING_OPEN_JOB_QUEUE`**, **`className="font-medium text-accent underline"`**).

Implement OPS-03 UI: three D-07 buckets only, **UI-SPEC labels** centralized in **`strings.ts`**, **`encode_failed` hidden**, counts **`<=0` hidden**, diagnostics **card omitted** when **no** positive bucket — update Vitest.

## Verification criteria (wave)

- **`cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend && npx vitest run`** exits **0**

## must_haves

- [ ] **`rg "no_clip_embedding"`** over **`apps/visualizer/frontend/src/**/*.ts(x)`** resolves to **only** `SearchPage.tsx` **or** every non-SearchPage hit is patched to D-02 pattern (links + copy from **`strings.ts`** per **`12-UI-SPEC.md`**).
- [ ] **`rg "Visual similarity is unavailable"`** under **`apps/visualizer/frontend/src`** returns **no** matches (exit code **1**) **unless** a caller is added with D-02 pattern in the same PR.
- [ ] `strings.ts` exports **`JOB_SKIP_MISSING_FILE`**, **`JOB_SKIP_EMPTY_PATH`**, **`JOB_SKIP_NO_DB_ROW`** with values exactly **`"Missing file"`**, **`"Empty path"`**, **`"No DB row"`** (quoted strings).
- [ ] `JobDetailModal.tsx` maps **`unresolved_or_missing` → `JOB_SKIP_MISSING_FILE`**, **`empty_path` → `JOB_SKIP_EMPTY_PATH`**, **`no_row` → `JOB_SKIP_NO_DB_ROW`**, renders **no row** for **`encode_failed`**, renders **no** row whose count is **≤0**, renders **no embed diagnostics card** when **`skip_reason_counts` absent** or **all three bucket counts ≤0**.

## Tasks

<task id="12-02-01" type="execute">
<title>OPS-01: frontend sweep for no_clip_embedding / visual similarity unavailable</title>
<read_first>
- `/Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend/src/pages/SearchPage.tsx` — reference **`no_clip_embedding`**, **`SEARCH_PIN_HELP_EMBED`**, **`Link`** blocks ~423–446 and ~462–483
- `/Users/ccanales/projects/lightroom-tagger/.planning/phases/12-operational-baseline-embed-reliability/12-UI-SPEC.md` — OPS-01 canonical pattern
- `/Users/ccanales/projects/lightroom-tagger/.planning/phases/12-operational-baseline-embed-reliability/12-RESEARCH.md` — prior sweep (validate, do not trust blindly)
</read_first>
<action>

Run from repo root:

```bash
rg "no_clip_embedding" /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend/src --glob "*.tsx" --glob "*.ts" -n
```

```bash
rg "Visual similarity is unavailable" /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend/src --glob "*.tsx" --glob "*.ts"
```

```bash
rg -i "visual similarity" /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend/src --glob "*.tsx" --glob "*.ts" | head -n 50
```

Interpretation:

1. If **every** **`no_clip_embedding`** occurrence lives only in `apps/visualizer/frontend/src/pages/SearchPage.tsx` (no other file in **`rg --files-with-matches`**), **make no TS/TSX edits** for OPS-01.

2. For **any other file** listing **`no_clip_embedding`**: add the same **`Link`** duo as SearchPage — primary **`to={PROCESSING_CATALOG_CACHE_ROUTE}`** with children **`SEARCH_PIN_LINK_CACHE`**, secondary **`to={PROCESSING_JOB_QUEUE_ROUTE}`** with **`PROCESSING_OPEN_JOB_QUEUE`**, helper paragraph **`SEARCH_PIN_HELP_EMBED`**, each link **`className="font-medium text-accent underline"`** (mirror SearchPage imports: **`react-router-dom` **`Link`**, routes from the same module SearchPage uses, strings from **`constants/strings`**).

3. If **`Visual similarity is unavailable`** is matched in **`src`**: attach the same D-02 block next to that error copy.

If **(1)** holds, this task introduces **no diff** besides what **12-02-02** changes.

</action>
<acceptance_criteria>
- `rg --files-with-matches "no_clip_embedding" apps/visualizer/frontend/src -g "*.tsx" -g "*.ts"` outputs only `apps/visualizer/frontend/src/pages/SearchPage.tsx` — OR — any additional files it lists also appear in `rg --files-with-matches "SEARCH_PIN_HELP_EMBED" apps/visualizer/frontend/src -g "*.tsx" -g "*.ts"` output.
- `rg "Visual similarity is unavailable" apps/visualizer/frontend/src -g "*.tsx" -g "*.ts"` exits 1 (no matches in frontend source).

</acceptance_criteria>
</task>

<task id="12-02-02" type="execute">
<title>OPS-03: strings + JobDetailModal filter + Vitest</title>
<read_first>
- `/Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend/src/constants/strings.ts` — **`JOB_DETAILS_EMBED_REASON_*`** ~346–349; **`JOB_DETAILS_EMBED_DIAGNOSTICS_TITLE`**
- `/Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend/src/components/jobs/JobDetailModal.tsx` — **`embedReasonCounts`**, diagnostics card ~299–477
- `/Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend/src/components/jobs/__tests__/JobDetailModal.test.tsx` — grouped embed test ~161–186
- `/Users/ccanales/projects/lightroom-tagger/.planning/phases/12-operational-baseline-embed-reliability/12-UI-SPEC.md` — OPS-03 D-09 hide zeros / **`encode_failed`**
</read_first>
<action>

1. In **`strings.ts`**, add **`export const JOB_SKIP_MISSING_FILE = "Missing file"`**, **`export const JOB_SKIP_EMPTY_PATH = "Empty path"`**, **`export const JOB_SKIP_NO_DB_ROW = "No DB row"`** next to adjacent job/embed copy.

2. Delete **`JOB_DETAILS_EMBED_REASON_NO_ROW`**, **`JOB_DETAILS_EMBED_REASON_EMPTY_PATH`**, **`JOB_DETAILS_EMBED_REASON_UNRESOLVED`**, **`JOB_DETAILS_EMBED_REASON_ENCODE_FAILED`** after confirming **`rg "JOB_DETAILS_EMBED_REASON"`** hits only **`JobDetailModal.tsx`** + test (remove + fix imports).

3. Replace **`EMBED_REASON_LABELS`** in **`JobDetailModal.tsx`** with **three entries only**: **`no_row → JOB_SKIP_NO_DB_ROW`**, **`empty_path → JOB_SKIP_EMPTY_PATH`**, **`unresolved_or_missing → JOB_SKIP_MISSING_FILE`**.

4. In **`embedReasonCounts` useMemo**, after computing rows, **`const visible = rows.filter((r) => r.count > 0)`**; **`return null`** if **`!rawCounts`** **or **`visible.length === 0`**.

5. Render diagnostics card **only when** **`embedReasonCounts`** non-null; **`.map` over **`embedReasonCounts` directly** (already filtered).

6. **Preserve existing row Tailwind classes** from **`12-UI-SPEC` / current grid** (OPS-03): label uses **`text-text-secondary`**; count span uses **`font-mono text-xs`** (match current **`embedReasonCounts`** markup ~466–477 in **`JobDetailModal.tsx`**).

7. Update existing grouped diagnostics test imports + expectations to **`JOB_SKIP_*`** strings (**`Missing file`** row shows **`2`** for **`unresolved_or_missing: 2`**, etc.).

8. Add test **`embed diagnostics hidden when only encode_failed is positive`**: payload **`skip_reason_counts: { no_row: 0, empty_path: 0, unresolved_or_missing: 0, encode_failed: 5 }`** — **`expect(screen.queryByText(JOB_DETAILS_EMBED_DIAGNOSTICS_TITLE)).toBeNull()`**.

9. Add test **`embed diagnostics omits zero buckets`**: **`skip_reason_counts: { no_row: 0, empty_path: 3, unresolved_or_missing: 0 }`** — assert **`JOB_SKIP_EMPTY_PATH`** + **`3`** rendered; **`queryByText(JOB_SKIP_NO_DB_ROW)`** and **`queryByText(JOB_SKIP_MISSING_FILE)`** null.

</action>
<acceptance_criteria>
- **`rg 'JOB_SKIP_MISSING_FILE = "Missing file"' /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend/src/constants/strings.ts`** exits **0**
- Shell **`rg "JOB_DETAILS_EMBED_REASON" /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend/src; test $? -eq 1`** exits **0** (rg finds nothing).
- **`cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend && npx vitest run src/components/jobs/__tests__/JobDetailModal.test.tsx`** exits **0**

</acceptance_criteria>
</task>
