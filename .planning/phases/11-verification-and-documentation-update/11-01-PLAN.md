---
phase: 11
plan: "11-01"
title: "Create VERIFICATION.md for Phases 6–9 (full-depth, Phase 5 template)"
type: docs
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-scoring-pipeline-catalog-ux/06-VERIFICATION.md
  - .planning/phases/07-posting-analytics/07-VERIFICATION.md
  - .planning/phases/08-identity-suggestions/08-VERIFICATION.md
  - .planning/phases/09-insights-dashboard/09-VERIFICATION.md
autonomous: true
requirements:
  - SCORE-01
  - SCORE-03
  - SCORE-04
  - POST-01
  - POST-02
  - POST-03
  - POST-04
  - IDENT-01
  - IDENT-02
  - IDENT-03
  - DASH-01
---

<objective>
Author four new verification documents — one per phase directory — that formally certify Phases 6–9 against `.planning/ROADMAP.md` success criteria, each phase plan’s must-haves (06-01…06-04, 07-01…07-03, 08-01…08-02, 09-01…09-02), and automated test evidence. Each file must mirror the **same structure, YAML frontmatter keys, and section ordering** as `.planning/phases/05-structured-scoring-foundation/05-VERIFICATION.md` (D-01, D-02). For requirements also fixed in Phase 10 (**SCORE-01**, **SCORE-04**, **IDENT-01**, **IDENT-02**, **IDENT-03**), include explicit cross-references to `.planning/phases/10-batch-scoring-fix-and-integration-bugs/10-VERIFICATION.md` (D-04).
</objective>

<tasks>

### Task 1: Author `06-VERIFICATION.md` (Phase 6 — SCORE-01, SCORE-03, SCORE-04)

<read_first>
- .planning/phases/05-structured-scoring-foundation/05-VERIFICATION.md
- .planning/phases/10-batch-scoring-fix-and-integration-bugs/10-VERIFICATION.md
- .planning/ROADMAP.md (section “Phase 6 — Scoring pipeline & catalog score UX” — success criteria numbered list)
- .planning/phases/06-scoring-pipeline-catalog-ux/SUMMARY.md
- .planning/phases/06-scoring-pipeline-catalog-ux/06-01-PLAN.md
- .planning/phases/06-scoring-pipeline-catalog-ux/06-02-PLAN.md
- .planning/phases/06-scoring-pipeline-catalog-ux/06-03-PLAN.md
- .planning/phases/06-scoring-pipeline-catalog-ux/06-04-PLAN.md
</read_first>

<action>
Create **`.planning/phases/06-scoring-pipeline-catalog-ux/06-VERIFICATION.md`** with YAML frontmatter:

```yaml
---
status: passed
phase: "06-scoring-pipeline-catalog-ux"
requirements_verified:
  - SCORE-01
  - SCORE-03
  - SCORE-04
---
```

Use this **fixed section order** (same headings as `05-VERIFICATION.md`): title line `# Phase 06 verification — …`, optional one-line verification date note, **`## Plan frontmatter → requirement coverage`** table mapping **06-01**→SCORE-01+SCORE-03, **06-02**→SCORE-03+SCORE-04, **06-03**→SCORE-01+SCORE-03+SCORE-04, **06-04**→SCORE-04.

**`## Requirement-by-requirement verification`:** Markdown table with columns **ID | Requirement (summary) | Verified in codebase | Evidence**. Minimum evidence anchors (adjust line numbers after `read_file` at execution time):

- **SCORE-01:** `lightroom_tagger/core/scoring_service.py` (`parse_score_response_with_retry`, `make_score_json_llm_fixer`); `apps/visualizer/backend/jobs/handlers.py` (`handle_single_score`, `handle_batch_score`, `JOB_HANDLERS` registration); `apps/visualizer/frontend/src/components/processing/DescriptionsTab.tsx` (`batch_score`); `apps/visualizer/frontend/src/components/catalog/CatalogImageModal.tsx` + `ImageScoresPanel.tsx` (`single_score`, scores display). **Add subsection or table rows** stating Phase 10 verification of non-force `batch_score` selection and tests — cite `10-VERIFICATION.md` Plan 10-01 and `apps/visualizer/backend/tests/test_handlers_batch_score.py`.

- **SCORE-03:** `compute_prompt_version` / persisted `prompt_version`, `model_used` in score rows; `apps/visualizer/backend/api/scores.py`; `ScoresAPI` in `apps/visualizer/frontend/src/services/api.ts`; UI in `ImageScoresPanel.tsx` showing model and prompt version.

- **SCORE-04:** `supersede_previous_current_scores` + history APIs (`list_score_history_for_perspective`); `GET .../history` in `scores.py`; per-perspective history UI; catalog `query_catalog_images` score filter/sort (`score_perspective`, `min_score`, `sort_by_score`).

**`## Phase success criteria (cross-check)`:** Table mapping each numbered bullet from ROADMAP Phase 6 (five bullets) to **Pass** + one evidence sentence each.

**`## Must-have verification by plan`:** Four subsections **### 06-01** … **### 06-04**. Each must-have table lists plan must-haves from the corresponding `06-0X-PLAN.md` `<must_haves>` / task bars (e.g. 06-01: `make_score_json_llm_fixer` + `parse_score_response_with_retry`, `supersede_previous_current_scores` + `insert_image_score`, `JOB_HANDLERS` entries) with **Verified: Yes** and file/function proof.

**`## Automated check results`:** Table of commands and results. **Minimum required rows (exit code 0):**

1. `uv run pytest lightroom_tagger/core/test_scoring_service.py lightroom_tagger/core/test_database_scores.py apps/visualizer/backend/tests/test_scores_api.py apps/visualizer/backend/tests/test_catalog_score_query.py apps/visualizer/backend/tests/test_handlers_batch_score.py apps/visualizer/backend/tests/test_job_checkpoint.py -q`
2. `cd apps/visualizer/frontend && npm run lint`
3. `cd apps/visualizer/frontend && npm run build`

If a command fails at execution time, set `status` in frontmatter to `failed` and document the failure in the Automated check results table (do not invent passing results).

**`## Human verification items`:** Numbered list (≥2) for manual smoke: e.g. enqueue `batch_score` / `single_score`, open catalog modal scores + history, apply catalog score sort/filter.

**`## Conclusion`:** Short paragraph stating Phase 6 meets goals; mention Phase 10 cross-check for SCORE-01/SCORE-04 where relevant.
</action>

<acceptance_criteria>
- `test -f .planning/phases/06-scoring-pipeline-catalog-ux/06-VERIFICATION.md` exits 0.
- `grep -n '^---$' .planning/phases/06-scoring-pipeline-catalog-ux/06-VERIFICATION.md` exits 0 (YAML frontmatter present).
- `grep -n 'requirements_verified:' .planning/phases/06-scoring-pipeline-catalog-ux/06-VERIFICATION.md` exits 0 and file contains lines `SCORE-01`, `SCORE-03`, `SCORE-04` under that key.
- `grep -n '## Plan frontmatter' .planning/phases/06-scoring-pipeline-catalog-ux/06-VERIFICATION.md` exits 0.
- `grep -n '## Requirement-by-requirement verification' .planning/phases/06-scoring-pipeline-catalog-ux/06-VERIFICATION.md` exits 0.
- `grep -n '## Phase success criteria' .planning/phases/06-scoring-pipeline-catalog-ux/06-VERIFICATION.md` exits 0.
- `grep -n '## Must-have verification by plan' .planning/phases/06-scoring-pipeline-catalog-ux/06-VERIFICATION.md` exits 0.
- `grep -n '## Automated check results' .planning/phases/06-scoring-pipeline-catalog-ux/06-VERIFICATION.md` exits 0.
- `grep -n '## Human verification items' .planning/phases/06-scoring-pipeline-catalog-ux/06-VERIFICATION.md` exits 0.
- `grep -n '10-VERIFICATION' .planning/phases/06-scoring-pipeline-catalog-ux/06-VERIFICATION.md` exits 0 (Phase 10 cross-reference for bug-fixed requirements).
- `grep -n 'uv run pytest' .planning/phases/06-scoring-pipeline-catalog-ux/06-VERIFICATION.md` exits 0.
- `grep -n 'npm run lint' .planning/phases/06-scoring-pipeline-catalog-ux/06-VERIFICATION.md` exits 0.
- `grep -n '06-01' .planning/phases/06-scoring-pipeline-catalog-ux/06-VERIFICATION.md` exits 0 and `grep -n '06-04' .planning/phases/06-scoring-pipeline-catalog-ux/06-VERIFICATION.md` exits 0.
</acceptance_criteria>

### Task 2: Author `07-VERIFICATION.md` (Phase 7 — POST-01, POST-02, POST-03, POST-04)

<read_first>
- .planning/phases/05-structured-scoring-foundation/05-VERIFICATION.md
- .planning/ROADMAP.md (section “Phase 7 — Posting analytics” — success criteria)
- .planning/phases/07-posting-analytics/SUMMARY.md
- .planning/phases/07-posting-analytics/07-01-PLAN.md
- .planning/phases/07-posting-analytics/07-02-PLAN.md
- .planning/phases/07-posting-analytics/07-03-PLAN.md
- .planning/phases/07-posting-analytics/07-04-PLAN.md
</read_first>

<action>
Create **`.planning/phases/07-posting-analytics/07-VERIFICATION.md`** with frontmatter:

```yaml
---
status: passed
phase: "07-posting-analytics"
requirements_verified:
  - POST-01
  - POST-02
  - POST-03
  - POST-04
---
```

Use this **fixed section order** (same headings as `05-VERIFICATION.md`): title line `# Phase 07 verification — …`, optional one-line verification date note, then sections in this exact order: **`## Plan frontmatter → requirement coverage`**, **`## Requirement-by-requirement verification`**, **`## Phase success criteria (cross-check)`**, **`## Must-have verification by plan`**, **`## Automated check results`**, **`## Human verification items`**, **`## Conclusion`**.

**Plan coverage table:** **07-01**→POST-01+POST-02+POST-03+POST-04 (backend); **07-02**→POST-01+POST-02+POST-03; **07-03**→POST-04. Note in prose if ROADMAP lists a **07-04** row that was subsumed — evidence lives in 07-01 + 07-03 deliverables.

**Requirement evidence (minimum anchors):**

- **POST-01:** `lightroom_tagger/core/posting_analytics.py` (`get_posting_frequency`); `apps/visualizer/backend/api/analytics.py` (`posting-frequency`); `PostingFrequencyChart.tsx`; `AnalyticsPage.tsx`.
- **POST-02:** `get_posting_time_heatmap`; `posting-heatmap` route; `PostingHeatmap.tsx`; `meta.dow_labels` / UTC disclaimer strings.
- **POST-03:** `get_caption_hashtag_stats`; `caption-stats`; `CaptionHashtagPanel.tsx`.
- **POST-04:** `query_unposted_catalog` / `unposted-catalog` route; `UnpostedCatalogPanel.tsx`; catalog **Not posted** cross-link per SUMMARY (`ImagesPage` / `CatalogTab` — cite actual files).

**Must-have tables:** Pull must-haves from each `07-0X-PLAN.md` (e.g. `validated_at` join semantics, `Blueprint('analytics'`, routes under `/api/analytics`, `NAV_ANALYTICS`, `/analytics` route, `getUnpostedCatalog`, tests in `test_posting_analytics.py` / `test_analytics_api.py`, frontend tests for `UnpostedCatalogPanel`).

**Automated checks (minimum):**

1. `uv run pytest lightroom_tagger/core/test_posting_analytics.py apps/visualizer/backend/tests/test_analytics_api.py -q`
2. `cd apps/visualizer/frontend && npm run lint`
3. `cd apps/visualizer/frontend && npm run build`
4. `cd apps/visualizer/frontend && npm test -- --run` (include only if package.json defines `test`; if command absent, replace with documented `npx vitest run` for the analytics-related test file from SUMMARY and note why).

**Human items:** e.g. open `/analytics`, set date range, confirm frequency + heatmap + captions + not-posted grid; optional `curl` sample for `/api/analytics/posting-frequency` with `meta.timezone_assumption` documented in verification notes.
</action>

<acceptance_criteria>
- `test -f .planning/phases/07-posting-analytics/07-VERIFICATION.md` exits 0.
- `grep -n '^---$' .planning/phases/07-posting-analytics/07-VERIFICATION.md` exits 0 (YAML frontmatter present).
- `grep -n 'requirements_verified:' .planning/phases/07-posting-analytics/07-VERIFICATION.md` exits 0 and file contains `POST-01`, `POST-02`, `POST-03`, `POST-04` under that block.
- `grep -n '## Plan frontmatter' .planning/phases/07-posting-analytics/07-VERIFICATION.md` exits 0.
- `grep -n '## Requirement-by-requirement verification' .planning/phases/07-posting-analytics/07-VERIFICATION.md` exits 0.
- `grep -n '## Phase success criteria' .planning/phases/07-posting-analytics/07-VERIFICATION.md` exits 0.
- `grep -n '## Must-have verification by plan' .planning/phases/07-posting-analytics/07-VERIFICATION.md` exits 0.
- `grep -n '## Automated check results' .planning/phases/07-posting-analytics/07-VERIFICATION.md` exits 0.
- `grep -n '## Human verification items' .planning/phases/07-posting-analytics/07-VERIFICATION.md` exits 0.
- `grep -n '## Conclusion' .planning/phases/07-posting-analytics/07-VERIFICATION.md` exits 0.
- `grep -n 'lightroom_tagger/core/posting_analytics.py' .planning/phases/07-posting-analytics/07-VERIFICATION.md` exits 0.
- `grep -n 'apps/visualizer/backend/api/analytics.py' .planning/phases/07-posting-analytics/07-VERIFICATION.md` exits 0.
- `grep -n 'uv run pytest lightroom_tagger/core/test_posting_analytics.py' .planning/phases/07-posting-analytics/07-VERIFICATION.md` exits 0.
- `grep -n 'npm run lint' .planning/phases/07-posting-analytics/07-VERIFICATION.md` exits 0.
- `grep -n '07-01' .planning/phases/07-posting-analytics/07-VERIFICATION.md` exits 0 and `grep -n '07-03' .planning/phases/07-posting-analytics/07-VERIFICATION.md` exits 0.
</acceptance_criteria>

### Task 3: Author `08-VERIFICATION.md` (Phase 8 — IDENT-01, IDENT-02, IDENT-03)

<read_first>
- .planning/phases/05-structured-scoring-foundation/05-VERIFICATION.md
- .planning/phases/10-batch-scoring-fix-and-integration-bugs/10-VERIFICATION.md
- .planning/ROADMAP.md (section “Phase 8 — Identity & what to post next”)
- .planning/phases/08-identity-suggestions/SUMMARY.md
- .planning/phases/08-identity-suggestions/08-01-PLAN.md
- .planning/phases/08-identity-suggestions/08-02-PLAN.md
- .planning/phases/08-identity-suggestions/08-03-PLAN.md
</read_first>

<action>
Create **`.planning/phases/08-identity-suggestions/08-VERIFICATION.md`** with frontmatter:

```yaml
---
status: passed
phase: "08-identity-suggestions"
requirements_verified:
  - IDENT-01
  - IDENT-02
  - IDENT-03
---
```

Use this **fixed section order** (same headings as `05-VERIFICATION.md`): title line `# Phase 08 verification — …`, optional one-line verification date note, then sections in this exact order: **`## Plan frontmatter → requirement coverage`**, **`## Requirement-by-requirement verification`**, **`## Phase success criteria (cross-check)`**, **`## Must-have verification by plan`**, **`## Automated check results`**, **`## Human verification items`**, **`## Conclusion`**.

**Plan coverage:** **08-01**→IDENT-01+IDENT-02+IDENT-03; **08-02**→same (UI).

**Requirement evidence:**

- **IDENT-01:** `rank_best_photos` in `lightroom_tagger/core/identity_service.py`; `GET /api/identity/best-photos` in `apps/visualizer/backend/api/identity.py`; `BestPhotosGrid.tsx`; tests `lightroom_tagger/core/test_identity_service.py`, `apps/visualizer/backend/tests/test_identity_api.py`. **Cross-reference Phase 10:** `_SCORES_BASE_SQL` includes `s.image_type = 'catalog'` and suggestions `offset`/`total` — cite `10-VERIFICATION.md` and `identity_service.py` / `identity.py` line refs from Phase 10 doc.

- **IDENT-02:** `build_style_fingerprint`; `GET /api/identity/style-fingerprint`; `StyleFingerprintPanel.tsx`.

- **IDENT-03:** `suggest_what_to_post_next`; `GET /api/identity/suggestions`; `PostNextSuggestionsPanel.tsx`; Phase 10 pagination (`offset`, `total`, `PostNextSuggestionsResponse` in `api.ts`).

**Must-haves:** From 08-01/08-02 plans (e.g. blueprint routes `best-photos`, `style-fingerprint`, `suggestions`; `IdentityPage` at `/identity`; coverage meta / empty states; reason chips).

**Automated checks (minimum):**

1. `uv run pytest lightroom_tagger/core/test_identity_service.py apps/visualizer/backend/tests/test_identity_api.py -q`
2. `cd apps/visualizer/frontend && npm run lint`
3. `cd apps/visualizer/frontend && npm run build`
4. Include vitest command for `IdentityPage.test.tsx` if present per SUMMARY (`npx vitest run src/pages/IdentityPage.test.tsx` or project equivalent — verify path with `glob_file_search` at execution).

**Human items:** `/identity` smoke: best photos grid, fingerprint charts, post-next list with **Load more** when `total` > page size (post–Phase 10 behavior).
</action>

<acceptance_criteria>
- `test -f .planning/phases/08-identity-suggestions/08-VERIFICATION.md` exits 0.
- `grep -n '^---$' .planning/phases/08-identity-suggestions/08-VERIFICATION.md` exits 0 (YAML frontmatter present).
- `grep -n 'IDENT-01' .planning/phases/08-identity-suggestions/08-VERIFICATION.md` exits 0.
- `grep -n '10-VERIFICATION' .planning/phases/08-identity-suggestions/08-VERIFICATION.md` exits 0.
- `grep -n '## Plan frontmatter' .planning/phases/08-identity-suggestions/08-VERIFICATION.md` exits 0.
- `grep -n '## Requirement-by-requirement verification' .planning/phases/08-identity-suggestions/08-VERIFICATION.md` exits 0.
- `grep -n '## Phase success criteria' .planning/phases/08-identity-suggestions/08-VERIFICATION.md` exits 0.
- `grep -n '## Must-have verification by plan' .planning/phases/08-identity-suggestions/08-VERIFICATION.md` exits 0.
- `grep -n '## Automated check results' .planning/phases/08-identity-suggestions/08-VERIFICATION.md` exits 0.
- `grep -n '## Human verification items' .planning/phases/08-identity-suggestions/08-VERIFICATION.md` exits 0.
- `grep -n '## Conclusion' .planning/phases/08-identity-suggestions/08-VERIFICATION.md` exits 0.
- `grep -n 'identity_service.py' .planning/phases/08-identity-suggestions/08-VERIFICATION.md` exits 0.
- `grep -n 'apps/visualizer/backend/api/identity.py' .planning/phases/08-identity-suggestions/08-VERIFICATION.md` exits 0.
- `grep -n 'uv run pytest lightroom_tagger/core/test_identity_service.py' .planning/phases/08-identity-suggestions/08-VERIFICATION.md` exits 0.
- `grep -n 'npm run lint' .planning/phases/08-identity-suggestions/08-VERIFICATION.md` exits 0.
- `grep -n '08-01' .planning/phases/08-identity-suggestions/08-VERIFICATION.md` exits 0 and `grep -n '08-02' .planning/phases/08-identity-suggestions/08-VERIFICATION.md` exits 0.
</acceptance_criteria>

### Task 4: Author `09-VERIFICATION.md` (Phase 9 — DASH-01)

<read_first>
- .planning/phases/05-structured-scoring-foundation/05-VERIFICATION.md
- .planning/ROADMAP.md (section “Phase 9 — Insights dashboard” — success criteria)
- .planning/phases/09-insights-dashboard/SUMMARY.md
- .planning/phases/09-insights-dashboard/09-01-PLAN.md
- .planning/phases/09-insights-dashboard/09-02-PLAN.md
- .planning/phases/09-insights-dashboard/09-03-PLAN.md
- .planning/phases/09-insights-dashboard/09-CONTEXT.md (decision **D-52** — default path no monolithic insights API)
</read_first>

<action>
Create **`.planning/phases/09-insights-dashboard/09-VERIFICATION.md`** with frontmatter:

```yaml
---
status: passed
phase: "09-insights-dashboard"
requirements_verified:
  - DASH-01
---
```

Use this **fixed section order** (same headings as `05-VERIFICATION.md`): title line `# Phase 09 verification — …`, optional one-line verification date note, then sections in this exact order: **`## Plan frontmatter → requirement coverage`**, **`## Requirement-by-requirement verification`**, **`## Phase success criteria (cross-check)`**, **`## Must-have verification by plan`**, **`## Automated check results`**, **`## Human verification items`**, **`## Conclusion`**.

**Plan coverage:** **09-01** documents D-52 default (no new Flask routes; composition matrix from SUMMARY — UI blocks → endpoints); **09-02** delivers `DashboardPage.tsx` orchestration + `components/insights/*` + `Layout` **NAV_INSIGHTS**.

**Requirement DASH-01 evidence:** `apps/visualizer/frontend/src/pages/DashboardPage.tsx` (`Promise.allSettled`, imports `SystemAPI.stats`, `IdentityAPI.getStyleFingerprint`, `IdentityAPI.getBestPhotos`, `AnalyticsAPI.getPostingFrequency`, `JobsAPI.list`); files under `apps/visualizer/frontend/src/components/insights/` (`InsightsKpiRow`, `ScoreDistributionChart`, `PerspectiveRadarSummary`, `MiniPostingFrequencyChart`, `TopPhotosStrip`, `InsightsQuickNav` per SUMMARY); route `/` unchanged; nav label Insights in `Layout.tsx` + `NAV_INSIGHTS` in `strings.ts`.

**Must-haves:** From 09-02 plan (per-section errors, empty states, KPI row, score distribution + radar, mini posting chart, top photos strip, quick nav links to `/analytics`, `/identity`, `/processing`).

**Automated checks (minimum):**

1. `cd apps/visualizer/frontend && npm run lint`
2. `cd apps/visualizer/frontend && npm run build`
3. `cd apps/visualizer/frontend && npm test -- --run src/pages/DashboardPage.test.tsx` (or `npx vitest run` with the path verified to exist)

**Backend tests:** Optional row: `uv run pytest apps/visualizer/backend/tests/test_identity_api.py apps/visualizer/backend/tests/test_analytics_api.py apps/visualizer/backend/tests/test_jobs_api.py -q` as regression guard for composed APIs (matches SUMMARY §09-01 verification).

**Human items:** Load `/` — KPIs, charts, top photos strip, quick nav; confirm partial failure still renders other sections (if easy to simulate).
</action>

<acceptance_criteria>
- `test -f .planning/phases/09-insights-dashboard/09-VERIFICATION.md` exits 0.
- `grep -n '^---$' .planning/phases/09-insights-dashboard/09-VERIFICATION.md` exits 0 (YAML frontmatter present).
- `grep -n 'DASH-01' .planning/phases/09-insights-dashboard/09-VERIFICATION.md` exits 0.
- `grep -n 'D-52' .planning/phases/09-insights-dashboard/09-VERIFICATION.md` exits 0.
- `grep -n '## Plan frontmatter' .planning/phases/09-insights-dashboard/09-VERIFICATION.md` exits 0.
- `grep -n '## Requirement-by-requirement verification' .planning/phases/09-insights-dashboard/09-VERIFICATION.md` exits 0.
- `grep -n '## Phase success criteria' .planning/phases/09-insights-dashboard/09-VERIFICATION.md` exits 0.
- `grep -n '## Must-have verification by plan' .planning/phases/09-insights-dashboard/09-VERIFICATION.md` exits 0.
- `grep -n '## Automated check results' .planning/phases/09-insights-dashboard/09-VERIFICATION.md` exits 0.
- `grep -n '## Human verification items' .planning/phases/09-insights-dashboard/09-VERIFICATION.md` exits 0.
- `grep -n '## Conclusion' .planning/phases/09-insights-dashboard/09-VERIFICATION.md` exits 0.
- `grep -n 'DashboardPage.tsx' .planning/phases/09-insights-dashboard/09-VERIFICATION.md` exits 0.
- `grep -n 'components/insights' .planning/phases/09-insights-dashboard/09-VERIFICATION.md` exits 0.
- `grep -n 'npm run lint' .planning/phases/09-insights-dashboard/09-VERIFICATION.md` exits 0.
- `grep -n '09-01' .planning/phases/09-insights-dashboard/09-VERIFICATION.md` exits 0 and `grep -n '09-02' .planning/phases/09-insights-dashboard/09-VERIFICATION.md` exits 0.
</acceptance_criteria>

</tasks>

<verification>
After all four files exist, run from repo root:

1. `test -f .planning/phases/06-scoring-pipeline-catalog-ux/06-VERIFICATION.md && test -f .planning/phases/07-posting-analytics/07-VERIFICATION.md && test -f .planning/phases/08-identity-suggestions/08-VERIFICATION.md && test -f .planning/phases/09-insights-dashboard/09-VERIFICATION.md` — exit 0.

2. `grep -R '^status: passed' .planning/phases/06-scoring-pipeline-catalog-ux/06-VERIFICATION.md .planning/phases/07-posting-analytics/07-VERIFICATION.md .planning/phases/08-identity-suggestions/08-VERIFICATION.md .planning/phases/09-insights-dashboard/09-VERIFICATION.md` — four matches (if any `status: failed`, stop and fix content).

3. Re-run the **Automated check results** commands listed inside each new `*-VERIFICATION.md` (at least pytest + `npm run lint` + `npm run build` where applicable) and ensure exit code 0; if any command fails, update that file’s automated table with actual output and set that phase’s frontmatter `status` to `failed`.
</verification>

<must_haves>
- Four files exist: `06-VERIFICATION.md`, `07-VERIFICATION.md`, `08-VERIFICATION.md`, `09-VERIFICATION.md` under their phase directories.
- Each file matches `05-VERIFICATION.md` section ordering and includes plan coverage, requirement tables, ROADMAP success-criteria cross-check, per-plan must-haves, automated commands, human checklist, conclusion.
- Phase 10–touched requirements (SCORE-01, SCORE-04, IDENT-01–03) cite `.planning/phases/10-batch-scoring-fix-and-integration-bugs/10-VERIFICATION.md` in Phase 6 and/or Phase 8 docs as specified in tasks.
- Automated sections document real command output or honest failure; frontmatter `status` matches pass/fail.
</must_haves>
