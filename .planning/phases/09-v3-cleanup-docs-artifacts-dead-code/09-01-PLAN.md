---
phase: 09-v3-cleanup-docs-artifacts-dead-code
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SIM-02
  - STACK-02
must_haves:
  truths:
    - "Body checkboxes for NLS-02, VIS-01, NLS-06, STACK-04, STACK-05 are [x] and match audit final status"
    - "SIM-02 bullet describes job-driven materialized similarity in CatalogCacheTab (quick 260427-f75 / b6e8885), not on-demand catalog-modal flow"
    - "STACK-02 is removed from active Photo Stacking list and recorded under Out of Scope with 2026-04-24 descope rationale"
    - "Traceability table Status column matches phase verdicts; MATCH-02 row is not flipped to Complete"
    - "Dependencies no longer state STACK-04 requires STACK-02"
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: Synced v3.0 requirements and traceability for gap closure
  key_links:
    - from: SIM-02 bullet
      to: shipped CatalogCacheTab / batch_catalog_similarity pivot
      via: text rewrite per D-02 and gap-closure scope item 2
---

<objective>
Synchronize `.planning/REQUIREMENTS.md` with the as-shipped v3.0 state: flip satisfied checkboxes, rewrite SIM-02 for the materialization pivot, relocate descoped STACK-02, refresh the traceability table (excluding MATCH-02 per Phase 10), and fix the STACK-04 dependency line.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (# Phase 9 — v3.0 cleanup)
@.planning/v3.0-MILESTONE-AUDIT.md (Final status column § Requirement coverage matrix)
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Relocate STACK-02 and fix Dependencies line</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/REQUIREMENTS.md (Photo Stacking, Out of Scope, Dependencies)
    .planning/v3.0-MILESTONE-AUDIT.md (STACK-02 evidence)
    .planning/ROADMAP.md (STACK-02 descoped note)
  </read_first>
  <action>
  1. **Remove** the **STACK-02** bullet from `### Photo Stacking` (currently lines ~25–26):
     Delete the entire line:
     `- [ ] **STACK-02**: Job clusters visually similar but time-separated photos into stacks using pHash similarity`

  2. **Append** under `## Out of Scope (v3.0)` (before the `---` that precedes Traceability — i.e., after voice/embedding bullets, preserve section order):

     `- **STACK-02** *(descoped 2026-04-24)*: pHash clustering for time-separated near-duplicates was dropped from Phase 4 scope; burst-only stacks (**STACK-01**) are sufficient for v3.0.`

  3. **Dependencies §** — **Line ~99 (`STACK-04` rule):**

     **Before:**
     `- **STACK-04** depends on **STACK-01** and **STACK-02** (stacks must exist before matching can use representatives)`

     **After:**
     `- **STACK-04** depends on **STACK-01** (burst stacks and representatives required for representative-only matching; **STACK-02** pHash clustering is descoped for v3.0 — see Out of Scope).`

  Do not remove **STACK-01** wording from other dependency lines elsewhere.
  </action>
  <verify>
    <automated>cd "/Users/ccanales/projects/lightroom-tagger" && rg "depends on STACK-01 and STACK-02" .planning/REQUIREMENTS.md; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `rg "depends on STACK-01 and STACK-02" .planning/REQUIREMENTS.md` returns **exit code 1** (zero matches)
    - `rg "STACK-02" .planning/REQUIREMENTS.md` — active `### Photo Stacking` section no longer contains a `[ ] **STACK-02**` backlog line (STACK-02 only appears under Out of Scope / traceability / Dependencies explanation)
    - `rg "## Out of Scope" -n .planning/REQUIREMENTS.md` followed by reading that region shows new STACK-02 descope bullet present
  </acceptance_criteria>
  <done>STACK-02 relocated; STACK-04 dependency excludes STACK-02 prerequisite chain error.</done>
</task>

<task type="auto">
  <name>Flip body checkboxes and rewrite SIM-02</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/v3.0-MILESTONE-AUDIT.md
    .planning/ROADMAP.md (Phase 9 SIM-02 gap closure)
  </read_first>
  <action>
  **D-02:** Planning-doc edits only — no functional scope beyond ROADMAP Phase 9 § gap closure.

  1. **NLS-02** (under `### Natural Language Search`): Replace `- [ ] **NLS-02**:` with `- [x] **NLS-02**:` preserving the remainder of that line unchanged.

  2. **NLS-06:** Replace `- [ ] **NLS-06**:` with `- [x] **NLS-06**:` preserving the remainder of that line.

  3. **STACK-04 / STACK-05:** On the two bullets for **STACK-04** and **STACK-05** only (`### Photo Stacking`), replace `- [ ]` at line start with `- [x]` (preserve each line’s prose after `**STACK-XX**:`).

  4. **VIS-01:** Replace `- [ ] **VIS-01**:` with `- [x] **VIS-01**:` preserving the remainder of that line.

  5. **SIM-02 — replace the entire markdown line containing `**SIM-02**:`** with exactly one line (use real backticks — not LaTeX):

  `- [x] **SIM-02**: Similarity discovery is delivered via job-driven similarity groups (`batch_catalog_similarity` → materialized groups) surfaced on **Processing → Catalog cache** (including latest similarity-groups preview). On-demand “More like this” from every catalog detail view is **not** the shipped primary path after quick task `260427-f75` (commit `b6e8885`, 2026-04-27). Chat-panel pinned similarity stays covered by **NLS-06**.`

  Remove any superseded trailing italic parenthetical (`*(Phase 6 … pending in Phase 9)*`, etc.) that was on the old SIM-02 line — it must **not** remain as a duplicate line below.

  6. **Do not** change the **MATCH-02** bullet (remains unchecked for Phase 10).
  </action>
  <verify>
    <automated>cd "/Users/ccanales/projects/lightroom-tagger" && rg "^- \[x\] \*\*NLS-02\*\*:" .planning/REQUIREMENTS.md && rg "^- \[x\] \*\*SIM-02\*\*:" .planning/REQUIREMENTS.md && rg "batch_catalog_similarity|Catalog cache|260427-f75|b6e8885" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg '^- \[x\] \*\*NLS-02\*\*:' .planning/REQUIREMENTS.md` exits 0 with exactly one line
    - `rg '^- \[x\] \*\*(NLS-06|VIS-01|SIM-02|STACK-04|STACK-05)\*\*:' .planning/REQUIREMENTS.md` exits 0 with exactly five lines (count: `wc -l` piped equals 5)
    - `rg 'More like this from any catalog photo' .planning/REQUIREMENTS.md` exits 1 (legacy SIM-02 sentence removed)
    - `rg '^- \[ \] \*\*MATCH-02\*\*:' .planning/REQUIREMENTS.md` exits 0 (MATCH-02 still unchecked)
  </acceptance_criteria>
  <done>Body checkboxes and SIM-02 text synced to shipped UX; MATCH-02 backlog unchanged.</done>
</task>

<task type="auto">
  <name>Refresh traceability table statuses</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/REQUIREMENTS.md (## Traceability table)
    .planning/v3.0-MILESTONE-AUDIT.md (Final status column)
  </read_first>
  <action>
  In `## Traceability` table (lines ~72–89), **replace only the `Status` cell** text for these REQ-ID rows:

  | REQ-ID | New Status column (exact string) |
  |--------|-----------------------------------|
  | NLS-02 | `✅ Complete (2026-04-23)` |
  | NLS-06 | `✅ Complete (Phase 7)` |
  | STACK-02 | `Descoped (2026-04-24)` |
  | STACK-04 | `✅ Complete (Phase 7)` |
  | STACK-05 | `✅ Complete (Phase 7)` |
  | VIS-01 | `✅ Complete (2026-04-23)` |

  **SIM-02 row:** Set Status to **`✅ Complete (Phase 6 + Phase 9)`** replacing any `Partial — Phase 9 (...)` wording so doc/dead-code sync is explicit after this phase lands.

  **MATCH-02 row:** Leave as **`Partial — Phase 10 (gap closure: quantitative ≥10× benchmark on user-validated match pairs)`** — do **not** change to Complete (Phase 10 owns quantitative closure).

  Do not add/remove table rows except as needed for spacing; REQ-ID columns stay aligned.
  </action>
  <verify>
    <automated>cd "/Users/ccanales/projects/lightroom-tagger" && rg "\\| STACK-02 \\|" .planning/REQUIREMENTS.md | rg "Descoped \(2026-04-24\)"</automated>
  </verify>
  <acceptance_criteria>
    - Pipe-table line for STACK-02 contains `Descoped (2026-04-24)` in Status column
    - Pipe-table line for NLS-02 contains `✅ Complete (2026-04-23)`
    - Pipe-table line for MATCH-02 contains `Partial — Phase 10` (still partial, not falsely Complete)
    - Pipe-table line for SIM-02 contains `✅ Complete (Phase 6 + Phase 9)` after edit
  </acceptance_criteria>
  <done>Traceability Status column matches audit phase verdicts; MATCH-02 stays Phase 10 partial.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Edits are planning markdown only — no executable code or runtime trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | .planning/REQUIREMENTS.md | accept | Documentation-only edit; factual accuracy verified against audit + commits in acceptance greps |
</threat_model>

<verification>
After all tasks: `rg "MATCH-02" .planning/REQUIREMENTS.md` shows traceability Partial — Phase 10; `rg "depends on STACK-01 and STACK-02" .planning/REQUIREMENTS.md` exits 1.
</verification>

<success_criteria>
From ROADMAP Phase 9: REQUIREMENTS traceability aligns with verifier verdicts without false Pending for audited rows except MATCH-02 pending Phase 10; STACK-02 is under Out of Scope / Descoped status; STACK-04 dependency omits STACK-02 prerequisite wording.
</success_criteria>

<output>
`.planning/phases/09-v3-cleanup-docs-artifacts-dead-code/09-01-SUMMARY.md`
</output>
