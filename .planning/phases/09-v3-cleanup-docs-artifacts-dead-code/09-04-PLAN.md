---
phase: 09-v3-cleanup-docs-artifacts-dead-code
plan: 4
type: execute
wave: 2
depends_on:
  - 1
  - 2
  - 3
files_modified:
  - .planning/phases/09-v3-cleanup-docs-artifacts-dead-code/09-04-SUMMARY.md
autonomous: true
requirements:
  - SIM-02
  - STACK-02
must_haves:
  truths:
    - "Frontend `tsc --noEmit` and `npm run lint` pass after Wave 1 merges"
    - "`pytest` over `apps/visualizer/backend/tests/` exits 0 (full backend test tree; sweeps include `tests/test_images_clip_similar_api.py`, satisfying D-03 unchanged CLIP Similar API coverage)"
    - "`pytest` over `lightroom_tagger/core/` exits 0 (core suite sweep aligned with Phase 6/5 rollup convention)"
    - "Zero hits for orphan-symbol and REQUIREMENTS Dependency greps listed in roadmap § Phase 9"
    - "`09-VERIFICATION.md` walkthrough disposition documented for gsd-verifier via `09-04-SUMMARY.md` (hook reads VERIFICATION.md, not SUMMARY)"
  artifacts: []
  key_links: []
---

<objective>
Run Phase 09 **final verification** after Wave-1 plans `09-01`–`09-03`: frontend `tsc` + ESLint (`npm run lint`), **`pytest` sweeps** for `apps/visualizer/backend/tests/` **and** `lightroom_tagger/core/` per ROADMAP success criterion “backend pytest sweep passes”. Record pass counts / tail output lines in **`09-04-SUMMARY.md`**, plus **walkthrough disposition** instructions for **`09-VERIFICATION.md`** (see action step 7).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 9 § **Success criteria** — lint + rg + backend pytest sweep)
@apps/visualizer/frontend/package.json (`lint` script)
@pyproject.toml (pytest declared as dev dependency)
</context>

<tasks>

<task type="auto">
  <name>Wave-2 rollup: tsc, lint, rg, dual pytest sweeps, SUMMARY + verifier handoff</name>
  <files>.planning/phases/09-v3-cleanup-docs-artifacts-dead-code/09-04-SUMMARY.md</files>
  <read_first>
    `.planning/ROADMAP.md` (Phase 9 § success criteria items 7 and grep bullets)
    apps/visualizer/frontend/package.json
  </read_first>
  <action>
  From repo root `/Users/ccanales/projects/lightroom-tagger`:

  1. **Typecheck:**  
     `cd apps/visualizer/frontend && npx tsc --noEmit`

  2. **Lint:**  
     `cd apps/visualizer/frontend && npm run lint`

  3. **Orphan-symbol + dependency greps:**

     ```
     rg "getCatalogSimilar" apps/visualizer/frontend/src
     rg "CATALOG_SIMILAR_MORE_LIKE_THIS" apps/visualizer/frontend/src
     rg "CATALOG_SIMILAR_" apps/visualizer/frontend/src
     rg "depends on STACK-01 and STACK-02" .planning/REQUIREMENTS.md
     ```

     **Expected:** exit code **1** (zero matches) for every frontend `rg`; exit **1** for REQUIREMENTS dependency grep (STACK-02 prerequisite removed post 09-01).

  4. **Backend pytest sweep** (15+ modules under `apps/visualizer/backend/tests/`, includes `tests/test_images_clip_similar_api.py` → D-03 unchanged API tests still exercised):

     ```bash
     cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend && PYTHONPATH=.:../.. .venv/bin/python -m pytest tests/ -q --tb=short 2>&1 | tail -30
     ```

     For **PASS/FAIL judgment** (acceptance gates), rerun **without** the `| tail` pipe so **`pytest`** exit propagates (`tail` masks failure exit codes).

     ```bash
     cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend && PYTHONPATH=.:../.. .venv/bin/python -m pytest tests/ -q --tb=short
     ```

  5. **Core pytest sweep:**

     ```bash
     cd /Users/ccanales/projects/lightroom-tagger && .venv/bin/python -m pytest lightroom_tagger/core/ -q --tb=short 2>&1 | tail -30
     ```

     Gate run **without** `tail`:

     ```bash
     cd /Users/ccanales/projects/lightroom-tagger && .venv/bin/python -m pytest lightroom_tagger/core/ -q --tb=short
     ```

  6. **Write** `.planning/phases/09-v3-cleanup-docs-artifacts-dead-code/09-04-SUMMARY.md` including:
     - Outcome line for frontend (`tsc` / lint).
     - **Last summary line** from each pytest run (paste the `N passed …` footer or bounded tail) so auditors see counts (e.g. `245 passed`, `67 passed`) — aligns with roadmap “no new regressions” evidence.
     - If either sweep fails due to known pre-existing flakes, note exact test names + prior SHA in SUMMARY (**only** exceptions allowed per acceptance wording).

  7. **Phase-level walkthrough disposition (informational chain for gsd-verifier):** Append a prose subsection **`### Walkthrough disposition (Phase 09 → 09-VERIFICATION.md)`** documenting that **`phase-exit-walkthrough`** watches **`*-VERIFICATION.md`**, **not** `*-SUMMARY.md`. instruct **gsd-verifier** (or human closer) to set **`walkthrough_exempt: true`** on the eventual **`09-VERIFICATION.md`** when stamped `status: passed`, with consolidated rationale bullets:
     - **09-01:** pure `.planning/` doc edits — **zero** changes under `apps/visualizer/frontend/**`
     - **09-02:** edits only `.planning/phases/*/…-VERIFICATION.md` stubs — zero frontend repo paths
     - **09-03:** frontend **orphan-deletion** only (`api.ts`, `strings.ts`) — no UI surface added or changed; **`tsc` + `rg`** cover observable contracts (**D-01** delete path)
     - **09-04:** commands-only rollup — zero production code edits this plan

     This satisfies INFO guidance: SUMMARY is the breadcrumb trail; **`09-VERIFICATION.md`** is where the exemption must live when the verifier writes it.

  **Do not** add `walkthrough_exempt` to SUMMARY YAML expecting the hook to honor it (documented explicitly in subsection above).
  </action>
  <verify>
    <automated>cd "/Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend" && npx tsc --noEmit && npm run lint && cd "/Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend" && PYTHONPATH=.:../.. .venv/bin/python -m pytest tests/ -q --tb=short && cd "/Users/ccanales/projects/lightroom-tagger" && .venv/bin/python -m pytest lightroom_tagger/core/ -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/visualizer/frontend && npx tsc --noEmit` exits **0** (from repo root or absolute cwd equivalent)
    - `cd apps/visualizer/frontend && npm run lint` exits **0**
    - `rg getCatalogSimilar apps/visualizer/frontend/src` exits **1**
    - `rg CATALOG_SIMILAR_MORE_LIKE_THIS apps/visualizer/frontend/src` exits **1**
    - `rg "depends on STACK-01 and STACK-02" .planning/REQUIREMENTS.md` exits **1**
    - Backend sweep **`cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/backend && PYTHONPATH=.:../.. .venv/bin/python -m pytest tests/ -q --tb=short`** (no `tail`) exits **0** — or failure explicitly documented as pre-existing flake in `09-04-SUMMARY.md` only
    - Core sweep **`cd /Users/ccanales/projects/lightroom-tagger && .venv/bin/python -m pytest lightroom_tagger/core/ -q --tb=short`** exits **0** — or same documented-exception rule
    - `test -f .planning/phases/09-v3-cleanup-docs-artifacts-dead-code/09-04-SUMMARY.md` exits **0`; file contains pasted pytest summary counts (substring `passed`) and **`### Walkthrough disposition`** heading with verifier instruction for **`09-VERIFICATION.md`**
  </acceptance_criteria>
  <done>ROADMAP Phase 9 rollup + dual pytest sweeps + verifier hand-off text committed in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI / local commands | Read-only verification — no attack surface besides dev tooling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Denial of Service | Extended pytest suites | accept | Matches milestone verification precedent; scoped to regression detection |
</threat_model>

<verification>
`09-VERIFICATION.md` walkthrough exemptions are **applied when gsd-verifier authors that file** — Plan 09-04 **`09-04-SUMMARY.md`** subsection records the evidence chain (`phase-exit-walkthrough` ignores SUMMARY edits).
All `<acceptance_criteria>` exits as specified; nondeterministic flakes require documented waiver in **`09-04-SUMMARY.md`** only.
</verification>

<success_criteria>
ROADMAP Phase 9 criterion 7: frontend `tsc --noEmit` + backend pytest sweeps (backend `tests/` + `lightroom_tagger/core/`) green with no unexplained regressions; grep bullets hold; verifier hand-off recorded.
</success_criteria>

<output>
`.planning/phases/09-v3-cleanup-docs-artifacts-dead-code/09-04-SUMMARY.md` **created/overwritten by this plan** — must include verifier-facing walkthrough disposition for **`09-VERIFICATION.md`**, not ineffective SUMMARY frontmatter flags.
</output>
