---
phase: 09-v3-cleanup-docs-artifacts-dead-code
plan: 4
type: execute
wave: 2
depends_on:
  - 1
  - 2
  - 3
files_modified: []
autonomous: true
requirements:
  - SIM-02
  - STACK-02
must_haves:
  truths:
    - "Frontend lint + tsc and targeted backend pytest for catalog/similar succeed"
    - "Zero matches for forbidden symbols in frontend srcgrep checks from ROADMAP § Phase 9"
    - "REQUIREMENTS Dependencies line has no STACK-02 prerequisite for STACK-04 (assertion via rg)"
  artifacts: []
  key_links: []
---

<objective>
Run the Phase 09 **final verification sweep** after Wave-1 plans `09-01`, `09-02`, and `09-03` land: frontend `tsc` + ESLint (`npm run lint`), targeted backend pytest for the untouched CLIP similar API, and roadmap grep checks documenting zero orphan symbols / dependency regressions.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 9 success criteria)
@apps/visualizer/frontend/package.json (`lint`, `build` scripts)
</context>

<tasks>

<task type="auto">
  <name>Wave-2 rollup: tsc, lint, rg, pytest</name>
  <files></files>
  <read_first>
    `.planning/ROADMAP.md` (§ Phase 9 success criteria)
    apps/visualizer/frontend/package.json
    apps/visualizer/backend/test_images_clip_similar_api.py (file path existence)
  </read_first>
  <action>
  From repo root `/Users/ccanales/projects/lightroom-tagger`:

  1. **Typecheck:**  
     `cd apps/visualizer/frontend && npx tsc --noEmit`

  2. **Lint (script exists — `npm run lint` in package.json):**  
     `cd apps/visualizer/frontend && npm run lint`

  3. **Orphan-symbol sweep (exact patterns from roadmap / gap sweep):**

     ```
     rg "getCatalogSimilar" apps/visualizer/frontend/src
     rg "CATALOG_SIMILAR_MORE_LIKE_THIS" apps/visualizer/frontend/src
     rg "CATALOG_SIMILAR_" apps/visualizer/frontend/src
     rg "depends on STACK-01 and STACK-02" .planning/REQUIREMENTS.md
     ```

     **Expected:** exit code **1** (zero matches) for every `apps/visualizer/frontend/src` rg; exit code **1** for STACK-dependency grep on REQUIREMENTS (line must be gone post 09-01).

  4. **Backend route smoke (unchanged endpoint per D-03):**

     ```
     cd /Users/ccanales/projects/lightroom-tagger
     python -m pytest apps/visualizer/backend/test_images_clip_similar_api.py -q --tb=short
     ```

  5. **SUMMARY hygiene:** Writes `09-04-SUMMARY.md`; set **`walkthrough_exempt: true`** if no additional FE edits occurred in Wave 2 (verification-only sweep) — rationale: “Verification rollup only — no frontend code changes this plan”.
  </action>
  <verify>
    <automated>cd "/Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend" && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/visualizer/frontend && npx tsc --noEmit` exits **0**
    - `cd apps/visualizer/frontend && npm run lint` exits **0**
    - `rg getCatalogSimilar apps/visualizer/frontend/src` exits **1**
    - `rg CATALOG_SIMILAR_MORE_LIKE_THIS apps/visualizer/frontend/src` exits **1**
    - `rg "depends on STACK-01 and STACK-02" .planning/REQUIREMENTS.md` exits **1** (run from repo root)
    - `python -m pytest apps/visualizer/backend/test_images_clip_similar_api.py -q --tb=short` exits **0** (warnings acceptable if pre-existing — record in SUMMARY only if surfaced)
  </acceptance_criteria>
  <done>Phase 9 automated gates green; backend similar-route tests still pass without FE client.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI / local commands | Read-only verification — no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Denial of Service | Pytest / eslint run | accept | Local dev checks only; no production deploy |
</threat_model>

<verification>
All `<acceptance_criteria>` commands exit with codes specified; failures block phase close.
</verification>

<success_criteria>
ROADMAP Phase 9 items 7 + success-criteria block: `tsc`, `lint`, targeted pytest, zero-hit `rg` sweep, REQUIREMENTS dependency grep clean.
</success_criteria>

<output>
`.planning/phases/09-v3-cleanup-docs-artifacts-dead-code/09-04-SUMMARY.md` with `walkthrough_exempt: true` when verification-only.
</output>

