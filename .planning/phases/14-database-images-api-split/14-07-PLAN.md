---
plan: 14-07
wave: 7
depends_on: [14-06]
phase: 14
files_modified:
  - lightroom_tagger/core/database/_legacy.py
  - lightroom_tagger/core/database/__init__.py
  - apps/visualizer/backend/api/images/_legacy.py
  - apps/visualizer/backend/tests/
  - lightroom_tagger/core/
autonomous: true
requirements_addressed: [REFACTOR-02, REFACTOR-03]
---

```xml
<objective>
Final cleanup and parity check (D-12 final): ensure **no** transitional `_legacy.py` files remain for **`database/`** or **`api/images/`** (delete files if still present); ensure **no** `from ._legacy import` in `lightroom_tagger/core/database/__init__.py`. Run **full** Python test suite from repo root and **frontend** typecheck. Smoke-import critical modules: `lightroom_tagger.core.database`, `api.images` package from backend cwd. Document any deliberate exceptions in commit message (should be none).
</objective>

<threat_model>
- Orphan `_legacy.py` files confuse future edits.
- Leftover barrel imports from `_legacy` break once file removed.
Mitigations: grep gates + full pytest + import smoke.
</threat_model>

<tasks>
  <task id="14-07-T01">
    <title>Remove any remaining `_legacy.py` shims under database and api/images</title>
    <read_first>
lightroom_tagger/core/database/__init__.py
apps/visualizer/backend/api/images/__init__.py
.planning/phases/14-database-images-api-split/14-CONTEXT.md (D-12)
    </read_first>
    <action>
1. If `lightroom_tagger/core/database/_legacy.py` exists: **delete** it; ensure `database/__init__.py` imports **only** from concrete submodules (`.db_init`, `.catalog`, …).

2. If `apps/visualizer/backend/api/images/_legacy.py` exists: **delete** it after confirming no imports reference it.

3. Repo-wide `rg "_legacy" lightroom_tagger/core/database` should only hit comments if any (prefer zero matches).
    </action>
    <acceptance_criteria>
`test ! -f lightroom_tagger/core/database/_legacy.py` exits 0.

`test ! -f apps/visualizer/backend/api/images/_legacy.py` exits 0.

`rg '_legacy' lightroom_tagger/core/database/__init__.py | wc -l` outputs `0`.
    </acceptance_criteria>
  </task>

  <task id="14-07-T02">
    <title>Symbol count parity for database package</title>
    <read_first>
lightroom_tagger/core/database/__init__.py
    </read_first>
    <action>
Sum top-level `def`/`class` across `lightroom_tagger/core/database/*.py` **excluding** `__init__.py`:

`wc -l` trick: use  
`grep -Eh '^(def |class )' lightroom_tagger/core/database/*.py | grep -v __init__ | wc -l` is brittle — instead:

`for f in lightroom_tagger/core/database/*.py; do [[ $f == *__init__* ]] && continue; grep -Ec '^(def |class )' "$f"; done`

**Expected total:** `124` (matches plan 14-01 baseline).

If mismatch, restore missing defs — **do not** ship parity drift.
    </action>
    <acceptance_criteria>
One-liner (bash) summing `grep -Ec '^(def |class )'` over `db_init.py catalog.py instagram.py matches.py descriptions.py scores.py stacks.py embeddings.py similarity.py vision_cache.py` prints **`124`**.
    </acceptance_criteria>
  </task>

  <task id="14-07-T03">
    <title>Full pytest + backend import smoke + frontend `tsc`</title>
    <read_first>
.cursor/rules/backend-restart.mdc
    </read_first>
    <action>
1. Run **all** backend tests: `cd apps/visualizer/backend && pytest -q`

2. Run **core** library tests: `pytest lightroom_tagger/ -q` from repo root (or combine — prefer **one** full-suite command if CI uses it; execute both if project historically uses split).

3. Import smoke (repo root):
   `python -c "from lightroom_tagger.core.database import init_database, query_catalog_images, StackMutationError"`

4. Import smoke (backend cwd):  
   `cd apps/visualizer/backend && python -c "from api.images import catalog_bp, search_bp"`

5. Frontend: `cd apps/visualizer/frontend && npx tsc --noEmit`
    </action>
    <acceptance_criteria>
`cd apps/visualizer/backend && pytest -q` exits 0.

`pytest lightroom_tagger/ -q` exits 0.

`cd apps/visualizer/frontend && npx tsc --noEmit` exits 0.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- No `_legacy.py` under the two packages.
- Database submodule `def`/`class` count totals **124**.
- Full pytest + `tsc` clean.
</verification>

<must_haves>
- Phase 14 goal: **zero behavior change** aside from intentional D-09 URL routing (already in 14-06).
- Import paths stable for downstream code (`lightroom_tagger.core.database` barrel; `api.images` blueprints).
- No stray monolith files: `test ! -f lightroom_tagger/core/database.py`, `test ! -f apps/visualizer/backend/api/images.py`.
</must_haves>
```
