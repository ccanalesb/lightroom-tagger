---
plan: 14-05
wave: 5
depends_on: [14-04]
phase: 14
files_modified:
  - apps/visualizer/backend/api/images.py
  - apps/visualizer/backend/api/images/_legacy.py
  - apps/visualizer/backend/api/images/__init__.py
  - apps/visualizer/backend/api/images/common.py
  - apps/visualizer/backend/api/images/catalog.py
  - apps/visualizer/backend/api/images/stacks.py
  - apps/visualizer/backend/api/images/instagram.py
  - apps/visualizer/backend/api/images/matches.py
  - apps/visualizer/backend/api/images/search.py
  - apps/visualizer/backend/api/identity.py
  - apps/visualizer/backend/api/analytics.py
autonomous: true
requirements_addressed: [REFACTOR-03]
---

```xml
<objective>
REFACTOR-03 scaffold + route migration (D-08/D-10/D-11): replace flat `apps/visualizer/backend/api/images.py` with package `apps/visualizer/backend/api/images/`. **Scaffold commit:** rename monolith → `api/images/_legacy.py`, add six modules (`common.py` + five route modules) with stubs, and `__init__.py` that composes a single **umbrella** `bp = Blueprint("images", __name__)` via `bp.register_blueprint(<child_bp>)` so **`app.py` keeps `app.register_blueprint(images.bp, url_prefix='/api/images')` unchanged** and **all effective HTTP paths stay identical** to pre-phase-14 (zero URL churn until plan 14-06). Then migrate family-by-family off `_legacy.py` into submodules; update `identity.py` + `analytics.py` to `from api.images.common import _clamp_pagination` once `common.py` exists. After final task `_legacy.py` must be empty of routes/functions. **`with_db` stays in `utils.db`** (import in each route module).
</objective>

<threat_model>
- **`api.images` import path:** tests use `import api.images`; package `__init__.py` must expose `bp` and any symbols tests patch.
- **Circular imports:** `common.py` must not import `catalog_bp` / route modules; route modules may import `common`.
Mitigations: migrate helpers to `common.py` first after scaffold; use lazy imports inside view funcs if a cycle appears.
</threat_model>

<tasks>
  <task id="14-05-T00">
    <title>Baseline route and helper count on monolith</title>
    <read_first>
apps/visualizer/backend/api/images.py
    </read_first>
    <action>
From repo root:
- `grep -c "@bp.route" apps/visualizer/backend/api/images.py` — record integer **R** (expected `20` per 14-RESEARCH).
- `grep -c '^def _' apps/visualizer/backend/api/images.py` — private helper count for split tracking.

Store **R** in commit message for scaffold.
    </action>
    <acceptance_criteria>
`grep -c "@bp.route" apps/visualizer/backend/api/images.py` outputs `20`.
    </acceptance_criteria>
  </task>

  <task id="14-05-T01">
    <title>Scaffold `api/images/` package + `_legacy.py` + umbrella `bp` composition</title>
    <read_first>
apps/visualizer/backend/api/images.py
apps/visualizer/backend/app.py (lines 119–132 only)
.planning/phases/14-database-images-api-split/14-PATTERNS.md (§3 images package)
.planning/phases/13-handlers-split-per-job-family/13-PATTERNS.md (§6 cannot coexist file+package)
    </read_first>
    <action>
1. Create directory `apps/visualizer/backend/api/images/`.

2. **Move** `apps/visualizer/backend/api/images.py` → `apps/visualizer/backend/api/images/_legacy.py` (file `api/images.py` must not remain).

3. Add placeholder modules with docstring-only stubs: `common.py`, `catalog.py`, `stacks.py`, `instagram.py`, `matches.py`, `search.py`.

4. Implement `apps/visualizer/backend/api/images/__init__.py`:
   - Import umbrella pattern for **scaffold only**: `from ._legacy import bp` **or** build `bp` from `_legacy` — **must** preserve `images.bp` symbol for `app.py`.
   - Export `bp` as today: `from api import images` / `images.bp` works.

5. **Do not** change `app.py` in this task (still single `register_blueprint(images.bp, url_prefix='/api/images')`).

6. No changes to route handlers yet — entire Flask surface remains in `_legacy.py`.

Smoke: `cd apps/visualizer/backend && python -c "from api.images import bp; print(bp.name)"` prints `images`.
    </action>
    <acceptance_criteria>
`test ! -f apps/visualizer/backend/api/images.py` exits 0.

`test -f apps/visualizer/backend/api/images/_legacy.py` exits 0.

`grep -c "@bp.route" apps/visualizer/backend/api/images/_legacy.py` outputs `20`.

`cd apps/visualizer/backend && python -c "from api.images import bp"` exits 0.

`cd apps/visualizer/backend && pytest tests/test_images_catalog_api.py -q` exits 0 (representative backend images test).
    </acceptance_criteria>
  </task>

  <task id="14-05-T02">
    <title>Populate `common.py` (D-08 cross-cutting helpers) + rewire identity/analytics imports</title>
    <read_first>
apps/visualizer/backend/api/images/_legacy.py
apps/visualizer/backend/api/images/common.py
apps/visualizer/backend/api/identity.py
apps/visualizer/backend/api/analytics.py
.planning/phases/14-database-images-api-split/14-CONTEXT.md (D-08 common.py list)
    </read_first>
    <action>
Move these **exact** function definitions from `_legacy.py` into `common.py`:

1. `_clamp_pagination`
2. `_canonical_path`
3. `_parent_dir_if_exists`
4. `_is_path_under_allowed_roots`
5. `_instagram_thumbnail_roots`
6. `_catalog_thumbnail_roots`
7. `_filter_by_date`
8. `_extract_source_folder`

Add module docstring + required stdlib/third-party imports.

Update **`identity.py`** line importing `_clamp_pagination` to:  
`from api.images.common import _clamp_pagination`

Update **`analytics.py`** similarly.

In `_legacy.py`, **replace** moved bodies with imports from `api.images.common` (temporary shim) **or** delete defs and import used names at top of `_legacy` — no duplicate logic.
    </action>
    <acceptance_criteria>
`grep -c "def _clamp_pagination" apps/visualizer/backend/api/images/common.py` outputs `1`.

`identity.py` contains the exact substring `from api.images.common import _clamp_pagination`.

`analytics.py` contains the exact substring `from api.images.common import _clamp_pagination`.

`grep -c "def _clamp_pagination" apps/visualizer/backend/api/images/_legacy.py` outputs `0`.

`cd apps/visualizer/backend && pytest tests/test_images_catalog_api.py tests/test_identity_api.py tests/test_analytics_api.py -q` exits 0.
    </acceptance_criteria>
  </task>

  <task id="14-05-T03">
    <title>Migrate **catalog** routes + catalog-local helpers into `catalog.py`</title>
    <read_first>
apps/visualizer/backend/api/images/_legacy.py
apps/visualizer/backend/api/images/catalog.py
apps/visualizer/backend/api/images/__init__.py
lightroom_tagger/core/database/__init__.py (barrel — unchanged imports)
utils/db.py
.planning/phases/14-database-images-api-split/14-CONTEXT.md (D-08 catalog, D-10 catalog helpers)
    </read_first>
    <action>
Create `catalog_bp = Blueprint("images_catalog", __name__)` in `catalog.py`.

Move these **helpers** from `_legacy.py` into `catalog.py` (D-10):

1. `_clip_similarity_why_matched_line`
2. `_query_catalog_rows_for_stack_member_keys`
3. `_parse_clip_similar_catalog_params`
4. `_effective_catalog_nl_kwargs`
5. `_rows_to_catalog_api_images`
6. `_build_catalog_detail`

Move these **routes** from `_legacy.py` onto `catalog_bp` (preserve method lists):

1. `GET /catalog/<path:image_key>/thumbnail` → function `get_catalog_thumbnail`
2. `GET /catalog/months` → `get_catalog_months`
3. `GET /catalog` → `list_catalog_images`
4. `GET /catalog/<path:image_key>/similar` → `get_catalog_image_similar`
5. `GET /catalog-similarity-groups` → `list_catalog_similarity_groups`
6. `GET /<string:image_type>/<path:image_key>` → `get_image_detail` **only if** implementation stays catalog-owned per D-08 (keep dispatcher that calls into instagram module functions for IG branch — use **`from api.images.instagram import _build_instagram_detail`** style import inside function if needed to avoid cycles)

Update `api/images/__init__.py` to `bp.register_blueprint(catalog_bp)` (nested blueprint). Remove migrated symbols from `_legacy.py`.

Run: `cd apps/visualizer/backend && pytest tests/test_images_catalog_api.py tests/test_images_detail_api.py tests/test_images_clip_similar_api.py -q`
    </action>
    <acceptance_criteria>
`grep -c "@catalog_bp.route" apps/visualizer/backend/api/images/catalog.py` is `>= 6`.

`grep -c "def _rows_to_catalog_api_images" apps/visualizer/backend/api/images/catalog.py` outputs `1`.

`grep -c "@bp.route(\"/catalog\"" apps/visualizer/backend/api/images/_legacy.py` outputs `0`.

`cd apps/visualizer/backend && pytest tests/test_images_catalog_api.py tests/test_images_detail_api.py tests/test_images_clip_similar_api.py -q` exits 0.
    </acceptance_criteria>
  </task>

  <task id="14-05-T04">
    <title>Migrate **stacks** into `stacks.py`</title>
    <read_first>
apps/visualizer/backend/api/images/_legacy.py
apps/visualizer/backend/api/images/stacks.py
apps/visualizer/backend/api/images/__init__.py
    </read_first>
    <action>
Define `stacks_bp = Blueprint("images_stacks", __name__)`.

Move all `@bp.route("/stacks/` …)` handlers from `_legacy.py` to `stacks_bp` keeping paths:

- `/stacks/<int:stack_id>/members` GET
- `/stacks/<int:stack_id>/split-member` POST
- `/stacks/<int:target_stack_id>/merge` POST
- `/stacks/<int:stack_id>/representative` POST

Register: `bp.register_blueprint(stacks_bp)` in `__init__.py`. Strip moved code from `_legacy.py`.

Run: `cd apps/visualizer/backend && pytest tests/test_images_stacks_api.py -q`
    </action>
    <acceptance_criteria>
`grep -c "@stacks_bp.route" apps/visualizer/backend/api/images/stacks.py` outputs `4`.

`grep -c "/stacks/" apps/visualizer/backend/api/images/_legacy.py` outputs `0`.

`cd apps/visualizer/backend && pytest tests/test_images_stacks_api.py -q` exits 0.
    </acceptance_criteria>
  </task>

  <task id="14-05-T05">
    <title>Migrate **instagram** into `instagram.py`</title>
    <read_first>
apps/visualizer/backend/api/images/_legacy.py
apps/visualizer/backend/api/images/instagram.py
apps/visualizer/backend/api/images/__init__.py
.planning/phases/14-database-images-api-split/14-CONTEXT.md (D-10 instagram)
    </read_first>
    <action>
Define `instagram_bp = Blueprint("images_instagram", __name__)`.

Move from `_legacy.py`:

**Helpers (D-10):** `_deserialize_description`, `_enrich_instagram_media`, `_build_instagram_detail`

**Routes:**
- `GET /instagram`
- `GET /instagram/months`
- `GET /instagram/<path:image_key>/thumbnail`
- `GET /dump-media`

Register `instagram_bp` on umbrella `bp`. Remove from `_legacy.py`.

Run: `cd apps/visualizer/backend && pytest tests/test_images_catalog_api.py` (skip if no IG-only file — use **`pytest tests/` matching `instagram` if present**); minimum: `cd apps/visualizer/backend && pytest -q` passes full backend suite if faster to run whole suite once at end — for this task run **at least** `pytest tests/test_images_detail_api.py` (detail uses IG path).
    </action>
    <acceptance_criteria>
`grep -c "@instagram_bp.route" apps/visualizer/backend/api/images/instagram.py` outputs `4`.

`grep -c "def _build_instagram_detail" apps/visualizer/backend/api/images/instagram.py` outputs `1`.

`grep -c "/instagram" apps/visualizer/backend/api/images/_legacy.py` outputs `0`.

`cd apps/visualizer/backend && pytest tests/test_images_detail_api.py -q` exits 0.
    </acceptance_criteria>
  </task>

  <task id="14-05-T06">
    <title>Migrate **matches** into `matches.py`</title>
    <read_first>
apps/visualizer/backend/api/images/_legacy.py
apps/visualizer/backend/api/images/matches.py
apps/visualizer/backend/api/images/__init__.py
    </read_first>
    <action>
Define `matches_bp = Blueprint("images_matches", __name__)`.

Move routes:

- `GET /matches`
- `PATCH /matches/<path:catalog_key>/<path:insta_key>/validate`
- `PATCH /matches/<path:catalog_key>/<path:insta_key>/reject`

Register on `bp`. Remove from `_legacy.py`.

Run: `cd apps/visualizer/backend && pytest tests/test_match_groups.py tests/test_match_validation.py -q`
    </action>
    <acceptance_criteria>
`grep -c "@matches_bp.route" apps/visualizer/backend/api/images/matches.py` outputs `3`.

`grep -c "/matches" apps/visualizer/backend/api/images/_legacy.py` outputs `0`.

`cd apps/visualizer/backend && pytest tests/test_match_groups.py tests/test_match_validation.py -q` exits 0.
    </acceptance_criteria>
  </task>

  <task id="14-05-T07">
    <title>Migrate **search** into `search.py` + delete remaining `_legacy.py` bodies</title>
    <read_first>
apps/visualizer/backend/api/images/_legacy.py
apps/visualizer/backend/api/images/search.py
apps/visualizer/backend/api/images/__init__.py
.planning/phases/14-database-images-api-split/14-CONTEXT.md (D-10 search helpers)
    </read_first>
    <action>
Define `search_bp = Blueprint("images_search", __name__)`.

Move helpers: `_merge_chat_search_metadata`, `_chat_pin_context`, `_extract_images_from_tool_messages`

Move routes:

- `POST /nl-search` → `nl_search_images`
- `POST /semantic-search` → `semantic_search_images`
- `POST /chat-search` → `chat_search_images`

Register `search_bp` on `bp`.

Ensure `_legacy.py` contains **no** remaining route defs, helpers, or `Blueprint` — file may be deleted or reduced to blank; prefer **delete** `_legacy.py` in this commit if imports allow.

Update `__init__.py` per D-11 to export: `bp`, `catalog_bp`, `stacks_bp`, `instagram_bp`, `matches_bp`, `search_bp` (and `common` helpers if tests import `api.images` attributes — optional).

Run full backend: `cd apps/visualizer/backend && pytest -q`
    </action>
    <acceptance_criteria>
`grep -c "@search_bp.route" apps/visualizer/backend/api/images/search.py` outputs `3`.

`test -f apps/visualizer/backend/api/images/_legacy.py` exits `1` **or** `grep -Ec '^(def |class )' apps/visualizer/backend/api/images/_legacy.py` outputs `0`.

`api/images/__init__.py` contains the substring `catalog_bp` and the substring `search_bp`.

`cd apps/visualizer/backend && pytest -q` exits 0.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- Single umbrella `images.bp` still works with existing `app.py` registration during this plan.
- All 20 routes moved off any monolith residue; nested blueprint registration preserves URLs.
- Identity + analytics imports hit `api.images.common`.
- Full backend pytest passes at end.
</verification>

<must_haves>
- D-08/D-10 placement: helpers appear exactly once in the designated module.
- D-11: `__init__.py` exports five Blueprint objects by name (`catalog_bp`, `stacks_bp`, `instagram_bp`, `matches_bp`, `search_bp`) plus umbrella `bp`.
- No URL changes vs pre-split baseline (Defer D-09 to plan **14-06**).
</must_haves>
```
