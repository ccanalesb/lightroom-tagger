---
plan: 14-01
wave: 1
depends_on: []
phase: 14
files_modified:
  - lightroom_tagger/core/database.py
  - lightroom_tagger/core/database/__init__.py
  - lightroom_tagger/core/database/_legacy.py
  - lightroom_tagger/core/database/db_init.py
  - lightroom_tagger/core/database/catalog.py
  - lightroom_tagger/core/database/instagram.py
  - lightroom_tagger/core/database/matches.py
  - lightroom_tagger/core/database/descriptions.py
  - lightroom_tagger/core/database/scores.py
  - lightroom_tagger/core/database/stacks.py
  - lightroom_tagger/core/database/embeddings.py
  - lightroom_tagger/core/database/similarity.py
  - lightroom_tagger/core/database/vision_cache.py
autonomous: true
requirements_addressed: [REFACTOR-02]
---

```xml
<objective>
Establish `lightroom_tagger/core/database/` as a package per D-12: rename monolithic `database.py` to `database/_legacy.py`, add ten domain stub modules (`db_init.py` … `vision_cache.py`; see task list) with module docstrings only, and implement `database/__init__.py` as a full barrel (D-02) that re-exports the entire public surface from `_legacy` so **zero** import churn at call sites. Preserve `124` top-level `def`/`class` symbols in `_legacy` (verify with `grep -Ec '^(def |class )'`). No logic edits inside `_legacy` beyond what is required for package-relative imports (none in this plan). Tests must pass after this single commit.
</objective>

<threat_model>
- **Python package shadowing:** `database.py` and `database/` cannot coexist (same rule as Phase 13 `handlers`).
- **Incomplete barrel:** missing re-exports break `from lightroom_tagger.core.database import …` across the repo.
Mitigations: `_legacy.py` retains all code; `__init__.py` re-exports only via **explicit** `from ._legacy import (…)` (see T01); run import smoke + `pytest lightroom_tagger/core/`.
</threat_model>

<tasks>
  <task id="14-01-T00">
    <title>Confirm top-level symbol count on monolith (baseline)</title>
    <read_first>
lightroom_tagger/core/database.py
.planning/phases/14-database-images-api-split/14-CONTEXT.md (D-01, D-02, D-12)
    </read_first>
    <action>
From repo root, record baseline:
- `grep -Ec '^(def |class )' lightroom_tagger/core/database.py` — expected output exactly `124`.

Save this integer in the commit message (scaffold baseline).
    </action>
    <acceptance_criteria>
Command `grep -Ec '^(def |class )' lightroom_tagger/core/database.py` prints `124` before any file moves.
    </acceptance_criteria>
  </task>
  <task id="14-01-T01">
    <title>Create `database/` package: move monolith to `_legacy.py` + stubs + barrel `__init__.py`</title>
    <read_first>
lightroom_tagger/core/database.py
lightroom_tagger/core/__init__.py
lightroom_tagger/database.py
.planning/phases/14-database-images-api-split/14-PATTERNS.md (§1.1, §7)
.planning/phases/13-handlers-split-per-job-family/13-PATTERNS.md (§6 `_legacy.py` scaffold)
    </read_first>
    <action>
1. Create directory `lightroom_tagger/core/database/`.
2. **Move** (not copy) `lightroom_tagger/core/database.py` → `lightroom_tagger/core/database/_legacy.py` so `lightroom_tagger/core/database.py` **does not exist** as a file after this step.
3. Do **not** change the body of `_legacy.py` in this plan (no relative-import rewires yet — every symbol remains defined in `_legacy.py`).
4. Add **stub** modules — each file contains **only** a one-line module docstring indicating scaffold + plan id (e.g. `"""Catalog DB helpers (scaffold; populated in plan 14-02)."""`), **no** imports, **no** functions:
   - `db_init.py`
   - `catalog.py`
   - `instagram.py`
   - `matches.py`
   - `descriptions.py`
   - `scores.py`
   - `stacks.py`
   - `embeddings.py`
   - `similarity.py`
   - `vision_cache.py`
5. Add `lightroom_tagger/core/database/__init__.py` that implements **full re-export** of `_legacy` (D-02) using **only** this pattern (no `from ._legacy import *`, no wildcard or implied star re-exports):  
   `from ._legacy import (`  
   …comma-separated list of every name that must remain importable from `lightroom_tagger.core.database`…  
   `)`  
   Executor may generate the parenthesized list with a one-shot `ast` walk of `_legacy` restricted to top-level `FunctionDef` and `ClassDef` **or** maintain the list by hand — either way the committed `__init__.py` must be the explicit import form above.

Minimum smoke names that **must** resolve from `lightroom_tagger.core.database`: `init_database`, `store_image`, `query_catalog_images`, `migrate_unified_image_keys`, `StackMutationError`, `store_match`, `upsert_image_clip_embedding`, `get_cache_stats`, `list_perspectives`.

6. Ensure `lightroom_tagger.database` (legacy shim) and `lightroom_tagger.core.__init__` need **no** edits if barrel is complete — if tests fail, add exports only to `database/__init__.py`, not duplicate business logic.
    </action>
    <acceptance_criteria>
`test -f lightroom_tagger/core/database/_legacy.py` exits 0.

`test ! -f lightroom_tagger/core/database.py` exits 0.

`grep -Ec '^(def |class )' lightroom_tagger/core/database/_legacy.py` prints `124`.

Each stub file exists:  
`test -f lightroom_tagger/core/database/db_init.py`  
`test -f lightroom_tagger/core/database/catalog.py`  
`test -f lightroom_tagger/core/database/instagram.py`  
`test -f lightroom_tagger/core/database/matches.py`  
`test -f lightroom_tagger/core/database/descriptions.py`  
`test -f lightroom_tagger/core/database/scores.py`  
`test -f lightroom_tagger/core/database/stacks.py`  
`test -f lightroom_tagger/core/database/embeddings.py`  
`test -f lightroom_tagger/core/database/similarity.py`  
`test -f lightroom_tagger/core/database/vision_cache.py`

`lightroom_tagger/core/database/__init__.py` uses **only** explicit re-export: contains `from ._legacy import (` … `)` with a parenthesized symbol list (**no** `from ._legacy import *`).

Smoke (repo root, venv python):  
`python -c "from lightroom_tagger.core.database import init_database, store_image, StackMutationError, migrate_unified_image_keys, query_catalog_images, store_match, upsert_image_clip_embedding"` exits 0.

Core DB tests:  
`pytest lightroom_tagger/core/` exits 0.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- Baseline symbol count `124` preserved in `_legacy.py`.
- No coexisting `lightroom_tagger/core/database.py` file next to `lightroom_tagger/core/database/` package.
- `pytest lightroom_tagger/core/` passes.
- Smoke import of representative DB API surface succeeds.
</verification>

<must_haves>
- D-02: `from lightroom_tagger.core.database import <any pre-split symbol>` continues to work without updating downstream import lines.
- D-12: scaffold commit exists **before** any per-family code migration (plans 14-02+).
- Zero behavior change: `_legacy.py` byte-identical to old `database.py` aside from path (no logic edits).
</must_haves>
```
