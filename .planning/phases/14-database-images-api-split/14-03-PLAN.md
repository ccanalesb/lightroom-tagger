---
plan: 14-03
wave: 3
depends_on: [14-02]
phase: 14
files_modified:
  - lightroom_tagger/core/database/_legacy.py
  - lightroom_tagger/core/database/descriptions.py
  - lightroom_tagger/core/database/scores.py
  - lightroom_tagger/core/database/embeddings.py
  - lightroom_tagger/core/database/similarity.py
  - lightroom_tagger/core/database/vision_cache.py
  - lightroom_tagger/core/database/__init__.py
autonomous: true
requirements_addressed: [REFACTOR-02]
---

```xml
<objective>
Complete REFACTOR-02 **wave-3** migrations: five sequential commits in this **exact order** — descriptions → scores → embeddings → similarity → vision_cache — moving remaining symbols out of `_legacy.py` per D-01/D-03. After each commit `pytest lightroom_tagger/core/` is green. After the last commit, `_legacy.py` must contain **zero** `def`/`class` definitions (or the file is deleted if empty — deletion happens in plan 14-07 per workflow; if tooling prefers, leave `_legacy.py` as an empty module with only a comment — prefer **no empty stub**: last migration commit removes file only if 14-07 owns deletion; **this plan** ends with `_legacy.py` having **no** remaining function definitions).
</objective>

<threat_model>
- **Order sensitivity:** `scores.py` must not import `embeddings.py` in a cycle; use `conn` param style only.
- **Partial move:** leaving duplicates in `_legacy` causes `ImportError` on duplicate definitions.
Mitigations: grep-based zero checks on `_legacy` after each task; tests after each commit.
</threat_model>

<tasks>
  <task id="14-03-T01">
    <title>Commit A — migrate **descriptions** into `descriptions.py`</title>
    <read_first>
lightroom_tagger/core/database/_legacy.py
lightroom_tagger/core/database/descriptions.py
lightroom_tagger/core/database/__init__.py
.planning/phases/14-database-images-api-split/14-CONTEXT.md (D-01 descriptions)
    </read_first>
    <action>
Move these **exact** definitions into `descriptions.py`:

1. `build_description_search_document`
2. `build_description_fts_query`
3. `_coerce_has_repetition`
4. `_visual_attr_json`
5. `init_image_descriptions_table`
6. `store_image_description`
7. `get_image_description`
8. `get_undescribed_catalog_images`
9. `get_undescribed_instagram_images`
10. `get_all_images_with_descriptions`

Add required imports (stdlib + `sqlite3` + anything referenced).

Update `__init__.py`; remove defs from `_legacy.py`.

Run `pytest lightroom_tagger/core/`.
    </action>
    <acceptance_criteria>
`grep -c "def get_all_images_with_descriptions" lightroom_tagger/core/database/descriptions.py` outputs `1`.

`grep -c "def build_description_fts_query" lightroom_tagger/core/database/_legacy.py` outputs `0`.

`pytest lightroom_tagger/core/` exits 0.
    </acceptance_criteria>
  </task>

  <task id="14-03-T02">
    <title>Commit B — migrate **scores** into `scores.py`</title>
    <read_first>
lightroom_tagger/core/database/_legacy.py
lightroom_tagger/core/database/scores.py
lightroom_tagger/core/database/__init__.py
.planning/phases/14-database-images-api-split/14-CONTEXT.md (D-01 scores; RESEARCH §8 seed stays in db_init)
    </read_first>
    <action>
Move these **exact** definitions into `scores.py`:

1. `list_perspectives`
2. `get_perspective_by_slug`
3. `insert_perspective`
4. `update_perspective`
5. `delete_perspective`
6. `insert_image_score`
7. `supersede_previous_current_scores`
8. `get_current_scores_for_image`
9. `list_score_history_for_perspective`
10. `list_all_scores_for_image`

Update `__init__.py`; remove from `_legacy.py`.

Run `pytest lightroom_tagger/core/`.
    </action>
    <acceptance_criteria>
`grep -c "def list_perspectives" lightroom_tagger/core/database/scores.py` outputs `1`.

`grep -c "def list_all_scores_for_image" lightroom_tagger/core/database/_legacy.py` outputs `0`.

`pytest lightroom_tagger/core/` exits 0.
    </acceptance_criteria>
  </task>

  <task id="14-03-T03">
    <title>Commit C — migrate **embeddings** into `embeddings.py`</title>
    <read_first>
lightroom_tagger/core/database/_legacy.py
lightroom_tagger/core/database/embeddings.py
lightroom_tagger/core/database/__init__.py
.planning/phases/14-database-images-api-split/14-CONTEXT.md (D-01 embeddings)
    </read_first>
    <action>
Move these **exact** definitions into `embeddings.py`:

1. `_embeddable_catalog_description_sql`
2. `count_catalog_images_missing_text_embedding`
3. `_sort_catalog_key_rows_newest_first`
4. `_list_catalog_keys_text_embed_sql_params`
5. `list_catalog_keys_needing_text_embedding`
6. `list_catalog_keys_for_text_embed_force`
7. `upsert_image_text_embedding`
8. `upsert_image_clip_embedding`
9. `_list_catalog_keys_clip_embed_sql_params`
10. `list_catalog_keys_needing_clip_embedding`
11. `list_catalog_keys_for_clip_embed_force`
12. `_instagram_dump_clip_embed_filters`
13. `_list_instagram_dump_clip_embed_sql_params`
14. `list_instagram_dump_keys_needing_clip_embedding`
15. `list_instagram_dump_keys_for_clip_embed_force`

Update `__init__.py`; remove from `_legacy.py`.

Run `pytest lightroom_tagger/core/`.
    </action>
    <acceptance_criteria>
`grep -c "def upsert_image_clip_embedding" lightroom_tagger/core/database/embeddings.py` outputs `1`.

`grep -c "def list_catalog_keys_needing_clip_embedding" lightroom_tagger/core/database/_legacy.py` outputs `0`.

`pytest lightroom_tagger/core/` exits 0.
    </acceptance_criteria>
  </task>

  <task id="14-03-T04">
    <title>Commit D — migrate **similarity** into `similarity.py`</title>
    <read_first>
lightroom_tagger/core/database/_legacy.py
lightroom_tagger/core/database/similarity.py
lightroom_tagger/core/database/__init__.py
.planning/phases/14-database-images-api-split/14-PATTERNS.md (§1.1 similarity.py DB layer)
    </read_first>
    <action>
Move these **exact** definitions into `similarity.py`:

1. `list_clip_embedded_catalog_keys_newest_first`
2. `clear_catalog_similarity_results`
3. `insert_catalog_similarity_group`

Update `__init__.py`; remove from `_legacy.py`.

Run `pytest lightroom_tagger/core/`.
    </action>
    <acceptance_criteria>
`grep -c "def insert_catalog_similarity_group" lightroom_tagger/core/database/similarity.py` outputs `1`.

`grep -c "def clear_catalog_similarity_results" lightroom_tagger/core/database/_legacy.py` outputs `0`.

`pytest lightroom_tagger/core/` exits 0.
    </acceptance_criteria>
  </task>

  <task id="14-03-T05">
    <title>Commit E — migrate **vision_cache** into `vision_cache.py`</title>
    <read_first>
lightroom_tagger/core/database/_legacy.py
lightroom_tagger/core/database/vision_cache.py
lightroom_tagger/core/database/__init__.py
.planning/phases/14-database-images-api-split/14-CONTEXT.md (D-01 vision_cache)
    </read_first>
    <action>
Move these **exact** definitions into `vision_cache.py`:

1. `init_vision_cache_table`
2. `init_vision_comparisons_table`
3. `get_vision_cached_image`
4. `store_vision_cached_image`
5. `is_vision_cache_valid`
6. `get_cache_stats`
7. `get_vision_comparison`
8. `store_vision_comparison`

Update `__init__.py`; remove from `_legacy.py`.

Run `pytest lightroom_tagger/core/`.
    </action>
    <acceptance_criteria>
`grep -c "def get_cache_stats" lightroom_tagger/core/database/vision_cache.py` outputs `1`.

`grep -Ec '^(def |class )' lightroom_tagger/core/database/_legacy.py` outputs `0`.

`pytest lightroom_tagger/core/` exits 0.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- Migrations occur in order: descriptions → scores → embeddings → similarity → vision_cache.
- After final task, `_legacy.py` has `grep -Ec '^(def |class )'` equal to `0`.
- `lightroom_tagger/core/database/__init__.py` contains **no** `from ._legacy import` lines (all exports resolve to concrete submodules).
- All 124 symbols still importable via `lightroom_tagger.core.database` through `__init__.py` re-exports from submodules only.
</verification>

<must_haves>
- `seed_perspectives_from_prompts_dir` remains in `db_init.py` from 14-02 (not moved to scores).
- No circular imports between new modules.
- Zero behavior change — only physical module placement changes.
</must_haves>
```
