---
plan: 14-04
wave: 4
depends_on: [14-03]
phase: 14
files_modified:
  - lightroom_tagger/core/test_database.py
  - lightroom_tagger/core/test_database_db_init.py
  - lightroom_tagger/core/test_database_catalog.py
  - lightroom_tagger/core/test_database_instagram.py
  - lightroom_tagger/core/test_database_matches.py
  - lightroom_tagger/core/test_database_descriptions.py
  - lightroom_tagger/core/test_database_stacks.py
  - lightroom_tagger/core/test_database_embeddings.py
  - lightroom_tagger/core/test_database_similarity.py
  - lightroom_tagger/core/test_database_vision_cache.py
  - lightroom_tagger/core/test_database_stack_collapse.py
  - lightroom_tagger/core/test_database_nl_filter_arrays.py
  - lightroom_tagger/core/test_database_scores.py
autonomous: true
requirements_addressed: [REFACTOR-02]
---

```xml
<objective>
Mirror the production `lightroom_tagger/core/database/` package structure in tests per D-06/D-07: split `test_database.py` into focused modules (`test_database_db_init.py`, `test_database_catalog.py`, …), absorb `test_database_stack_collapse.py` into `test_database_stacks.py`, keep `test_database_nl_filter_arrays.py` and `test_database_scores.py` filenames. **Imports remain `from lightroom_tagger.core.database import …`** (barrel D-02) unless a test is intentionally targeting a submodule — default is **no** submodule imports. Zero assertion changes — only file moves + import line trims per file.
</objective>

<threat_model>
- **Accidental test loss:** moving classes/methods without running pytest leaves dead coverage.
- **Duplicate test module names:** pytest collection errors if two files define same test class names — rename classes if needed.
Mitigations: `pytest lightroom_tagger/core/` after each logical chunk; `pytest --collect-only` if import errors.
</threat_model>

<tasks>
  <task id="14-04-T01">
    <title>Inventory test cases in `test_database.py` by domain</title>
    <read_first>
lightroom_tagger/core/test_database.py
lightroom_tagger/core/test_database_stack_collapse.py
lightroom_tagger/core/test_database_scores.py
lightroom_tagger/core/test_database_nl_filter_arrays.py
.planning/phases/14-database-images-api-split/14-CONTEXT.md (D-06, D-07)
    </read_first>
    <action>
Read `test_database.py` fully and build a **static map** (table in commit message or scratch file) assigning each `unittest.TestCase` subclass (or each `def test_`) to exactly one target file:

- `test_database_db_init.py` — init/migrations/`init_database`/`migrate_unified_image_keys`/schema presence tests
- `test_database_catalog.py` — CRUD, `query_catalog_images`, filters, `library_write`, `generate_key`, etc.
- `test_database_instagram.py` — Instagram dump / hash / status helpers
- `test_database_matches.py` — matches validation/rejection/store
- `test_database_descriptions.py` — descriptions + FTS builders + undescribed helpers
- `test_database_stacks.py` — stacks + **all** tests from `test_database_stack_collapse.py`
- `test_database_embeddings.py` — embedding upserts + list helpers
- `test_database_similarity.py` — catalog similarity job table helpers (`insert_catalog_similarity_group`, `clear_catalog_similarity_results`, `list_clip_embedded_catalog_keys_newest_first`)
- `test_database_vision_cache.py` — vision cache + comparisons

Do **not** move NL filter array tests — they stay in `test_database_nl_filter_arrays.py` (D-06).
    </action>
    <acceptance_criteria>
Checked-in mapping file (e.g. `.planning/phases/14-database-images-api-split/scratch/14-04-T01-test-case-map.txt`): one line per `unittest.TestCase` subclass in `test_database.py`, format `ClassName<TAB>target_filename.py`.

Machine-verifiable gates (repo root; adjust `MAP` to the path above):

- Counts match:  
  `test "$(grep -E '^class .*\(.*\):' lightroom_tagger/core/test_database.py | wc -l | tr -d ' ')" -eq "$(wc -l < MAP | tr -d ' ')"` exits 0.

- Class set matches (sorted class names from source vs first column of map):  
  `diff -q <(grep -E '^class .*\(.*\):' lightroom_tagger/core/test_database.py | sed -E 's/^class ([A-Za-z0-9_]+)\(.*/\1/' | sort) <(cut -f1 MAP | sort)` exits 0.
    </acceptance_criteria>
  </task>

  <task id="14-04-T02">
    <title>Create new test modules and move tests; shrink or delete `test_database.py`</title>
    <read_first>
lightroom_tagger/core/test_database.py
lightroom_tagger/core/test_database_stack_collapse.py
planning artifact from 14-04-T01
.planning/phases/14-database-images-api-split/14-PATTERNS.md (§6 co-split)
    </read_first>
    <action>
1. Add new files with standard header: `from __future__ import annotations` where used elsewhere, `import unittest`, and **only** the `lightroom_tagger.core.database` imports required by **that file’s** tests.

2. Move test classes/methods from `test_database.py` into the mapped targets per T01.

3. Move **all** content from `test_database_stack_collapse.py` into `test_database_stacks.py`; then **delete** `test_database_stack_collapse.py` (D-06 absorb).

4. **`test_database_nl_filter_arrays.py`:** only adjust imports if needed — still imports from `lightroom_tagger.core.database`.

5. **`test_database_scores.py`:** no rename — verify still passes unchanged aside from optional import tidy.

6. After moves, either **delete** `test_database.py` if empty or leave a **thin** file re-exporting nothing — prefer **delete** if all tests migrated; if a few integration tests span domains, keep **one** `test_database.py` with ≤5 tests and document in file docstring.

Run: `pytest lightroom_tagger/core/`
    </action>
    <acceptance_criteria>
File `lightroom_tagger/core/test_database_stack_collapse.py` does not exist: `test ! -f lightroom_tagger/core/test_database_stack_collapse.py` exits 0.

New files exist (each `test -f`):  
`lightroom_tagger/core/test_database_db_init.py`  
`lightroom_tagger/core/test_database_catalog.py`  
`lightroom_tagger/core/test_database_instagram.py`  
`lightroom_tagger/core/test_database_matches.py`  
`lightroom_tagger/core/test_database_descriptions.py`  
`lightroom_tagger/core/test_database_stacks.py`  
`lightroom_tagger/core/test_database_embeddings.py`  
`lightroom_tagger/core/test_database_similarity.py`  
`lightroom_tagger/core/test_database_vision_cache.py`

`grep -l "test_database_stack_collapse" lightroom_tagger/core/*.py` returns **no** files (no stale imports of deleted module).

`pytest lightroom_tagger/core/` exits 0.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- Test module names align with D-06.
- `test_database_stack_collapse.py` removed (absorbed).
- `test_database_nl_filter_arrays.py` still present.
- Full core pytest passes.
</verification>

<must_haves>
- D-02 preserved: production imports `from lightroom_tagger.core.database`; tests do the same.
- No behavior assertions changed — file moves only.
- `test_database_scores.py` remains and still passes.
</must_haves>
```
