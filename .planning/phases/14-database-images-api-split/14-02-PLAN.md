---
plan: 14-02
wave: 2
depends_on: [14-01]
phase: 14
files_modified:
  - lightroom_tagger/core/database/_legacy.py
  - lightroom_tagger/core/database/db_init.py
  - lightroom_tagger/core/database/catalog.py
  - lightroom_tagger/core/database/instagram.py
  - lightroom_tagger/core/database/matches.py
  - lightroom_tagger/core/database/stacks.py
  - lightroom_tagger/core/database/__init__.py
autonomous: true
requirements_addressed: [REFACTOR-02]
---

```xml
<objective>
Execute REFACTOR-02 **wave-2** migrations (D-12): five sequential commits, each moving one domain family out of `database/_legacy.py` into its submodule with **relative imports inside** `lightroom_tagger.core.database` only (`from .db_init import …`, never `from lightroom_tagger.core.database import …` inside submodules — D-03). After each commit, update `database/__init__.py` to re-export moved symbols from the submodule (not from `_legacy`). Remove moved definitions from `_legacy.py` so `_legacy` shrinks. **`grep -Ec '^(def |class )'` across package** increases in submodules and decreases in `_legacy`; total top-level defs/classes across all package Python files remains **124** until later plans remove `_legacy`.
</objective>

<threat_model>
- **Circular imports:** submodules must not import each other at module load except acyclic graph (db_init has no deps on catalog; catalog may import from `.db_init` only if needed for types — prefer avoid).
- **Missed re-export:** `__init__.py` must list every moved public name.
Mitigations: one family per commit; `pytest lightroom_tagger/core/` after each commit.
</threat_model>

<tasks>
  <task id="14-02-T01">
    <title>Commit A — migrate **db_init** family into `db_init.py`</title>
    <read_first>
lightroom_tagger/core/database/_legacy.py
lightroom_tagger/core/database/db_init.py
lightroom_tagger/core/database/__init__.py
.planning/phases/14-database-images-api-split/14-CONTEXT.md (D-01 db_init bullet, D-03)
.planning/phases/14-database-images-api-split/14-RESEARCH.md (§8 seed placement)
    </read_first>
    <action>
Move these **exact** definitions from `_legacy.py` into `db_init.py` (preserve source order inside `db_init.py` as much as practical):

1. `_dict_factory`
2. `_ensure_sqlite_vec_loaded`
3. `_deserialize_row`
4. `_serialize_json`
5. `_perspective_seed_description`
6. `seed_perspectives_from_prompts_dir`
7. `init_database` (entire function body intact)
8. `_migrate_add_column`
9. `_migrate_images_schema`
10. `_library_db_file_path`
11. `_backfill_matched_catalog_key_from_validated_matches`
12. `_migrate_image_descriptions_fts`
13. `_migrate_image_text_embeddings_vec0`
14. `_migrate_image_clip_embeddings_vec0`
15. `_migrate_image_stacks`
16. `_migrate_catalog_similarity`
17. `_migrate_unified_image_keys`
18. `migrate_unified_image_keys`

**Imports for `db_init.py`:** start from the monolith header imports in `_legacy.py`; add **only** what these functions use (stdlib, `sqlite3`, `sqlite_vec`, `pathlib.Path`, `json`, `os`, `shutil`, `time`, `datetime`, `threading`, `re`, `hashlib`, `contextlib`, `collections.abc` — copy the subset verbatim from `_legacy.py`).

**Do not move** `build_description_search_document`, `build_description_fts_query`, `_coerce_has_repetition`, `_visual_attr_json` — those belong to `descriptions.py` (plan 14-03).

Update `database/__init__.py` to import these symbols **from `.db_init`** instead of `_legacy`.

Delete the moved blocks from `_legacy.py` (no duplicate defs).

Test after commit.
    </action>
    <acceptance_criteria>
`grep -c "def init_database" lightroom_tagger/core/database/db_init.py` outputs `1`.

`grep -c "def init_database" lightroom_tagger/core/database/_legacy.py` outputs `0`.

`grep -c "def seed_perspectives_from_prompts_dir" lightroom_tagger/core/database/db_init.py` outputs `1`.

`db_init.py` contains the line `def _migrate_unified_image_keys(` (exact substring).

`pytest lightroom_tagger/core/` exits 0.
    </acceptance_criteria>
  </task>

  <task id="14-02-T02">
    <title>Commit B — migrate **catalog** family into `catalog.py`</title>
    <read_first>
lightroom_tagger/core/database/_legacy.py
lightroom_tagger/core/database/catalog.py
lightroom_tagger/core/database/db_init.py
lightroom_tagger/core/database/__init__.py
.planning/phases/14-database-images-api-split/14-CONTEXT.md (D-01 catalog + D-05 `library_write`/`resolve_filepath`)
    </read_first>
    <action>
Move these **exact** definitions from `_legacy.py` into `catalog.py`:

1. `library_write`
2. `resolve_filepath`
3. `generate_key`
4. `store_image`
5. `store_images_batch`
6. `get_image`
7. `search_by_keyword`
8. `search_by_rating`
9. `search_by_date`
10. `search_by_color_label`
11. `get_all_images`
12. `get_image_count`
13. `delete_image`
14. `clear_all`
15. `_non_empty_str_list_for_json_array_filter`
16. `_append_query_catalog_image_filters`
17. `filter_order_keys_in_catalog`
18. `query_catalog_images`
19. `query_catalog_images_by_keys`
20. `catalog_key_is_primary_grid_row`
21. `init_catalog_table`
22. `store_catalog_image`
23. `get_catalog_images_needing_analysis`
24. `get_catalog_images_missing_cache`
25. `get_all_catalog_images`

**Import line additions for `catalog.py`:** `import sqlite3` plus any stdlib/third-party from `_legacy` needed by these bodies; for cross-package use `from lightroom_tagger.core…` only when required (prefer relative `.db_init` **only if** a helper is imported — catalog must **not** import `instagram`/`matches`).

Update `__init__.py` exports from `.catalog`. Remove defs from `_legacy.py`.

Run `pytest lightroom_tagger/core/`.
    </action>
    <acceptance_criteria>
`grep -c "def query_catalog_images" lightroom_tagger/core/database/catalog.py` outputs `1`.

`grep -c "def library_write" lightroom_tagger/core/database/catalog.py` outputs `1`.

`grep -c "def query_catalog_images" lightroom_tagger/core/database/_legacy.py` outputs `0`.

`grep -c "def store_image" lightroom_tagger/core/database/catalog.py` outputs `1`.

`pytest lightroom_tagger/core/` exits 0.
    </acceptance_criteria>
  </task>

  <task id="14-02-T03">
    <title>Commit C — migrate **instagram** family into `instagram.py`</title>
    <read_first>
lightroom_tagger/core/database/_legacy.py
lightroom_tagger/core/database/instagram.py
lightroom_tagger/core/database/__init__.py
.planning/phases/14-database-images-api-split/14-CONTEXT.md (D-01 instagram)
    </read_first>
    <action>
Move these **exact** definitions from `_legacy.py` into `instagram.py`:

1. `update_instagram_status`
2. `search_by_instagram_posted`
3. `get_images_without_hash`
4. `update_image_hash`
5. `batch_update_hashes`
6. `init_instagram_dump_table`
7. `store_instagram_dump_media`
8. `get_instagram_dump_media`
9. `get_dump_media_by_hash`
10. `get_unprocessed_dump_media`
11. `get_instagram_by_date_filter`
12. `mark_dump_media_processed`
13. `mark_dump_media_attempted`
14. `init_instagram_table`
15. `_instagram_row_key`
16. `store_instagram_image`
17. `get_instagram_images_needing_analysis`

Wire imports; update `__init__.py`; delete from `_legacy.py`.

Run `pytest lightroom_tagger/core/`.
    </action>
    <acceptance_criteria>
`grep -c "def store_instagram_image" lightroom_tagger/core/database/instagram.py` outputs `1`.

`grep -c "def update_instagram_status" lightroom_tagger/core/database/_legacy.py` outputs `0`.

`grep -c "def get_instagram_images_needing_analysis" lightroom_tagger/core/database/instagram.py` outputs `1`.

`pytest lightroom_tagger/core/` exits 0.
    </acceptance_criteria>
  </task>

  <task id="14-02-T04">
    <title>Commit D — migrate **matches** family into `matches.py`</title>
    <read_first>
lightroom_tagger/core/database/_legacy.py
lightroom_tagger/core/database/matches.py
lightroom_tagger/core/database/__init__.py
.planning/phases/14-database-images-api-split/14-CONTEXT.md (D-01 matches: crossover helpers)
    </read_first>
    <action>
Move these **exact** definitions from `_legacy.py` into `matches.py`:

1. `init_matches_table`
2. `delete_matches_for_insta_key`
3. `store_match`
4. `_backfill_instagram_created_at_from_catalog`
5. `validate_match`
6. `unvalidate_match`
7. `reject_match`
8. `get_rejected_pairs`
9. `catalog_has_instagram_match_conflict`
10. `apply_instagram_match_to_stack_members`

Wire imports; update `__init__.py`; delete from `_legacy.py`.

Run `pytest lightroom_tagger/core/`.
    </action>
    <acceptance_criteria>
`grep -c "def store_match" lightroom_tagger/core/database/matches.py` outputs `1`.

`grep -c "def apply_instagram_match_to_stack_members" lightroom_tagger/core/database/matches.py` outputs `1`.

`grep -c "def store_match" lightroom_tagger/core/database/_legacy.py` outputs `0`.

`pytest lightroom_tagger/core/` exits 0.
    </acceptance_criteria>
  </task>

  <task id="14-02-T05">
    <title>Commit E — migrate **stacks** family into `stacks.py`</title>
    <read_first>
lightroom_tagger/core/database/_legacy.py
lightroom_tagger/core/database/stacks.py
lightroom_tagger/core/database/__init__.py
.planning/phases/14-database-images-api-split/14-CONTEXT.md (D-01 stacks includes `StackMutationError`)
    </read_first>
    <action>
Move these **exact** definitions from `_legacy.py` into `stacks.py`:

1. `StackMutationError` (class)
2. `list_catalog_stack_member_keys`
3. `select_stack_representative_key_for_keys`
4. `stack_metadata_for_api`
5. `catalog_image_stack_row_fields`
6. `stack_split_member_out`
7. `stack_merge_into`
8. `stack_set_representative`

**Note:** Similarity-table helpers (`list_clip_embedded_catalog_keys_newest_first`, …) remain in `_legacy.py` until plan **14-03** (they belong to `similarity.py`).

Wire imports; update `__init__.py`; delete from `_legacy.py`.

Run `pytest lightroom_tagger/core/`.
    </action>
    <acceptance_criteria>
`grep -c "class StackMutationError" lightroom_tagger/core/database/stacks.py` outputs `1`.

`grep -c "def stack_merge_into" lightroom_tagger/core/database/stacks.py` outputs `1`.

`grep -c "class StackMutationError" lightroom_tagger/core/database/_legacy.py` outputs `0`.

`pytest lightroom_tagger/core/` exits 0.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- Five commits (or one plan execution with five git commits) each leaving `pytest lightroom_tagger/core/` green.
- `_legacy.py` no longer contains migrated family symbols (grep counts 0 for moved `def` names).
- `database/__init__.py` re-exports all moved names from their submodules.
</verification>

<must_haves>
- D-03: no `from lightroom_tagger.core.database import` inside `database/*.py` submodules; use relative imports within the package.
- D-04 / D-05: `library_write` + `resolve_filepath` live in `catalog.py`.
- After wave-2, `_legacy.py` still holds: descriptions, embeddings, similarity (3 functions), vision_cache, scores — until 14-03.
</must_haves>
```
