---
plan: 14-06
wave: 6
depends_on: [14-05]
phase: 14
files_modified:
  - apps/visualizer/backend/app.py
  - apps/visualizer/backend/api/images/catalog.py
  - apps/visualizer/backend/api/images/stacks.py
  - apps/visualizer/backend/api/images/instagram.py
  - apps/visualizer/backend/api/images/matches.py
  - apps/visualizer/backend/api/images/search.py
  - apps/visualizer/backend/api/images/__init__.py
  - apps/visualizer/backend/tests/test_images_*.py
  - apps/visualizer/frontend/src/services/api.ts
  - apps/visualizer/frontend/src/utils/imageUrl.ts
  - apps/visualizer/frontend/src/**/*.tsx
  - apps/visualizer/frontend/src/**/*.ts
autonomous: true
requirements_addressed: [REFACTOR-03]
---

```xml
<objective>
Apply **D-09 URL prefix migration:** register **five** Flask Blueprints in `app.py` with url_prefixes exactly:

- `catalog_bp` → `/api/images/catalog`
- `stacks_bp` → `/api/images/stacks`
- `instagram_bp` → `/api/images/instagram`
- `matches_bp` → `/api/images/matches`
- `search_bp` → `/api/images/search`

**Remove** the single umbrella `app.register_blueprint(images.bp, url_prefix='/api/images')`.

Adjust `@<bp>.route(...)` strings in each submodule so **effective browser URLs stay byte-identical** to pre-migration except where D-09 intentionally nests search under `/api/images/search/...` (see action table). Handle **`GET /api/images/catalog-similarity-groups`** which is **not** under `/api/images/catalog/` — use **`app.add_url_rule('/api/images/catalog-similarity-groups', ...)`** in `app.py` bound to the existing `list_catalog_similarity_groups` view func **or** an equivalent handler import from `api.images.catalog` (keeps **five** Blueprint objects; one extra explicit rule is OK).

Audit and update **all** frontend fetch/`request()` paths (`rg '/images/'` in `apps/visualizer/frontend/src` shows **42** lines — many are component **imports**, not network URLs): every **real** API path string must match mounted routes after migration (focus `services/api.ts`, `utils/imageUrl.ts`, hardcoded `/api/images/...` in components/tests per 14-RESEARCH §5).

Update backend integration tests that assert full paths (e.g. `client.get("/api/images/catalog?…")`) in **lockstep**.

Run `pytest` (backend) + `npx tsc --noEmit` or `npm run build` / project-standard typecheck for frontend.
</objective>

<threat_model>
- **Broken similarity-groups URL** if blueprint prefix is naively applied (14-RESEARCH §5.4).
- **Missed frontend string** leaving UI calling wrong path (500/404).
- **Thumbnail URL JSON** from backend must stay aligned with `imageUrl.ts` expectations.
Mitigations: explicit grep criteria + backend route list + targeted pytest + `tsc --noEmit`.
</threat_model>

<tasks>
  <task id="14-06-T01">
    <title>Rewire `app.py` registrations (remove umbrella `images.bp`, add five prefixes + optional `add_url_rule`)</title>
    <read_first>
apps/visualizer/backend/app.py (blueprint block lines 119–140)
apps/visualizer/backend/api/images/__init__.py
apps/visualizer/backend/api/images/catalog.py
.cursor/rules/backend-restart.mdc
.planning/phases/14-database-images-api-split/14-CONTEXT.md (D-09)
.planning/phases/14-database-images-api-split/14-RESEARCH.md (§4–§5)
    </read_first>
    <action>
1. Replace `from api import … images …` with explicit imports per D-11:
   `from api.images import catalog_bp, stacks_bp, instagram_bp, matches_bp, search_bp`

2. **Remove** `app.register_blueprint(images.bp, url_prefix='/api/images')`.

3. **Add** these five lines after other `register_blueprint` calls (order arbitrary):
   - `app.register_blueprint(catalog_bp, url_prefix='/api/images/catalog')`
   - `app.register_blueprint(stacks_bp, url_prefix='/api/images/stacks')`
   - `app.register_blueprint(instagram_bp, url_prefix='/api/images/instagram')`
   - `app.register_blueprint(matches_bp, url_prefix='/api/images/matches')`
   - `app.register_blueprint(search_bp, url_prefix='/api/images/search')`

4. For **`/api/images/catalog-similarity-groups`** add **one** `app.add_url_rule` registering the view function at the **full** path (must remain **identical** to pre-split public URL). Import the view callable from `api.images.catalog` (export if needed).

5. If JSON payloads include **absolute thumbnail URLs**, update **string builders** in `catalog.py` / `instagram.py` so emitted paths match `imageUrl.ts` (`/api/images/{type}/…`).
    </action>
    <acceptance_criteria>
`app.py` contains the substring `register_blueprint(catalog_bp, url_prefix='/api/images/catalog')`.

`app.py` contains `url_prefix='/api/images/search'`.

`app.py` does **not** contain `register_blueprint(images.bp` (grep count 0).

`app.py` contains **either** the literal path string `'/api/images/catalog-similarity-groups'` **or** documents in a trailing comment on `add_url_rule` the exact full path if using keyword args — executable must grep-find substring `catalog-similarity-groups` in `app.py`.

`cd apps/visualizer/backend && python -c "from app import create_app; create_app()"` exits 0.
    </acceptance_criteria>
  </task>

  <task id="14-06-T02">
    <title>Normalize `@route` paths inside each images submodule for new prefixes</title>
    <read_first>
apps/visualizer/backend/api/images/catalog.py
apps/visualizer/backend/api/images/stacks.py
apps/visualizer/backend/api/images/instagram.py
apps/visualizer/backend/api/images/matches.py
apps/visualizer/backend/api/images/search.py
    </read_first>
    <action>
Normative **target effective URLs** (must match production before 14-06 except search nest):

| Must equal | Blueprint prefix | New `@route` (decorator path) |
|------------|------------------|-------------------------------|
| `/api/images/catalog` | `/api/images/catalog` | `''` or `'/'` (Flask allows `''`) |
| `/api/images/catalog/months` | `/api/images/catalog` | `'/months'` |
| `/api/images/catalog/<key>/thumbnail` | same | `'/<path:image_key>/thumbnail'` |
| `/api/images/instagram?…` | `/api/images/instagram` | `''` or `'/'` |
| `/api/images/instagram/months` | `/api/images/instagram` | `'/months'` |
| `/api/images/stacks/<id>/members` | `/api/images/stacks` | `'/<int:stack_id>/members'` |
| `/api/images/chat-search` | `/api/images/search` | `'/chat-search'` (effective `/api/images/search/chat-search`) |
| `/api/images/nl-search` | `/api/images/search` | `'/nl-search'` |
| `/api/images/semantic-search` | `/api/images/search` | `'/semantic-search'` |

Implement these route strings; remove redundant `/catalog`, `/instagram`, `/stacks`, `/matches` **prefix segments** from decorator paths now carried by `url_prefix`.

**Polymorphic detail** `/api/images/<type>/<key>`: choose explicit routes on `catalog_bp` + `instagram_bp` both mounted so paths remain `/api/images/catalog/...` vs `/api/images/instagram/...` if that was prior behavior — **verify** with `test_images_detail_api.py` and adjust to keep **identical** effective URLs.

Run: `cd apps/visualizer/backend && pytest tests/test_images_catalog_api.py tests/test_images_detail_api.py tests/test_images_stacks_api.py tests/test_images_nl_search_api.py tests/test_images_semantic_search_api.py tests/test_images_chat_search_api.py -q`
    </action>
    <acceptance_criteria>
`grep -c "@catalog_bp.route" apps/visualizer/backend/api/images/catalog.py` outputs a number `>= 6`.

`grep "@search_bp.route('/chat-search'" apps/visualizer/backend/api/images/search.py` exits 0 **or** equivalent route producing `/api/images/search/chat-search`.

`cd apps/visualizer/backend && pytest tests/test_images_catalog_api.py tests/test_images_detail_api.py tests/test_images_stacks_api.py tests/test_images_nl_search_api.py tests/test_images_semantic_search_api.py tests/test_images_chat_search_api.py -q` exits 0.
    </acceptance_criteria>
  </task>

  <task id="14-06-T03">
    <title>Update backend tests hardcoding `/api/images/...`</title>
    <read_first>
apps/visualizer/backend/tests/test_images_catalog_api.py
apps/visualizer/backend/tests/test_images_detail_api.py
apps/visualizer/backend/tests/test_images_stacks_api.py
apps/visualizer/backend/tests/test_match_validation.py
apps/visualizer/backend/tests/test_match_groups.py
apps/visualizer/backend/tests/test_catalog_score_query.py
    </read_first>
    <action>
`rg '"/api/images' apps/visualizer/backend/tests` and update every `client.get/post/patch` path to match **actual** mounted URLs after T02.

Pay special attention to search tests if paths move under `/api/images/search/`.
    </action>
    <acceptance_criteria>
`cd apps/visualizer/backend && pytest -q` exits 0 (full backend suite).
    </acceptance_criteria>
  </task>

  <task id="14-06-T04">
    <title>Frontend audit — `services/api.ts`, `imageUrl.ts`, components, frontend tests</title>
    <read_first>
apps/visualizer/frontend/src/services/api.ts
apps/visualizer/frontend/src/utils/imageUrl.ts
.planning/phases/14-database-images-api-split/14-PATTERNS.md (§5)
    </read_first>
    <action>
1. Update `ImagesAPI` / `MatchingAPI` / related `request(\`/images/...\`)` calls so effective URLs hit new Flask paths (especially **`/images/search/nl-search`**, **`/images/search/semantic-search`**, **`/images/search/chat-search`** if those are the chosen path segments).

2. Update **`imageUrl.ts`** and hardcoded `/api/images/...` in TSX if thumbnail/detail URLs shift (must stay consistent with backend JSON `thumbnail_url` fields).

3. Update Vitest expectations that compare string URLs (`**/__tests__/**/*.test.tsx`).

4. Run **`tsc --noEmit`** from `apps/visualizer/frontend` (or repo-standard script).

5. Verification grep: `rg "/images/chat-search" apps/visualizer/frontend/src` returns **0** matches **if** new path is `/images/search/chat-search` — grep for the **new** substring instead with `rg "/images/search/chat-search"` **≥ 1** match in `api.ts`.
    </action>
    <acceptance_criteria>
`grep -r "/images/search/chat-search" apps/visualizer/frontend/src --include='*.ts' --include='*.tsx' | wc -l` outputs a value `>= 1`.

`cd apps/visualizer/frontend && npx tsc --noEmit` exits 0 (adjust command if `pnpm`/`yarn` documented in package — prefer project lockfile).

`cd apps/visualizer/frontend && npm test -- --run` **or** `pnpm vitest run` per `package.json` exists and exits 0 — if no script, **`npx tsc --noEmit` alone** satisfies minimum with note in commit.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- Five blueprint registrations with exact D-09 prefixes present in `app.py`.
- Similarity-groups path still reachable at `/api/images/catalog-similarity-groups`.
- Backend `pytest` full green; frontend `tsc --noEmit` green.
- Frontend `request()` paths updated coherently with backend mounts.
</verification>

<must_haves>
- D-09 prefix list honored verbatim for the five `register_blueprint` calls.
- No accidental change to JSON response **shapes** — URLs inside JSON updated only as required for routing.
- `.cursor/rules/job-ui-contract.mdc` / `.cursor/rules/job-log-contract.mdc`: no new job types or log schema changes.
</must_haves>
```
