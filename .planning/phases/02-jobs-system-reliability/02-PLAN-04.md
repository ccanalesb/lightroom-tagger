---
phase: 2
plan: 04
title: "Job status UX alignment, orphan recovery copy, and handler cancel checks"
type: full-stack
wave: 3
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - apps/visualizer/frontend/src/constants/strings.ts
  - apps/visualizer/frontend/src/components/jobs/JobDetailModal.tsx
  - apps/visualizer/backend/app.py
  - apps/visualizer/backend/jobs/handlers.py
autonomous: true
requirements:
  - SYS-01
---

<objective>
Close SYS-01 gaps identified in CONTEXT/RESEARCH: user-facing vocabulary maps `pending` to “Queued” while keeping API values unchanged, job detail status matches list badges, orphan recovery explains restart clearly, and long-running non-vision handlers honor cooperative cancellation between work units (enrich, describe, prepare) without claiming tighter guarantees than implemented.
</objective>

<tasks>

### Task 1: STATUS_LABELS — pending displays as Queued
**Type:** modify
**Files:** apps/visualizer/frontend/src/constants/strings.ts

<read_first>
- apps/visualizer/frontend/src/constants/strings.ts
- apps/visualizer/frontend/src/components/ui/badges/StatusBadge.tsx
- .planning/phases/02-jobs-system-reliability/02-RESEARCH.md
</read_first>

<action>
In `STATUS_LABELS`, change the value for key `pending` from `'Pending'` to **`'Queued'`** (exact string with capital Q, rest as shown). Leave keys `running`, `completed`, `failed`, `cancelled` unchanged.
</action>

<acceptance_criteria>
- `rg "pending: 'Queued'" apps/visualizer/frontend/src/constants/strings.ts` matches exactly.
- `rg "pending: 'Pending'" apps/visualizer/frontend/src/constants/strings.ts` returns no matches.
</acceptance_criteria>

### Task 2: JobDetailModal status uses STATUS_LABELS
**Type:** modify
**Files:** apps/visualizer/frontend/src/components/jobs/JobDetailModal.tsx

<read_first>
- apps/visualizer/frontend/src/components/jobs/JobDetailModal.tsx
- apps/visualizer/frontend/src/constants/strings.ts
</read_first>

<action>
1. Import `STATUS_LABELS` from `../../constants/strings`.
2. Replace the `Badge` child text `{displayJob.status}` in the Status row (grid cell under label `Status:`) with `{STATUS_LABELS[displayJob.status] ?? displayJob.status}` so detail view matches `StatusBadge` labeling.
</action>

<acceptance_criteria>
- `rg "STATUS_LABELS\[displayJob.status\]" apps/visualizer/frontend/src/components/jobs/JobDetailModal.tsx` matches.
- `rg ">\\{displayJob.status\\}<" apps/visualizer/frontend/src/components/jobs/JobDetailModal.tsx` returns no matches inside the status Badge (grep may be skipped if ambiguous; file read must show Badge shows STATUS_LABELS).
</acceptance_criteria>

### Task 3: Orphan recovery log message for running jobs on startup
**Type:** modify
**Files:** apps/visualizer/backend/app.py

<read_first>
- apps/visualizer/backend/app.py
- .planning/phases/02-jobs-system-reliability/02-CONTEXT.md
</read_first>

<action>
In `_recover_orphaned_jobs`, replace the `add_job_log` message string `'Job interrupted by server restart'` with **exactly**:
`This job was still running when the server restarted. It was marked failed; use Retry if you want to run it again.`
Keep log `level` as `'error'`.
</action>

<acceptance_criteria>
- `rg "This job was still running when the server restarted" apps/visualizer/backend/app.py` matches exactly the full string in one line or concatenation-free string literal.
- `rg "Job interrupted by server restart" apps/visualizer/backend/app.py` returns no matches.
</acceptance_criteria>

### Task 4: handle_enrich_catalog — cancel check each image iteration
**Type:** modify
**Files:** apps/visualizer/backend/jobs/handlers.py

<read_first>
- apps/visualizer/backend/jobs/handlers.py
- apps/visualizer/backend/jobs/runner.py
</read_first>

<action>
Inside `handle_enrich_catalog`, in the `for i, record in enumerate(catalog_images):` loop, at the **top** of the loop body (before processing the record), insert:
```python
if runner.is_cancelled(job_id):
    runner.finalize_cancelled(job_id)
    return
```
Ensure `db` is closed in a `finally` as today when returning early (existing `finally: if db: db.close()` pattern — if missing, add `try/finally` around the main work so early return still closes `db`).
</action>

<acceptance_criteria>
- `rg "handle_enrich_catalog" apps/visualizer/backend/jobs/handlers.py` -A 200 contains `is_cancelled(job_id)` inside the `for i, record` loop.
- `rg "finalize_cancelled\(job_id\)" apps/visualizer/backend/jobs/handlers.py` matches in `handle_enrich_catalog` context (line numbers within 80 lines of `def handle_enrich_catalog`).
</acceptance_criteria>

### Task 5: handle_batch_describe — cancel check sequential path
**Type:** modify
**Files:** apps/visualizer/backend/jobs/handlers.py

<read_first>
- apps/visualizer/backend/jobs/handlers.py
- apps/visualizer/backend/jobs/runner.py
</read_first>

<action>
In `handle_batch_describe`, in the **sequential** branch (`else:` under `if max_workers > 1 and total > 3`), at the **start** of each `for idx, (key, itype) in enumerate(images_to_describe, 1):` iteration, add the same three lines as Task 4 (`is_cancelled` → `finalize_cancelled` → `return`).
Add an `add_job_log` call when cancel is detected: `add_job_log(runner.db, job_id, 'info', 'Batch describe stopped: cancel requested')` **before** `return` (exact message string).
</action>

<acceptance_criteria>
- `rg "Batch describe stopped: cancel requested" apps/visualizer/backend/jobs/handlers.py` matches exactly.
- Sequential `for idx, (key, itype)` loop in `handle_batch_describe` contains `runner.is_cancelled(job_id)` (use read_file if grep context insufficient).
</acceptance_criteria>

### Task 6: handle_batch_describe — cancel check parallel path between futures
**Type:** modify
**Files:** apps/visualizer/backend/jobs/handlers.py

<read_first>
- apps/visualizer/backend/jobs/handlers.py
</read_first>

<action>
In the `ThreadPoolExecutor` branch (`for future in as_completed(futures):`), as the **first** statement inside the loop body, add:
```python
if runner.is_cancelled(job_id):
    add_job_log(runner.db, job_id, 'info', 'Batch describe cancel noted; finishing already-running tasks')
    break
```
Use **exact** message string `Batch describe cancel noted; finishing already-running tasks`.
After the `with ThreadPoolExecutor` block ends (still inside `if max_workers > 1`), if `runner.is_cancelled(job_id)`, call `runner.finalize_cancelled(job_id)` and `return` before `runner.complete_job(...)`.
</action>

<acceptance_criteria>
- `rg "Batch describe cancel noted; finishing already-running tasks" apps/visualizer/backend/jobs/handlers.py` matches exactly.
- `rg "as_completed\(futures\)" apps/visualizer/backend/jobs/handlers.py` -B 0 -A 25 includes `runner.is_cancelled(job_id)` as first check in loop.
</acceptance_criteria>

### Task 7: handle_prepare_catalog — cancel between executor completions
**Type:** modify
**Files:** apps/visualizer/backend/jobs/handlers.py

<read_first>
- apps/visualizer/backend/jobs/handlers.py
</read_first>

<action>
In `handle_prepare_catalog`, inside `for _future in as_completed(futures):`, first line of loop body: same `is_cancelled` / `add_job_log` / `break` pattern as Task 6 but use log message **exactly** `Prepare catalog cache stopped: cancel requested`.
After the executor loop, if `runner.is_cancelled(job_id)`, call `runner.finalize_cancelled(job_id)` and `return` before the final stats / `complete_job`.
</action>

<acceptance_criteria>
- `rg "Prepare catalog cache stopped: cancel requested" apps/visualizer/backend/jobs/handlers.py` matches exactly.
- `handle_prepare_catalog` function body contains `runner.is_cancelled(job_id)` (grep with context).
</acceptance_criteria>

</tasks>

<verification>
1. `npm run lint` in `apps/visualizer/frontend` exits 0.
2. Grep confirms old orphan string gone and new string present.
3. Manual: create pending job, confirm UI label reads `Queued` via `StatusBadge` on a page that lists jobs.
</verification>

<success_criteria>
- `pending` jobs read as “Queued” in the UI while API still returns `pending`.
- Job detail status label matches list badges.
- Orphan recovery log tells users what happened and what to do next.
- Enrich/describe/prepare paths observe cancel between iterations or between futures with explicit log lines.
</success_criteria>

<must_haves>
- Observable lifecycle language matches requirement intent (queued/running/complete/failed + cancelled) without breaking API contract (SYS-01).
- Server restart does not leave silent mystery failures for previously running jobs.
- Cooperative cancel applies beyond vision match for other long jobs.
</must_haves>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Log injection via job metadata in UI | Low | No change to log rendering model; messages are static server strings for new cancel lines. |
| Misleading “cancelled” while workers still run | Medium | Documented log line for parallel describe; executor context manager waits for shutdown — do not claim immediate kill in UI. |
</threat_model>
