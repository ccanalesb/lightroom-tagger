---
phase: 2
plan: 03
title: "Job failure severity in API and UI badges"
type: full-stack
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - apps/visualizer/backend/database.py
  - apps/visualizer/backend/jobs/runner.py
  - apps/visualizer/backend/api/jobs.py
  - apps/visualizer/backend/jobs/handlers.py
  - apps/visualizer/frontend/src/types/job.ts
  - apps/visualizer/frontend/src/components/jobs/JobDetailModal.tsx
  - apps/visualizer/frontend/src/components/jobs/JobCard.tsx
  - apps/visualizer/frontend/src/constants/strings.ts
autonomous: true
requirements:
  - SYS-04
---

<objective>
Persist a structured failure severity (`warning` / `error` / `critical`) alongside the existing free-text `error` field, set it from typed failures in job handlers and `fail_job`, and render DESIGN.md-aligned badges next to the error in job detail and list views — without rewriting user-visible error message text (D-10/D-11).
</objective>

<tasks>

### Task 1: jobs.error_severity column and migration in init_db
**Type:** modify
**Files:** apps/visualizer/backend/database.py

<read_first>
- apps/visualizer/backend/database.py
- apps/visualizer/backend/jobs/runner.py
</read_first>

<action>
1. Extend the `CREATE TABLE IF NOT EXISTS jobs` DDL to include column `error_severity TEXT` **only if** SQLite supports adding in CREATE for fresh DBs: add `error_severity TEXT` to the column list for new installs (insert after `error TEXT` in the CREATE statement).
2. For existing databases, after `CREATE TABLE IF NOT EXISTS jobs`, add idempotent migration logic:
   - `pragma_info = db.execute("PRAGMA table_info(jobs)").fetchall()`
   - If no row has `name == 'error_severity'`, execute `ALTER TABLE jobs ADD COLUMN error_severity TEXT`.
3. Ensure `get_job` / `_deserialize_job` requires no change if SELECT * already picks up new column (it will).
4. Add function `def clear_job_failure_details(db: sqlite3.Connection, job_id: str) -> None:` executing `UPDATE jobs SET error = NULL, error_severity = NULL WHERE id = ?` with `(job_id,)`.
</action>

<acceptance_criteria>
- `rg "error_severity" apps/visualizer/backend/database.py` matches at least 3 times (CREATE and/or ALTER and clear function).
- `rg "PRAGMA table_info\(jobs\)" apps/visualizer/backend/database.py` matches OR file contains `table_info` and `error_severity` migration branch.
- `rg "def clear_job_failure_details" apps/visualizer/backend/database.py` matches.
</acceptance_criteria>

### Task 2: fail_job writes error_severity; complete_job clears it
**Type:** modify
**Files:** apps/visualizer/backend/jobs/runner.py

<read_first>
- apps/visualizer/backend/jobs/runner.py
- apps/visualizer/backend/database.py
</read_first>

<action>
1. Change signature to `def fail_job(self, job_id: str, error: str, *, severity: str = 'error') -> None:` where `severity` must be one of `'warning'`, `'error'`, `'critical'` (validate with `if severity not in (...): severity = 'error'`).
2. In `fail_job`, when updating DB, set both `error` and `error_severity`:
   - Prefer a single `db.execute("UPDATE jobs SET status = 'failed', error = ?, error_severity = ?, completed_at = ? WHERE id = ?", ...)` **or** reuse `update_job_status` plus `update_job_field` — if using `update_job_field`, add `'error_severity'` to `_ALLOWED_JOB_UPDATE_FIELDS` in `database.py` and document that it is stored as JSON string vs plain TEXT mismatch: **do not JSON-encode** severity; use direct SQL UPDATE for `error_severity` column to avoid JSON quoting issues.
3. Minimum concrete implementation: after `update_job_status(..., 'failed')`, run `db.execute("UPDATE jobs SET error_severity = ? WHERE id = ?", (severity, job_id))` before `update_job_field` for `error` OR combine into one UPDATE with existing timestamp logic — keep `completed_at` behavior identical to current `update_job_status` for `failed`.
4. In `complete_job`, after success updates, execute `db.execute("UPDATE jobs SET error_severity = NULL, error = NULL WHERE id = ?", (job_id,))` only if you need to clear stale error from retry — **instead**, rely on retry route clearing fields; for `complete_job`, add `UPDATE jobs SET error_severity = NULL WHERE id = ?` alongside clearing error if the row still has error text (optional). **Required:** `complete_job` path must not leave a **new** severity; if easier, run `UPDATE jobs SET error_severity = NULL` in `complete_job` unconditionally for that job id after status `completed`.
5. Extend cancelled guard from Plan 01: still return early if status is `cancelled`.
</action>

<acceptance_criteria>
- `rg "def fail_job" apps/visualizer/backend/jobs/runner.py` shows `severity` keyword parameter with default `'error'`.
- `rg "error_severity" apps/visualizer/backend/jobs/runner.py` matches.
- `rg "'critical'" apps/visualizer/backend/jobs/runner.py` matches inside a severity validation tuple or set.
</acceptance_criteria>

### Task 3: Retry route clears error_severity
**Type:** modify
**Files:** apps/visualizer/backend/api/jobs.py

<read_first>
- apps/visualizer/backend/api/jobs.py
- apps/visualizer/backend/database.py
</read_first>

<action>
In `retry_job`, after clearing `error` and `result` via `update_job_field`, call `clear_job_failure_details` **or** add `update_job_field(..., 'error_severity', None)` only if that field is whitelisted — **preferred:** call `clear_job_failure_details(current_app.db, job_id)` from `database` import, but ensure it does not duplicate clearing `error` incorrectly. Adjust `clear_job_failure_details` to only clear `error_severity` if `error` is already cleared by existing code — **simplest:** replace separate `update_job_field` error clear with single `clear_job_failure_details` that sets both `error` and `error_severity` NULL, then set status pending as today, then re-set `result` clear as today. If that reorders logic, instead add one line: `db.execute("UPDATE jobs SET error_severity = NULL WHERE id = ?", (job_id,))` after existing error clear.
Concrete minimal line to add after `update_job_field(..., 'error', None)`:
`current_app.db.execute("UPDATE jobs SET error_severity = NULL WHERE id = ?", (job_id,)); current_app.db.commit()`
</action>

<acceptance_criteria>
- `rg "error_severity = NULL" apps/visualizer/backend/api/jobs.py` matches inside `retry_job` function body.
</acceptance_criteria>

### Task 4: Classify severity in handle_vision_match except path
**Type:** modify
**Files:** apps/visualizer/backend/jobs/handlers.py

<read_first>
- apps/visualizer/backend/jobs/handlers.py
- lightroom_tagger/core/provider_errors.py
- apps/visualizer/backend/jobs/runner.py
</read_first>

<action>
In `handle_vision_match` outer `except Exception as e:` (the one that calls `runner.fail_job`):
1. Import `ProviderError` from `lightroom_tagger.core.provider_errors` and these subclasses: `AuthenticationError`, `InvalidRequestError`.
2. Compute `severity = 'error'` default.
3. If `isinstance(e, (AuthenticationError, InvalidRequestError))`, set `severity = 'warning'`.
4. If `isinstance(e, (PermissionError, OSError))`, set `severity = 'critical'`.
5. If `isinstance(e, RuntimeError)` and `str(e) == 'Close Lightroom before writing to catalog.'`, set `severity = 'critical'`.
6. Call `runner.fail_job(job_id, str(e), severity=severity)` (exact keyword `severity=`).
Apply the same classification pattern to `handle_enrich_catalog`, `handle_prepare_catalog`, and `handle_batch_describe` exception handlers that call `runner.fail_job(job_id, str(e))` so all job failures get severity metadata.
</action>

<acceptance_criteria>
- `rg "fail_job\(job_id, str\(e\), severity=" apps/visualizer/backend/jobs/handlers.py` matches at least 4 times OR `rg "severity=severity" apps/visualizer/backend/jobs/handlers.py` matches at least 4 times.
- `rg "AuthenticationError" apps/visualizer/backend/jobs/handlers.py` matches.
- `rg "Close Lightroom before writing to catalog" apps/visualizer/backend/jobs/handlers.py` matches inside severity classification (same line block as `critical` assignment).
</acceptance_criteria>

### Task 5: Frontend Job type includes error_severity
**Type:** modify
**Files:** apps/visualizer/frontend/src/types/job.ts

<read_first>
- apps/visualizer/frontend/src/types/job.ts
- apps/visualizer/backend/database.py
</read_first>

<action>
Add optional field to `Job` interface:
`error_severity?: 'warning' | 'error' | 'critical' | null`
Place it immediately after `error: string | null` in the interface.
</action>

<acceptance_criteria>
- `rg "error_severity" apps/visualizer/frontend/src/types/job.ts` matches.
- `rg "'warning' \\| 'error' \\| 'critical'" apps/visualizer/frontend/src/types/job.ts` matches OR the union appears equivalent on one line.
</acceptance_criteria>

### Task 6: Strings for severity badges
**Type:** modify
**Files:** apps/visualizer/frontend/src/constants/strings.ts

<read_first>
- apps/visualizer/frontend/src/constants/strings.ts
- apps/visualizer/frontend/DESIGN.md
</read_first>

<action>
Export a new constant object:
```ts
export const ERROR_SEVERITY_LABELS: Record<string, string> = {
  warning: 'Warning',
  error: 'Error',
  critical: 'Critical',
}
```
Place it adjacent to `STATUS_LABELS` in the same file.
</action>

<acceptance_criteria>
- `rg "ERROR_SEVERITY_LABELS" apps/visualizer/frontend/src/constants/strings.ts` matches.
- `rg "critical: 'Critical'" apps/visualizer/frontend/src/constants/strings.ts` matches.
</acceptance_criteria>

### Task 7: JobDetailModal — severity Badge next to error heading
**Type:** modify
**Files:** apps/visualizer/frontend/src/components/jobs/JobDetailModal.tsx

<read_first>
- apps/visualizer/frontend/src/components/jobs/JobDetailModal.tsx
- apps/visualizer/frontend/src/components/ui/Badge/Badge.tsx
- apps/visualizer/frontend/DESIGN.md
</read_first>

<action>
1. Import `ERROR_SEVERITY_LABELS` from `../../constants/strings`.
2. In the `{displayJob.error && (` block where `{JOB_DETAILS_ERROR}` heading is rendered, change the `<h4>` row to a flex row:
   - Left: existing title `{JOB_DETAILS_ERROR}`.
   - Right: when `displayJob.error_severity` is `'warning' | 'error' | 'critical'`, render `<Badge variant={...}>`:
     - `warning` → `variant="warning"`
     - `error` → `variant="error"`
     - `critical` → `variant="error"` plus Tailwind classes `ring-2 ring-error` on the same `Badge` (use `className` prop if `Badge` supports merging).
3. Badge text: `ERROR_SEVERITY_LABELS[displayJob.error_severity]`.
4. Keep the error message `<p>` content **exactly** `displayJob.error` with no string transformation.
</action>

<acceptance_criteria>
- `rg "ERROR_SEVERITY_LABELS" apps/visualizer/frontend/src/components/jobs/JobDetailModal.tsx` matches.
- `rg "displayJob.error_severity" apps/visualizer/frontend/src/components/jobs/JobDetailModal.tsx` matches.
- `rg "JOB_DETAILS_ERROR" apps/visualizer/frontend/src/components/jobs/JobDetailModal.tsx` still matches (heading preserved).
</acceptance_criteria>

### Task 8: JobCard — failed row shows severity when present
**Type:** modify
**Files:** apps/visualizer/frontend/src/components/jobs/JobCard.tsx

<read_first>
- apps/visualizer/frontend/src/components/jobs/JobCard.tsx
- apps/visualizer/frontend/src/components/ui/Badge/Badge.tsx
- apps/visualizer/frontend/src/constants/strings.ts
</read_first>

<action>
1. Import `Badge` from `../ui/Badge` and `ERROR_SEVERITY_LABELS` from `../../constants/strings`.
2. Next to `<StatusBadge status={job.status} />` (same flex row), when `job.status === 'failed' && job.error_severity`, render a second `Badge` with same variant mapping as Task 7 and label from `ERROR_SEVERITY_LABELS[job.error_severity]`.
</action>

<acceptance_criteria>
- `rg "ERROR_SEVERITY_LABELS" apps/visualizer/frontend/src/components/jobs/JobCard.tsx` matches.
- `rg "job.error_severity" apps/visualizer/frontend/src/components/jobs/JobCard.tsx` matches.
</acceptance_criteria>

</tasks>

<verification>
1. `cd apps/visualizer/frontend && npm run lint` exits 0.
2. From backend: create a job row with `failed` status via test harness or sqlite, set `error` and `error_severity` to `critical`, GET `/api/jobs/<id>` JSON includes both keys.
3. Open Job detail in UI (manual): failed job shows badge text `Critical` when `error_severity` is `critical` and error paragraph unchanged.
</verification>

<success_criteria>
- API returns `error_severity` for failed jobs when set.
- UI shows severity badge for failed jobs without altering raw `error` text.
- Retry clears `error_severity` together with `error`.
</success_criteria>

<must_haves>
- Failed jobs carry machine-readable severity for support and scanning (SYS-04).
- Raw error strings remain the source of truth for the message body (D-10).
- Visual severity uses existing Badge variants from DESIGN.md (warning/error + ring for critical).
</must_haves>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| XSS via error message | Medium | No change to rendering approach — still text content; do not switch to `dangerouslySetInnerHTML`. |
| Severity field injection via API | Low | Only server-written column from `fail_job`; no new public write endpoint for severity. |
| Enumeration of failure classes | Low | Severity is non-secret metadata. |
</threat_model>
