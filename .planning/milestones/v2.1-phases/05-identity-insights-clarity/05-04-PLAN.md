---
phase: 5
plan: 04
slug: dashboard-top-photos-tabs-filters
wave: 3
depends_on: [01]
files_modified:
  - apps/visualizer/frontend/src/components/ui/Tabs/Tabs.tsx
  - apps/visualizer/frontend/src/components/ui/Tabs/index.ts
  - apps/visualizer/frontend/src/constants/strings.ts
  - apps/visualizer/frontend/src/pages/DashboardPage.tsx
  - apps/visualizer/frontend/src/pages/DashboardPage.test.tsx
requirements:
  - DASH-02
  - DASH-03
autonomous: true
---

# Plan 04: Dashboard Top Photos — tabs + useFilters + per-tab fetch

## Objective

Replace the single `TopPhotosStrip` dataset on the Insights Dashboard with a **Unposted | Posted | All** tab control (default **Unposted**), satisfy DASH-02 (unposted primary) and DASH-03 (tri-state posted filter via shared `useFilters` schema without `FilterBar`), and fetch correct ranked slices using `IdentityAPI.getBestPhotos` with `posted` from Plan 01.

## Context

- `TopPhotosStrip` stays presentational; `DashboardPage` owns fetch state (research).
- `useFilters` + minimal schema (single `select` or tri-state pattern) satisfies DASH-03 — same idea as InstagramTab migration (Phase 4.1).
- `Tabs.tsx` uses `py-2.5`; `05-UI-SPEC.md` requires `py-2` for tab nav padding.
- Avoid `Tabs` default `mt-6` content gap for a dense strip: extract **tab navigation only** (`TabNav`) and render **one** `TopPhotosStrip` below the nav inside the Highlights card.
- Prefetch all three datasets on initial dashboard load so tab switches do not flash loading (unless a request fails — show that tab’s error).

## Tasks

<task id="01" type="execute">
<title>Tabs.tsx: extract TabNav + py-2 padding</title>
<read_first>
- apps/visualizer/frontend/src/components/ui/Tabs/Tabs.tsx — full file
- .planning/phases/05-identity-insights-clarity/05-UI-SPEC.md — DASH tab nav contract (border-b, px-4 py-2, font-semibold, active border-accent)
</read_first>
<action>
1. Add and **export** a new component `TabNav` in the same file `Tabs.tsx` (or `TabNav.tsx` colocated — prefer **same file** to avoid import churn) with props:

```ts
export type TabNavItem = { id: string; label: string }
export function TabNav(props: {
  tabs: TabNavItem[]
  activeTab: string
  onTabChange: (tabId: string) => void
  className?: string
}): JSX.Element
```

2. Move the existing nav markup (the `border-b border-border` wrapper + `nav` + buttons) from `Tabs` into `TabNav`, preserving button classes except: replace `py-2.5` with **`py-2`** on tab buttons.

3. Refactor `Tabs` to render `<TabNav tabs={tabs.map(t => ({ id: t.id, label: t.label }))} ... />` plus the existing `mt-6` content wrapper unchanged.

4. Add `role="tablist"` on the `nav`, `role="tab"` on each button, `aria-selected={activeTab === tab.id}` on each button, and `aria-controls` only if you also add matching `id`/`aria-labelledby` — **minimum** per UI-SPEC: `role="tablist"` + `role="tab"` + `aria-selected` on buttons (add `id={`tab-${tab.id}`}` on buttons if needed for a11y consistency).

5. Export `TabNav` and `TabNavItem` (if used) from `apps/visualizer/frontend/src/components/ui/Tabs/index.ts` alongside `Tabs` — add lines `export { TabNav } from './Tabs'` and `export type { TabNavItem } from './Tabs'` (exact names must match exports from `Tabs.tsx`).
</action>
<acceptance_criteria>
- `grep -n "export function TabNav" apps/visualizer/frontend/src/components/ui/Tabs/Tabs.tsx` returns a match
- `grep -n "py-2.5" apps/visualizer/frontend/src/components/ui/Tabs/Tabs.tsx` returns **no** matches (padding updated)
- `grep -n "py-2" apps/visualizer/frontend/src/components/ui/Tabs/Tabs.tsx` matches tab button class list
- `grep -n 'role="tablist"' apps/visualizer/frontend/src/components/ui/Tabs/Tabs.tsx` returns a match
- `grep -n 'aria-selected' apps/visualizer/frontend/src/components/ui/Tabs/Tabs.tsx` returns at least one match
- `grep -n "TabNav" apps/visualizer/frontend/src/components/ui/Tabs/index.ts` returns a match
- `cd apps/visualizer/frontend && npx tsc --noEmit` exits 0
</acceptance_criteria>
</task>

<task id="02" type="execute">
<title>strings.ts: Insights tab labels + optional region label</title>
<read_first>
- apps/visualizer/frontend/src/constants/strings.ts — `INSIGHTS_SECTION_*` block
- .planning/phases/05-identity-insights-clarity/05-UI-SPEC.md — tab labels row
</read_first>
<action>
Add exported constants with **exact** label text:

- `INSIGHTS_TOP_PHOTOS_TAB_UNPOSTED = 'Unposted'`
- `INSIGHTS_TOP_PHOTOS_TAB_POSTED = 'Posted'`
- `INSIGHTS_TOP_PHOTOS_TAB_ALL = 'All'`

Add `INSIGHTS_TOP_PHOTOS_REGION_ARIA = 'Top scored photos'` (used for `aria-label` on the region wrapping tab nav + strip).
</action>
<acceptance_criteria>
- `grep -n "INSIGHTS_TOP_PHOTOS_TAB_UNPOSTED = 'Unposted'" apps/visualizer/frontend/src/constants/strings.ts` returns a match
- `grep -n "INSIGHTS_TOP_PHOTOS_REGION_ARIA" apps/visualizer/frontend/src/constants/strings.ts` returns a match
</acceptance_criteria>
</task>

<task id="03" type="execute">
<title>DashboardPage: useFilters + TabNav + parallel getBestPhotos</title>
<read_first>
- apps/visualizer/frontend/src/pages/DashboardPage.tsx — full file
- apps/visualizer/frontend/src/hooks/useFilters.ts — `useFilters` (from `pages/`, import as `../hooks/useFilters`)
- apps/visualizer/frontend/src/components/images/InstagramTab.tsx — `useMemo` + `useFilters` pattern
- apps/visualizer/frontend/src/components/filters/types.ts — `FilterSchema` / `SelectFilterDescriptor`
- apps/visualizer/frontend/src/services/api.ts — `IdentityAPI.getBestPhotos` after Plan 01
</read_first>
<action>
1. Import `useMemo` from `react` and `useFilters` from `../hooks/useFilters`; import `FilterSchema` from `../components/filters/types`, `TabNav` from `../components/ui/Tabs/Tabs` (adjust path to actual export).

2. Define `dashboardTopPhotosSchema = useMemo<FilterSchema>(() => [{ type: 'select', key: 'topPhotosPosted', label: INSIGHTS_TOP_PHOTOS_REGION_ARIA, paramName: 'posted', defaultValue: 'unposted', options: [ { value: 'unposted', label: INSIGHTS_TOP_PHOTOS_TAB_UNPOSTED }, { value: 'posted', label: INSIGHTS_TOP_PHOTOS_TAB_POSTED }, { value: 'all', label: INSIGHTS_TOP_PHOTOS_TAB_ALL } ], toParam: (v) => (v === 'unposted' ? false : v === 'posted' ? true : undefined) }], [])` — **note:** if `toParam` typing rejects boolean, use the same tri-state mapping pattern as documented in `useFilters` tests; if necessary use `as const` or match existing `select` + `toParam` examples from `BestPhotosGrid.tsx`.

3. `const filters = useFilters(dashboardTopPhotosSchema)`.

4. Replace the single `bestItems`/`bestMeta`/`bestTotal`/`errBest`/`loadingBest` state with **per-tab** state keyed by `'unposted' | 'posted' | 'all'`, each holding `{ items: IdentityBestPhotoItem[]; total: number; meta: IdentityBestPhotosMeta | null; loading: boolean; error: string | null }`.

5. In the main `useEffect` that currently calls `Promise.allSettled`, replace the single `IdentityAPI.getBestPhotos({ limit: 8 })` call with **three** settled calls in the same `Promise.allSettled` array:
   - `IdentityAPI.getBestPhotos({ limit: 8, posted: false })` → store in `unposted` bucket
   - `IdentityAPI.getBestPhotos({ limit: 8, posted: true })` → `posted` bucket
   - `IdentityAPI.getBestPhotos({ limit: 8 })` (omit `posted`) → `all` bucket

6. Map `activeTab = filters.values.topPhotosPosted as string` (default `'unposted'`). Derive `active = activeTab === 'posted' ? 'posted' : activeTab === 'all' ? 'all' : 'unposted'` if needed for key typing.

7. Inside the Highlights `<Card>`, wrap `TabNav` + `TopPhotosStrip` in a `<div role="region" aria-label={INSIGHTS_TOP_PHOTOS_REGION_ARIA}>`.

8. Render `<TabNav tabs={[...ids unposted/posted/all with labels from strings...]} activeTab={active} onTabChange={(id) => filters.setValue('topPhotosPosted', id)} />` **above** `<TopPhotosStrip ... />`.

9. Pass the **active** bucket into `TopPhotosStrip`: `items={perTab[active].items}`, `loading={perTab[active].loading}`, `error={perTab[active].error}`, `emptyMessage={!loading && !err && total===0 ? meta?.coverage_note ?? IDENTITY_BEST_PHOTOS_EMPTY_FALLBACK : null}` mirroring existing `bestEmptyMessage` logic per active bucket.

10. Update the `a11yErrors` join string to use the active tab’s error label, e.g. `` `Best photos (${active}): ${err}` `` or three separate optional fragments — must include which tab failed when `error` is non-null.

11. Remove unused imports/state from the old single-strip implementation.
</action>
<acceptance_criteria>
- `grep -n "useFilters(dashboardTopPhotosSchema" apps/visualizer/frontend/src/pages/DashboardPage.tsx` OR `grep -n "useFilters(" apps/visualizer/frontend/src/pages/DashboardPage.tsx` combined with `grep -n "topPhotosPosted" apps/visualizer/frontend/src/pages/DashboardPage.tsx` shows both hooks and key
- `grep -n "getBestPhotos({ limit: 8, posted: false })" apps/visualizer/frontend/src/pages/DashboardPage.tsx` returns a match
- `grep -n "getBestPhotos({ limit: 8, posted: true })" apps/visualizer/frontend/src/pages/DashboardPage.tsx` returns a match
- `grep -n "TabNav" apps/visualizer/frontend/src/pages/DashboardPage.tsx` returns a match
- `grep -n "role=\"region\"" apps/visualizer/frontend/src/pages/DashboardPage.tsx` returns a match
- `grep -n "FilterBar" apps/visualizer/frontend/src/pages/DashboardPage.tsx` returns **no** matches
- `cd apps/visualizer/frontend && npx tsc --noEmit` exits 0
</acceptance_criteria>
</task>

<task id="04" type="execute">
<title>DashboardPage.test.tsx: tabs + getBestPhotos params</title>
<read_first>
- apps/visualizer/frontend/src/pages/DashboardPage.test.tsx — full file
- apps/visualizer/frontend/src/pages/DashboardPage.tsx — after edits
</read_first>
<action>
1. Update `IdentityAPI.getBestPhotos` mock to `mockImplementation((params) => { ... })` returning valid `{ items: [], total: 0, meta: {} }` for any call.

2. Add assertions after `waitFor`:
   - `screen.getByRole('tab', { name: 'Unposted' })` exists
   - `screen.getByRole('tab', { name: 'Posted' })` exists
   - `screen.getByRole('tab', { name: 'All' })` exists

3. Assert `getBestPhotos` was called with `posted: false` at least once:  
   `expect(IdentityAPI.getBestPhotos).toHaveBeenCalledWith(expect.objectContaining({ limit: 8, posted: false }))`

4. Optionally assert `posted: true` and a call **without** `posted` in the same objectContaining style (or `expect.objectContaining({ limit: 8 })` for the third call that omits posted — use `toHaveBeenCalledWith` three times with different matchers if needed).

Use `import { IdentityAPI } from '...'` already in file; keep `MemoryRouter` wrapper.
</action>
<acceptance_criteria>
- `grep -n "getByRole('tab'" apps/visualizer/frontend/src/pages/DashboardPage.test.tsx` returns at least 3 lines (Unposted/Posted/All)
- `grep -n "posted: false" apps/visualizer/frontend/src/pages/DashboardPage.test.tsx` returns a match inside `toHaveBeenCalledWith` or `objectContaining`
- `cd apps/visualizer/frontend && npx vitest run src/pages/DashboardPage.test.tsx` exits 0
</acceptance_criteria>
</task>

## Verification

```bash
cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend && npx tsc --noEmit && npx vitest run
```

## must_haves

- [ ] Dashboard Highlights shows tab labels **Unposted** (default), **Posted**, **All**
- [ ] `useFilters` is instantiated on `DashboardPage` with schema key `topPhotosPosted` (DASH-03 shared framework)
- [ ] Three API calls use `posted: false`, `posted: true`, and omitted `posted` respectively, each with `limit: 8`
- [ ] `FilterBar` is not introduced on Dashboard for this control (tabs per CONTEXT)
