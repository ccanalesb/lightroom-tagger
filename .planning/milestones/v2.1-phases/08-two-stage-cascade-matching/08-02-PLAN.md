---
phase: 8
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - lightroom_tagger/scripts/match_instagram_dump.py
  - lightroom_tagger/core/matcher.py
  - lightroom_tagger/core/test_matcher.py
autonomous: true
requirements:
  - MATCH-03
---

<objective>
Load Instagram and catalog `ai_summary` in `match_dump_media`, optionally inline-describe via `generate_description` + `store_image_description`, then implement two-stage batch scoring in `score_candidates_with_vision`: `compare_descriptions_batch` when `desc_weight > 0`, vision batch only when `vision_weight > 0`, nominal fixed weights (no `desc_available` renormalization), full vision/compression skip when `vision_weight == 0`, and tests proving API call counts and merge behavior.
</objective>

<tasks>

<task id="02-match-dump-media-summaries">
<read_first>
- /Users/ccanales/projects/lightroom-tagger/lightroom_tagger/scripts/match_instagram_dump.py (full `match_dump_media`)
- /Users/ccanales/projects/lightroom-tagger/lightroom_tagger/core/database.py (`get_image_description`, `store_image_description`)
- /Users/ccanales/projects/lightroom-tagger/lightroom_tagger/core/vision_client.py (`generate_description`)
- /Users/ccanales/projects/lightroom-tagger/lightroom_tagger/core/description_service.py (how `store_image_description` is called for catalog vs instagram)
- /Users/ccanales/projects/lightroom-tagger/lightroom_tagger/core/analyzer.py (`get_vision_model`)
- /Users/ccanales/projects/lightroom-tagger/lightroom_tagger/core/provider_registry.py (`ProviderRegistry`)
</read_first>
<action>
Add parameter `skip_undescribed: bool = True` to `match_dump_media` (same style as `force_descriptions`).

After building `dump_image` with `description` from `dump_media.get('caption', '')`, set AI summary: call `get_image_description(db, dump_media['media_key'])`; if row has non-empty `summary`, set `dump_image['ai_summary']` to that string; else `dump_image['ai_summary'] = ''`.

If `dump_image['ai_summary']` is empty, `skip_undescribed is False`, and `dump_image.get('local_path')` exists: obtain client via `ProviderRegistry().get_client(provider_id or fallback[0])`, model via `provider_model or get_vision_model()`, call `generate_description(client, model, local_path, log_callback=log_callback)`, assign result to `dump_image['ai_summary']`, persist using `store_image_description` with `image_type='instagram'` matching existing description_service records.

When building each `vision_candidates` entry from `catalog_img`, keep `description` from `catalog_img.get('description', '')` and add `ai_summary` from `catalog_img.get('ai_summary', '')`. If `skip_undescribed is False` and catalog `ai_summary` is empty and resolved path exists, call `generate_description` + `store_image_description` for `image_type='catalog'` and set `candidate['ai_summary']` to the generated text.

Pass `skip_undescribed=skip_undescribed` into `score_candidates_with_vision`.

Do not put AI summary into the `description` key on `dump_image` or candidates.
</action>
<acceptance_criteria>
- `grep -n "def match_dump_media" lightroom_tagger/scripts/match_instagram_dump.py` shows `skip_undescribed` in the definition line.
- `match_instagram_dump.py` contains `dump_image[` with `ai_summary` in the substring (grep: `ai_summary` appears at least twice: dump + candidate).
- `match_instagram_dump.py` contains `score_candidates_with_vision(` with keyword `skip_undescribed=`.
- `match_instagram_dump.py` imports or references `generate_description` (line count from `rg "generate_description" match_instagram_dump.py` is ≥ 1).
</acceptance_criteria>
</task>

<task id="02-score-candidates-two-stage">
<read_first>
- /Users/ccanales/projects/lightroom-tagger/lightroom_tagger/core/matcher.py (`score_candidates_with_vision`, `_call_batch_chunk`, batch loop, sequential loop)
- /Users/ccanales/projects/lightroom-tagger/lightroom_tagger/core/vision_client.py (`compare_descriptions_batch`, `compare_images_batch`)
</read_first>
<action>
Add `skip_undescribed: bool = True` to `score_candidates_with_vision` after existing parameters (before `should_cancel` or at end of keyword-only group — match file style).

Import `compare_descriptions_batch` from `vision_client` alongside `compare_images_batch`.

Guard `InstagramCache.compress_instagram_image` (lines ~168–180): run only when `vision_weight > 0`. If `vision_weight == 0`, set `compressed_insta` to `None` and skip all vision batch preparation (`get_or_create_cached_image` for batch candidates, `_call_batch_chunk`, `compare_with_vision` sequential, `store_vision_comparison`).

When `desc_weight > 0`: for each batch chunk aligned with vision chunks (same candidate indices in `chunk` as `batch_candidates` slices), compute description scores as follows:

- Reference text: `insta_image.get('ai_summary') or ''`.
- For each `(idx, _path)` in `chunk`, let `cand = candidate_by_idx[idx]`. If `skip_undescribed` is True and `(cand.get('ai_summary') or '').strip()` is empty, assign description score **0.0** for that `idx` **without** sending that row to the API.
- Otherwise build `text_candidates = [(idx, cand.get('ai_summary') or '') for ...]` for indices not skipped; if the list is non-empty, call `compare_descriptions_batch(client, requested_model, reference_text, text_candidates, log_callback, max_tokens=4096)` and map returned confidences to `desc_sim_01 = confidence / 100.0` per id; any skipped id stays 0.0.

Convert LLM confidences to 0–1: `desc_sim_01 = confidence / 100.0` (already 0–1 after division).

**Nominal merge (D-10):** `total_score_val = (phash_weight * phash_score_val) + (desc_weight * desc_sim_01) + (vision_weight * vision_score_val)` — fixed `phash_weight`, `desc_weight`, `vision_weight` from arguments; **no** `weight_sum` division; **no** `desc_available` gating that removes `desc_weight` from the sum.

When `desc_weight == 0`: do **not** call `compare_descriptions_batch`. Do **not** use LLM description scores; keep `desc_sim_01` from `text_similarity(insta_image.get('description',''), candidate.get('description',''))` for the `desc_similarity` field only if needed for logging — the **weighted** contribution of description must be **0** because `desc_weight * desc_sim_01` uses `desc_weight=0`.

When `vision_weight == 0`: `vision_score_val = 0.0`; do not call `compress_instagram_image`, `compare_images_batch`, `_call_batch_chunk`, `compare_with_vision`, or `store_vision_comparison`. Apply the same rule in **both** the batch branch and the **sequential** fallback branch (skip `compare_with_vision`, skip vision cache reads that trigger network, set `vision_score_val = 0.0`).

Remove all uses of `desc_available`, `active_weights`, and `weight_sum` renormalization inside `score_candidates_with_vision` (delete the `desc_available = ...` blocks at former lines ~285–297 and ~492–504 and replace with the nominal formula above).
</action>
<acceptance_criteria>
- `rg "def score_candidates_with_vision" -A 15 lightroom_tagger/core/matcher.py` shows `skip_undescribed` in the signature.
- `rg "compare_descriptions_batch" lightroom_tagger/core/matcher.py` returns at least 2 lines (import + use).
- `rg "compress_instagram_image" lightroom_tagger/core/matcher.py` is only called inside a block guarded by `vision_weight` (e.g. line after `if vision_weight > 0:`) — verify with read/grep context.
- `rg "desc_available" lightroom_tagger/core/matcher.py` returns **no** matches (all removed from `score_candidates_with_vision`).
- `rg "weight_sum" lightroom_tagger/core/matcher.py` returns **no** matches inside `score_candidates_with_vision` (nominal merge only).
- File `matcher.py` contains literal `phash_weight * phash_score_val` and `desc_weight *` and `vision_weight * vision_score_val` in the merged scoring block.
</acceptance_criteria>
</task>

<task id="02-tests-matcher">
<read_first>
- /Users/ccanales/projects/lightroom-tagger/lightroom_tagger/core/test_matcher.py (existing patches for `compare_images_batch`, `InstagramCache`, `score_candidates_with_vision`)
- /Users/ccanales/projects/lightroom-tagger/lightroom_tagger/core/matcher.py (post-change signatures)
</read_first>
<action>
Extend `lightroom_tagger/core/test_matcher.py` with tests using `unittest.mock.patch`:

1. **`vision_weight=0`:** Patch `compare_images_batch` and assert it was **not** called; patch `InstagramCache.compress_instagram_image` or constructor usage and assert **not** called when scoring runs with `vision_weight=0`, `desc_weight>0`, and minimal candidates (use mocks so batch path triggers if `batch_threshold` met — set `batch_threshold` to 1 and `batch_size` 10 in call kwargs from test).

2. **`desc_weight=0`:** Patch `compare_descriptions_batch` and assert **not** called when `desc_weight=0`, `vision_weight=1`.

3. **Backward compatible (SC-7):** Use weights `phash_weight=0`, `desc_weight=0`, `vision_weight=1` (matches default UI). Mock vision so one candidate gets a fixed vision confidence; assert `total_score` equals `vision_score_val` within `1e-9` (description contribution zero).

4. **Weighted merge:** Use weights `0.4`, `0.3`, `0.3` with mocked phash/desc/vision contributions 1.0/1.0/1.0; assert `total_score` equals `0.4 + 0.3 + 0.3` == `1.0` (or match mocked partials exactly).

5. **`skip_undescribed=True` (empty summaries):** Candidates whose `ai_summary` is empty must get `desc_score=0.0` **without** any `compare_descriptions_batch` call for those rows: assert the mock is **not** invoked for empty-summary candidates (or, if invoked, only with the non-empty subset — i.e. empty-summary candidates are excluded from the batch payload). At minimum: patch `compare_descriptions_batch`; assert `call_count == 0` or that `call_args`/`call_args_list` contain only non-empty-summary candidate tuples.

6. **Call order (description before vision):** Patch both `compare_descriptions_batch` and `compare_images_batch`. After scoring a batch of candidates with `desc_weight > 0` and `vision_weight > 0`, assert description runs before vision: e.g. collect `mock_compare_desc.call_args_list` and `mock_compare_images.call_args_list`, or a single list of `(name, call)` in invocation order; assert the first `compare_descriptions_batch` call occurs at a lower index than the first `compare_images_batch` call for that chunk.

7. **All description scores zero / no redistribution:** All three candidates have empty `ai_summary`, `skip_undescribed=True`, `desc_weight=0.3`, `vision_weight=0.7`. Assert `compare_descriptions_batch` is **not** called (skip path assigns 0 without API). Assert merged scores use **nominal** weights `0.3` / `0.7` with no redistribution to vision: description contribution is `0 * 0.3`, vision contribution is `vision_score * 0.7` (match expected `total_score` from mocked vision scores).

Each test must `assert` at least one numeric or call-count condition.
</action>
<acceptance_criteria>
- `pytest /Users/ccanales/projects/lightroom-tagger/lightroom_tagger/core/test_matcher.py -q` exits 0.
- `test_matcher.py` contains the substring `compare_descriptions_batch`.
- `test_matcher.py` contains `vision_weight` and `0` in a test that asserts no vision call (e.g. `assert mock_compare_images.call_count == 0` or equivalent).
- A test asserts `compare_descriptions_batch` is invoked before `compare_images_batch` for a batch chunk when both stages run (call-order / index check on mocks).
- A test covers `skip_undescribed=True` with empty `ai_summary`: `compare_descriptions_batch` must not receive empty-summary candidates (or must not be called when all are empty); empty summaries yield `desc_score=0.0`.
- A test covers all candidates empty `ai_summary`, `skip_undescribed=True`, `desc_weight=0.3`, `vision_weight=0.7`: `compare_descriptions_batch` not called (or zero-desc API path), nominal weights unchanged — no redistribution of `desc_weight` to vision.
</acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd /Users/ccanales/projects/lightroom-tagger && python -m pytest lightroom_tagger/core/test_matcher.py -q
```

```bash
cd /Users/ccanales/projects/lightroom-tagger && python -m pytest lightroom_tagger/core/test_description_batch.py -q
```

```bash
rg -n "skip_undescribed|compare_descriptions_batch|ai_summary" lightroom_tagger/core/matcher.py lightroom_tagger/scripts/match_instagram_dump.py
```
</verification>

<must_haves>
- **MATCH-03 / SC-3:** Per batch chunk, description stage runs before vision when `desc_weight > 0`; vision runs when `vision_weight > 0`.
- **MATCH-03 / SC-4:** `vision_weight == 0` implies no Instagram compression for vision, no `compare_images_batch`, no vision cache writes / vision API.
- **MATCH-03 / SC-7:** `desc_weight == 0`, `vision_weight == 1` preserves prior effective scoring (tests lock this).
- **D-10:** Nominal weights; no silent redistribution of `desc_weight` to vision when descriptions are zero.
</must_haves>

<success_criteria>
Wave 2 completes when matcher and match script tests pass, two-stage behavior is covered by mocks, and grep confirms `skip_undescribed` plumbed into `score_candidates_with_vision`.
</success_criteria>
