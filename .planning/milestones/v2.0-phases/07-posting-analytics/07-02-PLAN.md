---
phase: 7
plan: "07-02"
title: "Analytics page: posting timeline, heatmap, and caption/hashtag views"
type: feature
wave: 2
depends_on:
  - "07-01"
files_modified:
  - apps/visualizer/frontend/package.json
  - apps/visualizer/frontend/package-lock.json
  - apps/visualizer/frontend/src/constants/strings.ts
  - apps/visualizer/frontend/src/services/api.ts
  - apps/visualizer/frontend/src/App.tsx
  - apps/visualizer/frontend/src/components/Layout.tsx
  - apps/visualizer/frontend/src/pages/AnalyticsPage.tsx
  - apps/visualizer/frontend/src/components/analytics/PostingFrequencyChart.tsx
  - apps/visualizer/frontend/src/components/analytics/PostingHeatmap.tsx
  - apps/visualizer/frontend/src/components/analytics/CaptionHashtagPanel.tsx
autonomous: true
requirements:
  - POST-01
  - POST-02
  - POST-03
---

<objective>
Ship a dedicated **`/analytics`** experience: global nav item **Analytics**, client methods for the Phase 7 analytics API, and three UI blocks — **Recharts** line/area chart for posting frequency, a **heatmap** for dow×hour (CSS grid or Recharts `Bar` matrix — implementer choice; must be readable in light/dark theme), and a **caption/hashtag** summary panel with top hashtags list and length stats. Include shared **date range** controls and loading/empty/error states consistent with `DashboardPage` / `ImagesPage`.
</objective>

<tasks>
<task>
<type>implementation</type>
<files>apps/visualizer/frontend/package.json</files>
<action>
Add **`recharts`** dependency (version aligned with [.planning/research/STACK.md](../../../research/STACK.md), e.g. `^3.8.1` or current stable). Run **`npm install`** in `apps/visualizer/frontend` so `package-lock.json` updates.
</action>
<read_first>
- apps/visualizer/frontend/package.json
- .planning/research/STACK.md
</read_first>
<acceptance_criteria>
- `grep -n '"recharts"' apps/visualizer/frontend/package.json` exits 0.
</acceptance_criteria>
</task>

<task>
<type>implementation</type>
<files>apps/visualizer/frontend/src/services/api.ts</files>
<action>
Add **`AnalyticsAPI`** object with methods:

- `getPostingFrequency(params?: { date_from?: string; date_to?: string; granularity?: 'day' | 'week' | 'month' })`
- `getPostingHeatmap(params?: { date_from?: string; date_to?: string })`
- `getCaptionStats(params?: { date_from?: string; date_to?: string })`

Use **`request<T>`** and paths matching backend: `/analytics/posting-frequency`, etc. (full path `/api/...` via `API_URL`). Export TypeScript types for responses (inline interfaces or `types/analytics.ts` if file grows).

**Note:** `unposted-catalog` remains for 07-03; optional to stub here if convenient.
</action>
<read_first>
- apps/visualizer/frontend/src/services/api.ts (`ImagesAPI`, `request`)
- apps/visualizer/backend/api/analytics.py (final route paths from 07-01)
</read_first>
<acceptance_criteria>
- `grep -n 'AnalyticsAPI' apps/visualizer/frontend/src/services/api.ts` exits 0.
- `grep -n 'posting-frequency' apps/visualizer/frontend/src/services/api.ts` exits 0.
</acceptance_criteria>
</task>

<task>
<type>implementation</type>
<files>apps/visualizer/frontend/src/constants/strings.ts</files>
<action>
Add **`NAV_ANALYTICS = 'Analytics'`** and user-facing strings for section titles, empty states (no dump / no validated posts), and footer **timezone disclaimer** placeholder referencing API `meta` text when present.
</action>
<read_first>
- apps/visualizer/frontend/src/constants/strings.ts
</read_first>
<acceptance_criteria>
- `grep -n 'NAV_ANALYTICS' apps/visualizer/frontend/src/constants/strings.ts` exits 0.
</acceptance_criteria>
</task>

<task>
<type>implementation</type>
<files>apps/visualizer/frontend/src/components/Layout.tsx</files>
<action>
Insert **`{ to: '/analytics', label: NAV_ANALYTICS }`** into `navItems` (desktop + mobile). Import label from `constants/strings`.
</action>
<read_first>
- apps/visualizer/frontend/src/components/Layout.tsx
</read_first>
<acceptance_criteria>
- `grep -n '/analytics' apps/visualizer/frontend/src/components/Layout.tsx` exits 0.
</acceptance_criteria>
</task>

<task>
<type>implementation</type>
<files>apps/visualizer/frontend/src/App.tsx</files>
<action>
Add route **`path="analytics"`** rendering **`AnalyticsPage`** inside **`ErrorBoundary`** like other pages.
</action>
<read_first>
- apps/visualizer/frontend/src/App.tsx
</read_first>
<acceptance_criteria>
- `grep -n 'AnalyticsPage' apps/visualizer/frontend/src/App.tsx` exits 0.
</acceptance_criteria>
</task>

<task>
<type>implementation</type>
<files>apps/visualizer/frontend/src/components/analytics/PostingFrequencyChart.tsx</files>
<action>
Create client component using **Recharts** (`ResponsiveContainer`, `AreaChart` or `LineChart`, `XAxis`, `YAxis`, `Tooltip`, `CartesianGrid`). Props: `buckets`, `meta`, loading, error. Format X axis labels from `bucket_start` (locale-aware short date). Use theme CSS variables (`var(--color-text)`, etc.) for stroke/fill where possible to match `ThemeContext`.
</action>
<read_first>
- apps/visualizer/frontend/src/contexts/ThemeContext.tsx (if chart colors need awareness)
- apps/visualizer/frontend/src/pages/DashboardPage.tsx (async fetch patterns)
</read_first>
<acceptance_criteria>
- `grep -n 'recharts' apps/visualizer/frontend/src/components/analytics/PostingFrequencyChart.tsx` exits 0.
</acceptance_criteria>
</task>

<task>
<type>implementation</type>
<files>apps/visualizer/frontend/src/components/analytics/PostingHeatmap.tsx</files>
<action>
Render **7×24** grid (dow × hour). Map `cells` from API to background intensity (e.g. alpha from count / max). Show row labels (Mon–Sun or Sun–Sat consistent with `meta.dow_labels`) and column labels (0–23). Include legend “darker = more posts”. Ensure keyboard-accessible `title` tooltips on cells.
</action>
<read_first>
- 07-CONTEXT.md D-33
</read_first>
<acceptance_criteria>
- File exists and exports a named React component used by `AnalyticsPage`.
</acceptance_criteria>
</task>

<task>
<type>implementation</type>
<files>apps/visualizer/frontend/src/components/analytics/CaptionHashtagPanel.tsx</files>
<action>
Display summary stats (counts, averages, median) and a **scrollable list** or table of top hashtags with counts. Optional: simple bar visualization for top N. When `post_count === 0`, show empty state string from constants.
</action>
<read_first>
- apps/visualizer/frontend/src/components/ui/Card.tsx
</read_first>
<acceptance_criteria>
- `grep -n 'top_hashtags' apps/visualizer/frontend/src/components/analytics/CaptionHashtagPanel.tsx` exits 0 OR component clearly maps API field names from 07-01.
</acceptance_criteria>
</task>

<task>
<type>implementation</type>
<files>apps/visualizer/frontend/src/pages/AnalyticsPage.tsx</files>
<action>
Compose page: page title, **date range** inputs (default last 12 months or “all time” if API omits filters — implementer choice; must match backend expectations), **Apply** button, parallel `useEffect` fetch of three endpoints, pass data to three components. Footer line: show `meta.timezone_assumption` + short user-facing disclaimer (POST-01..03 roadmap footnote).

**Accessibility:** associate labels with inputs; announce errors.
</action>
<read_first>
- apps/visualizer/frontend/src/pages/ImagesPage.tsx (filter patterns if any)
</read_first>
<acceptance_criteria>
- `grep -n 'AnalyticsAPI' apps/visualizer/frontend/src/pages/AnalyticsPage.tsx` exits 0.
- `grep -n 'PostingFrequencyChart' apps/visualizer/frontend/src/pages/AnalyticsPage.tsx` exits 0.
</acceptance_criteria>
</task>
</tasks>

<verification>
- `npm run build` in `apps/visualizer/frontend` (exit 0).
- `npm run lint` in `apps/visualizer/frontend` (exit 0).
- Manual: open `/analytics`, verify chart renders with seeded DB, toggle date range triggers refetch.
</verification>

<success_criteria>
- **POST-01:** User sees posting frequency over time from real API data.
- **POST-02:** User sees dow×hour heatmap from real API data.
- **POST-03:** User sees caption/hashtag aggregates from real API data.
- Timezone / timestamp assumptions are visible in the UI (roadmap Phase 7 criterion 5).
</success_criteria>

<must_haves>
- **`recharts`** in dependencies; no duplicate chart library.
- **`AnalyticsAPI`** typed fetch layer; no raw `fetch` scattered across components except inside `api.ts`.
- **Nav + route** wired; page reachable without manual URL typing for typical users.
</must_haves>

<threat_model>
**ASVS L1 — frontend**

- **Threat:** XSS from caption/hashtag display — render hashtags and stats as **text nodes** only; never `dangerouslySetInnerHTML` for caption samples. If showing example captions later, sanitize or truncate.
- **Threat:** Open redirect — N/A for this page if no external links; post URLs not required here.
</threat_model>
