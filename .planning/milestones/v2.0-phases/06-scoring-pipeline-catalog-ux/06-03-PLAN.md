---
phase: 6
plan: "06-03"
title: "Catalog image modal — scores panel, metadata, and per-perspective history UI"
type: feature
wave: 2
depends_on:
  - "06-02"
files_modified:
  - apps/visualizer/frontend/src/components/catalog/CatalogImageModal.tsx
  - apps/visualizer/frontend/src/components/catalog/ImageScoresPanel.tsx
  - apps/visualizer/frontend/src/constants/strings.ts
autonomous: true
requirements:
  - SCORE-01
  - SCORE-03
  - SCORE-04
---

<objective>
In **`CatalogImageModal`**, add a first-class **Scores** section that loads **`ScoresAPI.getCurrent`**, displays each current perspective score with **numeric badge**, **rationale** text, **`model_used`**, and **`prompt_version`**, and lets the user open **per-perspective version history** via **`ScoresAPI.getHistory`** (collapsible list or `<select>` + list). Add **Run scoring** affordance calling **`JobsAPI.create('single_score', { image_key, image_type: 'catalog', perspective_slugs, force, provider_id?, provider_model? })`** with the same provider override pattern as description generation and **`useJobSocket`** refresh on job completion.
</objective>

<tasks>
<task>
<type>implementation</type>
<files>apps/visualizer/frontend/src/components/catalog/ImageScoresPanel.tsx</files>
<action>
Create **`ImageScoresPanel`** component (props roughly):

- `imageKey: string`
- `imageType?: 'catalog' | 'instagram'` (default `catalog`)

**Behavior:**

1. On mount / `imageKey` change: fetch `ScoresAPI.getCurrent(imageKey, { image_type })`.
2. Render **empty state** copy when `current.length === 0` (explain user can run scoring from Processing or use button below).
3. For each current row (group by `perspective_slug` — should be unique while `is_current=1`):
   - Show **`perspective_slug`** + **`display_name`** if available (optional second fetch to `PerspectivesAPI.list` cached in parent — **prefer** lightweight: show slug + score pill using `Badge` component variant `accent` or numeric emphasis per `DESIGN.md`).
   - Show **`score / 10`** prominently.
   - Show **`rationale`** in `text-sm text-text-secondary` block.
   - Metadata row or muted line: **`model_used`** · **`prompt_version`** · formatted **`scored_at`**.
   - **`repaired_from_malformed`**: if true, show subtle `text-warning` hint “Output was repaired before save” (short).

4. **History affordance:** per perspective, `button` or chevron toggles expansion; on expand call `ScoresAPI.getHistory(imageKey, { perspective_slug, image_type })` (lazy). List prior rows with `prompt_version`, `score`, `scored_at`, `is_current` badge (Latest vs Archived).

5. **Loading / error:** mirror `CatalogImageModal` description states (`Loading…`, red error text).

Export from same file as default export.
</action>
<read_first>
- apps/visualizer/frontend/src/components/catalog/CatalogImageModal.tsx (description panel layout)
- apps/visualizer/frontend/src/services/api.ts (`ScoresAPI` from plan 06-02)
- apps/visualizer/frontend/DESIGN.md (semantic colors, typography)
- apps/visualizer/frontend/src/components/ui/Badge.tsx
</read_first>
<acceptance_criteria>
- File `apps/visualizer/frontend/src/components/catalog/ImageScoresPanel.tsx` exists.
- `grep -n 'ScoresAPI' apps/visualizer/frontend/src/components/catalog/ImageScoresPanel.tsx` exits 0.
- `grep -n 'getHistory' apps/visualizer/frontend/src/components/catalog/ImageScoresPanel.tsx` exits 0.
- `grep -n 'prompt_version' apps/visualizer/frontend/src/components/catalog/ImageScoresPanel.tsx` exits 0.
</acceptance_criteria>
</task>

<task>
<type>implementation</type>
<files>apps/visualizer/frontend/src/components/catalog/CatalogImageModal.tsx</files>
<action>
1. Import and render **`<ImageScoresPanel imageKey={image.key} imageType="catalog" />`** below the AI description card (or in a second column on wide screens — **prefer** stacked below description for mobile-first).

2. Add **Score this image** button (or reuse `GenerateButton` styling with distinct label constant):
   - State: `scoring`, `scoreError`, `pendingScoreJobId` parallel to description flow.
   - `JobsAPI.create('single_score', { image_key: image.key, image_type: 'catalog', perspective_slugs: <default all active from PerspectivesAPI.list>, force: false, ...provider fields })`.
   - **Perspective selection:** minimal v1 — fetch active perspectives on modal open, default all checked (mirror `DescriptionsTab` checkbox list in compact form inside modal advanced section) **or** pass `null`/omit to let backend default — **06-01 chose explicit list**: UI must send explicit `perspective_slugs` array; load active slugs on modal mount.

3. Extend **`useJobSocket` `onJobUpdated`**: if `pendingScoreJobId` matches and job completes, call refresh callback passed into `ImageScoresPanel` via **`ref`** or **`key={refreshNonce}`** increment — simplest: lift scores fetch to parent with `refreshScores()` and pass to panel as prop `reloadToken`.

4. Add **`force` toggle** (optional checkbox) for power users, maps to metadata `force`.

**Do not** break existing description job wiring.
</action>
<read_first>
- apps/visualizer/frontend/src/components/catalog/CatalogImageModal.tsx (full file)
- apps/visualizer/frontend/src/components/processing/DescriptionsTab.tsx (perspective checkbox pattern)
</read_first>
<acceptance_criteria>
- `grep -n 'ImageScoresPanel' apps/visualizer/frontend/src/components/catalog/CatalogImageModal.tsx` exits 0.
- `grep -n "single_score" apps/visualizer/frontend/src/components/catalog/CatalogImageModal.tsx` exits 0.
- `grep -n 'useJobSocket' apps/visualizer/frontend/src/components/catalog/CatalogImageModal.tsx` exits 0.
</acceptance_criteria>
</task>

<task>
<type>implementation</type>
<files>apps/visualizer/frontend/src/constants/strings.ts</files>
<action>
Add user-visible strings for the modal (examples — adjust names to match existing naming style):

- Section title: e.g. `SECTION_IMAGE_SCORES = 'Critique scores'`
- Button label: `ACTION_RUN_SCORING = 'Run scoring'`
- Loading label: `SCORES_LOADING = 'Loading scores…'`

Use these constants in `CatalogImageModal` / `ImageScoresPanel` instead of raw strings where other sections use `IMAGE_DETAILS_TITLE` pattern.
</action>
<read_first>
- apps/visualizer/frontend/src/constants/strings.ts (existing `IMAGE_DETAILS_TITLE`, description strings)
</read_first>
<acceptance_criteria>
- `grep -n 'SCORE' apps/visualizer/frontend/src/constants/strings.ts` exits 0 with at least 2 matches.
</acceptance_criteria>
</task>
</tasks>

<verification>
- `npm run build` inside `apps/visualizer/frontend` (exit 0) **or** `npx tsc --noEmit` if that is the project standard — pick the script defined in `apps/visualizer/frontend/package.json`.
- `npm run lint` if configured and fast (optional if not present).
</verification>

<success_criteria>
- Opening a catalog image shows **current scores** with **model** and **prompt version** visible.
- User can **see prior rubric generations** per perspective after re-scoring.
- User can **enqueue single-image scoring** and see results without manual refresh when the job completes.
</success_criteria>

<must_haves>
- **No duplicate fetch storms:** history fetched lazily per expanded perspective.
- **Accessible text:** buttons have clear labels; errors are readable.
- **Design consistency:** reuse `Card`-like `border border-border bg-surface` panels already in modal.
</must_haves>

<threat_model>
**ASVS L1 — UI data exposure**

- **Threat:** XSS via rationale — React text nodes auto-escape; do not `dangerouslySetInnerHTML`.
- **Privacy:** Same as descriptions; rationale displayed only to authenticated local user (existing app model).
</threat_model>
