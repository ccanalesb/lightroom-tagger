---
phase: 11
plan: "11-02"
title: "Update ROADMAP.md (Phases 5–9 plan tables) and REQUIREMENTS.md (all 17 v2 requirements)"
type: docs
wave: 2
depends_on: ["11-01"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SCORE-01
  - SCORE-03
  - SCORE-04
  - POST-01
  - POST-02
  - POST-03
  - POST-04
  - IDENT-01
  - IDENT-02
  - IDENT-03
  - DASH-01
---

<objective>
Bring `.planning/ROADMAP.md` in sync with shipped work: every plan row under **Phase 5** through **Phase 9** execution tables shows **`Done`** **without** parenthetical dates (D-05, D-06). Update `.planning/REQUIREMENTS.md` so **all 17** v2 checklist items use **`[x]`** with an explicit validation-note parenthetical (D-03), and the **Traceability** table’s **Status** column reads **`Complete`** for every v2 requirement row (D-03). For requirements touched by Phase 10 bug fixes, validation notes must name **both** the original delivery phase **and** Phase 10 (D-04). Align the **Phase 11** success-criteria bullet in `ROADMAP.md` that currently implies dates with decision **D-05** (no dates in plan tables).
</objective>

<tasks>

### Task 1: Update ROADMAP.md — Phase 5 through Phase 9 plan progress tables

<read_first>
- .planning/ROADMAP.md
- .planning/phases/11-verification-and-documentation-update/11-CONTEXT.md (D-05, D-06)
</read_first>

<action>
Edit **`.planning/ROADMAP.md`** only within the **“Plan progress (execution)”** markdown tables for **Phase 5** through **Phase 9** (sections starting at `## Phase 5 — Structured scoring foundation` through `## Phase 9 — Insights dashboard`).

For **each** data row in those tables whose **Status** cell is exactly `Not started`, replace that cell with:

`| **Done** |`

**Do not** append dates such as `(2026-04-12)` or any other parenthetical — D-05.

**Concrete row coverage (verify every line exists and is updated):**

- **Phase 5 table:** Plans **05-01** through **05-06** (six rows).
- **Phase 6 table:** Plans **06-01** through **06-04** (four rows).
- **Phase 7 table:** Plans **07-01** through **07-04** (four rows).
- **Phase 8 table:** Plans **08-01** through **08-03** (three rows).
- **Phase 9 table:** Plans **09-01** through **09-03** (three rows).

In the **“Phase 11 — Phase 6–9 verification and documentation update”** section, change success criterion **2** from the current wording that requires dates to an explicit statement consistent with D-05, e.g. replace the sentence so it reads: **`ROADMAP.md` plan progress tables for Phases 5–9 reflect `Done` status without per-plan dates.**

Do **not** change v1 Phase 1–4 tables (they correctly retain historical dates like `**Done** (2026-04-10)`).
</action>

<acceptance_criteria>
- For each plan id **05-01, 05-02, 05-03, 05-04, 05-05, 05-06, 06-01, 06-02, 06-03, 06-04, 07-01, 07-02, 07-03, 07-04, 08-01, 08-02, 08-03, 09-01, 09-02, 09-03**, the command `grep "| <id> |" .planning/ROADMAP.md | grep "Not started"` exits **1** (no line with that plan id still has `Not started`). Example: `grep '| 05-01 |' .planning/ROADMAP.md | grep 'Not started'` exits 1.
- `grep -n '| 05-01 |' .planning/ROADMAP.md` exits 0 and the matching line contains `**Done**`.
- `grep -n '| 09-03 |' .planning/ROADMAP.md` exits 0 and the matching line contains `**Done**`.
- `grep -n 'Phase 11 — Phase 6–9 verification' .planning/ROADMAP.md` exits 0 and the Phase 11 success-criteria list includes the exact substring `without per-plan dates`.
- For **each** of the 20 plan ids in Task 1 action, `grep "| <id> |" .planning/ROADMAP.md | grep -E '202[0-9]'` exits **1** (the plan progress table line for v2 phases must not contain a year — D-05).
</acceptance_criteria>

### Task 2: Update REQUIREMENTS.md — v2 checkboxes (17) with validation notes

<read_first>
- .planning/REQUIREMENTS.md
- .planning/phases/11-verification-and-documentation-update/11-CONTEXT.md (D-03, D-04)
</read_first>

<action>
In **`.planning/REQUIREMENTS.md`**, under `## v2 Requirements`, replace **every** checklist line that currently begins with `- [ ] **` for the 17 IDs **SCORE-01** through **DASH-01** (all v2 bullets in that section through **DASH-01**) with `- [x] **` and append a **parenthetical validation note** after the requirement sentence on the same line.

Use these **exact parenthetical strings** (copy verbatim — includes phase numbers and verification file references):

- **SCORE-01:** `(Validated in Phase 6 + Phase 10 fix; see 06-VERIFICATION.md and 10-VERIFICATION.md)`
- **SCORE-02:** `(Validated in Phase 5; see 05-VERIFICATION.md)`
- **SCORE-03:** `(Validated in Phase 6; see 06-VERIFICATION.md)`
- **SCORE-04:** `(Validated in Phase 6 + Phase 10 fix; see 06-VERIFICATION.md and 10-VERIFICATION.md)`
- **SCORE-05:** `(Validated in Phase 5; see 05-VERIFICATION.md)`
- **SCORE-06:** `(Validated in Phase 5; see 05-VERIFICATION.md)`
- **SCORE-07:** `(Validated in Phase 5; see 05-VERIFICATION.md)`
- **POST-01:** `(Validated in Phase 7; see 07-VERIFICATION.md)`
- **POST-02:** `(Validated in Phase 7; see 07-VERIFICATION.md)`
- **POST-03:** `(Validated in Phase 7; see 07-VERIFICATION.md)`
- **POST-04:** `(Validated in Phase 7; see 07-VERIFICATION.md)`
- **IDENT-01:** `(Validated in Phase 8 + Phase 10 fix; see 08-VERIFICATION.md and 10-VERIFICATION.md)`
- **IDENT-02:** `(Validated in Phase 8 + Phase 10 fix; see 08-VERIFICATION.md and 10-VERIFICATION.md)`
- **IDENT-03:** `(Validated in Phase 8 + Phase 10 fix; see 08-VERIFICATION.md and 10-VERIFICATION.md)`
- **JOB-01:** `(Validated in Phase 5; see 05-VERIFICATION.md)`
- **JOB-02:** `(Validated in Phase 5; see 05-VERIFICATION.md)`
- **DASH-01:** `(Validated in Phase 9; see 09-VERIFICATION.md)`

Preserve each requirement’s existing bold ID and summary text; only add `[x]` and the parenthetical.

Update the short paragraph under **Coverage:** near the traceability section (currently “Phases 5–9 delivered; bug fixes Phase 10; verification Phase 11”) if needed so it does not contradict completed verification — minimal edit, e.g. append “— documentation updated Phase 11” only if the sentence would otherwise be false.
</action>

<acceptance_criteria>
- `grep -c '\[x\]' .planning/REQUIREMENTS.md` returns a count **≥ 17** (v2 section uses `[x]`; v1 section may add more — minimum bar: at least 17 `[x]` in file).
- `grep -n '\[ \] \*\*SCORE-01' .planning/REQUIREMENTS.md` exits 1 (no unchecked SCORE-01).
- `grep -n '\[x\] \*\*SCORE-01' .planning/REQUIREMENTS.md` exits 0 and the line contains `06-VERIFICATION.md and 10-VERIFICATION.md`.
- `grep -n '\[x\] \*\*IDENT-03' .planning/REQUIREMENTS.md` exits 0 and the line contains `08-VERIFICATION.md and 10-VERIFICATION.md`.
- `grep -n '\[x\] \*\*DASH-01' .planning/REQUIREMENTS.md` exits 0 and the line contains `09-VERIFICATION.md`.
- `grep -n '\[x\] \*\*JOB-02' .planning/REQUIREMENTS.md` exits 0 and the line contains `05-VERIFICATION.md`.
- Every line matching `grep -n '\*\*POST-0' .planning/REQUIREMENTS.md` for POST-01–04 contains `07-VERIFICATION.md`.
</acceptance_criteria>

### Task 3: Update REQUIREMENTS.md — Traceability table (Status = Complete)

<read_first>
- .planning/REQUIREMENTS.md
</read_first>

<action>
In **`.planning/REQUIREMENTS.md`**, locate the markdown table under `## Traceability` with columns **Requirement | Phase | Status**.

For **each** of these **17** requirement IDs in the first column — **SCORE-02, SCORE-05, SCORE-06, SCORE-07, JOB-01, JOB-02, SCORE-01, SCORE-03, SCORE-04, POST-01, POST-02, POST-03, POST-04, IDENT-01, IDENT-02, IDENT-03, DASH-01** — set the **Status** cell to exactly:

`Complete`

**SCORE-01, SCORE-04, IDENT-01, IDENT-02, IDENT-03** rows: ensure the **Phase** cell text includes **both** the original phase and Phase 10 where the current traceability already indicates that, e.g. keep or normalize to:

`| SCORE-01 | v2 Phase 6 + Phase 10 (bug fix) · verify Phase 11 | Complete |`

(match existing wording style in the table — if Phase column already reads `v2 Phase 6 + Phase 10 (bug fix) · verify Phase 11`, only change **Pending** → **Complete**).

Remove or rewrite any trailing “verify Phase 11” phrasing in the Phase column **only if** it would read awkwardly after completion; acceptable replacement: `v2 Phase 6 + Phase 10 (bug fix)` without the verify clause, **provided** the checkbox line still references verification files.

Update the **Coverage** bullet that counts v2 requirements if it still says “Pending” anywhere — set to reflect **17 / 17** verified / complete per documentation.
</action>

<acceptance_criteria>
- `grep -n '| SCORE-01 |' .planning/REQUIREMENTS.md` exits 0 and the line ends with `| Complete |`.
- `grep -n '| DASH-01 |' .planning/REQUIREMENTS.md` exits 0 and the line ends with `| Complete |`.
- `grep -n '| SCORE-02 |' .planning/REQUIREMENTS.md` exits 0 and the line contains `Complete` not `Pending`.
- `grep -n 'Pending' .planning/REQUIREMENTS.md` exits 1 **or** `grep 'Pending' .planning/REQUIREMENTS.md` shows **only** lines in Future/Out of Scope sections (not v2 traceability rows) — bar: no `| Pending |` in the v2 traceability table lines for the 17 IDs.
- `grep -n '17 / 17' .planning/REQUIREMENTS.md` exits 0 in the Coverage/traceability area (exact fraction text present).
</acceptance_criteria>

</tasks>

<verification>
From repo root:

1. `grep -n 'Not started' .planning/ROADMAP.md` — confirm Phase 5–9 plan tables no longer use `Not started` (per Task 1 acceptance).

2. `grep -E '\[ \].*\*\*(SCORE|POST|IDENT|JOB|DASH)-' .planning/REQUIREMENTS.md` — exit 1 (no unchecked v2 requirement IDs with that pattern).

3. `awk '/## Traceability/,/^## /' .planning/REQUIREMENTS.md | grep -E 'SCORE-|POST-|IDENT-|JOB-|DASH-' | grep -c Complete` — must equal **17** (or visually confirm 17 rows show `Complete`).

4. Manual read of Phase 11 success-criterion #2 in `ROADMAP.md` — confirms “without per-plan dates” wording.
</verification>

<must_haves>
- ROADMAP Phase 5–9 plan tables: all listed plans show `**Done**` with no date parentheticals (D-05).
- REQUIREMENTS v2: 17 checkboxes `[x]` with validation parentheticals exactly as specified in Task 2 for D-03/D-04.
- REQUIREMENTS traceability: 17 v2 rows show `Status` **Complete** (D-03).
- ROADMAP Phase 11 observable success criterion #2 text matches D-05 (no requirement for dates in plan tables).
</must_haves>
