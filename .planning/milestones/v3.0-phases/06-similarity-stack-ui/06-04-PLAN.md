---
phase: 06-similarity-stack-ui
plan: 4
type: execute
wave: 4
depends_on: ["06-03-PLAN"]
files_modified:
  - apps/visualizer/frontend/src/services/api.ts
  - apps/visualizer/frontend/src/constants/strings.ts
  - apps/visualizer/frontend/src/components/images/CatalogTab.tsx
  - apps/visualizer/frontend/src/components/identity/BestPhotosGrid.tsx
  - apps/visualizer/frontend/src/components/image-view/ImageDetailModal.tsx
  - apps/visualizer/frontend/src/components/identity/BestPhotosGrid.test.tsx
  - apps/visualizer/frontend/src/components/images/__tests__/CatalogTab.test.tsx
autonomous: true
requirements:
  - SIM-02
  - STACK-03
user_setup: []
must_haves:
  truths:
    - "User can open a catalog image and trigger More like this; Visually similar section uses UI-SPEC copy and loading/error from 06-UI-SPEC"
    - "Stack representative shows {n} in stack badge and Show stack expands member strip; strings live in strings.ts with exact keys named in action"
  artifacts:
    - path: apps/visualizer/frontend/src/constants/strings.ts
      provides: CATALOG_* stack/similarity strings
  key_links:
    - from: ImageDetailModal
      to: "GET /api/images/catalog/{key}/similar"
      via: ImagesAPI.getCatalogSimilar
---

<objective>
Deliver **SIM-02** and **STACK-03** UI: catalog + best photos stack badges/expand, **More like this** in image detail, **Visually similar** results grid, reusable `ImagesAPI` method for Phase 7 (D-02/D-03: no pin UI on SearchPage).

Purpose: Meets phase goal: reachable “more like this” and stack browse without breaking list performance (lazy members fetch).

Output: React changes + tests + types; **no** user-visible `SearchPage.tsx` changes (only shared types in `api.ts` is OK).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@apps/visualizer/frontend/DESIGN.md
</execution_context>

<context>
@.planning/phases/06-similarity-stack-ui/06-UI-SPEC.md (Copywriting, SIM-02, STACK-03, a11y)
@apps/visualizer/frontend/src/components/image-view/ImageTile.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: API types + ImagesAPI.getCatalogSimilar + strings</name>
  <read_first>
    - apps/visualizer/frontend/src/services/api.ts (`ImagesAPI`, `CatalogImage` ~909+)
    - apps/visualizer/frontend/src/constants/strings.ts (pattern for CATALOG_ / IDENTITY_ constants)
  </read_first>
  <files>apps/visualizer/frontend/src/services/api.ts, apps/visualizer/frontend/src/constants/strings.ts</files>
  <action>
  1. Extend **`CatalogImage`** with optional: `stack_id?: number | null`, `stack_member_count?: number | null`, `is_stack_representative?: boolean | null` (or `0|1` if API sends integer — **match backend JSON** from 06-03).
  2. Add **exported** method on `ImagesAPI` (D-04 generic name, reusable by SearchPage in Phase 7):
     `getCatalogSimilar: (key: string, params?: { limit?: number; offset?: number } &amp; …catalog filters… ) =>` returning `Promise&lt;{ images: CatalogImage[]; total: number; meta: { clip_model_id: string; clip_embed_dim: number } }&gt;` with request path EXACT: ``GET `/images/catalog/${encodeURIComponent(key)}/similar${qs}` `` (relative to API base) — must match backend route from 06-03.
  3. Add `getStackMembers: (stackId: number) =>` returning `Promise&lt;{ items: CatalogImage[] }&gt;` for ``GET /images/stacks/${stackId}/members`` (backend base path 06-03; align segment order with `rg` on backend file).
  4. In **`constants/strings.ts`**, add these **exact** export names and default English text (from 06-UI-SPEC):
     - `CATALOG_SIMILAR_MORE_LIKE_THIS` = "More like this"
     - `CATALOG_STACK_SHOW` = "Show stack"
     - `CATALOG_STACK_HIDE` = "Hide stack"
     - `CATALOG_SIMILAR_SECTION_TITLE` = "Visually similar"
     - `CATALOG_STACK_COUNT_BADGE` as a function or template: display "{n} in stack" (export helper `formatStackCountBadge(n: number) => string` if preferred)
     - `CATALOG_SIMILAR_EMPTY_HEADING` = "No close matches"
     - `CATALOG_SIMILAR_EMPTY_BODY` = "No other photos scored high enough in this model’s visual space. Try another photo or re-run the image embedding job if this catalog is new."
     - `CATALOG_SIMILAR_NO_EMBED_TITLE` = "Visual similarity is unavailable"
     - `CATALOG_SIMILAR_NO_EMBED_BODY` = "This photo has no image embedding yet. Run the “Embed images” (or your product name for `batch_embed_image`) job, then try again."
     - `CATALOG_SIMILAR_FETCH_ERROR` = "Couldn’t load similar photos"
     - `CATALOG_STACK_MEMBERS_ERROR` = "Couldn’t load stack members"
     - `CATALOG_STACK_MEMBERS_LOADING` = "Loading…"
     - `CATALOG_STACK_MEMBERS_REGION_ARIA` = "Stack members"
  5. No new **npm** dependencies.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend && npx tsc --noEmit 2>&1; echo EXIT:$?</automated>
  </verify>
  <acceptance_criteria>
  - `rg "getCatalogSimilar" apps/visualizer/frontend/src/services/api.ts` exits 0.
  - `rg "CATALOG_SIMILAR_MORE_LIKE_THIS" apps/visualizer/frontend/src/constants/strings.ts` exits 0.
  - `tsc --noEmit` **exit code 0**.
  - `rg "/similar" apps/visualizer/frontend/src/services/api.ts` exits 0.
  </acceptance_criteria>
  <done>Types and client methods ready for components; SIM-02 plumbing callable from future Search page.</done>
</task>

<task type="auto">
  <name>Task 2: BestPhotosGrid + CatalogTab stack UI + a11y</name>
  <read_first>
    - apps/visualizer/frontend/src/components/identity/BestPhotosGrid.tsx
    - apps/visualizer/frontend/src/components/images/CatalogTab.tsx
    - apps/visualizer/frontend/src/components/image-view/ImageTile.tsx
    - apps/visualizer/frontend/src/constants/strings.ts (new constants from Task 1)
  </read_first>
  <files>apps/visualizer/frontend/src/components/identity/BestPhotosGrid.tsx, apps/visualizer/frontend/src/components/images/CatalogTab.tsx</files>
  <action>
  **best_photos / catalog tiles:** When `is_stack_representative === true` and `stack_member_count &gt; 1` (or `&gt;=` as per UI-SPEC: badge only if `&gt; 1`), show **count badge** in `ImageTile` `overlayBadges` with `formatStackCountBadge(stack_member_count)` (e.g. "3 in stack") using `Badge` variant **per UI-SPEC** — use **default** or **accent** consistently in **both** files (one choice, grep must find same `variant=` in both for stack badge, or a shared `StackCountBadge` inline component in one file only if DRY is needed; do not introduce new unapproved packages).
  **Show stack / Hide stack** text `Button` in `footer` with `aria-expanded` tied to expanded state, `aria-controls` id pointing to a **member strip** with `role="region"` and `aria-label` from `CATALOG_STACK_MEMBERS_REGION_ARIA`.
  On first expand, call `ImagesAPI.getStackMembers(stack_id)`; cache in component `useState` keyed by `stack_id`; on failure show `CATALOG_STACK_MEMBERS_ERROR` with `role="alert"`.
  Member strip: horizontal flex, `gap-2` or `gap-3`, `ImageTile` for members at smaller `max-w-` (per UI-SPEC); no second stack badge on members.
  **No SearchPage** edits.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend && npm run test -- --run BestPhotosGrid CatalogTab 2>&1; echo EXIT:$?</automated>
  </verify>
  <acceptance_criteria>
  - `rg "CATALOG_STACK_SHOW" apps/visualizer/frontend/src/components/identity/BestPhotosGrid.tsx` exits 0.
  - `rg "CATALOG_STACK_SHOW" apps/visualizer/frontend/src/components/images/CatalogTab.tsx` exits 0.
  - `rg "aria-expanded" apps/visualizer/frontend/src/components/identity/BestPhotosGrid.tsx` exits 0.
  - `rg "getStackMembers" apps/visualizer/frontend/src/components/identity/BestPhotosGrid.tsx` OR CatalogTab exit 0 (at least one of the two).
  - Vitest run exit code 0; if tests are missing, add/extend `BestPhotosGrid.test.tsx` / `CatalogTab.test.tsx` with `jest-dom`/`vitest` assertions on **Show stack** + mock API (minimum 1 test per file touched or 2 tests in one file with clear describe block).
  </acceptance_criteria>
  <done>STACK-03 expand/collapse works on both grids; strings centralized.</done>
</task>

<task type="auto">
  <name>Task 3: ImageDetailModal — More like this + Visually similar</name>
  <read_first>
    - apps/visualizer/frontend/src/components/image-view/ImageDetailModal.tsx
    - apps/visualizer/frontend/src/services/api.ts (`ImagesAPI.getCatalogSimilar`)
    - .planning/phases/06-similarity-stack-ui/06-UI-SPEC.md (SIM-02, errors, loading "Finding similar…" — add `CATALOG_SIMILAR_LOADING` = "Finding similar…" in strings if not already)
  </read_first>
  <files>apps/visualizer/frontend/src/components/image-view/ImageDetailModal.tsx, apps/visualizer/frontend/src/constants/strings.ts</files>
  <action>
  1. Add `CATALOG_SIMILAR_LOADING` = "Finding similar…" to `strings.ts` if missing.
  2. Add secondary **`Button`**: label `CATALOG_SIMILAR_MORE_LIKE_THIS` on the modal body for the current catalog `image.key`; on click set loading state, call `ImagesAPI.getCatalogSimilar(key, { limit: 24, offset: 0 })` (or limit 20).
  3. If API returns 404 and error text contains "Visual similarity is unavailable" OR catch Error from client with that substring, show **CATALOG_SIMILAR_NO_EMBED_TITLE** and **CATALOG_SIMILAR_NO_EMBED_BODY** in `text-error` / `role="alert"` per UI-SPEC.
  4. If success, render a **`h3` or `h2`** with exact text from `CATALOG_SIMILAR_SECTION_TITLE` and a grid of `ImageTile` (reuse same variant/gaps as catalog `gap-4`); for each result show `similarity` as **NN%** under tile: `text-xs text-text-secondary` (compute `Math.round((row.similarity ?? 0) * 100)`; add optional `similarity?: number` on `CatalogImage` in api.ts if server sends it with key name `similarity`).
  5. Empty `images.length === 0` with 200: show CATALOG_SIMILAR_EMPTY_HEADING + CATALOG_SIMILAR_EMPTY_BODY.
  6. Do **not** add chat pin; do **not** import `SearchPage`.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend && npx tsc --noEmit && npm run test -- --run ImageDetail 2>&1; echo EXIT:$?</automated>
  </verify>
  <acceptance_criteria>
  - `rg "getCatalogSimilar" apps/visualizer/frontend/src/components/image-view/ImageDetailModal.tsx` exits 0.
  - `rg "CATALOG_SIMILAR_SECTION_TITLE" apps/visualizer/frontend/src/components/image-view/ImageDetailModal.tsx` exits 0.
  - `tsc --noEmit` exit 0.
  - `rg "SearchPage" apps/visualizer/frontend/src/components/image-view/ImageDetailModal.tsx` exits **1** (no pin to search).
  </acceptance_criteria>
  <done>SIM-02 discoverable from catalog image detail; Phase 7 can reuse `getCatalogSimilar` only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `/api` | Fetches use same `fetch` + JSON; no new secrets. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | I | `image_key` in URL from UI | accept | `encodeURIComponent` in client; no eval. |
| T-06-04-02 | S | API errors displayed | mitigate | Use server error strings; no `dangerouslySetInnerHTML`. |
| T-06-04-03 | S | Thumbnails | accept | Reuse `thumbnail_url` from API as existing catalog. |
| T-06-04-04 | D | Accidental DDoS | accept | single fetch on button click, not loops. |
</threat_model>

<verification>frontend `tsc --noEmit` and targeted vitest; `npm run lint` if in CI.</verification>

<success_criteria>Phase 6 goal met in UI: stack badges + expand, similar flow from detail modal, reusable API export.</success_criteria>

<output>After completion, create `.planning/phases/06-similarity-stack-ui/06-04-SUMMARY.md`</output>
