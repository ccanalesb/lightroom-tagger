---
phase: 06-similarity-stack-ui
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - lightroom_tagger/core/database.py
  - lightroom_tagger/core/identity_service.py
  - lightroom_tagger/core/test_database_stack_collapse.py
autonomous: true
requirements:
  - STACK-03
user_setup: []
must_haves:
  truths:
    - "Catalog list SQL returns at most one row per stack (representative or solo) and includes stack_id, stack_member_count, is_stack_representative on each row"
    - "Best Photos ranked list applies the same primary-row rule so member-only keys never occupy the paginated list"
  artifacts:
    - path: lightroom_tagger/core/database.py
      provides: query_catalog_images stack collapse + stack JOIN columns
    - path: lightroom_tagger/core/identity_service.py
      provides: rank_best_photos non-rep filter + stack fields on items
  key_links:
    - from: query_catalog_images WHERE clause
      to: image_stacks.representative_key
      via: "hide m.image_key where m.image_key != representative_key"
---

<objective>
Implement **STACK-03** data-layer: stack metadata on catalog and best-photos rows, and **list-collapse** so only stack representatives (or images not in a multi-member stack) appear in `query_catalog_images` and `rank_best_photos` primary lists, per D-04/D-07 and `06-UI-SPEC.md` (one representative tile per default grid).

Purpose: Unblocks correct counts/pagination and consistent badge data before the CLIP similar API and UI layers.

Output: Updated DB query paths + tests with grep-verified assertions.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-similarity-stack-ui/06-CONTEXT.md
@.planning/phases/06-similarity-stack-ui/06-RESEARCH.md
@.planning/phases/06-similarity-stack-ui/06-UI-SPEC.md
@.planning/phases/06-similarity-stack-ui/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse + stack metadata in query_catalog_images / by_keys</name>
  <read_first>
    - lightroom_tagger/core/database.py (entire `query_catalog_images` and `query_catalog_images_by_keys` functions, lines ~1161–1431, plus `_deserialize_row` ~212+)
    - .planning/phases/06-similarity-stack-ui/06-UI-SPEC.md (Data &amp; API surface, STACK-03 collapse item 4)
  </read_first>
  <files>lightroom_tagger/core/database.py</files>
  <action>
  1. Add **LEFT JOIN** pattern (both list and by-keys paths):
     - `LEFT JOIN image_stack_members AS m_st ON m_st.image_key = i.key`
     - `LEFT JOIN image_stacks AS st ON st.stack_id = m_st.stack_id`
  2. Add **collapse WHERE fragment** to the existing `clauses` list in `query_catalog_images` (same AND span as other filters), exact SQL:
     `(m_st.image_key IS NULL OR i.key = st.representative_key)` — so non-representative members of any stack are excluded from the primary list.
  3. Extend **SELECT** in both `query_catalog_images` and `query_catalog_images_by_keys` (same column set) with:
     - `st.stack_id AS stack_id`
     - `st.stack_size AS stack_member_count`
     - `CASE WHEN st.stack_id IS NOT NULL AND i.key = st.representative_key THEN 1 ELSE 0 END AS is_stack_representative` (or boolean equivalent SQLite accepts; document if INTEGER 0/1)
  4. For **not-in-stack** rows, `stack_id` / `stack_member_count` must be NULL and `is_stack_representative` 0; for representative rows with `stack_size &gt; 1`, expose real `stack_id` and `stack_size` (member count) per D-04 field names.
  5. Do **not** add new Python dependencies; keepParameterized bindings only; no string concatenation of user text into SQL.
  6. Optional export: add a small public helper `def catalog_key_is_primary_grid_row(db, image_key: str) -&gt; bool` returning False iff `EXISTS (SELECT 1 FROM image_stack_members m JOIN image_stacks s ... WHERE m.image_key = ? AND m.image_key &lt;&gt; s.representative_key)` — used by later plans for similar-search filtering; if added, place next to other catalog helpers in `database.py`.
  7. Create `lightroom_tagger/core/test_database_stack_collapse.py` with the tests referenced in **acceptance_criteria** (in-memory or `tmp_path` DB using existing `init_database` patterns from `lightroom_tagger/core/test_database.py`). Insert test fixtures for at least one `image_stacks` row with `representative_key` and two `image_stack_members` rows.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest lightroom_tagger/core/test_database_stack_collapse.py -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
  - `rg "m_st.image_key IS NULL OR i.key = st.representative_key" lightroom_tagger/core/database.py` exits 0 (exact collapse predicate in file).
  - `rg "stack_member_count" lightroom_tagger/core/database.py` exits 0.
  - `rg "st.stack_id AS stack_id" lightroom_tagger/core/database.py` exits 0.
  - `pytest lightroom_tagger/core/test_database_stack_collapse.py` exits 0 with at least 2 tests: (a) two stack members with same `stack_id` only the representative row appears in `query_catalog_images` for default params; (b) total count from `query_catalog_images` matches number of returned primary rows for that fixture.
  </acceptance_criteria>
  <done>Catalog and by-keys SQL paths return stack fields and hide non-representative members; new tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: rank_best_photos — drop non-reps + stack fields</name>
  <read_first>
    - lightroom_tagger/core/identity_service.py (`rank_best_photos` ~285–327 and `enriched` construction)
    - lightroom_tagger/core/database.py (image_stacks / image_stack_members schema from `_migrate_image_stacks`)
  </read_first>
  <files>lightroom_tagger/core/identity_service.py, lightroom_tagger/core/test_database_stack_collapse.py</files>
  <behavior>
  - If eligible list contains a key that is a **non-representative** `image_stack_members` row, that entry is **removed** before `total` and pagination slice.
  - Each kept row includes optional keys `stack_id: int | None`, `stack_member_count: int | None` (use `stack_size` from `image_stacks` when in stack), `is_stack_representative: bool` (True only for `i.key == representative_key` when in a stack).
  </behavior>
  <action>
  After the `enriched` list is built and **before** the `posted` filter (if that changes ordering, apply stack filter first on eligible keys, then reapply posted the same as today): remove any item whose `image_key` appears in SQL `SELECT m.image_key FROM image_stack_members m INNER JOIN image_stacks s ON s.stack_id = m.stack_id WHERE m.image_key &lt;&gt; s.representative_key` for the set of keys in the current `enriched` list (one batched query with `IN (...)` is fine).
  For remaining rows, run one batched lookup: for each `image_key` LEFT JOIN to stacks via `image_stack_members` to attach `stack_id`, `stack_size` as `stack_member_count`, and `is_stack_representative` as `(image_key == representative_key)`.
  Preserve existing return shape keys (`image_key`, `aggregate_score`, etc.); add the three new optional fields.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest lightroom_tagger/core/test_database_stack_collapse.py -q --tb=line 2>&1; echo EXIT:$?</automated>
  </verify>
  <acceptance_criteria>
  - `rg "stack_member_count" lightroom_tagger/core/identity_service.py` exits 0.
  - `rg "representative_key" lightroom_tagger/core/identity_service.py` exits 0.
  - New or extended test proves a non-representative member with high aggregate score does **not** appear in `rank_best_photos` page.
  - Full `pytest lightroom_tagger/core/test_database_stack_collapse.py` still exits 0.
  </acceptance_criteria>
  <done>Best Photos pipeline matches catalog collapse semantics; fields present for API serialization in plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library SQLite | All queries use bound parameters; no new user-controlled SQL. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | T | `query_catalog_images` | mitigate | Only parameterized WHERE/JOIN; collapse uses fixed template fragment. |
| T-06-01-02 | I | N/A (no new inputs) | accept | No new attack surface. |
| T-06-01-03 | E | Stack metadata exposure | accept | Local visualizer; same DB as existing catalog. |
</threat_model>

<verification>pytest for new `test_database_stack_collapse.py` + non-regression for identity if touched.</verification>

<success_criteria>STACK-03 data semantics implemented in `database.py` and `identity_service.py` with automated tests.</success_criteria>

<output>After completion, create `.planning/phases/06-similarity-stack-ui/06-01-SUMMARY.md`</output>
