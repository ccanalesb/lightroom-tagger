---
phase: 08-embedding-prefilter-and-cache-pipeline
plan: 2
type: execute
wave: 2
depends_on:
  - 1
files_modified:
  - apps/visualizer/backend/jobs/handlers.py
  - apps/visualizer/backend/jobs/checkpoint.py
  - apps/visualizer/backend/tests/test_handlers_single_match.py
autonomous: true
requirements:
  - MATCH-02
must_haves:
  truths:
    - "`handle_vision_match` clamps `metadata['clip_top_k']` into 1..500 and passes the clamped int to `match_dump_media(..., clip_top_k=...)` and `fingerprint_vision_match(..., clip_top_k=...)`"
    - "Throttled batch logs emit the template substring `date_window_in=` with `clip_shortlist_out=` and `judgments=` on the same line (D-07)"
  artifacts:
    - path: apps/visualizer/backend/jobs/checkpoint.py
      provides: resume identity includes clip_top_k
    - path: apps/visualizer/backend/jobs/handlers.py
      provides: vision_match metadata plumbing + throttled prefilter summaries + cumulative result counters
    - path: apps/visualizer/backend/tests/test_handlers_single_match.py
      provides: clamp, fingerprint delta, summary log regex coverage
  key_links:
    - from: match_dump_media clip_top_k
      to: handle_vision_match
      via: "keyword `clip_top_k=` in call"
    - from: batch_progress_callback / on_media_complete counters
      to: add_job_log summary
      via: "_VISION_MATCH_PREFILTER_SUMMARY_EVERY + formatter"
---

<objective>
Plumb `clip_top_k` from job metadata through `handle_vision_match`, extend `fingerprint_vision_match` for resume safety, clamp client input to 1..500, and emit D-07 throttled per-batch job log lines plus cumulative `complete_job` result keys for prefilter analytics.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-embedding-prefilter-and-cache-pipeline/08-CONTEXT.md
@.planning/phases/08-embedding-prefilter-and-cache-pipeline/08-RESEARCH.md
@.planning/phases/08-embedding-prefilter-and-cache-pipeline/08-VALIDATION.md
@.cursor/rules/job-log-contract.mdc
@.cursor/rules/gsd-live-validation.mdc
@.cursor/rules/backend-restart.mdc
</context>

<tasks>

<task type="auto">
  <name>Extend fingerprint_vision_match with clip_top_k</name>
  <files>apps/visualizer/backend/jobs/checkpoint.py, apps/visualizer/backend/tests/test_handlers_single_match.py</files>
  <read_first>
    apps/visualizer/backend/jobs/checkpoint.py
    apps/visualizer/backend/jobs/handlers.py
    apps/visualizer/backend/tests/test_handlers_single_match.py
    .planning/phases/08-embedding-prefilter-and-cache-pipeline/08-PATTERNS.md
  </read_first>
  <action>
  Add keyword-only parameter `clip_top_k: int = 50` to `fingerprint_vision_match` so existing callsites remain valid until Plan 2 task 2 updates them. Include `"clip_top_k": int(clip_top_k)` in the canonical `payload` dict before `json.dumps(..., sort_keys=True, separators=(",", ":"))`.
  Do not edit `handlers.py` in this task.
  Add a pytest case in `apps/visualizer/backend/tests/test_handlers_single_match.py` named with `fingerprint` in its identifier (e.g. `test_fingerprint_vision_match_includes_clip_top_k`) that asserts `fingerprint_vision_match(..., clip_top_k=50) != fingerprint_vision_match(..., clip_top_k=200)` with all other kwargs identical, so the `-k fingerprint` selector exercises this change.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest apps/visualizer/backend/tests/test_handlers_batch_embed_image.py apps/visualizer/backend/tests/test_handlers_single_match.py -k fingerprint -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
  - `grep -n 'clip_top_k' apps/visualizer/backend/jobs/checkpoint.py` shows the key inside `fingerprint_vision_match`'s payload.
  - `fingerprint_vision_match` signature includes default `clip_top_k=50` (grep `def fingerprint_vision_match` continuation lines or `*, clip_top_k: int = 50`).
  - A pytest case in `test_handlers_single_match.py` (or `test_handlers_batch_embed_image.py` if a fingerprint helper test already lives there) asserts `fingerprint_vision_match(..., clip_top_k=50) != fingerprint_vision_match(..., clip_top_k=200)` with all other kwargs equal — collected by `-k fingerprint`.
  </acceptance_criteria>
  <done>Resume identity distinguishes shortlist sizes.</done>
</task>

<task type="auto">
  <name>Plumb clip_top_k + D-07 summaries + result keys in handle_vision_match</name>
  <files>apps/visualizer/backend/jobs/handlers.py</files>
  <read_first>
    apps/visualizer/backend/jobs/handlers.py
    lightroom_tagger/scripts/match_instagram_dump.py
    apps/visualizer/backend/jobs/checkpoint.py
    .planning/phases/08-embedding-prefilter-and-cache-pipeline/08-RESEARCH.md
    .cursor/rules/job-log-contract.mdc
  </read_first>
  <action>
  In `handle_vision_match` after metadata extraction:
  - Read `raw = metadata.get('clip_top_k', 50)`; coerce with `int(float(raw))` inside try/except; on failure use `50`.
  - Clamp: `clip_top_k = max(1, min(raw_int, 500))`.
  Pass `clip_top_k` into `fingerprint_vision_match(..., clip_top_k=clip_top_k)` (update callsite) and `match_dump_media(..., clip_top_k=clip_top_k)`.
  Introduce module constant `_VISION_MATCH_PREFILTER_SUMMARY_EVERY = 40` (batch counter for media processed).
  Accumulate running totals across the job: `clip_prefilter_candidates_in`, `clip_prefilter_shortlist_total`, `vision_judgments_total` (derive judgment count from existing stats returned by `match_dump_media` or by counting scored pairs — use the same field names as `08-RESEARCH.md` Open Questions: `clip_prefilter_candidates_in`, `clip_prefilter_shortlist_total`, `vision_judgments_total`).
  On each batch tick (reuse existing `batch_progress_callback` if present), when `processed_since_last_summary >= _VISION_MATCH_PREFILTER_SUMMARY_EVERY` or job end, call `add_job_log` with **exact** level `info` and message body matching this template (single line, three integers):
  `vision-match-prefilter-summary date_window_in={N} clip_shortlist_out={M} judgments={J}`
  where N/M/J are the delta or cumulative counts per your implementation, but the substring `date_window_in=`, `clip_shortlist_out=`, and `judgments=` must all appear in the message.
  On successful job completion, include the three cumulative totals in `runner.complete_job` / result dict keys exactly: `clip_prefilter_candidates_in`, `clip_prefilter_shortlist_total`, `vision_judgments_total`.
  **Live validation:** No new HTTP routes and no new LLM endpoints; document in SUMMARY that regression is unit/integration only per `gsd-live-validation.mdc`.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest apps/visualizer/backend/tests/test_handlers_single_match.py -k "clip_top_k_bounds or summary_log or prefilter" -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
  - `grep -n 'clip_top_k' apps/visualizer/backend/jobs/handlers.py` shows pass-through into `match_dump_media`.
  - Test `metadata={'clip_top_k': 0}` results in effective `clip_top_k` 1 (clamped); `9999` clamps to `500`.
  - Test captures `add_job_log` text matching regex `date_window_in=\d+ clip_shortlist_out=\d+ judgments=\d+` with prefix `vision-match-prefilter-summary`.
  - `grep -n 'vision_judgments_total' apps/visualizer/backend/jobs/handlers.py` ≥1.
  - `fingerprint_vision_match(..., clip_top_k=50) != fingerprint_vision_match(..., clip_top_k=200)` with other kwargs identical (assert in `test_handlers_single_match.py`).
  </acceptance_criteria>
  <done>vision_match honors clip_top_k, logs throttled summaries, and exposes cumulative result keys.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | T | `clip_top_k` metadata | mitigate | Server-side clamp 1..500 before KNN and fingerprint; never trust raw client int. |
| T-08-02-02 | R | Checkpoints | mitigate | `fingerprint_vision_match` includes `clip_top_k` so resumed jobs cannot mix shortlist sizes. |
| T-08-02-03 | I | Job logs | mitigate | Summaries use counts only; avoid dumping candidate key lists in `info` logs. |
| T-08-02-04 | D | Log volume | mitigate | Throttle via `_VISION_MATCH_PREFILTER_SUMMARY_EVERY`; no per-image `info` prefilter lines. |
</threat_model>

<verification>
Handler tests prove clamping, fingerprint inequality, and throttled summary log regex; result keys present in completion path.
</verification>

<success_criteria>
MATCH-02 is operable from the job layer with observable D-07 summaries and safe resume semantics for `clip_top_k`.
</success_criteria>

<output>After completion, create `.planning/phases/08-embedding-prefilter-and-cache-pipeline/08-02-SUMMARY.md`.</output>
