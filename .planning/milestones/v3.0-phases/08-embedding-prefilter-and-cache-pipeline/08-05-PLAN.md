---
phase: 08-embedding-prefilter-and-cache-pipeline
plan: 5
type: execute
wave: 5
depends_on:
  - 2
  - 4
files_modified:
  - apps/visualizer/frontend/src/components/processing/MatchingTab.tsx
  - apps/visualizer/frontend/src/constants/strings.ts
  - apps/visualizer/frontend/src/components/processing/__tests__/MatchingTab.test.tsx
autonomous: true
requirements:
  - MATCH-02
  - CACHE-01
must_haves:
  truths:
    - "MatchingTab posts integer `clip_top_k` (default 50, UI bounds 1..500) on `JobsAPI.create('vision_match', metadata)`"
    - "`MatchingTab.tsx` contains zero imports or JSX references to `CatalogSimilarityGroupsPreview` and no 'Catalog Discovery Jobs' title string"
  artifacts:
    - path: apps/visualizer/frontend/src/constants/strings.ts
      provides: CLIP top-k label, helper, validation error, cache-tab pointer copy
    - path: apps/visualizer/frontend/src/components/processing/MatchingTab.tsx
      provides: top-k control + D-06 removal of cache-job card
    - path: apps/visualizer/frontend/src/components/processing/__tests__/MatchingTab.test.tsx
      provides: metadata + removal regressions
  key_links:
    - from: clip_top_k UI state
      to: JobsAPI.create
      via: "metadata key exactly `clip_top_k`"
    - from: ProcessingPage tab route
      to: informational link
      via: "`/processing?tab=cache` (tab id `cache` in `ProcessingPage.tsx`)"
---

<objective>
Add the D-02/D-06 Matching tab changes: numeric `clip_top_k` bound to `vision_match` metadata, remove the entire Catalog Discovery Jobs card (stack detect, similarity, preview), and add optional one-line navigation copy pointing operators to the Catalog cache tab per 08-UI-SPEC.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-embedding-prefilter-and-cache-pipeline/08-UI-SPEC.md
@.planning/phases/08-embedding-prefilter-and-cache-pipeline/08-CONTEXT.md
@.cursor/rules/frontend-design.md
@.cursor/rules/dry-callsite-sweep.mdc
</context>

<tasks>

<task type="auto">
  <name>Centralize Matching tab Phase 8 strings</name>
  <files>apps/visualizer/frontend/src/constants/strings.ts</files>
  <read_first>
    apps/visualizer/frontend/src/constants/strings.ts
    .planning/phases/08-embedding-prefilter-and-cache-pipeline/08-UI-SPEC.md
  </read_first>
  <action>
  Add exported constants (exact user-visible text per 08-UI-SPEC):
  - `MATCHING_CLIP_TOP_K_LABEL` = `CLIP shortlist size (top-k)`
  - `MATCHING_CLIP_TOP_K_HELPER` = sentence including `default 50`, `cosine shortlist`, and `recall vs cost` (exact wording in English, one line).
  - `MATCHING_CLIP_TOP_K_ERROR` = `Enter a value between 1 and 500.`
  - `MATCHING_CATALOG_CACHE_POINTER` = line containing the substring `Catalog cache` pointing users to stack/similarity jobs (08-UI-SPEC optional link).
  If `PROCESSING_OPEN_JOB_QUEUE` exists, reuse for any queue link; do not duplicate.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend && npm run lint -- src/constants/strings.ts</automated>
  </verify>
  <acceptance_criteria>
  - `grep -n 'MATCHING_CLIP_TOP_K_LABEL' apps/visualizer/frontend/src/constants/strings.ts` returns a match.
  - `grep -n 'Enter a value between 1 and 500' apps/visualizer/frontend/src/constants/strings.ts` returns exactly the error constant value.
  </acceptance_criteria>
  <done>All new Matching copy lives in strings.ts.</done>
</task>

<task type="auto">
  <name>MatchingTab clip_top_k + remove Catalog Discovery card</name>
  <files>apps/visualizer/frontend/src/components/processing/MatchingTab.tsx</files>
  <read_first>
    apps/visualizer/frontend/src/components/processing/MatchingTab.tsx
    apps/visualizer/frontend/src/components/matching/AdvancedOptions.tsx
    apps/visualizer/frontend/src/services/api.ts
    apps/visualizer/frontend/src/constants/strings.ts
    .planning/phases/08-embedding-prefilter-and-cache-pipeline/08-UI-SPEC.md
  </read_first>
  <action>
  - Add React state `clipTopK` initialized to `50`.
  - Between the date filter block and `AdvancedOptions`, render a labeled numeric `<input>` with `inputMode="numeric"`, `min={1}`, `max={500}`, `value={clipTopK}`, `onChange` parsing integers, clamping on blur to 1..500.
  - On "Start Vision Matching" / `startMatching`, if raw value outside 1..500, set inline error using `MATCHING_CLIP_TOP_K_ERROR` and do not call API.
  - Include `clip_top_k: clipTopK` (number) in the `metadata` object passed to `JobsAPI.create('vision_match', metadata)`.
  - **Remove** the full `Card` titled `Catalog Discovery Jobs`, functions `startStackDetection`, `startCatalogSimilarity`, related `useState`/`useQuery` for catalog similarity groups, and `import` of `CatalogSimilarityGroupsPreview`.
  - Add a single informational row with `MATCHING_CATALOG_CACHE_POINTER` linking to `/processing?tab=cache` (canonical catalog cache tab id in `ProcessingPage.tsx`).
  Keep `AdvancedOptions` usage DRY — do not fork advanced controls.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend && npm run test -- --run src/components/processing/__tests__/MatchingTab.test.tsx</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c 'CatalogSimilarityGroupsPreview' apps/visualizer/frontend/src/components/processing/MatchingTab.tsx` outputs `0`.
  - `grep -c 'Catalog Discovery Jobs' apps/visualizer/frontend/src/components/processing/MatchingTab.tsx` outputs `0`.
  - `rg -nF 'Catalog Discovery' apps/visualizer/frontend/src/components/processing/MatchingTab.tsx apps/visualizer/frontend/src/constants/strings.ts` returns 0 matches (orphan copy is fully removed; if the deletion leaves any `MATCHING_CATALOG_DISCOVERY_*` exports unused they must also be deleted from `strings.ts`).
  - `rg -nE 'MATCHING_(CATALOG_DISCOVERY|SIMILARITY_GROUPS)_' apps/visualizer/frontend/src/constants/strings.ts` returns 0 matches (or only matches that are still referenced from a non-MatchingTab surface — list any such references in the SUMMARY.md as intentional).
  - `grep -n 'clip_top_k' apps/visualizer/frontend/src/components/processing/MatchingTab.tsx` shows metadata field in job create path.
  - Test case asserts `JobsAPI.create` called with object containing `clip_top_k: 50` (or chosen test value) — name test `sends clip_top_k in metadata` or similar.
  - `input` for top-k has `min="1"` and `max="500"` in JSX (or `{1}` / `{500}`).
  </acceptance_criteria>
  <done>Matching tab matches D-02 and D-06 with regression tests.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | T | Client-supplied `clip_top_k` | mitigate | Browser validates 1..500 before POST; server clamps per Plan 2. |
| T-08-05-02 | I | Orphan UI vs backend | mitigate | Remove dead imports and strings; tests fail if CTAs reappear. |
| T-08-05-03 | R | User understanding | mitigate | Helper text explains shortlist purpose; link to Catalog cache for cache jobs. |
</threat_model>

<verification>
Vitest proves metadata wiring, bounds, and absence of removed discovery UI.
</verification>

<success_criteria>
MATCH-02 is controllable from the UI; CACHE-01 D-06 removes cache-job triggers from Matching.
</success_criteria>

<output>After completion, create `.planning/phases/08-embedding-prefilter-and-cache-pipeline/08-05-SUMMARY.md`.</output>
