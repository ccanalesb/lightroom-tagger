---
phase: 07-stacks-in-matching-pin-similarity
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - lightroom_tagger/core/database.py
  - apps/visualizer/backend/api/images.py
  - apps/visualizer/backend/tests/test_images_stacks_api.py
autonomous: true
requirements:
  - STACK-05
user_setup: []
must_haves:
  truths:
    - "Users can split, merge, and change representative with persistence"
    - "Operations enforce safe defaults and preserve stack integrity"
  artifacts:
    - path: apps/visualizer/backend/api/images.py
      provides: stack edit endpoints and validation responses
    - path: lightroom_tagger/core/database.py
      provides: transactional split/merge/representative mutation helpers
  key_links:
    - from: API mutation request
      to: transactional stack edit helper
      via: validated request schema and conflict-safe DB updates
---

<objective>
Implement STACK-05 backend mutation surface for split, merge, and change representative operations with transaction safety and predictable error semantics.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-stacks-in-matching-pin-similarity/07-CONTEXT.md
@.planning/phases/04-stack-detection/04-CONTEXT.md
@.planning/phases/06-similarity-stack-ui/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Transactional stack edit helpers</name>
  <files>lightroom_tagger/core/database.py</files>
  <action>
  Add/extend transaction helpers for split, merge, and representative change. Preserve `UNIQUE(image_key)` constraints, maintain representative validity, and keep stack_size metadata consistent.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest apps/visualizer/backend/tests/test_images_stacks_api.py -k split -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
  - Each operation is atomic and rollback-safe on failure.
  - Representative key always belongs to target stack after mutation.
  - Edge handling covers singleton splits and invalid merge targets.
  </acceptance_criteria>
  <done>Database mutation helpers preserve stack invariants transactionally.</done>
</task>

<task type="auto">
  <name>Expose stack edit APIs</name>
  <files>apps/visualizer/backend/api/images.py</files>
  <action>
  Add endpoints under existing images/stack API namespace for split, merge, and change-representative operations. Return updated stack metadata required by frontend refresh.
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest apps/visualizer/backend/tests/test_images_stacks_api.py -k representative -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
  - Invalid payloads return consistent 4xx error JSON.
  - Successful calls return updated representative/member counts.
  - Endpoint contracts are typed/serialized consistently with existing images API patterns.
  </acceptance_criteria>
  <done>Stack edit API endpoints are available with validated request/response contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>API and DB behavior tests</name>
  <files>apps/visualizer/backend/tests/test_images_stacks_api.py</files>
  <action>
  Add endpoint tests for happy paths and edge cases (invalid member, cross-stack merge mismatch, representative not in stack, split resulting stack validity).
  </action>
  <verify>
    <automated>cd /Users/ccanales/projects/lightroom-tagger && python -m pytest apps/visualizer/backend/tests/test_images_stacks_api.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
  - Test coverage includes split, merge, and representative change operations.
  - At least one failure test per mutation endpoint.
  </acceptance_criteria>
  <done>Stack mutation endpoint tests cover happy and failure paths.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | T | Stack mutation API | mitigate | Input validation + transaction boundaries + invariant checks. |
| T-07-02-02 | E | Unauthorized mutation scope | accept | Local trusted app context; still enforce explicit stack membership validation. |
</threat_model>

<verification>Stack edit API tests pass and preserve stack invariants after each mutation.</verification>

<success_criteria>STACK-05 backend edit contract is stable and ready for frontend wiring.</success_criteria>

<output>After completion, create `.planning/phases/07-stacks-in-matching-pin-similarity/07-02-SUMMARY.md`.</output>
