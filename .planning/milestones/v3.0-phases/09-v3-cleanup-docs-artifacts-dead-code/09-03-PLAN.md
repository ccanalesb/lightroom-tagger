---
phase: 09-v3-cleanup-docs-artifacts-dead-code
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/visualizer/frontend/src/services/api.ts
  - apps/visualizer/frontend/src/constants/strings.ts
autonomous: true
requirements:
  - SIM-02
must_haves:
  truths:
    - "No `ImagesAPI.getCatalogSimilar` remains in `apps/visualizer/frontend/src`"
    - "No `CatalogSimilarResponse` type export remains in frontend api.ts"
    - "No `CATALOG_SIMILAR_*` constants remain (13 strings); `CATALOG_STACK_SHOW` / `CATALOG_STACK_HIDE` remain"
    - "`npx tsc --noEmit` passes in apps/visualizer/frontend after deletion"
    - "`rg \"CATALOG_SIMILAR_|getCatalogSimilar|CatalogSimilarResponse\" apps/visualizer/frontend/src` returns zero hits"
  artifacts:
    - path: apps/visualizer/frontend/src/services/api.ts
      provides: Dead client method + response type removed; backend route untouched (D-03)
    - path: apps/visualizer/frontend/src/constants/strings.ts
      provides: Orphan similarity copy removed; stack expand/collapse copy preserved
  key_links:
    - from: api.ts ImagesAPI surface
      to: callers of catalog similar (must be zero in src after rg)
---

<objective>
Per **D-01** delete orphan frontend exports: remove `ImagesAPI.getCatalogSimilar`, remove `CatalogSimilarResponse`, delete the entire `CATALOG_SIMILAR_*` constant family (`CATALOG_SIMILAR_MORE_LIKE_THIS` through `CATALOG_SIMILAR_FETCH_ERROR`) while preserving `CATALOG_STACK_SHOW` and `CATALOG_STACK_HIDE`. Per **D-03** do not modify backend routes or pytest files — `GET …/similar` stays on the server and remains covered by backend tests separately.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/visualizer/frontend/src/services/api.ts (`getCatalogSimilar` ~399–410; `CatalogSimilarResponse` ~1077–1082)
@apps/visualizer/frontend/src/constants/strings.ts (`// Phase 06` comment ~279; `CATALOG_SIMILAR_*` ~279–297; `CATALOG_STACK_*` survivors)
@.cursor/rules/dry-callsite-sweep.mdc
@.cursor/rules/phase-exit-walkthrough.mdc
</context>

<tasks>

<task type="auto">
  <name>Remove getCatalogSimilar and CatalogSimilarResponse from api.ts</name>
  <files>apps/visualizer/frontend/src/services/api.ts</files>
  <read_first>
    apps/visualizer/frontend/src/services/api.ts (region around ImagesAPI methods and exported types)
    `.planning/ROADMAP.md` (Phase 9 — D-03 backend route stays)
  </read_first>
  <action>
  **D-01 / D-03**

  1. **Delete** the **`getCatalogSimilar` method** entirely, including its JSDoc block starting at `/**` line that references `CLIP visual neighbors` / `Phase 7` — from the opening `/**` through the closing `},` **before** the next property `listCatalogSimilarityGroups`.

  2. **Delete** the **`export type CatalogSimilarResponse = { … }`** type alias (**lines ~1078–1082** in current tree), including the `{ images, total, meta }` body and **`export`** keyword — ensure no orphaned references (search file for `CatalogSimilarResponse` after removal).

  3. **Do not** edit `apps/visualizer/backend/**` or remove `GET /api/images/catalog/<key>/similar` backend tests (**D-03**).
  </action>
  <verify>
    <automated>cd "/Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg "\\b(getCatalogSimilar|CatalogSimilarResponse)\\b" apps/visualizer/frontend/src/services/api.ts` exits 1 (zero matches). Run from repo root: `cd /Users/ccanales/projects/lightroom-tagger && rg "\\b(getCatalogSimilar|CatalogSimilarResponse)\\b" apps/visualizer/frontend/src/services/api.ts | wc -l` equals **0**.
    - `cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend && npx tsc --noEmit` exits **0**.
  </acceptance_criteria>
  <done>api.ts has no orphan catalog-similar client API or paired response type.</done>
</task>

<task type="auto">
  <name>Remove CATALOG_SIMILAR_* family from strings.ts and fix section comment</name>
  <files>apps/visualizer/frontend/src/constants/strings.ts</files>
  <read_first>
    apps/visualizer/frontend/src/constants/strings.ts (lines ~270–305)
    apps/visualizer/frontend/src/constants/strings.ts (grep for consumers of `CATALOG_STACK_SHOW`)
  </read_first>
  <action>
  1. **Delete** consecutive `export const` lines from **`CATALOG_SIMILAR_MORE_LIKE_THIS`** through **`CATALOG_SIMILAR_FETCH_ERROR`** (**inclusive**). Do **not** delete **`CATALOG_STACK_SHOW`**, **`CATALOG_STACK_HIDE`**, **`CATALOG_STACK_MEMBERS_*`**, nor stack-merge/split constants below (~298+).

  2. **Replace** the comment line preceding `CATALOG_STACK_SHOW` (currently `// Phase 06 — visual similarity & stack (SIM-02, STACK-03)` or similar) so it scopes only surviving stack-expand UI, e.g.:

     `// Phase 06 — stack expand/collapse (STACK-03). Job-driven similarity copy lived in removed CATALOG_SIMILAR_* (SIM-02); closed Phase 09.`

  3. **Dry-callsite sweep:** run `rg "CATALOG_SIMILAR_|CATALOG_STACK_SHOW|ImageDetailModal" apps/visualizer/frontend/src` — confirm **zero** imports/usages expect stack strings that still exist; do not delete used symbols.
  </action>
  <verify>
    <automated>cd "/Users/ccanales/projects/lightroom-tagger" && rg "CATALOG_SIMILAR_" apps/visualizer/frontend/src; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - Repo-root command `rg "CATALOG_SIMILAR_" apps/visualizer/frontend/src` exits **1** (zero hits after deletion).
    - `rg "^export const CATALOG_STACK_(SHOW|HIDE)\\b" apps/visualizer/frontend/src/constants/strings.ts` exits **0** (both constants remain exported exactly once).
    - `rg "CatalogSimilarResponse|getCatalogSimilar" apps/visualizer/frontend/src` exits **1** (zero hits path-wide).

  Combined verification rollup (executor must run):

  ```
  cd /Users/ccanales/projects/lightroom-tagger
  rg "CATALOG_SIMILAR_|getCatalogSimilar|CatalogSimilarResponse" apps/visualizer/frontend/src
  # expect exit code 1 (no matches)

  cd apps/visualizer/frontend && npx tsc --noEmit
  # expect exit code 0
  ```
  </acceptance_criteria>
  <done>Orphan SIM-UI strings gone; stack expand/hide unaffected; rg + tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → compile-time deps | Deleted symbols must truly have zero callers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Spoofing / Tampering | Orphan frontend API removal | accept | Compilation (`tsc --noEmit`) + `rg` prove no remaining callsites (`dry-callsite-sweep` satisfied) |
</threat_model>

<verification>
**D-03:** No production diff under `apps/visualizer/backend/**`. Hooks in **`phase-exit-walkthrough.mdc`** key off **`*-VERIFICATION.md`**, **not** `*-SUMMARY.md` — **`walkthrough_exempt` on SUMMARY is a no-op.** Note briefly in **`09-03-SUMMARY.md`** that deletes are orphan-only on **`api.ts`** / **`strings.ts`** with no observable UI churn; defer full verifier chain to **`09-04-SUMMARY.md` § `### Walkthrough disposition (Phase 09 → 09-VERIFICATION.md)`** instructing **`walkthrough_exempt: true`** on created **`09-VERIFICATION.md`** when gsd-verifier stamps **`status: passed`**.
</verification>

<success_criteria>
ROADMAP Phase 9 dead-code success criteria satisfied: frontend has zero `getCatalogSimilar` / `CATALOG_SIMILAR_MORE_LIKE_THIS` hits; backend similar route untouched; `tsc --noEmit` passes.
</success_criteria>

<output>
`.planning/phases/09-v3-cleanup-docs-artifacts-dead-code/09-03-SUMMARY.md` — short orphan-delete note pointing executors / verifiers at **Plan `09-04`** SUMMARY walkthroughDisposition subsection (**`09-VERIFICATION.md`** target, **not SUMMARY** frontmatter).
</output>

