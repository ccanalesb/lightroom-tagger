# Phase 7.1 - Pattern Map

**Phase:** 7.1 - Phase 7 remediation fixes  
**Generated:** 2026-04-26  
**Source inputs:** `07.1-CONTEXT.md`, `07.1-RESEARCH.md`

---

## Files to Modify and Analogs

| Target File | Role | Closest Analog | Reuse Pattern |
|---|---|---|---|
| `apps/visualizer/frontend/src/pages/ProcessingPage.tsx` or processing tab component | First-class embed trigger + queue deep-link UX | `apps/visualizer/frontend/src/components/processing/CatalogCacheTab.tsx` | Job enqueue button with immediate progress guidance and queue mention |
| `apps/visualizer/frontend/src/components/image-view/ImageDetailModal.tsx` | Fallback embed CTA + direct queue navigation affordance | Existing similarity no-embed block in same file | Keep inline fallback flow; add explicit actionable queue jump |
| `apps/visualizer/frontend/src/constants/strings.ts` | Centralized remediation copy | Existing `CATALOG_SIMILAR_*` constants | All user-visible text as constants (no inline literals) |
| `apps/visualizer/backend/jobs/handlers.py` | Path preflight, resolve-before-encode, grouped skip summary | Current `_handle_batch_embed_image_inner` batching/checkpoint flow | Preserve checkpoint/progress contract while improving path and diagnostics logic |
| `lightroom_tagger/core/database.py` | Shared path contract | `resolve_filepath()` | Use as single source of truth for UNC/mount resolution |
| `apps/visualizer/frontend/src/components/image-view/__tests__/ImageDetailModal.test.tsx` | Regression tests for no-embed + enqueue UX | Existing "enqueue embed job" test | Extend to assert queue navigation affordance and diagnostics copy |
| `apps/visualizer/backend/tests/test_handlers_batch_embed_image.py` (or equivalent) | Backend remediation tests | Existing handlers test style in backend tests | Verify fail-fast preflight, grouped reasons, and resolved-path behavior |

---

## Key Existing Snippets to Mirror

### Frontend enqueue mutation pattern
- Source: `apps/visualizer/frontend/src/services/api.ts` (`JobsAPI.create`)
- Contract: enqueue call invalidates job list and health queries; UI should consume this and provide direct route affordance.

### Queue route pattern
- Source: `apps/visualizer/frontend/src/pages/ProcessingPage.tsx`
- Contract: jobs tab is addressed by `?tab=jobs`; this is the canonical route target for post-enqueue UX.

### Backend embed batching/checkpoint pattern
- Source: `apps/visualizer/backend/jobs/handlers.py` (`_handle_batch_embed_image_inner`)
- Contract: preserve checkpoint fingerprint/progress semantics while upgrading path resolution and grouped diagnostics.

### Path normalization contract
- Source: `lightroom_tagger/core/database.py` (`resolve_filepath`)
- Contract: do not duplicate path rewrite logic in handlers; normalize through shared helper.

---

## Guardrails

- Do not add new frontend dependency packages for this remediation.
- Do not break current checkpoint resume behavior in embed job.
- Do not emit diagnostics only in logs; include grouped reasons in completion payload for UI consumers.
- Do not rely on modal fallback as the only user-discoverable launch path.
