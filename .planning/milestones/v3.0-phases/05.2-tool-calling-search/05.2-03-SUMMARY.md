---
phase: "05.2"
plan: "05.2-03"
subsystem: backend/api
tags: [chat-search, tool-calling, response-shape]
key-files:
  modified:
    - apps/visualizer/backend/api/images.py
    - apps/visualizer/backend/tests/test_images_chat_search_api.py
    - apps/visualizer/frontend/src/services/api.ts
    - apps/visualizer/frontend/src/pages/SearchPage.tsx
commit: 2ea3be0
completed: 2026-04-24
---

# Phase 5.2 Plan 03: API wiring Summary

Wired `POST /api/images/chat-search` to branch by provider capability and return tool-call conversation turns for multi-turn continuity.

## Tasks Completed
- **Task 1** - Added tool-calling branch in `chat_search_images` using `run_tool_calling_search(...)` when selected provider is tool-capable.
- **Task 2** - Added helper `_extract_images_from_tool_messages(...)` to parse last tool payload and re-hydrate catalog rows in API image shape.
- **Task 3** - Response payload now includes `search_mode: "tool_calling"`, `messages`, and `assistant_message` on tool path; non-tool providers keep existing NL-filter/semantic behavior.

## Delivered Behavior
- Tool-capable providers execute function-calling flow and return enriched conversation history for frontend persistence.
- Non-tool providers continue through legacy `nl_filter` path, preserving compatibility.
- Tool-path image results are returned in the same catalog list shape expected by existing frontend rendering.

## Verification Evidence
- Commit includes backend tests update (`test_images_chat_search_api.py`) to keep chat-search behavior guarded while preserving legacy fallback path checks.

## Deviations from Plan
- Tool-path result extraction re-queries by image keys to keep output parity with existing catalog API shape (instead of returning tool payload directly).

## Self-Check
- Commit exists and includes only planned API wiring/type updates (`2ea3be0`).
