---
phase: 10-match-02-quantitative-benchmark
plan: 2
type: execute
wave: 2
depends_on:
  - 10-01-PLAN
files_modified:
  - .planning/phases/10-match-02-quantitative-benchmark/10-RECALL.md
  - .planning/phases/10-match-02-quantitative-benchmark/10-recall-data.csv
autonomous: false
requirements:
  - MATCH-02
must_haves:
  truths:
    - "Operator completed batch_embed_image with image_type='catalog_and_instagram' before recall run (D-04)"
    - "10-RECALL.md lists funnel counts, recall %, miss table"
    - "10-recall-data.csv header matches D-12; each row status ∈ {hit,miss,filtered_out,skipped_no_embedding}"
  artifacts:
    - path: .planning/phases/10-match-02-quantitative-benchmark/10-RECALL.md
      provides: Measured recall report for REQUIREMENTS.md placeholder substitution
    - path: .planning/phases/10-match-02-quantitative-benchmark/10-recall-data.csv
      provides: Per-pair audit trail
  key_links:
    - from: headline recall percentage in 10-RECALL.md
      to: Wave 3 REQUIREMENTS.md [MEASURED: X%] substitution
      via: manual copy from report prose
---

<objective>
Run the Phase 10 recall CLI against the operator’s live library SQLite after IG CLIP embeddings are populated; write `10-RECALL.md` and `10-recall-data.csv` under the phase directory and commit them. This wave requires a real `library.db` (or equivalent path) on the executor machine.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-match-02-quantitative-benchmark/10-CONTEXT.md (D-04 prerequisite, D-11/D-12 artifacts)
@.planning/phases/10-match-02-quantitative-benchmark/10-VALIDATION.md (manual verifications)
@apps/visualizer/backend/jobs/handlers.py (reference only: batch_embed_image image_type catalog_and_instagram)
</context>

<verification_criteria>
- `test -f .planning/phases/10-match-02-quantitative-benchmark/10-RECALL.md` exits 0
- `test -f .planning/phases/10-match-02-quantitative-benchmark/10-recall-data.csv` exits 0
- `head -n 1 .planning/phases/10-match-02-quantitative-benchmark/10-recall-data.csv` equals `insta_key,validated_catalog_key,date_window_size,candidates_after_filters,shortlist_size,shortlist_includes_validated,status`
- `rg "miss" .planning/phases/10-match-02-quantitative-benchmark/10-RECALL.md` exits 0 (miss table or count section present)
- `rg "total_validated|funnel|Recall" .planning/phases/10-match-02-quantitative-benchmark/10-RECALL.md` exits 0
</verification_criteria>

<tasks>

<task type="manual">
  <name>Prerequisite — IG CLIP embeddings</name>
  <files>—</files>
  <read_first>
    .planning/phases/10-match-02-quantitative-benchmark/10-CONTEXT.md (D-04, D-05)
    apps/visualizer/frontend/src/services/api.ts (JobsAPI.create batch_embed_image pattern — optional reference)
  </read_first>
  <action>
  **Before** running the benchmark script, ensure the operator has run the existing **`batch_embed_image`** job with payload **`image_type='catalog_and_instagram'`** (Visualizer Processing UI or API) and waited until the job finishes successfully so `image_clip_embeddings` contains rows for Instagram `media_key` values tied to validated matches.

  Record in git commit message body or `10-RECALL.md` footer one line: `Prerequisite: batch_embed_image catalog_and_instagram completed before run.` (exact sentence optional; must mention both job type and image_type).
  </action>
  <verify>
    <automated>rg "batch_embed_image|catalog_and_instagram" .planning/phases/10-match-02-quantitative-benchmark/10-RECALL.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg "batch_embed_image|catalog_and_instagram" .planning/phases/10-match-02-quantitative-benchmark/10-RECALL.md` exits 0 (prerequisite must be documented in the report itself)
  </acceptance_criteria>
  <done>Prerequisite D-04 documented.</done>
</task>

<task type="manual">
  <name>Run benchmark_clip_recall and commit artifacts</name>
  <files>.planning/phases/10-match-02-quantitative-benchmark/10-RECALL.md
.planning/phases/10-match-02-quantitative-benchmark/10-recall-data.csv</files>
  <read_first>
    lightroom_tagger/scripts/benchmark_clip_recall.py
    .planning/phases/10-match-02-quantitative-benchmark/10-01-PLAN.md (expected outputs)
    .planning/phases/10-match-02-quantitative-benchmark/10-RECALL.md (artifact being created — check does not exist yet before running)
    .planning/phases/10-match-02-quantitative-benchmark/10-recall-data.csv (artifact being created — check does not exist yet before running)
    .planning/phases/10-match-02-quantitative-benchmark/10-CONTEXT.md (D-11/D-12 artifact specs)
  </read_first>
  <action>
  From repo root `/Users/ccanales/projects/lightroom-tagger`, run (replace `PATH_TO_LIBRARY_DB` with the absolute path to the live library SQLite, e.g. the file pointed to by `LIBRARY_DB` / `config.yaml`):

  ```bash
  cd /Users/ccanales/projects/lightroom-tagger
  python -m lightroom_tagger.scripts.benchmark_clip_recall --db PATH_TO_LIBRARY_DB --out-dir .planning/phases/10-match-02-quantitative-benchmark
  ```

  Exit code must be 0. Stage and commit **both** generated files under `.planning/phases/10-match-02-quantitative-benchmark/`.

  If the executor has **no** access to a library DB with validated matches, **stop** and mark this plan blocked — do not fabricate `10-RECALL.md` or CSV.
  </action>
  <verify>
    <automated>cd "/Users/ccanales/projects/lightroom-tagger" && test -f .planning/phases/10-match-02-quantitative-benchmark/10-RECALL.md && test -f .planning/phases/10-match-02-quantitative-benchmark/10-recall-data.csv && head -n 1 .planning/phases/10-match-02-quantitative-benchmark/10-recall-data.csv | grep -Fx "insta_key,validated_catalog_key,date_window_size,candidates_after_filters,shortlist_size,shortlist_includes_validated,status"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/10-match-02-quantitative-benchmark/10-RECALL.md` exits 0 from repo root
    - `test -f .planning/phases/10-match-02-quantitative-benchmark/10-recall-data.csv` exits 0
    - `head -n 1 .planning/phases/10-match-02-quantitative-benchmark/10-recall-data.csv` output is exactly: `insta_key,validated_catalog_key,date_window_size,candidates_after_filters,shortlist_size,shortlist_includes_validated,status`
    - `rg -n "hit|miss|filtered_out|skipped_no_embedding" .planning/phases/10-match-02-quantitative-benchmark/10-recall-data.csv` exits 0 (at least one status token appears in data rows)
    - `rg "Recall|%" .planning/phases/10-match-02-quantitative-benchmark/10-RECALL.md` exits 0
    - `tail -n +2 .planning/phases/10-match-02-quantitative-benchmark/10-recall-data.csv | cut -d, -f7 | sort -u | rg -x "hit|miss|filtered_out|skipped_no_embedding"` exits 0 (every data row status value is one of the four allowed tokens)
  </acceptance_criteria>
  <done>Measured artifacts committed from real DB.</done>
</task>

</tasks>
