---
phase: 10-match-02-quantitative-benchmark
plan: 3
type: execute
wave: 3
depends_on:
  - 10-02-PLAN
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/todos/pending/benchmark-embedding-recall.md
  - .planning/todos/done/benchmark-embedding-recall.md
autonomous: true
requirements:
  - MATCH-02
must_haves:
  truths:
    - "MATCH-02 bullet checked [x] with measured recall and link to 10-RECALL.md"
    - "REQUIREMENTS.md contains zero occurrences of ≥10×"
    - "Traceability MATCH-02 row marks Complete with same percentage as report"
    - "Todo benchmark-embedding-recall.md lives under todos/done/ with closing line per D-14"
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: MATCH-02 wording aligned to Phase 10 recall measurement
    - path: .planning/todos/done/benchmark-embedding-recall.md
      provides: Closed todo referencing 10-RECALL.md
  key_links:
    - from: .planning/REQUIREMENTS.md MATCH-02 bullet
      to: .planning/phases/10-match-02-quantitative-benchmark/10-RECALL.md
      via: markdown relative link in bullet text
---

<objective>
Close the planning loop: rewrite MATCH-02 and traceability in `REQUIREMENTS.md` using the measured recall from `10-RECALL.md`, purge the unmeasured ≥10× claim, and move the pending todo to `done/` with D-14 closing text.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md (line ~40 MATCH-02 bullet, line ~88 traceability)
@.planning/phases/10-match-02-quantitative-benchmark/10-RECALL.md (source of measured %)
@.planning/phases/10-match-02-quantitative-benchmark/10-CONTEXT.md (D-13, D-14)
@.planning/todos/pending/benchmark-embedding-recall.md
</context>

<verification_criteria>
- `rg "≥10×" .planning/REQUIREMENTS.md` exits 1 (no matches)
- `rg "^- \[x\] \*\*MATCH-02\*\*:" .planning/REQUIREMENTS.md` exits 0
- `rg "10-RECALL\.md" .planning/REQUIREMENTS.md` exits 0
- `rg "MATCH-02 \| 8, 10 \|.*Complete" .planning/REQUIREMENTS.md` exits 0
- `test -f .planning/todos/done/benchmark-embedding-recall.md` exits 0
- `test ! -f .planning/todos/pending/benchmark-embedding-recall.md` exits 0
</verification_criteria>

<tasks>

<task type="auto">
  <name>Rewrite MATCH-02 bullet and traceability row</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/REQUIREMENTS.md (### Matching Performance, ## Traceability table)
    .planning/phases/10-match-02-quantitative-benchmark/10-RECALL.md (Recall % line — copy numeric value)
  </read_first>
  <action>
  1. **Read** `.planning/phases/10-match-02-quantitative-benchmark/10-RECALL.md` and copy the **numeric recall percentage** for the headline metric (hits / (hits + misses)), e.g. **`92.3`** when the report states `92.3%`.

  2. **Replace** the entire **MATCH-02** markdown line under `### Matching Performance & Catalog Cache Pipeline` (currently starts with `- [ ] **MATCH-02**:`) with **exactly** this single line, substituting **`X`** in **`[MEASURED: X%]`** with the numeric value from step 1 (keep the `%` inside the brackets):

  `- [x] **MATCH-02**: `vision_match` consults `image_clip_embeddings` to cosine-shortlist date-windowed catalog candidates (default `clip_top_k=50`) before LLM judgment runs; per-stage candidate counts (date-window in → CLIP shortlist out → LLM judgments) emitted in the job log. Recall on user-validated match pairs: [MEASURED: X%] (see [Phase 10 recall report](.planning/phases/10-match-02-quantitative-benchmark/10-RECALL.md)). *(Phase 8 + Phase 10 — 2026-05-01)*`

  Example after substitution: `[MEASURED: 92.3%]`. If the report gives only a ratio, compute the same headline percent the report uses for **Recall**.

  3. **Replace** the **MATCH-02** row in the Traceability markdown table (line starting with `| MATCH-02 |`) with **exactly** (substitute the **same** numeric `X%` as inside `[MEASURED: X%]` on the bullet):

  `| MATCH-02 | 8, 10 | ✅ Complete (Phase 8 shipped prefilter; Phase 10 recall check: X% on validated pairs — 2026-05-01) |`

  Example: `Phase 10 recall check: 92.3% on validated pairs`

  4. Do **not** alter other requirement bullets in this task.
  </action>
  <verify>
    <automated>cd "/Users/ccanales/projects/lightroom-tagger" && ! rg "≥10×" .planning/REQUIREMENTS.md && rg "^- \[x\] \*\*MATCH-02\*\*:" .planning/REQUIREMENTS.md && rg "10-match-02-quantitative-benchmark/10-RECALL\.md" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg "≥10×" .planning/REQUIREMENTS.md` exit code is **1** (grep finds nothing)
    - `test "$(rg -c "^- \[x\] \*\*MATCH-02\*\*:" .planning/REQUIREMENTS.md)" = 1` exits 0
    - `rg "clip_top_k=50" .planning/REQUIREMENTS.md` exits 0
    - `rg "\[MEASURED: [0-9]+\.?[0-9]*%\]" .planning/REQUIREMENTS.md` exits 0
    - `rg "10-match-02-quantitative-benchmark/10-RECALL\.md" .planning/REQUIREMENTS.md` exits 0
    - `rg "Partial — Phase 10 \(gap closure: quantitative ≥10×" .planning/REQUIREMENTS.md` exit code is **1**
    - `rg "Phase 10 recall check:.*%" .planning/REQUIREMENTS.md` exits 0
    - `rg "2026-05-01" .planning/REQUIREMENTS.md` exits 0 (traceability row contains closure date)
  </acceptance_criteria>
  <done>MATCH-02 text and traceability synced to measured recall.</done>
</task>

<task type="auto">
  <name>Move benchmark-embedding-recall todo to done</name>
  <files>.planning/todos/pending/benchmark-embedding-recall.md
.planning/todos/done/benchmark-embedding-recall.md</files>
  <read_first>
    .planning/todos/pending/benchmark-embedding-recall.md
    .planning/phases/10-match-02-quantitative-benchmark/10-CONTEXT.md (D-14)
  </read_first>
  <action>
  1. **Move** file `.planning/todos/pending/benchmark-embedding-recall.md` → `.planning/todos/done/benchmark-embedding-recall.md` using `git mv` (preserve history).

  2. **Append** to the end of `.planning/todos/done/benchmark-embedding-recall.md` a horizontal rule then closing note: first line exactly `---`, second paragraph exactly:

  `Closed Phase 10 (2026-05-01): recall-only measurement — see [.planning/phases/10-match-02-quantitative-benchmark/10-RECALL.md](../phases/10-match-02-quantitative-benchmark/10-RECALL.md). Recall measurement only — cost-reduction benchmark and DINOv2/CLIP/SigLIP A/B comparison are deferred follow-ups.`
  </action>
  <verify>
    <automated>cd "/Users/ccanales/projects/lightroom-tagger" && test -f .planning/todos/done/benchmark-embedding-recall.md && test ! -f .planning/todos/pending/benchmark-embedding-recall.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/todos/done/benchmark-embedding-recall.md` exits 0
    - `test ! -f .planning/todos/pending/benchmark-embedding-recall.md` exits 0
    - `rg "Recall measurement only — cost-reduction benchmark and DINOv2/CLIP/SigLIP A/B comparison are deferred follow-ups" .planning/todos/done/benchmark-embedding-recall.md` exits 0
    - `rg "10-RECALL\.md" .planning/todos/done/benchmark-embedding-recall.md` exits 0
  </acceptance_criteria>
  <done>Todo D-14 closed with report link.</done>
</task>

<task type="auto">
  <name>Update ROADMAP Phase 10 success criteria to recall-only scope</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    .planning/ROADMAP.md (Phase 10 section under v3.0 — "MATCH-02 quantitative benchmark")
    .planning/phases/10-match-02-quantitative-benchmark/10-CONTEXT.md (scope shift note, domain boundary)
    .planning/phases/10-match-02-quantitative-benchmark/10-RECALL.md (measured recall % to insert)
  </read_first>
  <action>
  In `.planning/ROADMAP.md`, find the **Phase 10 success criteria** block (under `#### Phase 10 — MATCH-02 quantitative benchmark`) and replace the three original bullet points:

  Original (remove these three):
  ```
  - A benchmark report (e.g., `.planning/phases/10-match-02-quantitative-benchmark/BENCHMARK.md`) records: dataset size, IG-embedding coverage, pre/post LLM-call counts, measured reduction ratio, recall on user-validated match pairs, and recommended `clip_top_k` setting.
  - REQUIREMENTS.md MATCH-02 line states the measured reduction ratio (and updates the ≥10× target if reality differs).
  - `benchmark-embedding-recall.md` pending todo is moved to `done/` with a link to the benchmark report.
  ```

  Replace with (substitute X% with the recall percentage from `10-RECALL.md`):
  ```
  - `10-RECALL.md` records: total validated pairs, embedded, skipped (no IG embedding), filtered_out, hits, misses, and recall % on tested pairs (hits / (hits + misses)). Measured recall: [X%].
  - REQUIREMENTS.md MATCH-02 bullet updated to `[x]`, ≥10× claim removed, measured recall linked to `10-RECALL.md`.
  - `benchmark-embedding-recall.md` pending todo moved to `done/` with a link to `10-RECALL.md` and a note that cost-reduction benchmark and model A/B are deferred follow-ups.
  ```

  Also update the **Phase 10 "Gap closure scope"** description paragraph to replace any mention of "BENCHMARK.md", "pre/post LLM-call counts", "measured reduction ratio", or "clip_top_k sweep" with "recall-only check" phrasing consistent with `10-CONTEXT.md` domain boundary. Do not rename the phase directory or heading.
  </action>
  <verify>
    <automated>cd "/Users/ccanales/projects/lightroom-tagger" && ! rg "BENCHMARK\.md" .planning/ROADMAP.md && rg "10-RECALL\.md" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg "BENCHMARK\.md" .planning/ROADMAP.md` exits 1 (no matches — old artifact name gone)
    - `rg "10-RECALL\.md" .planning/ROADMAP.md` exits 0 (new artifact name present)
    - `rg "≥10×" .planning/ROADMAP.md` exits 1 (no matches — unmeasured claim removed from roadmap too)
  </acceptance_criteria>
  <done>ROADMAP Phase 10 success criteria aligned to recall-only scope.</done>
</task>

</tasks>
