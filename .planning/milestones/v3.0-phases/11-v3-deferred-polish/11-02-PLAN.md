---
phase: 11
plan: "11-02"
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - apps/visualizer/frontend/src/components/matching/AdvancedOptions.tsx
  - apps/visualizer/frontend/src/components/ui/ConfirmUndoAction.tsx
  - apps/visualizer/frontend/src/components/processing/CatalogCacheTab.tsx
autonomous: true
requirements: []
---

# Plan 11-02 — AdvancedOptions a11y (IN-08-01) + undo toast fix (Phase 7 #4) + CatalogCacheTab copy (IN-08-02)

<objective>
Implement disclosure accessibility on `AdvancedOptions`, fix `useUndoToast` / `UndoToastBar` so message-only toasts display for the full timeout, centralize all Phase 11 `CatalogCacheTab` user-visible literals via `strings.ts` constants from plan **11-01**, and render NAS troubleshooting copy from `CATALOG_CACHE_NAS_TROUBLESHOOTING`.
</objective>

<tasks>

<task id="T1">
  <read_first>
    - `apps/visualizer/frontend/src/components/matching/AdvancedOptions.tsx` (lines 69–85)
    - `apps/visualizer/frontend/src/constants/strings.ts` (verify `ADVANCED_OPTIONS_TITLE` import path unchanged)
  </read_first>
  <action>
    On the **first** `<button>` (lines ~71–76), add **exactly** these attributes:
    - `type="button"`
    - `aria-expanded={isOpen}`
    - `aria-controls="advanced-options-panel"`

    On the **expandable panel** outer `<div>` inside `{isOpen && (` (currently ~79), add **`id="advanced-options-panel"`** as the **first** attribute after `<div` for stable scanning.

    Do **not** change arrow characters, `ADVANCED_OPTIONS_TITLE`, or child control order.
  </action>
  <acceptance_criteria>
    - `rg 'aria-expanded=\{isOpen\}' apps/visualizer/frontend/src/components/matching/AdvancedOptions.tsx` exits 0.
    - `rg 'aria-controls="advanced-options-panel"' apps/visualizer/frontend/src/components/matching/AdvancedOptions.tsx` exits 0.
    - `rg 'id="advanced-options-panel"' apps/visualizer/frontend/src/components/matching/AdvancedOptions.tsx` exits 0.
    - `rg '<button' -n apps/visualizer/frontend/src/components/matching/AdvancedOptions.tsx` — the disclosure `<button>` line includes `type="button"` before or after other props (same opening tag).
    - `npx tsc --noEmit` from `apps/visualizer/frontend` exits 0.
  </acceptance_criteria>
</task>

<task id="T2">
  <read_first>
    - `apps/visualizer/frontend/src/components/ui/ConfirmUndoAction.tsx` (types `UndoToastState` through `UndoToastBar` ~78–168)
  </read_first>
  <action>
    1. **`offerUndo`** (~112–126): Replace the `if (!onUndo) { setToast({ kind: 'hidden' }); return }` branch with behavior that **sets** `setToast({ kind: 'visible', message })` (**omitting** `onUndo` in the object) and **starts the same** `setTimeout(..., timeoutMs)` as the branch where `onUndo` is provided, so the toast auto-hides after `timeoutMs`.

    2. **`UndoToastBar`** (~148–167): Change the guard from `toast.kind !== 'visible' || !toast.onUndo` to **`toast.kind !== 'visible'` only**. Keep the outer `<div>` with **`role="status"`** and **`aria-live={politeness}`** for **both** undo and message-only modes.

    3. Render the **Undo** `<button>` **only when** `toast.onUndo != null` (conditionally wrap the existing button). When the button is absent, the bar shows **only** `<p>{toast.message}</p>`.

    4. **`UndoToastBarProps`:** Change `onUndo: () => void` to **`onUndo?: () => void`** (optional). The Undo button’s `onClick` must call **`onUndo`** when rendered (callers with undo pass it; message-only may omit). **`ImageDetailModal.tsx` needs no edit** — it already calls `offerUndo(message, async () => { … })` with both arguments; optional `onUndo` is backward-compatible.
  </action>
  <acceptance_criteria>
    - `rg "if \\(!onUndo\\)" apps/visualizer/frontend/src/components/ui/ConfirmUndoAction.tsx` exits **1** (no match — branch removed or no longer immediate hidden return).
    - `ConfirmUndoAction.tsx` contains substring `setToast({ kind: 'visible', message })` in `offerUndo` (message-only path).
    - `rg "toast.onUndo" apps/visualizer/frontend/src/components/ui/ConfirmUndoAction.tsx` matches a conditional that wraps the Undo **`<button>`** (e.g. `{toast.onUndo ? (` or `toast.onUndo != null`).
    - `rg 'role="status"' apps/visualizer/frontend/src/components/ui/ConfirmUndoAction.tsx` matches inside `UndoToastBar` return (unchanged location acceptable).
    - `rg "onUndo\\?:" apps/visualizer/frontend/src/components/ui/ConfirmUndoAction.tsx` exits 0.
    - `npx tsc --noEmit` from `apps/visualizer/frontend` exits 0.
  </acceptance_criteria>
</task>

<task id="T3">
  <read_first>
    - `apps/visualizer/frontend/src/components/processing/CatalogCacheTab.tsx` (lines 1–50 imports; card ~148–315)
    - `apps/visualizer/frontend/src/constants/strings.ts` (all exports added in plan **11-01** for `CATALOG_CACHE_*` NAS/card/stats)
  </read_first>
  <action>
    In `apps/visualizer/frontend/src/components/processing/CatalogCacheTab.tsx`:

    1. Extend the import from `../../constants/strings` to include **every** new `CATALOG_CACHE_*` constant from **11-01** used by this file:  
       `CATALOG_CACHE_CARD_TITLE`, `CATALOG_CACHE_INTRO_BODY`, all eight stat label/helper pairs, `CATALOG_CACHE_PROGRESS_LABEL`, `CATALOG_CACHE_LOCATION_PREFIX`, `CATALOG_CACHE_NAS_TROUBLESHOOTING`.

    2. Replace **literals** with constants:
       - `<CardTitle>` children → `{CATALOG_CACHE_CARD_TITLE}`
       - Intro `<p>` two-sentence body → **single** `{CATALOG_CACHE_INTRO_BODY}`
       - Stat row labels/helpers → matching `CATALOG_CACHE_STAT_*` constants (four rows × label + helper).
       - **Cache Progress** label → `{CATALOG_CACHE_PROGRESS_LABEL}`
       - Cache location line: `<strong>{CATALOG_CACHE_LOCATION_PREFIX}</strong> {stats.cache_dir}` (remove raw `Cache Location:` string).

    3. After the **cache location** `<div className="mt-4 p-3 ...">` block (~308–312), add a **new** paragraph `<p className="text-xs text-text-tertiary mt-2">` whose **only** text child is `{CATALOG_CACHE_NAS_TROUBLESHOOTING}`.

    **Lint for leftover literals:** `rg "Catalog Vision Cache" apps/visualizer/frontend/src/components/processing/CatalogCacheTab.tsx` must exit **1** (code 1 = no matches).
  </action>
  <acceptance_criteria>
    - `rg "Catalog Vision Cache" apps/visualizer/frontend/src/components/processing/CatalogCacheTab.tsx` exits **1**.
    - `rg "CATALOG_CACHE_CARD_TITLE" apps/visualizer/frontend/src/components/processing/CatalogCacheTab.tsx` exits 0.
    - `rg "CATALOG_CACHE_NAS_TROUBLESHOOTING" apps/visualizer/frontend/src/components/processing/CatalogCacheTab.tsx` exits 0.
    - `rg -n "<CardTitle>" apps/visualizer/frontend/src/components/processing/CatalogCacheTab.tsx` — the matching block shows `{CATALOG_CACHE_CARD_TITLE}` as the title child (within 3 lines of `<CardTitle>`).
    - `rg "Total Images" apps/visualizer/frontend/src/components/processing/CatalogCacheTab.tsx` exits **1** (rg exit code 1 = zero matches for migrated literal).
    - `npx tsc --noEmit` from `apps/visualizer/frontend` exits 0.
    - `npx vitest run` from `apps/visualizer/frontend` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend && npx tsc --noEmit && npx vitest run`
</verification>

<must_haves>
- Disclosure control in `AdvancedOptions.tsx` exposes **`aria-expanded`**, **`aria-controls`**, **`type="button"`**, and panel **`id="advanced-options-panel"`**.
- `offerUndo(message)` without second argument shows a visible toast for **`DEFAULT_UNDO_TIMEOUT_MS`** (8000 ms), then hides.
- `UndoToastBar` renders message-only without Undo control; undo variant unchanged; live region **`role="status"`** / **`aria-live="polite"`** preserved.
- `CatalogCacheTab.tsx` has **no** inline literals for the twelve migrated strings + NAS paragraph; **`CATALOG_CACHE_SIMILARITY_EMPTY`** remains consumed only from `strings.ts` (unchanged usage site).
</must_haves>

<threat_model>
- **Undo toast:** Only displays application-controlled `message` strings; no new user HTML injection (still React text). Optional `onUndo` reduces accidental destructive rollback when omitted — no privilege change.
- **Catalog copy:** Static strings from `strings.ts`; no new data sinks.
- **AdvancedOptions:** Accessibility attributes only; no new network or storage.
</threat_model>
