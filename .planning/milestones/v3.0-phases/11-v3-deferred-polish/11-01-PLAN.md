---
phase: 11
plan: "11-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/visualizer/frontend/src/constants/strings.ts
  - apps/visualizer/backend/jobs/handlers.py
  - lightroom_tagger/core/database.py
autonomous: true
requirements: []
---

# Plan 11-01 — strings.ts (Phase 11 copy) + backend comment-only (D-01, D-02, D-03)

<objective>
Add all new `strings.ts` exports for Catalog Cache tab centralization, similarity empty-state fix, Search pin inactive copy, and embed-discoverability help/link labels. Add operator-facing **comment-only** documentation in `database.py` (stack_size drift + pin `restrict_to_keys` / schema behavior) and `handlers.py` (`vision_judgments_total` semantics). No paired Processing-tab UI edit: per `.cursor/rules/job-ui-contract.mdc`, “modified” excludes comment-only `handlers.py` changes (no handler shape or logging-call changes).
</objective>

<tasks>

Here is an ordered task list with mandatory XML fields:

<task id="T1">
  <read_first>
    - `apps/visualizer/frontend/src/constants/strings.ts` (full file around existing `PROCESSING_*` / `CATALOG_CACHE_*` block ~530–620)
    - `.planning/phases/11-v3-deferred-polish/11-CONTEXT.md` (D-01–D-04 binding copy)
  </read_first>
  <action>
    In `apps/visualizer/frontend/src/constants/strings.ts`, **after** `export const PROCESSING_CATALOG_CACHE_ROUTE = '/processing?tab=cache'` (line ~560) and **before** `export const CATALOG_CACHE_BUILD_CTA`, insert these **new exports** exactly:

    1. **Search pin / embed discoverability**
       - `export const SEARCH_PIN_INACTIVE_PREFIX = 'Similarity pin inactive:'`
       - `export const SEARCH_PIN_INACTIVE_SUFFIX = 'Results use your full catalog.'`
       - `export const SEARCH_PIN_WARN_NO_CLIP = 'CLIP embedding not yet built for pinned image'`
       - `export const SEARCH_PIN_HELP_EMBED = 'Run "Embed catalog images only" (or Build catalog cache) under Processing → Catalog cache, then try again.'`
       - `export const SEARCH_PIN_LINK_CACHE = 'Open Catalog cache'`

    2. **CatalogCacheTab card / stats / NAS** (place in the same `CATALOG_CACHE_*` cluster as `CATALOG_CACHE_PIPELINE_TITLE`, i.e. after `CATALOG_CACHE_BUILD_SUCCESS` block or immediately before `CATALOG_CACHE_PIPELINE_TITLE`—choose one contiguous block; prefer **after** `CATALOG_CACHE_BUILD_SUCCESS` and **before** `CATALOG_CACHE_PIPELINE_TITLE`)

       - `export const CATALOG_CACHE_CARD_TITLE = 'Catalog Vision Cache'`
       - `export const CATALOG_CACHE_INTRO_BODY = 'The vision cache stores preprocessed Lightroom catalog images for fast AI comparison. Rebuilding the cache will process all catalog images and may take several minutes.'`
       - `export const CATALOG_CACHE_STAT_TOTAL_LABEL = 'Total Images'`
       - `export const CATALOG_CACHE_STAT_TOTAL_HELPER = 'Images in Lightroom catalog'`
       - `export const CATALOG_CACHE_STAT_CACHED_LABEL = 'Cached Images'`
       - `export const CATALOG_CACHE_STAT_CACHED_HELPER = 'Processed for AI matching'`
       - `export const CATALOG_CACHE_STAT_MISSING_LABEL = 'Missing'`
       - `export const CATALOG_CACHE_STAT_MISSING_HELPER = 'Not yet cached'`
       - `export const CATALOG_CACHE_STAT_SIZE_LABEL = 'Cache Size'`
       - `export const CATALOG_CACHE_STAT_SIZE_HELPER = 'Disk space used'`
       - `export const CATALOG_CACHE_PROGRESS_LABEL = 'Cache Progress'`
       - `export const CATALOG_CACHE_LOCATION_PREFIX = 'Cache Location:'`
       - `export const CATALOG_CACHE_NAS_TROUBLESHOOTING = 'Network share (NAS) paths must be mounted and readable by the backend host. If the embed job skips most images, verify the catalog path is accessible from the server.'`

    3. **Replace** the value of `CATALOG_CACHE_SIMILARITY_EMPTY` so the string is **exactly**:  
       `'No catalog similarity groups yet. Run catalog similarity from Pipeline stages after Embed Images completes.'`  
       (substitute **Pipeline stages** for **Advanced options**; keep the rest of the sentence parallel to the current constant.)

    Do **not** add `SEARCH_PIN_LINK_JOBS` — `SearchPage` will use existing `PROCESSING_OPEN_JOB_QUEUE` + `PROCESSING_JOB_QUEUE_ROUTE` per 11-CONTEXT.
  </action>
  <acceptance_criteria>
    - `rg "export const SEARCH_PIN_WARN_NO_CLIP" apps/visualizer/frontend/src/constants/strings.ts` returns exactly one line containing `'CLIP embedding not yet built for pinned image'`.
    - `rg "CATALOG_CACHE_NAS_TROUBLESHOOTING" apps/visualizer/frontend/src/constants/strings.ts` matches a line containing `Network share (NAS) paths must be mounted`.
    - `rg "CATALOG_CACHE_SIMILARITY_EMPTY" -A0 apps/visualizer/frontend/src/constants/strings.ts` shows the assigned string includes `Pipeline stages` and does **not** include `Advanced options`.
    - `rg "export const CATALOG_CACHE_CARD_TITLE" apps/visualizer/frontend/src/constants/strings.ts` exits 0.
    - `npx tsc --noEmit` run from `apps/visualizer/frontend` exits 0.
  </acceptance_criteria>
</task>

<task id="T2">
  <read_first>
    - `lightroom_tagger/core/database.py` (function `_migrate_image_stacks` ~831–842; `query_catalog_images` / `restrict_to_keys` ~1494–1501; `stack_split…` UPDATE ~1902–1910; `stack_merge_into` UPDATE ~1977–1983; `stack_set_representative` recount UPDATE ~2024–2032; `stack_metadata_for_api` docstring / `stack_member_count` ~1766–1791)
    - `.planning/phases/11-v3-deferred-polish/11-CONTEXT.md` (D-01, D-02)
  </read_first>
  <action>
    **D-01 (`stack_size`):**  
    1. Inside the `executescript(""" ... """)` body in `_migrate_image_stacks`, on the `image_stacks` DDL line that defines `stack_size INTEGER NOT NULL DEFAULT 0,`, append an **end-of-line SQL comment** (SQLite `-- …`) **no longer than two lines**, text materially: **`stack_size` is updated on split/merge/set_representative; drift vs `image_stack_members` is only theoretical; API clients must use `stack_metadata_for_api` (live member count), not this column alone.** Shorten wording if needed to fit one SQL line.  
    2. Immediately **before** the `db.execute(` that runs `UPDATE image_stacks SET representative_key = ?, stack_size = ?` in **`stack_split…`** (~1903), insert a single Python `#` comment restating that **`stack_metadata_for_api` is authoritative for API `stack_member_count`.**  
    3. Same **before** the matching `UPDATE image_stacks … stack_size` in **`stack_merge_into`** (~1977).  
    4. Same **before** the line `db.execute("UPDATE image_stacks SET stack_size = ? WHERE stack_id = ?",` in **`stack_set_representative`** (~2030).

    **D-02 (`restrict_to_keys` / tool schema):**  
    Immediately **before** `if restrict_to_keys is not None:` (~1494), insert one Python `#` comment: **`get_catalog_schema` may expose global catalog counts; when a pin is active, catalog listing/search here is still restricted to `restrict_to_keys` at execution time.**
  </action>
  <acceptance_criteria>
    - `rg "restrict_to_keys" lightroom_tagger/core/database.py` shows the new `#` comment on the line immediately above `if restrict_to_keys is not None:`.
    - `rg "stack_metadata_for_api" lightroom_tagger/core/database.py` matches at least **4** occurrences: existing function + **3** new comment lines near the three `stack_size` mutations (split, merge, set_representative recount).
    - `rg "stack_size INTEGER NOT NULL DEFAULT 0" lightroom_tagger/core/database.py` shows the migration DDL line includes `--` SQL comment text containing **authoritative** or **`stack_metadata_for_api`** (either word acceptable).
    - `python -m pytest lightroom_tagger/ -q` exits 0.
  </acceptance_criteria>
</task>

<task id="T3">
  <read_first>
    - `apps/visualizer/backend/jobs/handlers.py` (nested `_emit_prefilter_summary` ~565–576; `result_payload` dict ~720–728)
    - `.planning/phases/11-v3-deferred-polish/11-CONTEXT.md` (D-03)
  </read_first>
  <action>
    **D-03:** In `apps/visualizer/backend/jobs/handlers.py`:
    1. Immediately **above** the inner `def _emit_prefilter_summary(stats_snap: dict) -> None:` (~565), add one Python `#` line: counter / **`judgments=`** log field / **`vision_judgments_total`** result key count **shortlisted catalog candidates** run through **`score_candidates_with_vision`**, **not** LLM HTTP request count — keep key name and log label unchanged for parsers.

    2. End-of-line Python comment on the **`'vision_judgments_total': stats.get('vision_judgments_total', 0),`** line (~727) repeating the same meaning in ≤15 words (may abbreviate “shortlisted candidates” / “vision scoring”).
  </action>
  <acceptance_criteria>
    - `rg "score_candidates_with_vision" apps/visualizer/backend/jobs/handlers.py` exits 0 (new comment must include this exact substring).
    - `rg "'vision_judgments_total'" apps/visualizer/backend/jobs/handlers.py` still matches the `result_payload` line with the same dict key literal.
    - `rg "judgments=" apps/visualizer/backend/jobs/handlers.py` still contains `vision_judgments_total` in that f-string (log shape unchanged).
    - `python -m pytest apps/visualizer/backend/ -q` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd /Users/ccanales/projects/lightroom-tagger/apps/visualizer/frontend && npx tsc --noEmit`
- `cd /Users/ccanales/projects/lightroom-tagger && python -m pytest lightroom_tagger/ apps/visualizer/backend/ -q`
</verification>

<must_haves>
- `strings.ts` exports exist for every Catalog Cache / Search pin constant listed in **T1** (grep-checkable).
- `CATALOG_CACHE_SIMILARITY_EMPTY` references **Pipeline stages**, not **Advanced options**.
- `database.py` documents **`stack_metadata_for_api`** authority + **`restrict_to_keys`** vs global schema counts without changing SQL behavior.
- `handlers.py` documents **`vision_judgments_total`** / **`judgments=`** semantics without renaming keys or log tokens.
</must_haves>

<threat_model>
- **strings.ts / comments:** No new runtime input handling; **no** new attack surface.
- **handlers.py / database.py:** Comment-only; no query or auth changes.
</threat_model>
