# Cursor API key (recommended)
# You can also pass --api-key directly to the agent CLI.
CURSOR_API_KEY=
# TODO: add any env vars your issue tracker needs (e.g. an API token).
# See .sandcastle/SETUP_ISSUE_TRACKER.md
